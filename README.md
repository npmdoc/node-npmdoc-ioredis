# api documentation for  [ioredis (v2.5.0)](https://github.com/luin/ioredis#readme)  [![npm package](https://img.shields.io/npm/v/npmdoc-ioredis.svg?style=flat-square)](https://www.npmjs.org/package/npmdoc-ioredis) [![travis-ci.org build-status](https://api.travis-ci.org/npmdoc/node-npmdoc-ioredis.svg)](https://travis-ci.org/npmdoc/node-npmdoc-ioredis)
#### A delightful, performance-focused Redis client for Node and io.js

[![NPM](https://nodei.co/npm/ioredis.png?downloads=true)](https://www.npmjs.com/package/ioredis)

[![apidoc](https://npmdoc.github.io/node-npmdoc-ioredis/build/screen-capture.buildNpmdoc.browser._2Fhome_2Ftravis_2Fbuild_2Fnpmdoc_2Fnode-npmdoc-ioredis_2Ftmp_2Fbuild_2Fapidoc.html.png)](https://npmdoc.github.io/node-npmdoc-ioredis/build..beta..travis-ci.org/apidoc.html)

![package-listing](https://npmdoc.github.io/node-npmdoc-ioredis/build/screen-capture.npmPackageListing.svg)



# package.json

```json

{
    "author": {
        "name": "luin",
        "email": "i@zihua.li",
        "url": "http://zihua.li"
    },
    "bugs": {
        "url": "https://github.com/luin/ioredis/issues"
    },
    "config": {
        "commitizen": {
            "path": "./node_modules/cz-conventional-changelog"
        }
    },
    "dependencies": {
        "bluebird": "^3.3.4",
        "cluster-key-slot": "^1.0.6",
        "debug": "^2.2.0",
        "double-ended-queue": "^2.1.0-0",
        "flexbuffer": "0.0.6",
        "lodash": "^4.8.2",
        "redis-commands": "^1.2.0",
        "redis-parser": "^1.3.0"
    },
    "description": "A delightful, performance-focused Redis client for Node and io.js",
    "devDependencies": {
        "chai": "^3.5.0",
        "codeclimate-test-reporter": "0.3.1",
        "cz-conventional-changelog": "^1.1.5",
        "istanbul": "^0.4.2",
        "jsdoc": "^3.4.0",
        "jsdoc-to-markdown": "^1.3.3",
        "matcha": "^0.7.0",
        "mocha": "^2.4.5",
        "redis": "^2.4.2",
        "server-destroy": "^1.0.1",
        "sinon": "^1.17.3"
    },
    "directories": {},
    "dist": {
        "shasum": "fb6fdf0a1a7e0974614c67b6e5e11308a8cf95b9",
        "tarball": "https://registry.npmjs.org/ioredis/-/ioredis-2.5.0.tgz"
    },
    "engines": {
        "iojs": ">= 1.0.0",
        "node": ">= 0.10.16"
    },
    "files": [
        "index.js",
        "lib/"
    ],
    "gitHead": "865219c7776cc8378dd51ab1c1cf89a0db3f7496",
    "homepage": "https://github.com/luin/ioredis#readme",
    "keywords": [
        "redis",
        "cluster",
        "sentinel",
        "pipelining"
    ],
    "license": "MIT",
    "main": "index.js",
    "maintainers": [
        {
            "name": "luin",
            "email": "i@zihua.li"
        }
    ],
    "name": "ioredis",
    "optionalDependencies": {},
    "readme": "ERROR: No README data found!",
    "repository": {
        "type": "git",
        "url": "git://github.com/luin/ioredis.git"
    },
    "scripts": {
        "bench": "matcha benchmarks/*.js",
        "generate-docs": "jsdoc2md lib/redis.js lib/cluster/index.js lib/commander.js > API.md",
        "test": "NODE_ENV=test mocha",
        "test:cov": "NODE_ENV=test node ./node_modules/istanbul/lib/cli.js cover --preserve-comments ./node_modules/mocha/bin/_mocha -- -R spec"
    },
    "version": "2.5.0"
}
```



# <a name="apidoc.tableOfContents"></a>[table of contents](#apidoc.tableOfContents)

#### [module ioredis](#apidoc.module.ioredis)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Cluster (startupNodes, options)](#apidoc.element.ioredis.Cluster)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Command (name, args, options, callback)](#apidoc.element.ioredis.Command)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Promise (executor)](#apidoc.element.ioredis.Promise)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Promise.AggregateError (message)](#apidoc.element.ioredis.Promise.AggregateError)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Promise.CancellationError (message)](#apidoc.element.ioredis.Promise.CancellationError)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Promise.OperationalError (message)](#apidoc.element.ioredis.Promise.OperationalError)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Promise.PromiseInspection (promise)](#apidoc.element.ioredis.Promise.PromiseInspection)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Promise.TimeoutError (message)](#apidoc.element.ioredis.Promise.TimeoutError)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Promise._SomePromiseArray (values)](#apidoc.element.ioredis.Promise._SomePromiseArray)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Promise.coroutine (generatorFunction, options)](#apidoc.element.ioredis.Promise.coroutine)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>ReplyError (message)](#apidoc.element.ioredis.ReplyError)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>ReplyError.super_ ()](#apidoc.element.ioredis.ReplyError.super_)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>commander ()](#apidoc.element.ioredis.commander)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>createClient ()](#apidoc.element.ioredis.createClient)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>pipeline (redis)](#apidoc.element.ioredis.pipeline)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>print (err, reply)](#apidoc.element.ioredis.print)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>scan_stream (opt)](#apidoc.element.ioredis.scan_stream)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>script (lua, numberOfKeys, keyPrefix)](#apidoc.element.ioredis.script)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>subscription_set ()](#apidoc.element.ioredis.subscription_set)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>super_ ()](#apidoc.element.ioredis.super_)
1.  object <span class="apidocSignatureSpan">ioredis.</span>Cluster.defaultOptions
1.  object <span class="apidocSignatureSpan">ioredis.</span>Cluster.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>Command.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>Promise.AggregateError.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>Promise.CancellationError.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>Promise.OperationalError.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>Promise.PromiseInspection.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>Promise.TimeoutError.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>Promise._SomePromiseArray.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>Promise.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>commander.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>defaultOptions
1.  object <span class="apidocSignatureSpan">ioredis.</span>pipeline.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>scan_stream.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>script.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>subscription_set.prototype
1.  object <span class="apidocSignatureSpan">ioredis.</span>transaction

#### [module ioredis.Cluster](#apidoc.module.ioredis.Cluster)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Cluster (startupNodes, options)](#apidoc.element.ioredis.Cluster.Cluster)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.</span>super_ ()](#apidoc.element.ioredis.Cluster.super_)
1.  object <span class="apidocSignatureSpan">ioredis.Cluster.</span>defaultOptions

#### [module ioredis.Cluster.defaultOptions](#apidoc.module.ioredis.Cluster.defaultOptions)
1.  boolean <span class="apidocSignatureSpan">ioredis.Cluster.defaultOptions.</span>enableOfflineQueue
1.  boolean <span class="apidocSignatureSpan">ioredis.Cluster.defaultOptions.</span>enableReadyCheck
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.defaultOptions.</span>clusterRetryStrategy (times)](#apidoc.element.ioredis.Cluster.defaultOptions.clusterRetryStrategy)
1.  number <span class="apidocSignatureSpan">ioredis.Cluster.defaultOptions.</span>maxRedirections
1.  number <span class="apidocSignatureSpan">ioredis.Cluster.defaultOptions.</span>retryDelayOnClusterDown
1.  number <span class="apidocSignatureSpan">ioredis.Cluster.defaultOptions.</span>retryDelayOnFailover
1.  number <span class="apidocSignatureSpan">ioredis.Cluster.defaultOptions.</span>retryDelayOnTryAgain
1.  string <span class="apidocSignatureSpan">ioredis.Cluster.defaultOptions.</span>scaleReads

#### [module ioredis.Cluster.prototype](#apidoc.module.ioredis.Cluster.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>_handleCloseEvent ()](#apidoc.element.ioredis.Cluster.prototype._handleCloseEvent)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>_readyCheck (callback)](#apidoc.element.ioredis.Cluster.prototype._readyCheck)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>append ()](#apidoc.element.ioredis.Cluster.prototype.append)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>appendBuffer ()](#apidoc.element.ioredis.Cluster.prototype.appendBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>asking ()](#apidoc.element.ioredis.Cluster.prototype.asking)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>askingBuffer ()](#apidoc.element.ioredis.Cluster.prototype.askingBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>auth ()](#apidoc.element.ioredis.Cluster.prototype.auth)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>authBuffer ()](#apidoc.element.ioredis.Cluster.prototype.authBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bgrewriteaof ()](#apidoc.element.ioredis.Cluster.prototype.bgrewriteaof)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bgrewriteaofBuffer ()](#apidoc.element.ioredis.Cluster.prototype.bgrewriteaofBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bgsave ()](#apidoc.element.ioredis.Cluster.prototype.bgsave)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bgsaveBuffer ()](#apidoc.element.ioredis.Cluster.prototype.bgsaveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitcount ()](#apidoc.element.ioredis.Cluster.prototype.bitcount)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitcountBuffer ()](#apidoc.element.ioredis.Cluster.prototype.bitcountBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitfield ()](#apidoc.element.ioredis.Cluster.prototype.bitfield)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitfieldBuffer ()](#apidoc.element.ioredis.Cluster.prototype.bitfieldBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitop ()](#apidoc.element.ioredis.Cluster.prototype.bitop)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitopBuffer ()](#apidoc.element.ioredis.Cluster.prototype.bitopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitpos ()](#apidoc.element.ioredis.Cluster.prototype.bitpos)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitposBuffer ()](#apidoc.element.ioredis.Cluster.prototype.bitposBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>blpop ()](#apidoc.element.ioredis.Cluster.prototype.blpop)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>blpopBuffer ()](#apidoc.element.ioredis.Cluster.prototype.blpopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>brpop ()](#apidoc.element.ioredis.Cluster.prototype.brpop)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>brpopBuffer ()](#apidoc.element.ioredis.Cluster.prototype.brpopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>brpoplpush ()](#apidoc.element.ioredis.Cluster.prototype.brpoplpush)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>brpoplpushBuffer ()](#apidoc.element.ioredis.Cluster.prototype.brpoplpushBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>call ()](#apidoc.element.ioredis.Cluster.prototype.call)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>callBuffer ()](#apidoc.element.ioredis.Cluster.prototype.callBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>client ()](#apidoc.element.ioredis.Cluster.prototype.client)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>clientBuffer ()](#apidoc.element.ioredis.Cluster.prototype.clientBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>cluster ()](#apidoc.element.ioredis.Cluster.prototype.cluster)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>clusterBuffer ()](#apidoc.element.ioredis.Cluster.prototype.clusterBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>command ()](#apidoc.element.ioredis.Cluster.prototype.command)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>commandBuffer ()](#apidoc.element.ioredis.Cluster.prototype.commandBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>config ()](#apidoc.element.ioredis.Cluster.prototype.config)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>configBuffer ()](#apidoc.element.ioredis.Cluster.prototype.configBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>connect ()](#apidoc.element.ioredis.Cluster.prototype.connect)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>createBuiltinCommand (commandName)](#apidoc.element.ioredis.Cluster.prototype.createBuiltinCommand)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>dbsize ()](#apidoc.element.ioredis.Cluster.prototype.dbsize)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>dbsizeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.dbsizeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>debug ()](#apidoc.element.ioredis.Cluster.prototype.debug)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>debugBuffer ()](#apidoc.element.ioredis.Cluster.prototype.debugBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>decr ()](#apidoc.element.ioredis.Cluster.prototype.decr)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>decrBuffer ()](#apidoc.element.ioredis.Cluster.prototype.decrBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>decrby ()](#apidoc.element.ioredis.Cluster.prototype.decrby)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>decrbyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.decrbyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>defineCommand (name, definition)](#apidoc.element.ioredis.Cluster.prototype.defineCommand)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>del ()](#apidoc.element.ioredis.Cluster.prototype.del)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>delBuffer ()](#apidoc.element.ioredis.Cluster.prototype.delBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>discard ()](#apidoc.element.ioredis.Cluster.prototype.discard)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>discardBuffer ()](#apidoc.element.ioredis.Cluster.prototype.discardBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>disconnect (reconnect)](#apidoc.element.ioredis.Cluster.prototype.disconnect)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>dump ()](#apidoc.element.ioredis.Cluster.prototype.dump)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>dumpBuffer ()](#apidoc.element.ioredis.Cluster.prototype.dumpBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>echo ()](#apidoc.element.ioredis.Cluster.prototype.echo)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>echoBuffer ()](#apidoc.element.ioredis.Cluster.prototype.echoBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>eval ()](#apidoc.element.ioredis.Cluster.prototype.eval)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>evalBuffer ()](#apidoc.element.ioredis.Cluster.prototype.evalBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>evalsha ()](#apidoc.element.ioredis.Cluster.prototype.evalsha)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>evalshaBuffer ()](#apidoc.element.ioredis.Cluster.prototype.evalshaBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>exec (callback)](#apidoc.element.ioredis.Cluster.prototype.exec)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>execBuffer ()](#apidoc.element.ioredis.Cluster.prototype.execBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>executeOfflineCommands ()](#apidoc.element.ioredis.Cluster.prototype.executeOfflineCommands)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>exists ()](#apidoc.element.ioredis.Cluster.prototype.exists)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>existsBuffer ()](#apidoc.element.ioredis.Cluster.prototype.existsBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>expire ()](#apidoc.element.ioredis.Cluster.prototype.expire)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>expireBuffer ()](#apidoc.element.ioredis.Cluster.prototype.expireBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>expireat ()](#apidoc.element.ioredis.Cluster.prototype.expireat)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>expireatBuffer ()](#apidoc.element.ioredis.Cluster.prototype.expireatBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>flushQueue (error)](#apidoc.element.ioredis.Cluster.prototype.flushQueue)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>flushall ()](#apidoc.element.ioredis.Cluster.prototype.flushall)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>flushallBuffer ()](#apidoc.element.ioredis.Cluster.prototype.flushallBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>flushdb ()](#apidoc.element.ioredis.Cluster.prototype.flushdb)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>flushdbBuffer ()](#apidoc.element.ioredis.Cluster.prototype.flushdbBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geoadd ()](#apidoc.element.ioredis.Cluster.prototype.geoadd)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geoaddBuffer ()](#apidoc.element.ioredis.Cluster.prototype.geoaddBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geodist ()](#apidoc.element.ioredis.Cluster.prototype.geodist)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geodistBuffer ()](#apidoc.element.ioredis.Cluster.prototype.geodistBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geohash ()](#apidoc.element.ioredis.Cluster.prototype.geohash)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geohashBuffer ()](#apidoc.element.ioredis.Cluster.prototype.geohashBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geopos ()](#apidoc.element.ioredis.Cluster.prototype.geopos)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geoposBuffer ()](#apidoc.element.ioredis.Cluster.prototype.geoposBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>georadius ()](#apidoc.element.ioredis.Cluster.prototype.georadius)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>georadiusBuffer ()](#apidoc.element.ioredis.Cluster.prototype.georadiusBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>georadiusbymember ()](#apidoc.element.ioredis.Cluster.prototype.georadiusbymember)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>georadiusbymemberBuffer ()](#apidoc.element.ioredis.Cluster.prototype.georadiusbymemberBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>get ()](#apidoc.element.ioredis.Cluster.prototype.get)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getBuffer ()](#apidoc.element.ioredis.Cluster.prototype.getBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getBuiltinCommands ()](#apidoc.element.ioredis.Cluster.prototype.getBuiltinCommands)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getInfoFromNode (redis, callback)](#apidoc.element.ioredis.Cluster.prototype.getInfoFromNode)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getbit ()](#apidoc.element.ioredis.Cluster.prototype.getbit)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getbitBuffer ()](#apidoc.element.ioredis.Cluster.prototype.getbitBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getrange ()](#apidoc.element.ioredis.Cluster.prototype.getrange)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getrangeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.getrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getset ()](#apidoc.element.ioredis.Cluster.prototype.getset)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getsetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.getsetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>handleError (error, ttl, handlers)](#apidoc.element.ioredis.Cluster.prototype.handleError)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hdel ()](#apidoc.element.ioredis.Cluster.prototype.hdel)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hdelBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hdelBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hexists ()](#apidoc.element.ioredis.Cluster.prototype.hexists)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hexistsBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hexistsBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hget ()](#apidoc.element.ioredis.Cluster.prototype.hget)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hgetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hgetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hgetall ()](#apidoc.element.ioredis.Cluster.prototype.hgetall)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hgetallBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hgetallBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hincrby ()](#apidoc.element.ioredis.Cluster.prototype.hincrby)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hincrbyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hincrbyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hincrbyfloat ()](#apidoc.element.ioredis.Cluster.prototype.hincrbyfloat)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hincrbyfloatBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hincrbyfloatBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hkeys ()](#apidoc.element.ioredis.Cluster.prototype.hkeys)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hkeysBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hkeysBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hlen ()](#apidoc.element.ioredis.Cluster.prototype.hlen)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hlenBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hlenBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hmget ()](#apidoc.element.ioredis.Cluster.prototype.hmget)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hmgetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hmgetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hmset ()](#apidoc.element.ioredis.Cluster.prototype.hmset)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hmsetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hmsetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hscan ()](#apidoc.element.ioredis.Cluster.prototype.hscan)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hscanBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hscanBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hscanBufferStream (key, options)](#apidoc.element.ioredis.Cluster.prototype.hscanBufferStream)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hscanStream (key, options)](#apidoc.element.ioredis.Cluster.prototype.hscanStream)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hset ()](#apidoc.element.ioredis.Cluster.prototype.hset)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hsetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hsetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hsetnx ()](#apidoc.element.ioredis.Cluster.prototype.hsetnx)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hsetnxBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hsetnxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hstrlen ()](#apidoc.element.ioredis.Cluster.prototype.hstrlen)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hstrlenBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hstrlenBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hvals ()](#apidoc.element.ioredis.Cluster.prototype.hvals)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hvalsBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hvalsBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>incr ()](#apidoc.element.ioredis.Cluster.prototype.incr)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>incrBuffer ()](#apidoc.element.ioredis.Cluster.prototype.incrBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>incrby ()](#apidoc.element.ioredis.Cluster.prototype.incrby)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>incrbyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.incrbyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>incrbyfloat ()](#apidoc.element.ioredis.Cluster.prototype.incrbyfloat)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>incrbyfloatBuffer ()](#apidoc.element.ioredis.Cluster.prototype.incrbyfloatBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>info ()](#apidoc.element.ioredis.Cluster.prototype.info)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>infoBuffer ()](#apidoc.element.ioredis.Cluster.prototype.infoBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>keys ()](#apidoc.element.ioredis.Cluster.prototype.keys)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>keysBuffer ()](#apidoc.element.ioredis.Cluster.prototype.keysBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lastsave ()](#apidoc.element.ioredis.Cluster.prototype.lastsave)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lastsaveBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lastsaveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>latency ()](#apidoc.element.ioredis.Cluster.prototype.latency)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>latencyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.latencyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lindex ()](#apidoc.element.ioredis.Cluster.prototype.lindex)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lindexBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lindexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>linsert ()](#apidoc.element.ioredis.Cluster.prototype.linsert)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>linsertBuffer ()](#apidoc.element.ioredis.Cluster.prototype.linsertBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>llen ()](#apidoc.element.ioredis.Cluster.prototype.llen)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>llenBuffer ()](#apidoc.element.ioredis.Cluster.prototype.llenBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lpop ()](#apidoc.element.ioredis.Cluster.prototype.lpop)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lpopBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lpopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lpush ()](#apidoc.element.ioredis.Cluster.prototype.lpush)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lpushBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lpushBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lpushx ()](#apidoc.element.ioredis.Cluster.prototype.lpushx)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lpushxBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lpushxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lrange ()](#apidoc.element.ioredis.Cluster.prototype.lrange)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lrangeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lrem ()](#apidoc.element.ioredis.Cluster.prototype.lrem)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lremBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lremBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lset ()](#apidoc.element.ioredis.Cluster.prototype.lset)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lsetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lsetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>ltrim ()](#apidoc.element.ioredis.Cluster.prototype.ltrim)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>ltrimBuffer ()](#apidoc.element.ioredis.Cluster.prototype.ltrimBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>memory ()](#apidoc.element.ioredis.Cluster.prototype.memory)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>memoryBuffer ()](#apidoc.element.ioredis.Cluster.prototype.memoryBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>mget ()](#apidoc.element.ioredis.Cluster.prototype.mget)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>mgetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.mgetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>migrate ()](#apidoc.element.ioredis.Cluster.prototype.migrate)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>migrateBuffer ()](#apidoc.element.ioredis.Cluster.prototype.migrateBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>module ()](#apidoc.element.ioredis.Cluster.prototype.module)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>moduleBuffer ()](#apidoc.element.ioredis.Cluster.prototype.moduleBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>move ()](#apidoc.element.ioredis.Cluster.prototype.move)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>moveBuffer ()](#apidoc.element.ioredis.Cluster.prototype.moveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>mset ()](#apidoc.element.ioredis.Cluster.prototype.mset)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>msetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.msetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>msetnx ()](#apidoc.element.ioredis.Cluster.prototype.msetnx)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>msetnxBuffer ()](#apidoc.element.ioredis.Cluster.prototype.msetnxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>multi (commands, options)](#apidoc.element.ioredis.Cluster.prototype.multi)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>multiBuffer ()](#apidoc.element.ioredis.Cluster.prototype.multiBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>nodes (role)](#apidoc.element.ioredis.Cluster.prototype.nodes)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>object ()](#apidoc.element.ioredis.Cluster.prototype.object)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>objectBuffer ()](#apidoc.element.ioredis.Cluster.prototype.objectBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>persist ()](#apidoc.element.ioredis.Cluster.prototype.persist)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>persistBuffer ()](#apidoc.element.ioredis.Cluster.prototype.persistBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pexpire ()](#apidoc.element.ioredis.Cluster.prototype.pexpire)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pexpireBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pexpireBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pexpireat ()](#apidoc.element.ioredis.Cluster.prototype.pexpireat)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pexpireatBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pexpireatBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfadd ()](#apidoc.element.ioredis.Cluster.prototype.pfadd)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfaddBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pfaddBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfcount ()](#apidoc.element.ioredis.Cluster.prototype.pfcount)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfcountBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pfcountBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfdebug ()](#apidoc.element.ioredis.Cluster.prototype.pfdebug)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfdebugBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pfdebugBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfmerge ()](#apidoc.element.ioredis.Cluster.prototype.pfmerge)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfmergeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pfmergeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfselftest ()](#apidoc.element.ioredis.Cluster.prototype.pfselftest)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfselftestBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pfselftestBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>ping ()](#apidoc.element.ioredis.Cluster.prototype.ping)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pingBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pingBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pipeline (commands)](#apidoc.element.ioredis.Cluster.prototype.pipeline)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>post ()](#apidoc.element.ioredis.Cluster.prototype.post)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>postBuffer ()](#apidoc.element.ioredis.Cluster.prototype.postBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>psetex ()](#apidoc.element.ioredis.Cluster.prototype.psetex)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>psetexBuffer ()](#apidoc.element.ioredis.Cluster.prototype.psetexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>psubscribe ()](#apidoc.element.ioredis.Cluster.prototype.psubscribe)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>psubscribeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.psubscribeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>psync ()](#apidoc.element.ioredis.Cluster.prototype.psync)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>psyncBuffer ()](#apidoc.element.ioredis.Cluster.prototype.psyncBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pttl ()](#apidoc.element.ioredis.Cluster.prototype.pttl)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pttlBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pttlBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>publish ()](#apidoc.element.ioredis.Cluster.prototype.publish)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>publishBuffer ()](#apidoc.element.ioredis.Cluster.prototype.publishBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pubsub ()](#apidoc.element.ioredis.Cluster.prototype.pubsub)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pubsubBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pubsubBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>punsubscribe ()](#apidoc.element.ioredis.Cluster.prototype.punsubscribe)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>punsubscribeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.punsubscribeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>quit (callback)](#apidoc.element.ioredis.Cluster.prototype.quit)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>quitBuffer ()](#apidoc.element.ioredis.Cluster.prototype.quitBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>randomkey ()](#apidoc.element.ioredis.Cluster.prototype.randomkey)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>randomkeyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.randomkeyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>readonly ()](#apidoc.element.ioredis.Cluster.prototype.readonly)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>readonlyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.readonlyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>readwrite ()](#apidoc.element.ioredis.Cluster.prototype.readwrite)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>readwriteBuffer ()](#apidoc.element.ioredis.Cluster.prototype.readwriteBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>refreshSlotsCache (callback)](#apidoc.element.ioredis.Cluster.prototype.refreshSlotsCache)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rename ()](#apidoc.element.ioredis.Cluster.prototype.rename)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>renameBuffer ()](#apidoc.element.ioredis.Cluster.prototype.renameBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>renamenx ()](#apidoc.element.ioredis.Cluster.prototype.renamenx)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>renamenxBuffer ()](#apidoc.element.ioredis.Cluster.prototype.renamenxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>replconf ()](#apidoc.element.ioredis.Cluster.prototype.replconf)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>replconfBuffer ()](#apidoc.element.ioredis.Cluster.prototype.replconfBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>resetOfflineQueue ()](#apidoc.element.ioredis.Cluster.prototype.resetOfflineQueue)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>restore ()](#apidoc.element.ioredis.Cluster.prototype.restore)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>restore-asking ()](#apidoc.element.ioredis.Cluster.prototype.restore-asking)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>restore-askingBuffer ()](#apidoc.element.ioredis.Cluster.prototype.restore-askingBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>restoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.restoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>role ()](#apidoc.element.ioredis.Cluster.prototype.role)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>roleBuffer ()](#apidoc.element.ioredis.Cluster.prototype.roleBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpop ()](#apidoc.element.ioredis.Cluster.prototype.rpop)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpopBuffer ()](#apidoc.element.ioredis.Cluster.prototype.rpopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpoplpush ()](#apidoc.element.ioredis.Cluster.prototype.rpoplpush)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpoplpushBuffer ()](#apidoc.element.ioredis.Cluster.prototype.rpoplpushBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpush ()](#apidoc.element.ioredis.Cluster.prototype.rpush)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpushBuffer ()](#apidoc.element.ioredis.Cluster.prototype.rpushBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpushx ()](#apidoc.element.ioredis.Cluster.prototype.rpushx)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpushxBuffer ()](#apidoc.element.ioredis.Cluster.prototype.rpushxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sadd ()](#apidoc.element.ioredis.Cluster.prototype.sadd)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>saddBuffer ()](#apidoc.element.ioredis.Cluster.prototype.saddBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>save ()](#apidoc.element.ioredis.Cluster.prototype.save)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>saveBuffer ()](#apidoc.element.ioredis.Cluster.prototype.saveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>scan ()](#apidoc.element.ioredis.Cluster.prototype.scan)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>scanBuffer ()](#apidoc.element.ioredis.Cluster.prototype.scanBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>scard ()](#apidoc.element.ioredis.Cluster.prototype.scard)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>scardBuffer ()](#apidoc.element.ioredis.Cluster.prototype.scardBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>script ()](#apidoc.element.ioredis.Cluster.prototype.script)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>scriptBuffer ()](#apidoc.element.ioredis.Cluster.prototype.scriptBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sdiff ()](#apidoc.element.ioredis.Cluster.prototype.sdiff)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sdiffBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sdiffBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sdiffstore ()](#apidoc.element.ioredis.Cluster.prototype.sdiffstore)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sdiffstoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sdiffstoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>select ()](#apidoc.element.ioredis.Cluster.prototype.select)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>selectBuffer ()](#apidoc.element.ioredis.Cluster.prototype.selectBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>selectSubscriber ()](#apidoc.element.ioredis.Cluster.prototype.selectSubscriber)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sendCommand (command, stream, node)](#apidoc.element.ioredis.Cluster.prototype.sendCommand)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>send_command ()](#apidoc.element.ioredis.Cluster.prototype.send_command)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sentinel ()](#apidoc.element.ioredis.Cluster.prototype.sentinel)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sentinelBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sentinelBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>set ()](#apidoc.element.ioredis.Cluster.prototype.set)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setBuffer ()](#apidoc.element.ioredis.Cluster.prototype.setBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setStatus (status)](#apidoc.element.ioredis.Cluster.prototype.setStatus)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setbit ()](#apidoc.element.ioredis.Cluster.prototype.setbit)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setbitBuffer ()](#apidoc.element.ioredis.Cluster.prototype.setbitBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setex ()](#apidoc.element.ioredis.Cluster.prototype.setex)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setexBuffer ()](#apidoc.element.ioredis.Cluster.prototype.setexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setnx ()](#apidoc.element.ioredis.Cluster.prototype.setnx)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setnxBuffer ()](#apidoc.element.ioredis.Cluster.prototype.setnxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setrange ()](#apidoc.element.ioredis.Cluster.prototype.setrange)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setrangeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.setrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>shutdown ()](#apidoc.element.ioredis.Cluster.prototype.shutdown)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>shutdownBuffer ()](#apidoc.element.ioredis.Cluster.prototype.shutdownBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sinter ()](#apidoc.element.ioredis.Cluster.prototype.sinter)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sinterBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sinterBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sinterstore ()](#apidoc.element.ioredis.Cluster.prototype.sinterstore)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sinterstoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sinterstoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sismember ()](#apidoc.element.ioredis.Cluster.prototype.sismember)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sismemberBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sismemberBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>slaveof ()](#apidoc.element.ioredis.Cluster.prototype.slaveof)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>slaveofBuffer ()](#apidoc.element.ioredis.Cluster.prototype.slaveofBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>slowlog ()](#apidoc.element.ioredis.Cluster.prototype.slowlog)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>slowlogBuffer ()](#apidoc.element.ioredis.Cluster.prototype.slowlogBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>smembers ()](#apidoc.element.ioredis.Cluster.prototype.smembers)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>smembersBuffer ()](#apidoc.element.ioredis.Cluster.prototype.smembersBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>smove ()](#apidoc.element.ioredis.Cluster.prototype.smove)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>smoveBuffer ()](#apidoc.element.ioredis.Cluster.prototype.smoveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sort ()](#apidoc.element.ioredis.Cluster.prototype.sort)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sortBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sortBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>spop ()](#apidoc.element.ioredis.Cluster.prototype.spop)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>spopBuffer ()](#apidoc.element.ioredis.Cluster.prototype.spopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>srandmember ()](#apidoc.element.ioredis.Cluster.prototype.srandmember)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>srandmemberBuffer ()](#apidoc.element.ioredis.Cluster.prototype.srandmemberBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>srem ()](#apidoc.element.ioredis.Cluster.prototype.srem)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sremBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sremBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sscan ()](#apidoc.element.ioredis.Cluster.prototype.sscan)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sscanBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sscanBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sscanBufferStream (key, options)](#apidoc.element.ioredis.Cluster.prototype.sscanBufferStream)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sscanStream (key, options)](#apidoc.element.ioredis.Cluster.prototype.sscanStream)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>strlen ()](#apidoc.element.ioredis.Cluster.prototype.strlen)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>strlenBuffer ()](#apidoc.element.ioredis.Cluster.prototype.strlenBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>subscribe ()](#apidoc.element.ioredis.Cluster.prototype.subscribe)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>subscribeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.subscribeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>substr ()](#apidoc.element.ioredis.Cluster.prototype.substr)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>substrBuffer ()](#apidoc.element.ioredis.Cluster.prototype.substrBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sunion ()](#apidoc.element.ioredis.Cluster.prototype.sunion)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sunionBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sunionBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sunionstore ()](#apidoc.element.ioredis.Cluster.prototype.sunionstore)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sunionstoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sunionstoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>swapdb ()](#apidoc.element.ioredis.Cluster.prototype.swapdb)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>swapdbBuffer ()](#apidoc.element.ioredis.Cluster.prototype.swapdbBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sync ()](#apidoc.element.ioredis.Cluster.prototype.sync)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>syncBuffer ()](#apidoc.element.ioredis.Cluster.prototype.syncBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>time ()](#apidoc.element.ioredis.Cluster.prototype.time)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>timeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.timeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>touch ()](#apidoc.element.ioredis.Cluster.prototype.touch)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>touchBuffer ()](#apidoc.element.ioredis.Cluster.prototype.touchBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>ttl ()](#apidoc.element.ioredis.Cluster.prototype.ttl)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>ttlBuffer ()](#apidoc.element.ioredis.Cluster.prototype.ttlBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>type ()](#apidoc.element.ioredis.Cluster.prototype.type)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>typeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.typeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>unlink ()](#apidoc.element.ioredis.Cluster.prototype.unlink)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>unlinkBuffer ()](#apidoc.element.ioredis.Cluster.prototype.unlinkBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>unsubscribe ()](#apidoc.element.ioredis.Cluster.prototype.unsubscribe)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>unsubscribeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.unsubscribeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>unwatch ()](#apidoc.element.ioredis.Cluster.prototype.unwatch)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>unwatchBuffer ()](#apidoc.element.ioredis.Cluster.prototype.unwatchBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>wait ()](#apidoc.element.ioredis.Cluster.prototype.wait)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>waitBuffer ()](#apidoc.element.ioredis.Cluster.prototype.waitBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>watch ()](#apidoc.element.ioredis.Cluster.prototype.watch)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>watchBuffer ()](#apidoc.element.ioredis.Cluster.prototype.watchBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zadd ()](#apidoc.element.ioredis.Cluster.prototype.zadd)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zaddBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zaddBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zcard ()](#apidoc.element.ioredis.Cluster.prototype.zcard)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zcardBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zcardBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zcount ()](#apidoc.element.ioredis.Cluster.prototype.zcount)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zcountBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zcountBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zincrby ()](#apidoc.element.ioredis.Cluster.prototype.zincrby)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zincrbyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zincrbyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zinterstore ()](#apidoc.element.ioredis.Cluster.prototype.zinterstore)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zinterstoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zinterstoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zlexcount ()](#apidoc.element.ioredis.Cluster.prototype.zlexcount)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zlexcountBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zlexcountBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrange ()](#apidoc.element.ioredis.Cluster.prototype.zrange)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrangeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrangebylex ()](#apidoc.element.ioredis.Cluster.prototype.zrangebylex)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrangebylexBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrangebylexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrangebyscore ()](#apidoc.element.ioredis.Cluster.prototype.zrangebyscore)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrangebyscoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrangebyscoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrank ()](#apidoc.element.ioredis.Cluster.prototype.zrank)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrankBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrankBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrem ()](#apidoc.element.ioredis.Cluster.prototype.zrem)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zremBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremrangebylex ()](#apidoc.element.ioredis.Cluster.prototype.zremrangebylex)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremrangebylexBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zremrangebylexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremrangebyrank ()](#apidoc.element.ioredis.Cluster.prototype.zremrangebyrank)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremrangebyrankBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zremrangebyrankBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremrangebyscore ()](#apidoc.element.ioredis.Cluster.prototype.zremrangebyscore)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremrangebyscoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zremrangebyscoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrange ()](#apidoc.element.ioredis.Cluster.prototype.zrevrange)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrangeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrevrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrangebylex ()](#apidoc.element.ioredis.Cluster.prototype.zrevrangebylex)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrangebylexBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrevrangebylexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrangebyscore ()](#apidoc.element.ioredis.Cluster.prototype.zrevrangebyscore)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrangebyscoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrevrangebyscoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrank ()](#apidoc.element.ioredis.Cluster.prototype.zrevrank)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrankBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrevrankBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zscan ()](#apidoc.element.ioredis.Cluster.prototype.zscan)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zscanBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zscanBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zscanBufferStream (key, options)](#apidoc.element.ioredis.Cluster.prototype.zscanBufferStream)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zscanStream (key, options)](#apidoc.element.ioredis.Cluster.prototype.zscanStream)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zscore ()](#apidoc.element.ioredis.Cluster.prototype.zscore)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zscoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zscoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zunionstore ()](#apidoc.element.ioredis.Cluster.prototype.zunionstore)
1.  [function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zunionstoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zunionstoreBuffer)

#### [module ioredis.Command](#apidoc.module.ioredis.Command)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Command (name, args, options, callback)](#apidoc.element.ioredis.Command.Command)
1.  [function <span class="apidocSignatureSpan">ioredis.Command.</span>checkFlag (flagName, commandName)](#apidoc.element.ioredis.Command.checkFlag)
1.  [function <span class="apidocSignatureSpan">ioredis.Command.</span>setArgumentTransformer (name, func)](#apidoc.element.ioredis.Command.setArgumentTransformer)
1.  [function <span class="apidocSignatureSpan">ioredis.Command.</span>setReplyTransformer (name, func)](#apidoc.element.ioredis.Command.setReplyTransformer)
1.  object <span class="apidocSignatureSpan">ioredis.Command.</span>FLAGS
1.  object <span class="apidocSignatureSpan">ioredis.Command.</span>_transformer

#### [module ioredis.Command.prototype](#apidoc.module.ioredis.Command.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>_convertValue (resolve)](#apidoc.element.ioredis.Command.prototype._convertValue)
1.  [function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>_iterateKeys (transform)](#apidoc.element.ioredis.Command.prototype._iterateKeys)
1.  [function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>getKeys ()](#apidoc.element.ioredis.Command.prototype.getKeys)
1.  [function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>getSlot ()](#apidoc.element.ioredis.Command.prototype.getSlot)
1.  [function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>initPromise ()](#apidoc.element.ioredis.Command.prototype.initPromise)
1.  [function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>stringifyArguments ()](#apidoc.element.ioredis.Command.prototype.stringifyArguments)
1.  [function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>toWritable ()](#apidoc.element.ioredis.Command.prototype.toWritable)
1.  [function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>transformReply (result)](#apidoc.element.ioredis.Command.prototype.transformReply)

#### [module ioredis.Promise](#apidoc.module.ioredis.Promise)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>Promise (executor)](#apidoc.element.ioredis.Promise.Promise)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>AggregateError (message)](#apidoc.element.ioredis.Promise.AggregateError)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>CancellationError (message)](#apidoc.element.ioredis.Promise.CancellationError)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>OperationalError (message)](#apidoc.element.ioredis.Promise.OperationalError)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>PromiseInspection (promise)](#apidoc.element.ioredis.Promise.PromiseInspection)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>RangeError ()](#apidoc.element.ioredis.Promise.RangeError)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>RejectionError (message)](#apidoc.element.ioredis.Promise.RejectionError)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>TimeoutError (message)](#apidoc.element.ioredis.Promise.TimeoutError)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>TypeError ()](#apidoc.element.ioredis.Promise.TypeError)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>_SomePromiseArray (values)](#apidoc.element.ioredis.Promise._SomePromiseArray)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>_peekContext ()](#apidoc.element.ioredis.Promise._peekContext)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>all (promises)](#apidoc.element.ioredis.Promise.all)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>any (promises)](#apidoc.element.ioredis.Promise.any)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>attempt (fn)](#apidoc.element.ioredis.Promise.attempt)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>bind (thisArg, value)](#apidoc.element.ioredis.Promise.bind)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>cast (obj)](#apidoc.element.ioredis.Promise.cast)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>config (opts)](#apidoc.element.ioredis.Promise.config)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>coroutine (generatorFunction, options)](#apidoc.element.ioredis.Promise.coroutine)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>defer ()](#apidoc.element.ioredis.Promise.defer)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>delay (ms, value)](#apidoc.element.ioredis.Promise.delay)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>each (promises, fn)](#apidoc.element.ioredis.Promise.each)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>filter (promises, fn, options)](#apidoc.element.ioredis.Promise.filter)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>fromCallback (fn)](#apidoc.element.ioredis.Promise.fromCallback)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>fromNode (fn)](#apidoc.element.ioredis.Promise.fromNode)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>fulfilled (obj)](#apidoc.element.ioredis.Promise.fulfilled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>getNewLibraryCopy ()](#apidoc.element.ioredis.Promise.getNewLibraryCopy)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>hasLongStackTraces ()](#apidoc.element.ioredis.Promise.hasLongStackTraces)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>is (val)](#apidoc.element.ioredis.Promise.is)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>join ()](#apidoc.element.ioredis.Promise.join)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>longStackTraces ()](#apidoc.element.ioredis.Promise.longStackTraces)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>map (promises, fn, options, _filter)](#apidoc.element.ioredis.Promise.map)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>mapSeries (promises, fn)](#apidoc.element.ioredis.Promise.mapSeries)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>method (fn)](#apidoc.element.ioredis.Promise.method)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>noConflict ()](#apidoc.element.ioredis.Promise.noConflict)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>onPossiblyUnhandledRejection (fn)](#apidoc.element.ioredis.Promise.onPossiblyUnhandledRejection)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>onUnhandledRejectionHandled (fn)](#apidoc.element.ioredis.Promise.onUnhandledRejectionHandled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>pending ()](#apidoc.element.ioredis.Promise.pending)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>promisify (fn, options)](#apidoc.element.ioredis.Promise.promisify)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>promisifyAll (target, options)](#apidoc.element.ioredis.Promise.promisifyAll)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>props (promises)](#apidoc.element.ioredis.Promise.props)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>race (promises)](#apidoc.element.ioredis.Promise.race)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>reduce (promises, fn, initialValue, _each)](#apidoc.element.ioredis.Promise.reduce)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>reject (reason)](#apidoc.element.ioredis.Promise.reject)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>rejected (reason)](#apidoc.element.ioredis.Promise.rejected)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>resolve (obj)](#apidoc.element.ioredis.Promise.resolve)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>setScheduler (fn)](#apidoc.element.ioredis.Promise.setScheduler)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>settle (promises)](#apidoc.element.ioredis.Promise.settle)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>some (promises, howMany)](#apidoc.element.ioredis.Promise.some)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>spawn (generatorFunction)](#apidoc.element.ioredis.Promise.spawn)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>try (fn)](#apidoc.element.ioredis.Promise.try)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>using ()](#apidoc.element.ioredis.Promise.using)
1.  string <span class="apidocSignatureSpan">ioredis.Promise.</span>version

#### [module ioredis.Promise.AggregateError](#apidoc.module.ioredis.Promise.AggregateError)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>AggregateError (message)](#apidoc.element.ioredis.Promise.AggregateError.AggregateError)

#### [module ioredis.Promise.AggregateError.prototype](#apidoc.module.ioredis.Promise.AggregateError.prototype)
1.  boolean <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>isOperational
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>constructor (message)](#apidoc.element.ioredis.Promise.AggregateError.prototype.constructor)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>every ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.every)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>filter ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.filter)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>forEach ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.forEach)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>indexOf ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.indexOf)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>join ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.join)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>lastIndexOf ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.lastIndexOf)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>map ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.map)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>pop ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.pop)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>push ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.push)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>reduce ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.reduce)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>reduceRight ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.reduceRight)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>reverse ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.reverse)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>shift ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.shift)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>slice ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.slice)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>some ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.some)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>sort ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.sort)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>toString ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.toString)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>unshift ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.unshift)
1.  number <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>length

#### [module ioredis.Promise.CancellationError](#apidoc.module.ioredis.Promise.CancellationError)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>CancellationError (message)](#apidoc.element.ioredis.Promise.CancellationError.CancellationError)

#### [module ioredis.Promise.CancellationError.prototype](#apidoc.module.ioredis.Promise.CancellationError.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.CancellationError.prototype.</span>constructor (message)](#apidoc.element.ioredis.Promise.CancellationError.prototype.constructor)

#### [module ioredis.Promise.OperationalError](#apidoc.module.ioredis.Promise.OperationalError)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>OperationalError (message)](#apidoc.element.ioredis.Promise.OperationalError.OperationalError)

#### [module ioredis.Promise.OperationalError.prototype](#apidoc.module.ioredis.Promise.OperationalError.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.OperationalError.prototype.</span>constructor (message)](#apidoc.element.ioredis.Promise.OperationalError.prototype.constructor)

#### [module ioredis.Promise.PromiseInspection](#apidoc.module.ioredis.Promise.PromiseInspection)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>PromiseInspection (promise)](#apidoc.element.ioredis.Promise.PromiseInspection.PromiseInspection)

#### [module ioredis.Promise.PromiseInspection.prototype](#apidoc.module.ioredis.Promise.PromiseInspection.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>_settledValue ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype._settledValue)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>error ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.error)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>isCancelled ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.isCancelled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>isFulfilled ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.isFulfilled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>isPending ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.isPending)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>isRejected ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.isRejected)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>isResolved ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.isResolved)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>reason ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.reason)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>value ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.value)

#### [module ioredis.Promise.TimeoutError](#apidoc.module.ioredis.Promise.TimeoutError)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>TimeoutError (message)](#apidoc.element.ioredis.Promise.TimeoutError.TimeoutError)

#### [module ioredis.Promise.TimeoutError.prototype](#apidoc.module.ioredis.Promise.TimeoutError.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.TimeoutError.prototype.</span>constructor (message)](#apidoc.element.ioredis.Promise.TimeoutError.prototype.constructor)

#### [module ioredis.Promise._SomePromiseArray](#apidoc.module.ioredis.Promise._SomePromiseArray)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>_SomePromiseArray (values)](#apidoc.element.ioredis.Promise._SomePromiseArray._SomePromiseArray)

#### [module ioredis.Promise._SomePromiseArray.prototype](#apidoc.module.ioredis.Promise._SomePromiseArray.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_addFulfilled (value)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._addFulfilled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_addRejected (reason)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._addRejected)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_canPossiblyFulfill ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._canPossiblyFulfill)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_checkOutcome ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._checkOutcome)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_fulfilled ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._fulfilled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_getRangeError (count)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._getRangeError)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_init ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._init)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_promiseCancelled ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._promiseCancelled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_promiseFulfilled (value)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._promiseFulfilled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_promiseRejected (reason)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._promiseRejected)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_rejected ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._rejected)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_resolveEmptyArray ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._resolveEmptyArray)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>constructor (values)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype.constructor)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>howMany ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype.howMany)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>init ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype.init)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>setHowMany (count)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype.setHowMany)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>setUnwrap ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype.setUnwrap)

#### [module ioredis.Promise.coroutine](#apidoc.module.ioredis.Promise.coroutine)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.</span>coroutine (generatorFunction, options)](#apidoc.element.ioredis.Promise.coroutine.coroutine)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.coroutine.</span>addYieldHandler (fn)](#apidoc.element.ioredis.Promise.coroutine.addYieldHandler)

#### [module ioredis.Promise.prototype](#apidoc.module.ioredis.Promise.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>__isCancelled ()](#apidoc.element.ioredis.Promise.prototype.__isCancelled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_addCallbacks ( fulfill, reject, promise, receiver, domain )](#apidoc.element.ioredis.Promise.prototype._addCallbacks)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_attachCancellationCallback (onCancel)](#apidoc.element.ioredis.Promise.prototype._attachCancellationCallback)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_attachExtraTrace ()](#apidoc.element.ioredis.Promise.prototype._attachExtraTrace)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_boundValue ()](#apidoc.element.ioredis.Promise.prototype._boundValue)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_branchHasCancelled ()](#apidoc.element.ioredis.Promise.prototype._branchHasCancelled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_cancel ()](#apidoc.element.ioredis.Promise.prototype._cancel)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_cancelBranched ()](#apidoc.element.ioredis.Promise.prototype._cancelBranched)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_cancelBy (canceller)](#apidoc.element.ioredis.Promise.prototype._cancelBy)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_cancelPromises ()](#apidoc.element.ioredis.Promise.prototype._cancelPromises)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_captureStackTrace ()](#apidoc.element.ioredis.Promise.prototype._captureStackTrace)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_clearCallbackDataAtIndex (index)](#apidoc.element.ioredis.Promise.prototype._clearCallbackDataAtIndex)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_clearCancellationData ()](#apidoc.element.ioredis.Promise.prototype._clearCancellationData)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_doInvokeOnCancel (onCancelCallback, internalOnly)](#apidoc.element.ioredis.Promise.prototype._doInvokeOnCancel)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_enoughBranchesHaveCancelled ()](#apidoc.element.ioredis.Promise.prototype._enoughBranchesHaveCancelled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_ensurePossibleRejectionHandled ()](#apidoc.element.ioredis.Promise.prototype._ensurePossibleRejectionHandled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_execute (executor, resolve, reject)](#apidoc.element.ioredis.Promise.prototype._execute)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_fireEvent ()](#apidoc.element.ioredis.Promise.prototype._fireEvent)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_followee ()](#apidoc.element.ioredis.Promise.prototype._followee)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_fulfill (value)](#apidoc.element.ioredis.Promise.prototype._fulfill)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_fulfillPromises (len, value)](#apidoc.element.ioredis.Promise.prototype._fulfillPromises)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_fulfillmentHandlerAt (index)](#apidoc.element.ioredis.Promise.prototype._fulfillmentHandlerAt)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_getDisposer ()](#apidoc.element.ioredis.Promise.prototype._getDisposer)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_invokeInternalOnCancel ()](#apidoc.element.ioredis.Promise.prototype._invokeInternalOnCancel)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_invokeOnCancel ()](#apidoc.element.ioredis.Promise.prototype._invokeOnCancel)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isBound ()](#apidoc.element.ioredis.Promise.prototype._isBound)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isCancellable ()](#apidoc.element.ioredis.Promise.prototype._isCancellable)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isCancelled ()](#apidoc.element.ioredis.Promise.prototype._isCancelled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isDisposable ()](#apidoc.element.ioredis.Promise.prototype._isDisposable)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isFateSealed ()](#apidoc.element.ioredis.Promise.prototype._isFateSealed)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isFinal ()](#apidoc.element.ioredis.Promise.prototype._isFinal)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isFollowing ()](#apidoc.element.ioredis.Promise.prototype._isFollowing)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isRejectionUnhandled ()](#apidoc.element.ioredis.Promise.prototype._isRejectionUnhandled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isUnhandledRejectionNotified ()](#apidoc.element.ioredis.Promise.prototype._isUnhandledRejectionNotified)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_length ()](#apidoc.element.ioredis.Promise.prototype._length)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_migrateCallback0 (follower)](#apidoc.element.ioredis.Promise.prototype._migrateCallback0)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_migrateCallbackAt (follower, index)](#apidoc.element.ioredis.Promise.prototype._migrateCallbackAt)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_notifyUnhandledRejection ()](#apidoc.element.ioredis.Promise.prototype._notifyUnhandledRejection)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_notifyUnhandledRejectionIsHandled ()](#apidoc.element.ioredis.Promise.prototype._notifyUnhandledRejectionIsHandled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_onCancel ()](#apidoc.element.ioredis.Promise.prototype._onCancel)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_passThrough (handler, type, success, fail)](#apidoc.element.ioredis.Promise.prototype._passThrough)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_peekContext ()](#apidoc.element.ioredis.Promise.prototype._peekContext)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_popContext ()](#apidoc.element.ioredis.Promise.prototype._popContext)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_promiseAt (index)](#apidoc.element.ioredis.Promise.prototype._promiseAt)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_promiseCreated ()](#apidoc.element.ioredis.Promise.prototype._promiseCreated)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_propagateFrom (parent, flags)](#apidoc.element.ioredis.Promise.prototype._propagateFrom)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_proxy (proxyable, arg)](#apidoc.element.ioredis.Promise.prototype._proxy)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_pushContext ()](#apidoc.element.ioredis.Promise.prototype._pushContext)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_reason ()](#apidoc.element.ioredis.Promise.prototype._reason)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_receiverAt (index)](#apidoc.element.ioredis.Promise.prototype._receiverAt)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_reject (reason)](#apidoc.element.ioredis.Promise.prototype._reject)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_rejectCallback (reason, synchronous, ignoreNonErrorWarnings)](#apidoc.element.ioredis.Promise.prototype._rejectCallback)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_rejectPromises (len, reason)](#apidoc.element.ioredis.Promise.prototype._rejectPromises)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_rejectionHandlerAt (index)](#apidoc.element.ioredis.Promise.prototype._rejectionHandlerAt)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_resolveCallback (value, shouldBind)](#apidoc.element.ioredis.Promise.prototype._resolveCallback)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_resolveFromExecutor (executor)](#apidoc.element.ioredis.Promise.prototype._resolveFromExecutor)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_resolveFromSyncValue (value)](#apidoc.element.ioredis.Promise.prototype._resolveFromSyncValue)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_resultCancelled ()](#apidoc.element.ioredis.Promise.prototype._resultCancelled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_returnedNonUndefined ()](#apidoc.element.ioredis.Promise.prototype._returnedNonUndefined)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setAsyncGuaranteed ()](#apidoc.element.ioredis.Promise.prototype._setAsyncGuaranteed)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setBoundTo (obj)](#apidoc.element.ioredis.Promise.prototype._setBoundTo)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setCancelled ()](#apidoc.element.ioredis.Promise.prototype._setCancelled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setDisposable (disposer)](#apidoc.element.ioredis.Promise.prototype._setDisposable)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setFollowee (promise)](#apidoc.element.ioredis.Promise.prototype._setFollowee)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setFollowing ()](#apidoc.element.ioredis.Promise.prototype._setFollowing)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setFulfilled ()](#apidoc.element.ioredis.Promise.prototype._setFulfilled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setIsFinal ()](#apidoc.element.ioredis.Promise.prototype._setIsFinal)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setLength (len)](#apidoc.element.ioredis.Promise.prototype._setLength)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setOnCancel (handler)](#apidoc.element.ioredis.Promise.prototype._setOnCancel)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setRejected ()](#apidoc.element.ioredis.Promise.prototype._setRejected)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setRejectionIsUnhandled ()](#apidoc.element.ioredis.Promise.prototype._setRejectionIsUnhandled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setReturnedNonUndefined ()](#apidoc.element.ioredis.Promise.prototype._setReturnedNonUndefined)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setUnhandledRejectionIsNotified ()](#apidoc.element.ioredis.Promise.prototype._setUnhandledRejectionIsNotified)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setWillBeCancelled ()](#apidoc.element.ioredis.Promise.prototype._setWillBeCancelled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settlePromise (promise, handler, receiver, value)](#apidoc.element.ioredis.Promise.prototype._settlePromise)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settlePromise0 (handler, value, bitField)](#apidoc.element.ioredis.Promise.prototype._settlePromise0)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settlePromiseCtx (ctx)](#apidoc.element.ioredis.Promise.prototype._settlePromiseCtx)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settlePromiseFromHandler ( handler, receiver, value, promise )](#apidoc.element.ioredis.Promise.prototype._settlePromiseFromHandler)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settlePromiseLateCancellationObserver (ctx)](#apidoc.element.ioredis.Promise.prototype._settlePromiseLateCancellationObserver)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settlePromises ()](#apidoc.element.ioredis.Promise.prototype._settlePromises)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settledValue ()](#apidoc.element.ioredis.Promise.prototype._settledValue)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_target ()](#apidoc.element.ioredis.Promise.prototype._target)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_then ( didFulfill, didReject, _, receiver, internalData )](#apidoc.element.ioredis.Promise.prototype._then)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_unsetCancelled ()](#apidoc.element.ioredis.Promise.prototype._unsetCancelled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_unsetDisposable ()](#apidoc.element.ioredis.Promise.prototype._unsetDisposable)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_unsetOnCancel ()](#apidoc.element.ioredis.Promise.prototype._unsetOnCancel)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_unsetRejectionIsUnhandled ()](#apidoc.element.ioredis.Promise.prototype._unsetRejectionIsUnhandled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_unsetUnhandledRejectionIsNotified ()](#apidoc.element.ioredis.Promise.prototype._unsetUnhandledRejectionIsNotified)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_value ()](#apidoc.element.ioredis.Promise.prototype._value)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_warn (message, shouldUseOwnTrace, promise)](#apidoc.element.ioredis.Promise.prototype._warn)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>all ()](#apidoc.element.ioredis.Promise.prototype.all)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>any ()](#apidoc.element.ioredis.Promise.prototype.any)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>asCallback (nodeback, options)](#apidoc.element.ioredis.Promise.prototype.asCallback)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>bind (thisArg)](#apidoc.element.ioredis.Promise.prototype.bind)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>break ()](#apidoc.element.ioredis.Promise.prototype.break)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>call (methodName)](#apidoc.element.ioredis.Promise.prototype.call)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>cancel ()](#apidoc.element.ioredis.Promise.prototype.cancel)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>catch (fn)](#apidoc.element.ioredis.Promise.prototype.catch)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>catchReturn (value)](#apidoc.element.ioredis.Promise.prototype.catchReturn)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>catchThrow (reason)](#apidoc.element.ioredis.Promise.prototype.catchThrow)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>caught (fn)](#apidoc.element.ioredis.Promise.prototype.caught)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>delay (ms)](#apidoc.element.ioredis.Promise.prototype.delay)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>disposer (fn)](#apidoc.element.ioredis.Promise.prototype.disposer)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>done (didFulfill, didReject)](#apidoc.element.ioredis.Promise.prototype.done)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>each (fn)](#apidoc.element.ioredis.Promise.prototype.each)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>error (fn)](#apidoc.element.ioredis.Promise.prototype.error)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>filter (fn, options)](#apidoc.element.ioredis.Promise.prototype.filter)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>finally (handler)](#apidoc.element.ioredis.Promise.prototype.finally)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>get (propertyName)](#apidoc.element.ioredis.Promise.prototype.get)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>isCancellable ()](#apidoc.element.ioredis.Promise.prototype.isCancellable)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>isCancelled ()](#apidoc.element.ioredis.Promise.prototype.isCancelled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>isFulfilled ()](#apidoc.element.ioredis.Promise.prototype.isFulfilled)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>isPending ()](#apidoc.element.ioredis.Promise.prototype.isPending)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>isRejected ()](#apidoc.element.ioredis.Promise.prototype.isRejected)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>isResolved ()](#apidoc.element.ioredis.Promise.prototype.isResolved)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>lastly (handler)](#apidoc.element.ioredis.Promise.prototype.lastly)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>map (fn, options)](#apidoc.element.ioredis.Promise.prototype.map)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>mapSeries (fn)](#apidoc.element.ioredis.Promise.prototype.mapSeries)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>nodeify (nodeback, options)](#apidoc.element.ioredis.Promise.prototype.nodeify)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>props ()](#apidoc.element.ioredis.Promise.prototype.props)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>race ()](#apidoc.element.ioredis.Promise.prototype.race)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>reason ()](#apidoc.element.ioredis.Promise.prototype.reason)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>reduce (fn, initialValue)](#apidoc.element.ioredis.Promise.prototype.reduce)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>reflect ()](#apidoc.element.ioredis.Promise.prototype.reflect)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>return (value)](#apidoc.element.ioredis.Promise.prototype.return)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>settle ()](#apidoc.element.ioredis.Promise.prototype.settle)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>some (howMany)](#apidoc.element.ioredis.Promise.prototype.some)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>spread (fn)](#apidoc.element.ioredis.Promise.prototype.spread)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>suppressUnhandledRejections ()](#apidoc.element.ioredis.Promise.prototype.suppressUnhandledRejections)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>tap (handler)](#apidoc.element.ioredis.Promise.prototype.tap)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>tapCatch (handlerOrPredicate)](#apidoc.element.ioredis.Promise.prototype.tapCatch)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>then (didFulfill, didReject)](#apidoc.element.ioredis.Promise.prototype.then)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>thenReturn (value)](#apidoc.element.ioredis.Promise.prototype.thenReturn)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>thenThrow (reason)](#apidoc.element.ioredis.Promise.prototype.thenThrow)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>throw (reason)](#apidoc.element.ioredis.Promise.prototype.throw)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>timeout (ms, message)](#apidoc.element.ioredis.Promise.prototype.timeout)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>toJSON ()](#apidoc.element.ioredis.Promise.prototype.toJSON)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>toString ()](#apidoc.element.ioredis.Promise.prototype.toString)
1.  [function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>value ()](#apidoc.element.ioredis.Promise.prototype.value)

#### [module ioredis.ReplyError](#apidoc.module.ioredis.ReplyError)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>ReplyError (message)](#apidoc.element.ioredis.ReplyError.ReplyError)
1.  [function <span class="apidocSignatureSpan">ioredis.ReplyError.</span>super_ ()](#apidoc.element.ioredis.ReplyError.super_)

#### [module ioredis.ReplyError.super_](#apidoc.module.ioredis.ReplyError.super_)
1.  [function <span class="apidocSignatureSpan">ioredis.ReplyError.</span>super_ ()](#apidoc.element.ioredis.ReplyError.super_.super_)
1.  [function <span class="apidocSignatureSpan">ioredis.ReplyError.super_.</span>captureStackTrace ()](#apidoc.element.ioredis.ReplyError.super_.captureStackTrace)
1.  number <span class="apidocSignatureSpan">ioredis.ReplyError.super_.</span>stackTraceLimit

#### [module ioredis.commander](#apidoc.module.ioredis.commander)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>commander ()](#apidoc.element.ioredis.commander.commander)

#### [module ioredis.commander.prototype](#apidoc.module.ioredis.commander.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>append ()](#apidoc.element.ioredis.commander.prototype.append)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>appendBuffer ()](#apidoc.element.ioredis.commander.prototype.appendBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>asking ()](#apidoc.element.ioredis.commander.prototype.asking)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>askingBuffer ()](#apidoc.element.ioredis.commander.prototype.askingBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>auth ()](#apidoc.element.ioredis.commander.prototype.auth)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>authBuffer ()](#apidoc.element.ioredis.commander.prototype.authBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bgrewriteaof ()](#apidoc.element.ioredis.commander.prototype.bgrewriteaof)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bgrewriteaofBuffer ()](#apidoc.element.ioredis.commander.prototype.bgrewriteaofBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bgsave ()](#apidoc.element.ioredis.commander.prototype.bgsave)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bgsaveBuffer ()](#apidoc.element.ioredis.commander.prototype.bgsaveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitcount ()](#apidoc.element.ioredis.commander.prototype.bitcount)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitcountBuffer ()](#apidoc.element.ioredis.commander.prototype.bitcountBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitfield ()](#apidoc.element.ioredis.commander.prototype.bitfield)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitfieldBuffer ()](#apidoc.element.ioredis.commander.prototype.bitfieldBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitop ()](#apidoc.element.ioredis.commander.prototype.bitop)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitopBuffer ()](#apidoc.element.ioredis.commander.prototype.bitopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitpos ()](#apidoc.element.ioredis.commander.prototype.bitpos)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitposBuffer ()](#apidoc.element.ioredis.commander.prototype.bitposBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>blpop ()](#apidoc.element.ioredis.commander.prototype.blpop)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>blpopBuffer ()](#apidoc.element.ioredis.commander.prototype.blpopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>brpop ()](#apidoc.element.ioredis.commander.prototype.brpop)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>brpopBuffer ()](#apidoc.element.ioredis.commander.prototype.brpopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>brpoplpush ()](#apidoc.element.ioredis.commander.prototype.brpoplpush)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>brpoplpushBuffer ()](#apidoc.element.ioredis.commander.prototype.brpoplpushBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>call ()](#apidoc.element.ioredis.commander.prototype.call)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>callBuffer ()](#apidoc.element.ioredis.commander.prototype.callBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>client ()](#apidoc.element.ioredis.commander.prototype.client)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>clientBuffer ()](#apidoc.element.ioredis.commander.prototype.clientBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>cluster ()](#apidoc.element.ioredis.commander.prototype.cluster)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>clusterBuffer ()](#apidoc.element.ioredis.commander.prototype.clusterBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>command ()](#apidoc.element.ioredis.commander.prototype.command)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>commandBuffer ()](#apidoc.element.ioredis.commander.prototype.commandBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>config ()](#apidoc.element.ioredis.commander.prototype.config)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>configBuffer ()](#apidoc.element.ioredis.commander.prototype.configBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>createBuiltinCommand (commandName)](#apidoc.element.ioredis.commander.prototype.createBuiltinCommand)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>dbsize ()](#apidoc.element.ioredis.commander.prototype.dbsize)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>dbsizeBuffer ()](#apidoc.element.ioredis.commander.prototype.dbsizeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>debug ()](#apidoc.element.ioredis.commander.prototype.debug)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>debugBuffer ()](#apidoc.element.ioredis.commander.prototype.debugBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>decr ()](#apidoc.element.ioredis.commander.prototype.decr)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>decrBuffer ()](#apidoc.element.ioredis.commander.prototype.decrBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>decrby ()](#apidoc.element.ioredis.commander.prototype.decrby)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>decrbyBuffer ()](#apidoc.element.ioredis.commander.prototype.decrbyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>defineCommand (name, definition)](#apidoc.element.ioredis.commander.prototype.defineCommand)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>del ()](#apidoc.element.ioredis.commander.prototype.del)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>delBuffer ()](#apidoc.element.ioredis.commander.prototype.delBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>discard ()](#apidoc.element.ioredis.commander.prototype.discard)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>discardBuffer ()](#apidoc.element.ioredis.commander.prototype.discardBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>dump ()](#apidoc.element.ioredis.commander.prototype.dump)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>dumpBuffer ()](#apidoc.element.ioredis.commander.prototype.dumpBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>echo ()](#apidoc.element.ioredis.commander.prototype.echo)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>echoBuffer ()](#apidoc.element.ioredis.commander.prototype.echoBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>eval ()](#apidoc.element.ioredis.commander.prototype.eval)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>evalBuffer ()](#apidoc.element.ioredis.commander.prototype.evalBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>evalsha ()](#apidoc.element.ioredis.commander.prototype.evalsha)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>evalshaBuffer ()](#apidoc.element.ioredis.commander.prototype.evalshaBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>exec ()](#apidoc.element.ioredis.commander.prototype.exec)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>execBuffer ()](#apidoc.element.ioredis.commander.prototype.execBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>exists ()](#apidoc.element.ioredis.commander.prototype.exists)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>existsBuffer ()](#apidoc.element.ioredis.commander.prototype.existsBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>expire ()](#apidoc.element.ioredis.commander.prototype.expire)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>expireBuffer ()](#apidoc.element.ioredis.commander.prototype.expireBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>expireat ()](#apidoc.element.ioredis.commander.prototype.expireat)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>expireatBuffer ()](#apidoc.element.ioredis.commander.prototype.expireatBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>flushall ()](#apidoc.element.ioredis.commander.prototype.flushall)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>flushallBuffer ()](#apidoc.element.ioredis.commander.prototype.flushallBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>flushdb ()](#apidoc.element.ioredis.commander.prototype.flushdb)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>flushdbBuffer ()](#apidoc.element.ioredis.commander.prototype.flushdbBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geoadd ()](#apidoc.element.ioredis.commander.prototype.geoadd)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geoaddBuffer ()](#apidoc.element.ioredis.commander.prototype.geoaddBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geodist ()](#apidoc.element.ioredis.commander.prototype.geodist)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geodistBuffer ()](#apidoc.element.ioredis.commander.prototype.geodistBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geohash ()](#apidoc.element.ioredis.commander.prototype.geohash)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geohashBuffer ()](#apidoc.element.ioredis.commander.prototype.geohashBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geopos ()](#apidoc.element.ioredis.commander.prototype.geopos)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geoposBuffer ()](#apidoc.element.ioredis.commander.prototype.geoposBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>georadius ()](#apidoc.element.ioredis.commander.prototype.georadius)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>georadiusBuffer ()](#apidoc.element.ioredis.commander.prototype.georadiusBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>georadiusbymember ()](#apidoc.element.ioredis.commander.prototype.georadiusbymember)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>georadiusbymemberBuffer ()](#apidoc.element.ioredis.commander.prototype.georadiusbymemberBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>get ()](#apidoc.element.ioredis.commander.prototype.get)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getBuffer ()](#apidoc.element.ioredis.commander.prototype.getBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getBuiltinCommands ()](#apidoc.element.ioredis.commander.prototype.getBuiltinCommands)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getbit ()](#apidoc.element.ioredis.commander.prototype.getbit)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getbitBuffer ()](#apidoc.element.ioredis.commander.prototype.getbitBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getrange ()](#apidoc.element.ioredis.commander.prototype.getrange)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getrangeBuffer ()](#apidoc.element.ioredis.commander.prototype.getrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getset ()](#apidoc.element.ioredis.commander.prototype.getset)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getsetBuffer ()](#apidoc.element.ioredis.commander.prototype.getsetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hdel ()](#apidoc.element.ioredis.commander.prototype.hdel)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hdelBuffer ()](#apidoc.element.ioredis.commander.prototype.hdelBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hexists ()](#apidoc.element.ioredis.commander.prototype.hexists)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hexistsBuffer ()](#apidoc.element.ioredis.commander.prototype.hexistsBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hget ()](#apidoc.element.ioredis.commander.prototype.hget)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hgetBuffer ()](#apidoc.element.ioredis.commander.prototype.hgetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hgetall ()](#apidoc.element.ioredis.commander.prototype.hgetall)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hgetallBuffer ()](#apidoc.element.ioredis.commander.prototype.hgetallBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hincrby ()](#apidoc.element.ioredis.commander.prototype.hincrby)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hincrbyBuffer ()](#apidoc.element.ioredis.commander.prototype.hincrbyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hincrbyfloat ()](#apidoc.element.ioredis.commander.prototype.hincrbyfloat)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hincrbyfloatBuffer ()](#apidoc.element.ioredis.commander.prototype.hincrbyfloatBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hkeys ()](#apidoc.element.ioredis.commander.prototype.hkeys)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hkeysBuffer ()](#apidoc.element.ioredis.commander.prototype.hkeysBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hlen ()](#apidoc.element.ioredis.commander.prototype.hlen)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hlenBuffer ()](#apidoc.element.ioredis.commander.prototype.hlenBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hmget ()](#apidoc.element.ioredis.commander.prototype.hmget)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hmgetBuffer ()](#apidoc.element.ioredis.commander.prototype.hmgetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hmset ()](#apidoc.element.ioredis.commander.prototype.hmset)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hmsetBuffer ()](#apidoc.element.ioredis.commander.prototype.hmsetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hscan ()](#apidoc.element.ioredis.commander.prototype.hscan)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hscanBuffer ()](#apidoc.element.ioredis.commander.prototype.hscanBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hset ()](#apidoc.element.ioredis.commander.prototype.hset)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hsetBuffer ()](#apidoc.element.ioredis.commander.prototype.hsetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hsetnx ()](#apidoc.element.ioredis.commander.prototype.hsetnx)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hsetnxBuffer ()](#apidoc.element.ioredis.commander.prototype.hsetnxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hstrlen ()](#apidoc.element.ioredis.commander.prototype.hstrlen)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hstrlenBuffer ()](#apidoc.element.ioredis.commander.prototype.hstrlenBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hvals ()](#apidoc.element.ioredis.commander.prototype.hvals)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hvalsBuffer ()](#apidoc.element.ioredis.commander.prototype.hvalsBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>incr ()](#apidoc.element.ioredis.commander.prototype.incr)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>incrBuffer ()](#apidoc.element.ioredis.commander.prototype.incrBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>incrby ()](#apidoc.element.ioredis.commander.prototype.incrby)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>incrbyBuffer ()](#apidoc.element.ioredis.commander.prototype.incrbyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>incrbyfloat ()](#apidoc.element.ioredis.commander.prototype.incrbyfloat)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>incrbyfloatBuffer ()](#apidoc.element.ioredis.commander.prototype.incrbyfloatBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>info ()](#apidoc.element.ioredis.commander.prototype.info)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>infoBuffer ()](#apidoc.element.ioredis.commander.prototype.infoBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>keys ()](#apidoc.element.ioredis.commander.prototype.keys)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>keysBuffer ()](#apidoc.element.ioredis.commander.prototype.keysBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lastsave ()](#apidoc.element.ioredis.commander.prototype.lastsave)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lastsaveBuffer ()](#apidoc.element.ioredis.commander.prototype.lastsaveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>latency ()](#apidoc.element.ioredis.commander.prototype.latency)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>latencyBuffer ()](#apidoc.element.ioredis.commander.prototype.latencyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lindex ()](#apidoc.element.ioredis.commander.prototype.lindex)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lindexBuffer ()](#apidoc.element.ioredis.commander.prototype.lindexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>linsert ()](#apidoc.element.ioredis.commander.prototype.linsert)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>linsertBuffer ()](#apidoc.element.ioredis.commander.prototype.linsertBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>llen ()](#apidoc.element.ioredis.commander.prototype.llen)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>llenBuffer ()](#apidoc.element.ioredis.commander.prototype.llenBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lpop ()](#apidoc.element.ioredis.commander.prototype.lpop)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lpopBuffer ()](#apidoc.element.ioredis.commander.prototype.lpopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lpush ()](#apidoc.element.ioredis.commander.prototype.lpush)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lpushBuffer ()](#apidoc.element.ioredis.commander.prototype.lpushBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lpushx ()](#apidoc.element.ioredis.commander.prototype.lpushx)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lpushxBuffer ()](#apidoc.element.ioredis.commander.prototype.lpushxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lrange ()](#apidoc.element.ioredis.commander.prototype.lrange)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lrangeBuffer ()](#apidoc.element.ioredis.commander.prototype.lrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lrem ()](#apidoc.element.ioredis.commander.prototype.lrem)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lremBuffer ()](#apidoc.element.ioredis.commander.prototype.lremBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lset ()](#apidoc.element.ioredis.commander.prototype.lset)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lsetBuffer ()](#apidoc.element.ioredis.commander.prototype.lsetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>ltrim ()](#apidoc.element.ioredis.commander.prototype.ltrim)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>ltrimBuffer ()](#apidoc.element.ioredis.commander.prototype.ltrimBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>memory ()](#apidoc.element.ioredis.commander.prototype.memory)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>memoryBuffer ()](#apidoc.element.ioredis.commander.prototype.memoryBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>mget ()](#apidoc.element.ioredis.commander.prototype.mget)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>mgetBuffer ()](#apidoc.element.ioredis.commander.prototype.mgetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>migrate ()](#apidoc.element.ioredis.commander.prototype.migrate)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>migrateBuffer ()](#apidoc.element.ioredis.commander.prototype.migrateBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>module ()](#apidoc.element.ioredis.commander.prototype.module)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>moduleBuffer ()](#apidoc.element.ioredis.commander.prototype.moduleBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>move ()](#apidoc.element.ioredis.commander.prototype.move)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>moveBuffer ()](#apidoc.element.ioredis.commander.prototype.moveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>mset ()](#apidoc.element.ioredis.commander.prototype.mset)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>msetBuffer ()](#apidoc.element.ioredis.commander.prototype.msetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>msetnx ()](#apidoc.element.ioredis.commander.prototype.msetnx)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>msetnxBuffer ()](#apidoc.element.ioredis.commander.prototype.msetnxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>multi ()](#apidoc.element.ioredis.commander.prototype.multi)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>multiBuffer ()](#apidoc.element.ioredis.commander.prototype.multiBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>object ()](#apidoc.element.ioredis.commander.prototype.object)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>objectBuffer ()](#apidoc.element.ioredis.commander.prototype.objectBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>persist ()](#apidoc.element.ioredis.commander.prototype.persist)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>persistBuffer ()](#apidoc.element.ioredis.commander.prototype.persistBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pexpire ()](#apidoc.element.ioredis.commander.prototype.pexpire)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pexpireBuffer ()](#apidoc.element.ioredis.commander.prototype.pexpireBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pexpireat ()](#apidoc.element.ioredis.commander.prototype.pexpireat)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pexpireatBuffer ()](#apidoc.element.ioredis.commander.prototype.pexpireatBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfadd ()](#apidoc.element.ioredis.commander.prototype.pfadd)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfaddBuffer ()](#apidoc.element.ioredis.commander.prototype.pfaddBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfcount ()](#apidoc.element.ioredis.commander.prototype.pfcount)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfcountBuffer ()](#apidoc.element.ioredis.commander.prototype.pfcountBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfdebug ()](#apidoc.element.ioredis.commander.prototype.pfdebug)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfdebugBuffer ()](#apidoc.element.ioredis.commander.prototype.pfdebugBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfmerge ()](#apidoc.element.ioredis.commander.prototype.pfmerge)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfmergeBuffer ()](#apidoc.element.ioredis.commander.prototype.pfmergeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfselftest ()](#apidoc.element.ioredis.commander.prototype.pfselftest)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfselftestBuffer ()](#apidoc.element.ioredis.commander.prototype.pfselftestBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>ping ()](#apidoc.element.ioredis.commander.prototype.ping)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pingBuffer ()](#apidoc.element.ioredis.commander.prototype.pingBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>post ()](#apidoc.element.ioredis.commander.prototype.post)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>postBuffer ()](#apidoc.element.ioredis.commander.prototype.postBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>psetex ()](#apidoc.element.ioredis.commander.prototype.psetex)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>psetexBuffer ()](#apidoc.element.ioredis.commander.prototype.psetexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>psubscribe ()](#apidoc.element.ioredis.commander.prototype.psubscribe)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>psubscribeBuffer ()](#apidoc.element.ioredis.commander.prototype.psubscribeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>psync ()](#apidoc.element.ioredis.commander.prototype.psync)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>psyncBuffer ()](#apidoc.element.ioredis.commander.prototype.psyncBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pttl ()](#apidoc.element.ioredis.commander.prototype.pttl)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pttlBuffer ()](#apidoc.element.ioredis.commander.prototype.pttlBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>publish ()](#apidoc.element.ioredis.commander.prototype.publish)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>publishBuffer ()](#apidoc.element.ioredis.commander.prototype.publishBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pubsub ()](#apidoc.element.ioredis.commander.prototype.pubsub)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pubsubBuffer ()](#apidoc.element.ioredis.commander.prototype.pubsubBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>punsubscribe ()](#apidoc.element.ioredis.commander.prototype.punsubscribe)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>punsubscribeBuffer ()](#apidoc.element.ioredis.commander.prototype.punsubscribeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>quit ()](#apidoc.element.ioredis.commander.prototype.quit)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>quitBuffer ()](#apidoc.element.ioredis.commander.prototype.quitBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>randomkey ()](#apidoc.element.ioredis.commander.prototype.randomkey)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>randomkeyBuffer ()](#apidoc.element.ioredis.commander.prototype.randomkeyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>readonly ()](#apidoc.element.ioredis.commander.prototype.readonly)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>readonlyBuffer ()](#apidoc.element.ioredis.commander.prototype.readonlyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>readwrite ()](#apidoc.element.ioredis.commander.prototype.readwrite)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>readwriteBuffer ()](#apidoc.element.ioredis.commander.prototype.readwriteBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rename ()](#apidoc.element.ioredis.commander.prototype.rename)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>renameBuffer ()](#apidoc.element.ioredis.commander.prototype.renameBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>renamenx ()](#apidoc.element.ioredis.commander.prototype.renamenx)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>renamenxBuffer ()](#apidoc.element.ioredis.commander.prototype.renamenxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>replconf ()](#apidoc.element.ioredis.commander.prototype.replconf)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>replconfBuffer ()](#apidoc.element.ioredis.commander.prototype.replconfBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>restore ()](#apidoc.element.ioredis.commander.prototype.restore)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>restore-asking ()](#apidoc.element.ioredis.commander.prototype.restore-asking)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>restore-askingBuffer ()](#apidoc.element.ioredis.commander.prototype.restore-askingBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>restoreBuffer ()](#apidoc.element.ioredis.commander.prototype.restoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>role ()](#apidoc.element.ioredis.commander.prototype.role)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>roleBuffer ()](#apidoc.element.ioredis.commander.prototype.roleBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpop ()](#apidoc.element.ioredis.commander.prototype.rpop)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpopBuffer ()](#apidoc.element.ioredis.commander.prototype.rpopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpoplpush ()](#apidoc.element.ioredis.commander.prototype.rpoplpush)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpoplpushBuffer ()](#apidoc.element.ioredis.commander.prototype.rpoplpushBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpush ()](#apidoc.element.ioredis.commander.prototype.rpush)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpushBuffer ()](#apidoc.element.ioredis.commander.prototype.rpushBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpushx ()](#apidoc.element.ioredis.commander.prototype.rpushx)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpushxBuffer ()](#apidoc.element.ioredis.commander.prototype.rpushxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sadd ()](#apidoc.element.ioredis.commander.prototype.sadd)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>saddBuffer ()](#apidoc.element.ioredis.commander.prototype.saddBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>save ()](#apidoc.element.ioredis.commander.prototype.save)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>saveBuffer ()](#apidoc.element.ioredis.commander.prototype.saveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>scan ()](#apidoc.element.ioredis.commander.prototype.scan)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>scanBuffer ()](#apidoc.element.ioredis.commander.prototype.scanBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>scard ()](#apidoc.element.ioredis.commander.prototype.scard)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>scardBuffer ()](#apidoc.element.ioredis.commander.prototype.scardBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>script ()](#apidoc.element.ioredis.commander.prototype.script)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>scriptBuffer ()](#apidoc.element.ioredis.commander.prototype.scriptBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sdiff ()](#apidoc.element.ioredis.commander.prototype.sdiff)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sdiffBuffer ()](#apidoc.element.ioredis.commander.prototype.sdiffBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sdiffstore ()](#apidoc.element.ioredis.commander.prototype.sdiffstore)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sdiffstoreBuffer ()](#apidoc.element.ioredis.commander.prototype.sdiffstoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>select ()](#apidoc.element.ioredis.commander.prototype.select)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>selectBuffer ()](#apidoc.element.ioredis.commander.prototype.selectBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sendCommand ()](#apidoc.element.ioredis.commander.prototype.sendCommand)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>send_command ()](#apidoc.element.ioredis.commander.prototype.send_command)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sentinel ()](#apidoc.element.ioredis.commander.prototype.sentinel)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sentinelBuffer ()](#apidoc.element.ioredis.commander.prototype.sentinelBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>set ()](#apidoc.element.ioredis.commander.prototype.set)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setBuffer ()](#apidoc.element.ioredis.commander.prototype.setBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setbit ()](#apidoc.element.ioredis.commander.prototype.setbit)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setbitBuffer ()](#apidoc.element.ioredis.commander.prototype.setbitBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setex ()](#apidoc.element.ioredis.commander.prototype.setex)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setexBuffer ()](#apidoc.element.ioredis.commander.prototype.setexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setnx ()](#apidoc.element.ioredis.commander.prototype.setnx)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setnxBuffer ()](#apidoc.element.ioredis.commander.prototype.setnxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setrange ()](#apidoc.element.ioredis.commander.prototype.setrange)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setrangeBuffer ()](#apidoc.element.ioredis.commander.prototype.setrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>shutdown ()](#apidoc.element.ioredis.commander.prototype.shutdown)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>shutdownBuffer ()](#apidoc.element.ioredis.commander.prototype.shutdownBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sinter ()](#apidoc.element.ioredis.commander.prototype.sinter)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sinterBuffer ()](#apidoc.element.ioredis.commander.prototype.sinterBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sinterstore ()](#apidoc.element.ioredis.commander.prototype.sinterstore)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sinterstoreBuffer ()](#apidoc.element.ioredis.commander.prototype.sinterstoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sismember ()](#apidoc.element.ioredis.commander.prototype.sismember)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sismemberBuffer ()](#apidoc.element.ioredis.commander.prototype.sismemberBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>slaveof ()](#apidoc.element.ioredis.commander.prototype.slaveof)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>slaveofBuffer ()](#apidoc.element.ioredis.commander.prototype.slaveofBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>slowlog ()](#apidoc.element.ioredis.commander.prototype.slowlog)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>slowlogBuffer ()](#apidoc.element.ioredis.commander.prototype.slowlogBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>smembers ()](#apidoc.element.ioredis.commander.prototype.smembers)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>smembersBuffer ()](#apidoc.element.ioredis.commander.prototype.smembersBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>smove ()](#apidoc.element.ioredis.commander.prototype.smove)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>smoveBuffer ()](#apidoc.element.ioredis.commander.prototype.smoveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sort ()](#apidoc.element.ioredis.commander.prototype.sort)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sortBuffer ()](#apidoc.element.ioredis.commander.prototype.sortBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>spop ()](#apidoc.element.ioredis.commander.prototype.spop)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>spopBuffer ()](#apidoc.element.ioredis.commander.prototype.spopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>srandmember ()](#apidoc.element.ioredis.commander.prototype.srandmember)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>srandmemberBuffer ()](#apidoc.element.ioredis.commander.prototype.srandmemberBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>srem ()](#apidoc.element.ioredis.commander.prototype.srem)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sremBuffer ()](#apidoc.element.ioredis.commander.prototype.sremBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sscan ()](#apidoc.element.ioredis.commander.prototype.sscan)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sscanBuffer ()](#apidoc.element.ioredis.commander.prototype.sscanBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>strlen ()](#apidoc.element.ioredis.commander.prototype.strlen)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>strlenBuffer ()](#apidoc.element.ioredis.commander.prototype.strlenBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>subscribe ()](#apidoc.element.ioredis.commander.prototype.subscribe)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>subscribeBuffer ()](#apidoc.element.ioredis.commander.prototype.subscribeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>substr ()](#apidoc.element.ioredis.commander.prototype.substr)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>substrBuffer ()](#apidoc.element.ioredis.commander.prototype.substrBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sunion ()](#apidoc.element.ioredis.commander.prototype.sunion)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sunionBuffer ()](#apidoc.element.ioredis.commander.prototype.sunionBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sunionstore ()](#apidoc.element.ioredis.commander.prototype.sunionstore)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sunionstoreBuffer ()](#apidoc.element.ioredis.commander.prototype.sunionstoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>swapdb ()](#apidoc.element.ioredis.commander.prototype.swapdb)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>swapdbBuffer ()](#apidoc.element.ioredis.commander.prototype.swapdbBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sync ()](#apidoc.element.ioredis.commander.prototype.sync)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>syncBuffer ()](#apidoc.element.ioredis.commander.prototype.syncBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>time ()](#apidoc.element.ioredis.commander.prototype.time)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>timeBuffer ()](#apidoc.element.ioredis.commander.prototype.timeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>touch ()](#apidoc.element.ioredis.commander.prototype.touch)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>touchBuffer ()](#apidoc.element.ioredis.commander.prototype.touchBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>ttl ()](#apidoc.element.ioredis.commander.prototype.ttl)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>ttlBuffer ()](#apidoc.element.ioredis.commander.prototype.ttlBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>type ()](#apidoc.element.ioredis.commander.prototype.type)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>typeBuffer ()](#apidoc.element.ioredis.commander.prototype.typeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>unlink ()](#apidoc.element.ioredis.commander.prototype.unlink)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>unlinkBuffer ()](#apidoc.element.ioredis.commander.prototype.unlinkBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>unsubscribe ()](#apidoc.element.ioredis.commander.prototype.unsubscribe)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>unsubscribeBuffer ()](#apidoc.element.ioredis.commander.prototype.unsubscribeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>unwatch ()](#apidoc.element.ioredis.commander.prototype.unwatch)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>unwatchBuffer ()](#apidoc.element.ioredis.commander.prototype.unwatchBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>wait ()](#apidoc.element.ioredis.commander.prototype.wait)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>waitBuffer ()](#apidoc.element.ioredis.commander.prototype.waitBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>watch ()](#apidoc.element.ioredis.commander.prototype.watch)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>watchBuffer ()](#apidoc.element.ioredis.commander.prototype.watchBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zadd ()](#apidoc.element.ioredis.commander.prototype.zadd)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zaddBuffer ()](#apidoc.element.ioredis.commander.prototype.zaddBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zcard ()](#apidoc.element.ioredis.commander.prototype.zcard)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zcardBuffer ()](#apidoc.element.ioredis.commander.prototype.zcardBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zcount ()](#apidoc.element.ioredis.commander.prototype.zcount)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zcountBuffer ()](#apidoc.element.ioredis.commander.prototype.zcountBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zincrby ()](#apidoc.element.ioredis.commander.prototype.zincrby)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zincrbyBuffer ()](#apidoc.element.ioredis.commander.prototype.zincrbyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zinterstore ()](#apidoc.element.ioredis.commander.prototype.zinterstore)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zinterstoreBuffer ()](#apidoc.element.ioredis.commander.prototype.zinterstoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zlexcount ()](#apidoc.element.ioredis.commander.prototype.zlexcount)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zlexcountBuffer ()](#apidoc.element.ioredis.commander.prototype.zlexcountBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrange ()](#apidoc.element.ioredis.commander.prototype.zrange)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrangeBuffer ()](#apidoc.element.ioredis.commander.prototype.zrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrangebylex ()](#apidoc.element.ioredis.commander.prototype.zrangebylex)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrangebylexBuffer ()](#apidoc.element.ioredis.commander.prototype.zrangebylexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrangebyscore ()](#apidoc.element.ioredis.commander.prototype.zrangebyscore)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrangebyscoreBuffer ()](#apidoc.element.ioredis.commander.prototype.zrangebyscoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrank ()](#apidoc.element.ioredis.commander.prototype.zrank)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrankBuffer ()](#apidoc.element.ioredis.commander.prototype.zrankBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrem ()](#apidoc.element.ioredis.commander.prototype.zrem)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremBuffer ()](#apidoc.element.ioredis.commander.prototype.zremBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremrangebylex ()](#apidoc.element.ioredis.commander.prototype.zremrangebylex)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremrangebylexBuffer ()](#apidoc.element.ioredis.commander.prototype.zremrangebylexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremrangebyrank ()](#apidoc.element.ioredis.commander.prototype.zremrangebyrank)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremrangebyrankBuffer ()](#apidoc.element.ioredis.commander.prototype.zremrangebyrankBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremrangebyscore ()](#apidoc.element.ioredis.commander.prototype.zremrangebyscore)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremrangebyscoreBuffer ()](#apidoc.element.ioredis.commander.prototype.zremrangebyscoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrange ()](#apidoc.element.ioredis.commander.prototype.zrevrange)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrangeBuffer ()](#apidoc.element.ioredis.commander.prototype.zrevrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrangebylex ()](#apidoc.element.ioredis.commander.prototype.zrevrangebylex)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrangebylexBuffer ()](#apidoc.element.ioredis.commander.prototype.zrevrangebylexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrangebyscore ()](#apidoc.element.ioredis.commander.prototype.zrevrangebyscore)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrangebyscoreBuffer ()](#apidoc.element.ioredis.commander.prototype.zrevrangebyscoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrank ()](#apidoc.element.ioredis.commander.prototype.zrevrank)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrankBuffer ()](#apidoc.element.ioredis.commander.prototype.zrevrankBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zscan ()](#apidoc.element.ioredis.commander.prototype.zscan)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zscanBuffer ()](#apidoc.element.ioredis.commander.prototype.zscanBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zscore ()](#apidoc.element.ioredis.commander.prototype.zscore)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zscoreBuffer ()](#apidoc.element.ioredis.commander.prototype.zscoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zunionstore ()](#apidoc.element.ioredis.commander.prototype.zunionstore)
1.  [function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zunionstoreBuffer ()](#apidoc.element.ioredis.commander.prototype.zunionstoreBuffer)

#### [module ioredis.defaultOptions](#apidoc.module.ioredis.defaultOptions)
1.  boolean <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>autoResendUnfulfilledCommands
1.  boolean <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>autoResubscribe
1.  boolean <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>dropBufferSupport
1.  boolean <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>enableOfflineQueue
1.  boolean <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>enableReadyCheck
1.  boolean <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>lazyConnect
1.  boolean <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>noDelay
1.  boolean <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>readOnly
1.  boolean <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>stringNumbers
1.  [function <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>retryStrategy (times)](#apidoc.element.ioredis.defaultOptions.retryStrategy)
1.  [function <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>sentinelRetryStrategy (times)](#apidoc.element.ioredis.defaultOptions.sentinelRetryStrategy)
1.  number <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>connectTimeout
1.  number <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>db
1.  number <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>family
1.  number <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>keepAlive
1.  number <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>port
1.  object <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>connectionName
1.  object <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>name
1.  object <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>parser
1.  object <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>password
1.  object <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>reconnectOnError
1.  object <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>sentinels
1.  string <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>host
1.  string <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>keyPrefix
1.  string <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>role

#### [module ioredis.pipeline](#apidoc.module.ioredis.pipeline)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>pipeline (redis)](#apidoc.element.ioredis.pipeline.pipeline)

#### [module ioredis.pipeline.prototype](#apidoc.module.ioredis.pipeline.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>addBatch (commands)](#apidoc.element.ioredis.pipeline.prototype.addBatch)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>append ()](#apidoc.element.ioredis.pipeline.prototype.append)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>appendBuffer ()](#apidoc.element.ioredis.pipeline.prototype.appendBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>asking ()](#apidoc.element.ioredis.pipeline.prototype.asking)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>askingBuffer ()](#apidoc.element.ioredis.pipeline.prototype.askingBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>auth ()](#apidoc.element.ioredis.pipeline.prototype.auth)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>authBuffer ()](#apidoc.element.ioredis.pipeline.prototype.authBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bgrewriteaof ()](#apidoc.element.ioredis.pipeline.prototype.bgrewriteaof)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bgrewriteaofBuffer ()](#apidoc.element.ioredis.pipeline.prototype.bgrewriteaofBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bgsave ()](#apidoc.element.ioredis.pipeline.prototype.bgsave)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bgsaveBuffer ()](#apidoc.element.ioredis.pipeline.prototype.bgsaveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitcount ()](#apidoc.element.ioredis.pipeline.prototype.bitcount)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitcountBuffer ()](#apidoc.element.ioredis.pipeline.prototype.bitcountBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitfield ()](#apidoc.element.ioredis.pipeline.prototype.bitfield)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitfieldBuffer ()](#apidoc.element.ioredis.pipeline.prototype.bitfieldBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitop ()](#apidoc.element.ioredis.pipeline.prototype.bitop)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitopBuffer ()](#apidoc.element.ioredis.pipeline.prototype.bitopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitpos ()](#apidoc.element.ioredis.pipeline.prototype.bitpos)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitposBuffer ()](#apidoc.element.ioredis.pipeline.prototype.bitposBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>blpop ()](#apidoc.element.ioredis.pipeline.prototype.blpop)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>blpopBuffer ()](#apidoc.element.ioredis.pipeline.prototype.blpopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>brpop ()](#apidoc.element.ioredis.pipeline.prototype.brpop)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>brpopBuffer ()](#apidoc.element.ioredis.pipeline.prototype.brpopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>brpoplpush ()](#apidoc.element.ioredis.pipeline.prototype.brpoplpush)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>brpoplpushBuffer ()](#apidoc.element.ioredis.pipeline.prototype.brpoplpushBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>call ()](#apidoc.element.ioredis.pipeline.prototype.call)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>callBuffer ()](#apidoc.element.ioredis.pipeline.prototype.callBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>client ()](#apidoc.element.ioredis.pipeline.prototype.client)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>clientBuffer ()](#apidoc.element.ioredis.pipeline.prototype.clientBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>cluster ()](#apidoc.element.ioredis.pipeline.prototype.cluster)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>clusterBuffer ()](#apidoc.element.ioredis.pipeline.prototype.clusterBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>command ()](#apidoc.element.ioredis.pipeline.prototype.command)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>commandBuffer ()](#apidoc.element.ioredis.pipeline.prototype.commandBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>config ()](#apidoc.element.ioredis.pipeline.prototype.config)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>configBuffer ()](#apidoc.element.ioredis.pipeline.prototype.configBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>createBuiltinCommand (commandName)](#apidoc.element.ioredis.pipeline.prototype.createBuiltinCommand)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>dbsize ()](#apidoc.element.ioredis.pipeline.prototype.dbsize)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>dbsizeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.dbsizeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>debug ()](#apidoc.element.ioredis.pipeline.prototype.debug)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>debugBuffer ()](#apidoc.element.ioredis.pipeline.prototype.debugBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>decr ()](#apidoc.element.ioredis.pipeline.prototype.decr)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>decrBuffer ()](#apidoc.element.ioredis.pipeline.prototype.decrBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>decrby ()](#apidoc.element.ioredis.pipeline.prototype.decrby)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>decrbyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.decrbyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>defineCommand (name, definition)](#apidoc.element.ioredis.pipeline.prototype.defineCommand)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>del ()](#apidoc.element.ioredis.pipeline.prototype.del)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>delBuffer ()](#apidoc.element.ioredis.pipeline.prototype.delBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>discard ()](#apidoc.element.ioredis.pipeline.prototype.discard)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>discardBuffer ()](#apidoc.element.ioredis.pipeline.prototype.discardBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>dump ()](#apidoc.element.ioredis.pipeline.prototype.dump)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>dumpBuffer ()](#apidoc.element.ioredis.pipeline.prototype.dumpBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>echo ()](#apidoc.element.ioredis.pipeline.prototype.echo)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>echoBuffer ()](#apidoc.element.ioredis.pipeline.prototype.echoBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>eval ()](#apidoc.element.ioredis.pipeline.prototype.eval)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>evalBuffer ()](#apidoc.element.ioredis.pipeline.prototype.evalBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>evalsha ()](#apidoc.element.ioredis.pipeline.prototype.evalsha)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>evalshaBuffer ()](#apidoc.element.ioredis.pipeline.prototype.evalshaBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>exec (callback)](#apidoc.element.ioredis.pipeline.prototype.exec)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>execBuffer ()](#apidoc.element.ioredis.pipeline.prototype.execBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>exists ()](#apidoc.element.ioredis.pipeline.prototype.exists)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>existsBuffer ()](#apidoc.element.ioredis.pipeline.prototype.existsBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>expire ()](#apidoc.element.ioredis.pipeline.prototype.expire)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>expireBuffer ()](#apidoc.element.ioredis.pipeline.prototype.expireBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>expireat ()](#apidoc.element.ioredis.pipeline.prototype.expireat)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>expireatBuffer ()](#apidoc.element.ioredis.pipeline.prototype.expireatBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>fillResult (value, position)](#apidoc.element.ioredis.pipeline.prototype.fillResult)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>flushall ()](#apidoc.element.ioredis.pipeline.prototype.flushall)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>flushallBuffer ()](#apidoc.element.ioredis.pipeline.prototype.flushallBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>flushdb ()](#apidoc.element.ioredis.pipeline.prototype.flushdb)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>flushdbBuffer ()](#apidoc.element.ioredis.pipeline.prototype.flushdbBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geoadd ()](#apidoc.element.ioredis.pipeline.prototype.geoadd)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geoaddBuffer ()](#apidoc.element.ioredis.pipeline.prototype.geoaddBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geodist ()](#apidoc.element.ioredis.pipeline.prototype.geodist)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geodistBuffer ()](#apidoc.element.ioredis.pipeline.prototype.geodistBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geohash ()](#apidoc.element.ioredis.pipeline.prototype.geohash)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geohashBuffer ()](#apidoc.element.ioredis.pipeline.prototype.geohashBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geopos ()](#apidoc.element.ioredis.pipeline.prototype.geopos)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geoposBuffer ()](#apidoc.element.ioredis.pipeline.prototype.geoposBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>georadius ()](#apidoc.element.ioredis.pipeline.prototype.georadius)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>georadiusBuffer ()](#apidoc.element.ioredis.pipeline.prototype.georadiusBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>georadiusbymember ()](#apidoc.element.ioredis.pipeline.prototype.georadiusbymember)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>georadiusbymemberBuffer ()](#apidoc.element.ioredis.pipeline.prototype.georadiusbymemberBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>get ()](#apidoc.element.ioredis.pipeline.prototype.get)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getBuffer ()](#apidoc.element.ioredis.pipeline.prototype.getBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getBuiltinCommands ()](#apidoc.element.ioredis.pipeline.prototype.getBuiltinCommands)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getbit ()](#apidoc.element.ioredis.pipeline.prototype.getbit)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getbitBuffer ()](#apidoc.element.ioredis.pipeline.prototype.getbitBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getrange ()](#apidoc.element.ioredis.pipeline.prototype.getrange)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getrangeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.getrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getset ()](#apidoc.element.ioredis.pipeline.prototype.getset)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getsetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.getsetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hdel ()](#apidoc.element.ioredis.pipeline.prototype.hdel)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hdelBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hdelBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hexists ()](#apidoc.element.ioredis.pipeline.prototype.hexists)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hexistsBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hexistsBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hget ()](#apidoc.element.ioredis.pipeline.prototype.hget)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hgetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hgetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hgetall ()](#apidoc.element.ioredis.pipeline.prototype.hgetall)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hgetallBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hgetallBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hincrby ()](#apidoc.element.ioredis.pipeline.prototype.hincrby)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hincrbyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hincrbyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hincrbyfloat ()](#apidoc.element.ioredis.pipeline.prototype.hincrbyfloat)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hincrbyfloatBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hincrbyfloatBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hkeys ()](#apidoc.element.ioredis.pipeline.prototype.hkeys)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hkeysBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hkeysBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hlen ()](#apidoc.element.ioredis.pipeline.prototype.hlen)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hlenBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hlenBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hmget ()](#apidoc.element.ioredis.pipeline.prototype.hmget)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hmgetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hmgetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hmset ()](#apidoc.element.ioredis.pipeline.prototype.hmset)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hmsetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hmsetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hscan ()](#apidoc.element.ioredis.pipeline.prototype.hscan)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hscanBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hscanBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hset ()](#apidoc.element.ioredis.pipeline.prototype.hset)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hsetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hsetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hsetnx ()](#apidoc.element.ioredis.pipeline.prototype.hsetnx)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hsetnxBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hsetnxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hstrlen ()](#apidoc.element.ioredis.pipeline.prototype.hstrlen)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hstrlenBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hstrlenBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hvals ()](#apidoc.element.ioredis.pipeline.prototype.hvals)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hvalsBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hvalsBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>incr ()](#apidoc.element.ioredis.pipeline.prototype.incr)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>incrBuffer ()](#apidoc.element.ioredis.pipeline.prototype.incrBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>incrby ()](#apidoc.element.ioredis.pipeline.prototype.incrby)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>incrbyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.incrbyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>incrbyfloat ()](#apidoc.element.ioredis.pipeline.prototype.incrbyfloat)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>incrbyfloatBuffer ()](#apidoc.element.ioredis.pipeline.prototype.incrbyfloatBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>info ()](#apidoc.element.ioredis.pipeline.prototype.info)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>infoBuffer ()](#apidoc.element.ioredis.pipeline.prototype.infoBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>keys ()](#apidoc.element.ioredis.pipeline.prototype.keys)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>keysBuffer ()](#apidoc.element.ioredis.pipeline.prototype.keysBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lastsave ()](#apidoc.element.ioredis.pipeline.prototype.lastsave)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lastsaveBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lastsaveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>latency ()](#apidoc.element.ioredis.pipeline.prototype.latency)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>latencyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.latencyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lindex ()](#apidoc.element.ioredis.pipeline.prototype.lindex)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lindexBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lindexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>linsert ()](#apidoc.element.ioredis.pipeline.prototype.linsert)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>linsertBuffer ()](#apidoc.element.ioredis.pipeline.prototype.linsertBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>llen ()](#apidoc.element.ioredis.pipeline.prototype.llen)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>llenBuffer ()](#apidoc.element.ioredis.pipeline.prototype.llenBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lpop ()](#apidoc.element.ioredis.pipeline.prototype.lpop)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lpopBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lpopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lpush ()](#apidoc.element.ioredis.pipeline.prototype.lpush)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lpushBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lpushBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lpushx ()](#apidoc.element.ioredis.pipeline.prototype.lpushx)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lpushxBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lpushxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lrange ()](#apidoc.element.ioredis.pipeline.prototype.lrange)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lrangeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lrem ()](#apidoc.element.ioredis.pipeline.prototype.lrem)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lremBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lremBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lset ()](#apidoc.element.ioredis.pipeline.prototype.lset)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lsetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lsetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>ltrim ()](#apidoc.element.ioredis.pipeline.prototype.ltrim)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>ltrimBuffer ()](#apidoc.element.ioredis.pipeline.prototype.ltrimBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>memory ()](#apidoc.element.ioredis.pipeline.prototype.memory)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>memoryBuffer ()](#apidoc.element.ioredis.pipeline.prototype.memoryBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>mget ()](#apidoc.element.ioredis.pipeline.prototype.mget)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>mgetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.mgetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>migrate ()](#apidoc.element.ioredis.pipeline.prototype.migrate)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>migrateBuffer ()](#apidoc.element.ioredis.pipeline.prototype.migrateBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>module ()](#apidoc.element.ioredis.pipeline.prototype.module)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>moduleBuffer ()](#apidoc.element.ioredis.pipeline.prototype.moduleBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>move ()](#apidoc.element.ioredis.pipeline.prototype.move)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>moveBuffer ()](#apidoc.element.ioredis.pipeline.prototype.moveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>mset ()](#apidoc.element.ioredis.pipeline.prototype.mset)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>msetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.msetBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>msetnx ()](#apidoc.element.ioredis.pipeline.prototype.msetnx)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>msetnxBuffer ()](#apidoc.element.ioredis.pipeline.prototype.msetnxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>multi ()](#apidoc.element.ioredis.pipeline.prototype.multi)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>multiBuffer ()](#apidoc.element.ioredis.pipeline.prototype.multiBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>object ()](#apidoc.element.ioredis.pipeline.prototype.object)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>objectBuffer ()](#apidoc.element.ioredis.pipeline.prototype.objectBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>persist ()](#apidoc.element.ioredis.pipeline.prototype.persist)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>persistBuffer ()](#apidoc.element.ioredis.pipeline.prototype.persistBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pexpire ()](#apidoc.element.ioredis.pipeline.prototype.pexpire)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pexpireBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pexpireBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pexpireat ()](#apidoc.element.ioredis.pipeline.prototype.pexpireat)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pexpireatBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pexpireatBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfadd ()](#apidoc.element.ioredis.pipeline.prototype.pfadd)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfaddBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pfaddBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfcount ()](#apidoc.element.ioredis.pipeline.prototype.pfcount)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfcountBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pfcountBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfdebug ()](#apidoc.element.ioredis.pipeline.prototype.pfdebug)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfdebugBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pfdebugBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfmerge ()](#apidoc.element.ioredis.pipeline.prototype.pfmerge)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfmergeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pfmergeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfselftest ()](#apidoc.element.ioredis.pipeline.prototype.pfselftest)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfselftestBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pfselftestBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>ping ()](#apidoc.element.ioredis.pipeline.prototype.ping)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pingBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pingBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>post ()](#apidoc.element.ioredis.pipeline.prototype.post)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>postBuffer ()](#apidoc.element.ioredis.pipeline.prototype.postBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>psetex ()](#apidoc.element.ioredis.pipeline.prototype.psetex)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>psetexBuffer ()](#apidoc.element.ioredis.pipeline.prototype.psetexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>psubscribe ()](#apidoc.element.ioredis.pipeline.prototype.psubscribe)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>psubscribeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.psubscribeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>psync ()](#apidoc.element.ioredis.pipeline.prototype.psync)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>psyncBuffer ()](#apidoc.element.ioredis.pipeline.prototype.psyncBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pttl ()](#apidoc.element.ioredis.pipeline.prototype.pttl)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pttlBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pttlBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>publish ()](#apidoc.element.ioredis.pipeline.prototype.publish)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>publishBuffer ()](#apidoc.element.ioredis.pipeline.prototype.publishBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pubsub ()](#apidoc.element.ioredis.pipeline.prototype.pubsub)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pubsubBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pubsubBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>punsubscribe ()](#apidoc.element.ioredis.pipeline.prototype.punsubscribe)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>punsubscribeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.punsubscribeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>quit ()](#apidoc.element.ioredis.pipeline.prototype.quit)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>quitBuffer ()](#apidoc.element.ioredis.pipeline.prototype.quitBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>randomkey ()](#apidoc.element.ioredis.pipeline.prototype.randomkey)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>randomkeyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.randomkeyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>readonly ()](#apidoc.element.ioredis.pipeline.prototype.readonly)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>readonlyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.readonlyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>readwrite ()](#apidoc.element.ioredis.pipeline.prototype.readwrite)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>readwriteBuffer ()](#apidoc.element.ioredis.pipeline.prototype.readwriteBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rename ()](#apidoc.element.ioredis.pipeline.prototype.rename)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>renameBuffer ()](#apidoc.element.ioredis.pipeline.prototype.renameBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>renamenx ()](#apidoc.element.ioredis.pipeline.prototype.renamenx)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>renamenxBuffer ()](#apidoc.element.ioredis.pipeline.prototype.renamenxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>replconf ()](#apidoc.element.ioredis.pipeline.prototype.replconf)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>replconfBuffer ()](#apidoc.element.ioredis.pipeline.prototype.replconfBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>restore ()](#apidoc.element.ioredis.pipeline.prototype.restore)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>restore-asking ()](#apidoc.element.ioredis.pipeline.prototype.restore-asking)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>restore-askingBuffer ()](#apidoc.element.ioredis.pipeline.prototype.restore-askingBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>restoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.restoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>role ()](#apidoc.element.ioredis.pipeline.prototype.role)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>roleBuffer ()](#apidoc.element.ioredis.pipeline.prototype.roleBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpop ()](#apidoc.element.ioredis.pipeline.prototype.rpop)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpopBuffer ()](#apidoc.element.ioredis.pipeline.prototype.rpopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpoplpush ()](#apidoc.element.ioredis.pipeline.prototype.rpoplpush)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpoplpushBuffer ()](#apidoc.element.ioredis.pipeline.prototype.rpoplpushBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpush ()](#apidoc.element.ioredis.pipeline.prototype.rpush)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpushBuffer ()](#apidoc.element.ioredis.pipeline.prototype.rpushBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpushx ()](#apidoc.element.ioredis.pipeline.prototype.rpushx)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpushxBuffer ()](#apidoc.element.ioredis.pipeline.prototype.rpushxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sadd ()](#apidoc.element.ioredis.pipeline.prototype.sadd)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>saddBuffer ()](#apidoc.element.ioredis.pipeline.prototype.saddBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>save ()](#apidoc.element.ioredis.pipeline.prototype.save)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>saveBuffer ()](#apidoc.element.ioredis.pipeline.prototype.saveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>scan ()](#apidoc.element.ioredis.pipeline.prototype.scan)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>scanBuffer ()](#apidoc.element.ioredis.pipeline.prototype.scanBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>scard ()](#apidoc.element.ioredis.pipeline.prototype.scard)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>scardBuffer ()](#apidoc.element.ioredis.pipeline.prototype.scardBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>script ()](#apidoc.element.ioredis.pipeline.prototype.script)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>scriptBuffer ()](#apidoc.element.ioredis.pipeline.prototype.scriptBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sdiff ()](#apidoc.element.ioredis.pipeline.prototype.sdiff)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sdiffBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sdiffBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sdiffstore ()](#apidoc.element.ioredis.pipeline.prototype.sdiffstore)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sdiffstoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sdiffstoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>select ()](#apidoc.element.ioredis.pipeline.prototype.select)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>selectBuffer ()](#apidoc.element.ioredis.pipeline.prototype.selectBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sendCommand (command)](#apidoc.element.ioredis.pipeline.prototype.sendCommand)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>send_command ()](#apidoc.element.ioredis.pipeline.prototype.send_command)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sentinel ()](#apidoc.element.ioredis.pipeline.prototype.sentinel)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sentinelBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sentinelBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>set ()](#apidoc.element.ioredis.pipeline.prototype.set)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setBuffer ()](#apidoc.element.ioredis.pipeline.prototype.setBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setbit ()](#apidoc.element.ioredis.pipeline.prototype.setbit)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setbitBuffer ()](#apidoc.element.ioredis.pipeline.prototype.setbitBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setex ()](#apidoc.element.ioredis.pipeline.prototype.setex)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setexBuffer ()](#apidoc.element.ioredis.pipeline.prototype.setexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setnx ()](#apidoc.element.ioredis.pipeline.prototype.setnx)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setnxBuffer ()](#apidoc.element.ioredis.pipeline.prototype.setnxBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setrange ()](#apidoc.element.ioredis.pipeline.prototype.setrange)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setrangeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.setrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>shutdown ()](#apidoc.element.ioredis.pipeline.prototype.shutdown)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>shutdownBuffer ()](#apidoc.element.ioredis.pipeline.prototype.shutdownBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sinter ()](#apidoc.element.ioredis.pipeline.prototype.sinter)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sinterBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sinterBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sinterstore ()](#apidoc.element.ioredis.pipeline.prototype.sinterstore)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sinterstoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sinterstoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sismember ()](#apidoc.element.ioredis.pipeline.prototype.sismember)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sismemberBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sismemberBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>slaveof ()](#apidoc.element.ioredis.pipeline.prototype.slaveof)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>slaveofBuffer ()](#apidoc.element.ioredis.pipeline.prototype.slaveofBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>slowlog ()](#apidoc.element.ioredis.pipeline.prototype.slowlog)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>slowlogBuffer ()](#apidoc.element.ioredis.pipeline.prototype.slowlogBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>smembers ()](#apidoc.element.ioredis.pipeline.prototype.smembers)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>smembersBuffer ()](#apidoc.element.ioredis.pipeline.prototype.smembersBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>smove ()](#apidoc.element.ioredis.pipeline.prototype.smove)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>smoveBuffer ()](#apidoc.element.ioredis.pipeline.prototype.smoveBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sort ()](#apidoc.element.ioredis.pipeline.prototype.sort)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sortBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sortBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>spop ()](#apidoc.element.ioredis.pipeline.prototype.spop)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>spopBuffer ()](#apidoc.element.ioredis.pipeline.prototype.spopBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>srandmember ()](#apidoc.element.ioredis.pipeline.prototype.srandmember)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>srandmemberBuffer ()](#apidoc.element.ioredis.pipeline.prototype.srandmemberBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>srem ()](#apidoc.element.ioredis.pipeline.prototype.srem)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sremBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sremBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sscan ()](#apidoc.element.ioredis.pipeline.prototype.sscan)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sscanBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sscanBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>strlen ()](#apidoc.element.ioredis.pipeline.prototype.strlen)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>strlenBuffer ()](#apidoc.element.ioredis.pipeline.prototype.strlenBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>subscribe ()](#apidoc.element.ioredis.pipeline.prototype.subscribe)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>subscribeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.subscribeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>substr ()](#apidoc.element.ioredis.pipeline.prototype.substr)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>substrBuffer ()](#apidoc.element.ioredis.pipeline.prototype.substrBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sunion ()](#apidoc.element.ioredis.pipeline.prototype.sunion)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sunionBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sunionBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sunionstore ()](#apidoc.element.ioredis.pipeline.prototype.sunionstore)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sunionstoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sunionstoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>swapdb ()](#apidoc.element.ioredis.pipeline.prototype.swapdb)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>swapdbBuffer ()](#apidoc.element.ioredis.pipeline.prototype.swapdbBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sync ()](#apidoc.element.ioredis.pipeline.prototype.sync)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>syncBuffer ()](#apidoc.element.ioredis.pipeline.prototype.syncBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>time ()](#apidoc.element.ioredis.pipeline.prototype.time)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>timeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.timeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>touch ()](#apidoc.element.ioredis.pipeline.prototype.touch)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>touchBuffer ()](#apidoc.element.ioredis.pipeline.prototype.touchBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>ttl ()](#apidoc.element.ioredis.pipeline.prototype.ttl)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>ttlBuffer ()](#apidoc.element.ioredis.pipeline.prototype.ttlBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>type ()](#apidoc.element.ioredis.pipeline.prototype.type)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>typeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.typeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>unlink ()](#apidoc.element.ioredis.pipeline.prototype.unlink)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>unlinkBuffer ()](#apidoc.element.ioredis.pipeline.prototype.unlinkBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>unsubscribe ()](#apidoc.element.ioredis.pipeline.prototype.unsubscribe)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>unsubscribeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.unsubscribeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>unwatch ()](#apidoc.element.ioredis.pipeline.prototype.unwatch)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>unwatchBuffer ()](#apidoc.element.ioredis.pipeline.prototype.unwatchBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>wait ()](#apidoc.element.ioredis.pipeline.prototype.wait)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>waitBuffer ()](#apidoc.element.ioredis.pipeline.prototype.waitBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>watch ()](#apidoc.element.ioredis.pipeline.prototype.watch)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>watchBuffer ()](#apidoc.element.ioredis.pipeline.prototype.watchBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zadd ()](#apidoc.element.ioredis.pipeline.prototype.zadd)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zaddBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zaddBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zcard ()](#apidoc.element.ioredis.pipeline.prototype.zcard)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zcardBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zcardBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zcount ()](#apidoc.element.ioredis.pipeline.prototype.zcount)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zcountBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zcountBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zincrby ()](#apidoc.element.ioredis.pipeline.prototype.zincrby)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zincrbyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zincrbyBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zinterstore ()](#apidoc.element.ioredis.pipeline.prototype.zinterstore)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zinterstoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zinterstoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zlexcount ()](#apidoc.element.ioredis.pipeline.prototype.zlexcount)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zlexcountBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zlexcountBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrange ()](#apidoc.element.ioredis.pipeline.prototype.zrange)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrangeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrangebylex ()](#apidoc.element.ioredis.pipeline.prototype.zrangebylex)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrangebylexBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrangebylexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrangebyscore ()](#apidoc.element.ioredis.pipeline.prototype.zrangebyscore)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrangebyscoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrangebyscoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrank ()](#apidoc.element.ioredis.pipeline.prototype.zrank)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrankBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrankBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrem ()](#apidoc.element.ioredis.pipeline.prototype.zrem)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zremBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremrangebylex ()](#apidoc.element.ioredis.pipeline.prototype.zremrangebylex)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremrangebylexBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zremrangebylexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremrangebyrank ()](#apidoc.element.ioredis.pipeline.prototype.zremrangebyrank)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremrangebyrankBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zremrangebyrankBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremrangebyscore ()](#apidoc.element.ioredis.pipeline.prototype.zremrangebyscore)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremrangebyscoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zremrangebyscoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrange ()](#apidoc.element.ioredis.pipeline.prototype.zrevrange)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrangeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrevrangeBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrangebylex ()](#apidoc.element.ioredis.pipeline.prototype.zrevrangebylex)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrangebylexBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrevrangebylexBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrangebyscore ()](#apidoc.element.ioredis.pipeline.prototype.zrevrangebyscore)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrangebyscoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrevrangebyscoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrank ()](#apidoc.element.ioredis.pipeline.prototype.zrevrank)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrankBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrevrankBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zscan ()](#apidoc.element.ioredis.pipeline.prototype.zscan)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zscanBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zscanBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zscore ()](#apidoc.element.ioredis.pipeline.prototype.zscore)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zscoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zscoreBuffer)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zunionstore ()](#apidoc.element.ioredis.pipeline.prototype.zunionstore)
1.  [function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zunionstoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zunionstoreBuffer)

#### [module ioredis.scan_stream](#apidoc.module.ioredis.scan_stream)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>scan_stream (opt)](#apidoc.element.ioredis.scan_stream.scan_stream)
1.  [function <span class="apidocSignatureSpan">ioredis.scan_stream.</span>super_ (options)](#apidoc.element.ioredis.scan_stream.super_)

#### [module ioredis.scan_stream.prototype](#apidoc.module.ioredis.scan_stream.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.scan_stream.prototype.</span>_read ()](#apidoc.element.ioredis.scan_stream.prototype._read)
1.  [function <span class="apidocSignatureSpan">ioredis.scan_stream.prototype.</span>close ()](#apidoc.element.ioredis.scan_stream.prototype.close)

#### [module ioredis.script](#apidoc.module.ioredis.script)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>script (lua, numberOfKeys, keyPrefix)](#apidoc.element.ioredis.script.script)

#### [module ioredis.script.prototype](#apidoc.module.ioredis.script.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.script.prototype.</span>execute (container, args, options, callback)](#apidoc.element.ioredis.script.prototype.execute)

#### [module ioredis.subscription_set](#apidoc.module.ioredis.subscription_set)
1.  [function <span class="apidocSignatureSpan">ioredis.</span>subscription_set ()](#apidoc.element.ioredis.subscription_set.subscription_set)

#### [module ioredis.subscription_set.prototype](#apidoc.module.ioredis.subscription_set.prototype)
1.  [function <span class="apidocSignatureSpan">ioredis.subscription_set.prototype.</span>add (set, channel)](#apidoc.element.ioredis.subscription_set.prototype.add)
1.  [function <span class="apidocSignatureSpan">ioredis.subscription_set.prototype.</span>channels (set)](#apidoc.element.ioredis.subscription_set.prototype.channels)
1.  [function <span class="apidocSignatureSpan">ioredis.subscription_set.prototype.</span>del (set, channel)](#apidoc.element.ioredis.subscription_set.prototype.del)
1.  [function <span class="apidocSignatureSpan">ioredis.subscription_set.prototype.</span>isEmpty ()](#apidoc.element.ioredis.subscription_set.prototype.isEmpty)

#### [module ioredis.transaction](#apidoc.module.ioredis.transaction)
1.  [function <span class="apidocSignatureSpan">ioredis.transaction.</span>addTransactionSupport (redis)](#apidoc.element.ioredis.transaction.addTransactionSupport)



# <a name="apidoc.module.ioredis"></a>[module ioredis](#apidoc.module.ioredis)

#### <a name="apidoc.element.ioredis.Cluster"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Cluster (startupNodes, options)](#apidoc.element.ioredis.Cluster)
- description and source-code
```javascript
function Cluster(startupNodes, options) {
  EventEmitter.call(this);
  Commander.call(this);

  this.options = _.defaults(this.options, options, Cluster.defaultOptions);

  // validate options
  if (typeof this.options.scaleReads !== 'function' &&
      ['all', 'master', 'slave'].indexOf(this.options.scaleReads) === -1) {
    throw new Error('Invalid option scaleReads "' + this.options.scaleReads +
      '". Expected "all", "master", "slave" or a custom function');
  }

  this.connectionPool = new ConnectionPool(this.options.redisOptions);
  this.startupNodes = startupNodes;

  var _this = this;
  this.connectionPool.on('-node', function (redis) {
    if (_this.status !== 'disconnecting' && _this.subscriber === redis) {
      _this.selectSubscriber();
    }
    _this.emit('-node', redis);
  });
  this.connectionPool.on('+node', function (redis) {
    _this.emit('+node', redis);
  });
  this.connectionPool.on('drain', function () {
    _this.setStatus('close');
  });
  this.connectionPool.on('nodeError', function (error) {
    _this.emit('node error', error);
  });

  this.slots = [];
  this.retryAttempts = 0;

  this.resetOfflineQueue();
  this.delayQueue = new DelayQueue();

  this.subscriber = null;

  if (this.options.lazyConnect) {
    this.setStatus('wait');
  } else {
    this.connect().catch(_.noop);
  }
}
```
- example usage
```shell
...
## Cluster
Redis Cluster provides a way to run a Redis installation where data is automatically sharded across multiple Redis nodes.
You can connect to a Redis Cluster like this:

'''javascript
var Redis = require('ioredis');

var cluster = new Redis.Cluster([{
  port: 6380,
  host: '127.0.0.1'
}, {
  port: 6381,
  host: '127.0.0.1'
}]);
...
```

#### <a name="apidoc.element.ioredis.Command"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Command (name, args, options, callback)](#apidoc.element.ioredis.Command)
- description and source-code
```javascript
function Command(name, args, options, callback) {
  if (typeof options === 'undefined') {
    options = {};
  }
  this.name = name;
  this.replyEncoding = options.replyEncoding;
  this.errorStack = options.errorStack;
  this.args = args ? _.flatten(args) : [];
  this.callback = callback;
  this.initPromise();

  var keyPrefix = options.keyPrefix;
  if (keyPrefix) {
    this._iterateKeys(function (key) {
      return keyPrefix + key;
    });
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Promise (executor)](#apidoc.element.ioredis.Promise)
- description and source-code
```javascript
function Promise(executor) {
    if (executor !== INTERNAL) {
        check(this, executor);
    }
    this._bitField = 0;
    this._fulfillmentHandler0 = undefined;
    this._rejectionHandler0 = undefined;
    this._promise0 = undefined;
    this._receiver0 = undefined;
    this._resolveFromExecutor(executor);
    this._promiseCreated();
    this._fireEvent("promiseCreated", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Promise.AggregateError (message)](#apidoc.element.ioredis.Promise.AggregateError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.CancellationError"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Promise.CancellationError (message)](#apidoc.element.ioredis.Promise.CancellationError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.OperationalError"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Promise.OperationalError (message)](#apidoc.element.ioredis.Promise.OperationalError)
- description and source-code
```javascript
function OperationalError(message) {
    if (!(this instanceof OperationalError))
        return new OperationalError(message);
    notEnumerableProp(this, "name", "OperationalError");
    notEnumerableProp(this, "message", message);
    this.cause = message;
    this["isOperational"] = true;

    if (message instanceof Error) {
        notEnumerableProp(this, "message", message.message);
        notEnumerableProp(this, "stack", message.stack);
    } else if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    }

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.PromiseInspection"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Promise.PromiseInspection (promise)](#apidoc.element.ioredis.Promise.PromiseInspection)
- description and source-code
```javascript
function PromiseInspection(promise) {
    if (promise !== undefined) {
        promise = promise._target();
        this._bitField = promise._bitField;
        this._settledValueField = promise._isFateSealed()
            ? promise._settledValue() : undefined;
    }
    else {
        this._bitField = 0;
        this._settledValueField = undefined;
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.TimeoutError"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Promise.TimeoutError (message)](#apidoc.element.ioredis.Promise.TimeoutError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Promise._SomePromiseArray (values)](#apidoc.element.ioredis.Promise._SomePromiseArray)
- description and source-code
```javascript
function SomePromiseArray(values) {
    this.constructor$(values);
    this._howMany = 0;
    this._unwrap = false;
    this._initialized = false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.coroutine"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Promise.coroutine (generatorFunction, options)](#apidoc.element.ioredis.Promise.coroutine)
- description and source-code
```javascript
Promise.coroutine = function (generatorFunction, options) {
    if (typeof generatorFunction !== "function") {
        throw new TypeError("generatorFunction must be a function\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }
    var yieldHandler = Object(options).yieldHandler;
    var PromiseSpawn$ = PromiseSpawn;
    var stack = new Error().stack;
    return function () {
        var generator = generatorFunction.apply(this, arguments);
        var spawn = new PromiseSpawn$(undefined, undefined, yieldHandler,
                                      stack);
        var ret = spawn.promise();
        spawn._generator = generator;
        spawn._promiseFulfilled(undefined);
        return ret;
    };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.ReplyError"></a>[function <span class="apidocSignatureSpan">ioredis.</span>ReplyError (message)](#apidoc.element.ioredis.ReplyError)
- description and source-code
```javascript
function ReplyError(message) {
  Error.captureStackTrace(this, this.constructor);
  this.name = this.constructor.name;
  this.message = message;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.ReplyError.super_"></a>[function <span class="apidocSignatureSpan">ioredis.</span>ReplyError.super_ ()](#apidoc.element.ioredis.ReplyError.super_)
- description and source-code
```javascript
function Error() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander"></a>[function <span class="apidocSignatureSpan">ioredis.</span>commander ()](#apidoc.element.ioredis.commander)
- description and source-code
```javascript
function Commander() {
  this.options = _.defaults({}, this.options || {}, {
    showFriendlyErrorStack: false
  });
  this.scriptsSet = {};
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.createClient"></a>[function <span class="apidocSignatureSpan">ioredis.</span>createClient ()](#apidoc.element.ioredis.createClient)
- description and source-code
```javascript
createClient = function () {
  return Redis.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline"></a>[function <span class="apidocSignatureSpan">ioredis.</span>pipeline (redis)](#apidoc.element.ioredis.pipeline)
- description and source-code
```javascript
function Pipeline(redis) {
  Commander.call(this);

  this.redis = redis;
  this.isCluster = this.redis.constructor.name === 'Cluster';
  this.options = redis.options;
  this._queue = [];
  this._result = [];
  this._transactions = 0;
  this._shaToScript = {};

  var _this = this;
  _.keys(redis.scriptsSet).forEach(function (name) {
    var script = redis.scriptsSet[name];
    _this._shaToScript[script.sha] = script;
    _this[name] = redis[name];
    _this[name + 'Buffer'] = redis[name + 'Buffer'];
  });

  this.promise = new Promise(function (resolve, reject) {
    _this.resolve = resolve;
    _this.reject = reject;
  });
}
```
- example usage
```shell
...
});
'''

## Pipelining
If you want to send a batch of commands (e.g. > 5), you can use pipelining to queue
the commands in memory and then send them to Redis all at once. This way the performance improves by 50%~300% (See [benchmark section
](#benchmark)).

'redis.pipeline()' creates a 'Pipeline' instance. You can call any Redis
commands on it just like the 'Redis' instance. The commands are queued in memory
and flushed to Redis by calling the 'exec' method:

'''javascript
var pipeline = redis.pipeline();
pipeline.set('foo', 'bar');
pipeline.del('cc');
...
```

#### <a name="apidoc.element.ioredis.print"></a>[function <span class="apidocSignatureSpan">ioredis.</span>print (err, reply)](#apidoc.element.ioredis.print)
- description and source-code
```javascript
print = function (err, reply) {
  if (err) {
    console.log('Error: ' + err);
  } else {
    console.log('Reply: ' + reply);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.scan_stream"></a>[function <span class="apidocSignatureSpan">ioredis.</span>scan_stream (opt)](#apidoc.element.ioredis.scan_stream)
- description and source-code
```javascript
function ScanStream(opt) {
  Readable.call(this, opt);
  this._redisCursor = '0';
  this.opt = opt;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.script"></a>[function <span class="apidocSignatureSpan">ioredis.</span>script (lua, numberOfKeys, keyPrefix)](#apidoc.element.ioredis.script)
- description and source-code
```javascript
function Script(lua, numberOfKeys, keyPrefix) {
  this.lua = lua;
  this.sha = crypto.createHash('sha1').update(this.lua).digest('hex');
  this.numberOfKeys = typeof numberOfKeys === 'number' ? numberOfKeys : null;
  this.keyPrefix = keyPrefix ? keyPrefix : '';
}
```
- example usage
```shell
...
}

var _this = this;
if (!scripts.length) {
  return execPipeline();
}

return this.redis.script('exists', scripts.map(function (item) {
  return item.sha;
})).then(function (results) {
  var pending = [];
  for (var i = 0; i < results.length; ++i) {
    if (!results[i]) {
      pending.push(scripts[i]);
    }
...
```

#### <a name="apidoc.element.ioredis.subscription_set"></a>[function <span class="apidocSignatureSpan">ioredis.</span>subscription_set ()](#apidoc.element.ioredis.subscription_set)
- description and source-code
```javascript
function SubscriptionSet() {
  this.set = {
    subscribe: {},
    psubscribe: {}
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.super_"></a>[function <span class="apidocSignatureSpan">ioredis.</span>super_ ()](#apidoc.element.ioredis.super_)
- description and source-code
```javascript
function EventEmitter() {
  EventEmitter.init.call(this);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Cluster"></a>[module ioredis.Cluster](#apidoc.module.ioredis.Cluster)

#### <a name="apidoc.element.ioredis.Cluster.Cluster"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Cluster (startupNodes, options)](#apidoc.element.ioredis.Cluster.Cluster)
- description and source-code
```javascript
function Cluster(startupNodes, options) {
  EventEmitter.call(this);
  Commander.call(this);

  this.options = _.defaults(this.options, options, Cluster.defaultOptions);

  // validate options
  if (typeof this.options.scaleReads !== 'function' &&
      ['all', 'master', 'slave'].indexOf(this.options.scaleReads) === -1) {
    throw new Error('Invalid option scaleReads "' + this.options.scaleReads +
      '". Expected "all", "master", "slave" or a custom function');
  }

  this.connectionPool = new ConnectionPool(this.options.redisOptions);
  this.startupNodes = startupNodes;

  var _this = this;
  this.connectionPool.on('-node', function (redis) {
    if (_this.status !== 'disconnecting' && _this.subscriber === redis) {
      _this.selectSubscriber();
    }
    _this.emit('-node', redis);
  });
  this.connectionPool.on('+node', function (redis) {
    _this.emit('+node', redis);
  });
  this.connectionPool.on('drain', function () {
    _this.setStatus('close');
  });
  this.connectionPool.on('nodeError', function (error) {
    _this.emit('node error', error);
  });

  this.slots = [];
  this.retryAttempts = 0;

  this.resetOfflineQueue();
  this.delayQueue = new DelayQueue();

  this.subscriber = null;

  if (this.options.lazyConnect) {
    this.setStatus('wait');
  } else {
    this.connect().catch(_.noop);
  }
}
```
- example usage
```shell
...
## Cluster
Redis Cluster provides a way to run a Redis installation where data is automatically sharded across multiple Redis nodes.
You can connect to a Redis Cluster like this:

'''javascript
var Redis = require('ioredis');

var cluster = new Redis.Cluster([{
  port: 6380,
  host: '127.0.0.1'
}, {
  port: 6381,
  host: '127.0.0.1'
}]);
...
```

#### <a name="apidoc.element.ioredis.Cluster.super_"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.</span>super_ ()](#apidoc.element.ioredis.Cluster.super_)
- description and source-code
```javascript
function EventEmitter() {
  EventEmitter.init.call(this);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Cluster.defaultOptions"></a>[module ioredis.Cluster.defaultOptions](#apidoc.module.ioredis.Cluster.defaultOptions)

#### <a name="apidoc.element.ioredis.Cluster.defaultOptions.clusterRetryStrategy"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.defaultOptions.</span>clusterRetryStrategy (times)](#apidoc.element.ioredis.Cluster.defaultOptions.clusterRetryStrategy)
- description and source-code
```javascript
clusterRetryStrategy = function (times) {
  return Math.min(100 + times * 2, 2000);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Cluster.prototype"></a>[module ioredis.Cluster.prototype](#apidoc.module.ioredis.Cluster.prototype)

#### <a name="apidoc.element.ioredis.Cluster.prototype._handleCloseEvent"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>_handleCloseEvent ()](#apidoc.element.ioredis.Cluster.prototype._handleCloseEvent)
- description and source-code
```javascript
_handleCloseEvent = function () {
  var retryDelay;
  if (!this.manuallyClosing && typeof this.options.clusterRetryStrategy === 'function') {
    retryDelay = this.options.clusterRetryStrategy.call(this, ++this.retryAttempts);
  }
  if (typeof retryDelay === 'number') {
    this.setStatus('reconnecting');
    this.reconnectTimeout = setTimeout(function () {
      this.reconnectTimeout = null;
      debug('Cluster is disconnected. Retrying after %dms', retryDelay);
      this.connect().catch(_.noop);
    }.bind(this), retryDelay);
  } else {
    this.setStatus('end');
    this.flushQueue(new Error('None of startup nodes is available'));
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype._readyCheck"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>_readyCheck (callback)](#apidoc.element.ioredis.Cluster.prototype._readyCheck)
- description and source-code
```javascript
_readyCheck = function (callback) {
  this.cluster('info', function (err, res) {
    if (err) {
      return callback(err);
    }
    if (typeof res !== 'string') {
      return callback();
    }

    var state;
    var lines = res.split('\r\n');
    for (var i = 0; i < lines.length; ++i) {
      var parts = lines[i].split(':');
      if (parts[0] === 'cluster_state') {
        state = parts[1];
        break;
      }
    }

    if (state === 'fail') {
      debug('cluster state not ok (%s)', state);
      callback(null, state);
    } else {
      callback();
    }
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.append"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>append ()](#apidoc.element.ioredis.Cluster.prototype.append)
- description and source-code
```javascript
append = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.appendBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>appendBuffer ()](#apidoc.element.ioredis.Cluster.prototype.appendBuffer)
- description and source-code
```javascript
appendBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.asking"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>asking ()](#apidoc.element.ioredis.Cluster.prototype.asking)
- description and source-code
```javascript
asking = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.askingBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>askingBuffer ()](#apidoc.element.ioredis.Cluster.prototype.askingBuffer)
- description and source-code
```javascript
askingBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.auth"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>auth ()](#apidoc.element.ioredis.Cluster.prototype.auth)
- description and source-code
```javascript
auth = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.authBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>authBuffer ()](#apidoc.element.ioredis.Cluster.prototype.authBuffer)
- description and source-code
```javascript
authBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.bgrewriteaof"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bgrewriteaof ()](#apidoc.element.ioredis.Cluster.prototype.bgrewriteaof)
- description and source-code
```javascript
bgrewriteaof = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.bgrewriteaofBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bgrewriteaofBuffer ()](#apidoc.element.ioredis.Cluster.prototype.bgrewriteaofBuffer)
- description and source-code
```javascript
bgrewriteaofBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.bgsave"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bgsave ()](#apidoc.element.ioredis.Cluster.prototype.bgsave)
- description and source-code
```javascript
bgsave = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.bgsaveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bgsaveBuffer ()](#apidoc.element.ioredis.Cluster.prototype.bgsaveBuffer)
- description and source-code
```javascript
bgsaveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.bitcount"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitcount ()](#apidoc.element.ioredis.Cluster.prototype.bitcount)
- description and source-code
```javascript
bitcount = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.bitcountBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitcountBuffer ()](#apidoc.element.ioredis.Cluster.prototype.bitcountBuffer)
- description and source-code
```javascript
bitcountBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.bitfield"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitfield ()](#apidoc.element.ioredis.Cluster.prototype.bitfield)
- description and source-code
```javascript
bitfield = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.bitfieldBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitfieldBuffer ()](#apidoc.element.ioredis.Cluster.prototype.bitfieldBuffer)
- description and source-code
```javascript
bitfieldBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.bitop"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitop ()](#apidoc.element.ioredis.Cluster.prototype.bitop)
- description and source-code
```javascript
bitop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.bitopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitopBuffer ()](#apidoc.element.ioredis.Cluster.prototype.bitopBuffer)
- description and source-code
```javascript
bitopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.bitpos"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitpos ()](#apidoc.element.ioredis.Cluster.prototype.bitpos)
- description and source-code
```javascript
bitpos = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.bitposBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>bitposBuffer ()](#apidoc.element.ioredis.Cluster.prototype.bitposBuffer)
- description and source-code
```javascript
bitposBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.blpop"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>blpop ()](#apidoc.element.ioredis.Cluster.prototype.blpop)
- description and source-code
```javascript
blpop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.blpopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>blpopBuffer ()](#apidoc.element.ioredis.Cluster.prototype.blpopBuffer)
- description and source-code
```javascript
blpopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.brpop"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>brpop ()](#apidoc.element.ioredis.Cluster.prototype.brpop)
- description and source-code
```javascript
brpop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.brpopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>brpopBuffer ()](#apidoc.element.ioredis.Cluster.prototype.brpopBuffer)
- description and source-code
```javascript
brpopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.brpoplpush"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>brpoplpush ()](#apidoc.element.ioredis.Cluster.prototype.brpoplpush)
- description and source-code
```javascript
brpoplpush = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.brpoplpushBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>brpoplpushBuffer ()](#apidoc.element.ioredis.Cluster.prototype.brpoplpushBuffer)
- description and source-code
```javascript
brpoplpushBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.call"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>call ()](#apidoc.element.ioredis.Cluster.prototype.call)
- description and source-code
```javascript
call = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
var fbuffer = require('flexbuffer');
var Promise = require('bluebird');
var util = require('util');
var commands = require('redis-commands');
var calculateSlot = require('cluster-key-slot');

function Pipeline(redis) {
Commander.call(this);

this.redis = redis;
this.isCluster = this.redis.constructor.name === 'Cluster';
this.options = redis.options;
this._queue = [];
this._result = [];
this._transactions = 0;
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.callBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>callBuffer ()](#apidoc.element.ioredis.Cluster.prototype.callBuffer)
- description and source-code
```javascript
callBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.client"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>client ()](#apidoc.element.ioredis.Cluster.prototype.client)
- description and source-code
```javascript
client = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.clientBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>clientBuffer ()](#apidoc.element.ioredis.Cluster.prototype.clientBuffer)
- description and source-code
```javascript
clientBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.cluster"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>cluster ()](#apidoc.element.ioredis.Cluster.prototype.cluster)
- description and source-code
```javascript
cluster = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.clusterBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>clusterBuffer ()](#apidoc.element.ioredis.Cluster.prototype.clusterBuffer)
- description and source-code
```javascript
clusterBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.command"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>command ()](#apidoc.element.ioredis.Cluster.prototype.command)
- description and source-code
```javascript
command = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.commandBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>commandBuffer ()](#apidoc.element.ioredis.Cluster.prototype.commandBuffer)
- description and source-code
```javascript
commandBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.config"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>config ()](#apidoc.element.ioredis.Cluster.prototype.config)
- description and source-code
```javascript
config = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.configBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>configBuffer ()](#apidoc.element.ioredis.Cluster.prototype.configBuffer)
- description and source-code
```javascript
configBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.connect"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>connect ()](#apidoc.element.ioredis.Cluster.prototype.connect)
- description and source-code
```javascript
connect = function () {
  function readyHandler() {
    this.setStatus('ready');
    this.retryAttempts = 0;
    this.executeOfflineCommands();
  }

  return new Promise(function (resolve, reject) {
    if (this.status === 'connecting' || this.status === 'connect' || this.status === 'ready') {
      reject(new Error('Redis is already connecting/connected'));
      return;
    }
    this.setStatus('connecting');

    if (!Array.isArray(this.startupNodes) || this.startupNodes.length === 0) {
      throw new Error(''startupNodes' should contain at least one node.');
    }

    this.connectionPool.reset(this.startupNodes);

    var closeListener;
    var refreshListener = function () {
      this.removeListener('close', closeListener);
      this.manuallyClosing = false;
      this.setStatus('connect');
      if (this.options.enableReadyCheck) {
        this._readyCheck(function (err, fail) {
          if (err || fail) {
            this.disconnect(true);
          } else {
            readyHandler.call(this);
          }
        }.bind(this));
      } else {
        readyHandler.call(this);
      }
      resolve();
    };

    closeListener = function () {
      this.removeListener('refresh', refreshListener);
      reject(new Error('None of startup nodes is available'));
    };

    this.once('refresh', refreshListener);
    this.once('close', closeListener);
    this.once('close', this._handleCloseEvent.bind(this));

    this.refreshSlotsCache(function (err) {
      if (err && err.message === 'Failed to refresh slots cache.') {
        Redis.prototype.silentEmit.call(this, 'error', err);
        this.connectionPool.reset([]);
      }
    }.bind(this));
    this.selectSubscriber();
  }.bind(this));
}
```
- example usage
```shell
...
});
'''

'retryStrategy' is a function that will be called when the connection is lost.
The argument 'times' means this is the nth reconnection being made and
the return value represents how long (in ms) to wait to reconnect. When the
return value isn't a number, ioredis will stop trying to reconnect, and the connection
will be lost forever if the user doesn't call 'redis.connect()' manually.

When reconnected, the client will auto subscribe to channels that the previous connection subscribed to.
This behavior can be disabled by setting the 'autoResubscribe' option to 'false'.

And if the previous connection has some unfulfilled commands (most likely blocking commands such as 'brpop' and 'blpop'),
the client will resend them when reconnected. This behavior can be disabled by setting the 'autoResendUnfulfilledCommands' option
 to 'false'.
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.createBuiltinCommand"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>createBuiltinCommand (commandName)](#apidoc.element.ioredis.Cluster.prototype.createBuiltinCommand)
- description and source-code
```javascript
createBuiltinCommand = function (commandName) {
  return {
    string: generateFunction(commandName, 'utf8'),
    buffer: generateFunction(commandName, null)
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.dbsize"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>dbsize ()](#apidoc.element.ioredis.Cluster.prototype.dbsize)
- description and source-code
```javascript
dbsize = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.dbsizeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>dbsizeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.dbsizeBuffer)
- description and source-code
```javascript
dbsizeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.debug"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>debug ()](#apidoc.element.ioredis.Cluster.prototype.debug)
- description and source-code
```javascript
debug = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.debugBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>debugBuffer ()](#apidoc.element.ioredis.Cluster.prototype.debugBuffer)
- description and source-code
```javascript
debugBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.decr"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>decr ()](#apidoc.element.ioredis.Cluster.prototype.decr)
- description and source-code
```javascript
decr = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.decrBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>decrBuffer ()](#apidoc.element.ioredis.Cluster.prototype.decrBuffer)
- description and source-code
```javascript
decrBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.decrby"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>decrby ()](#apidoc.element.ioredis.Cluster.prototype.decrby)
- description and source-code
```javascript
decrby = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.decrbyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>decrbyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.decrbyBuffer)
- description and source-code
```javascript
decrbyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.defineCommand"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>defineCommand (name, definition)](#apidoc.element.ioredis.Cluster.prototype.defineCommand)
- description and source-code
```javascript
defineCommand = function (name, definition) {
  var script = new Script(definition.lua, definition.numberOfKeys,
                          this.options.keyPrefix);
  this.scriptsSet[name] = script;
  this[name] = generateScriptingFunction(script, 'utf8');
  this[name + 'Buffer'] = generateScriptingFunction(script, null);
}
```
- example usage
```shell
...
care of script caching and to detect when to use 'EVAL' and when to use 'EVALSHA'.
ioredis expose a 'defineCommand' method to make scripting much easier to use:

'''javascript
var redis = new Redis();

// This will define a command echo:
redis.defineCommand('echo', {
  numberOfKeys: 2,
  lua: 'return {KEYS[1],KEYS[2],ARGV[1],ARGV[2]}'
});

// Now 'echo' can be used just like any other ordinary command,
// and ioredis will try to use 'EVALSHA' internally when possible for better performance.
redis.echo('k1', 'k2', 'a1', 'a2', function (err, result) {
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.del"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>del ()](#apidoc.element.ioredis.Cluster.prototype.del)
- description and source-code
```javascript
del = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
'redis.pipeline()' creates a 'Pipeline' instance. You can call any Redis
commands on it just like the 'Redis' instance. The commands are queued in memory
and flushed to Redis by calling the 'exec' method:

'''javascript
var pipeline = redis.pipeline();
pipeline.set('foo', 'bar');
pipeline.del('cc');
pipeline.exec(function (err, results) {
  // 'err' is always null, and 'results' is an array of responses
  // corresponding to the sequence of queued commands.
  // Each response follows the format '[err, result]'.
});

// You can even chain the commands:
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.delBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>delBuffer ()](#apidoc.element.ioredis.Cluster.prototype.delBuffer)
- description and source-code
```javascript
delBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.discard"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>discard ()](#apidoc.element.ioredis.Cluster.prototype.discard)
- description and source-code
```javascript
discard = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.discardBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>discardBuffer ()](#apidoc.element.ioredis.Cluster.prototype.discardBuffer)
- description and source-code
```javascript
discardBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.disconnect"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>disconnect (reconnect)](#apidoc.element.ioredis.Cluster.prototype.disconnect)
- description and source-code
```javascript
disconnect = function (reconnect) {
  var status = this.status;
  this.setStatus('disconnecting');

  if (!reconnect) {
    this.manuallyClosing = true;
  }
  if (this.reconnectTimeout) {
    clearTimeout(this.reconnectTimeout);
    this.reconnectTimeout = null;
  }

  if (status === 'wait') {
    this.setStatus('close');
    this._handleCloseEvent();
  } else {
    this.connectionPool.reset([]);
  }
}
```
- example usage
```shell
...
});
'''

You can learn more from the [Redis documentation](http://redis.io/commands/scan).

## Auto-reconnect
By default, ioredis will try to reconnect when the connection to Redis is lost
except when the connection is closed manually by 'redis.disconnect()' or 'redis.quit()'.

It's very flexible to control how long to wait to reconnect after disconnection
using the 'retryStrategy' option:

'''javascript
var redis = new Redis({
// This is the default value of 'retryStrategy'
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.dump"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>dump ()](#apidoc.element.ioredis.Cluster.prototype.dump)
- description and source-code
```javascript
dump = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.dumpBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>dumpBuffer ()](#apidoc.element.ioredis.Cluster.prototype.dumpBuffer)
- description and source-code
```javascript
dumpBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.echo"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>echo ()](#apidoc.element.ioredis.Cluster.prototype.echo)
- description and source-code
```javascript
echo = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
redis.defineCommand('echo', {
  numberOfKeys: 2,
  lua: 'return {KEYS[1],KEYS[2],ARGV[1],ARGV[2]}'
});

// Now 'echo' can be used just like any other ordinary command,
// and ioredis will try to use 'EVALSHA' internally when possible for better performance.
redis.echo('k1', 'k2', 'a1', 'a2', function (err, result) {
  // result === ['k1', 'k2', 'a1', 'a2']
});

// 'echoBuffer' is also defined automatically to return buffers instead of strings:
redis.echoBuffer('k1', 'k2', 'a1', 'a2', function (err, result) {
  // result[0] === new Buffer('k1');
});
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.echoBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>echoBuffer ()](#apidoc.element.ioredis.Cluster.prototype.echoBuffer)
- description and source-code
```javascript
echoBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
// Now 'echo' can be used just like any other ordinary command,
// and ioredis will try to use 'EVALSHA' internally when possible for better performance.
redis.echo('k1', 'k2', 'a1', 'a2', function (err, result) {
  // result === ['k1', 'k2', 'a1', 'a2']
});

// 'echoBuffer' is also defined automatically to return buffers instead of strings:
redis.echoBuffer('k1', 'k2', 'a1', 'a2', function (err, result) {
  // result[0] === new Buffer('k1');
});

// And of course it works with pipeline:
redis.pipeline().set('foo', 'bar').echo('k1', 'k2', 'a1', 'a2').exec();
'''
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.eval"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>eval ()](#apidoc.element.ioredis.Cluster.prototype.eval)
- description and source-code
```javascript
eval = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.evalBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>evalBuffer ()](#apidoc.element.ioredis.Cluster.prototype.evalBuffer)
- description and source-code
```javascript
evalBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.evalsha"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>evalsha ()](#apidoc.element.ioredis.Cluster.prototype.evalsha)
- description and source-code
```javascript
evalsha = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.evalshaBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>evalshaBuffer ()](#apidoc.element.ioredis.Cluster.prototype.evalshaBuffer)
- description and source-code
```javascript
evalshaBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.exec"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>exec (callback)](#apidoc.element.ioredis.Cluster.prototype.exec)
- description and source-code
```javascript
exec = function (callback) {
  return exec.call(this).then(function (results) {
    if (Array.isArray(results)) {
      results = utils.wrapMultiResult(results);
    }
    return results;
  }).nodeify(callback);
}
```
- example usage
```shell
...
commands on it just like the 'Redis' instance. The commands are queued in memory
and flushed to Redis by calling the 'exec' method:

'''javascript
var pipeline = redis.pipeline();
pipeline.set('foo', 'bar');
pipeline.del('cc');
pipeline.exec(function (err, results) {
  // 'err' is always null, and 'results' is an array of responses
  // corresponding to the sequence of queued commands.
  // Each response follows the format '[err, result]'.
});

// You can even chain the commands:
redis.pipeline().set('foo', 'bar').del('cc').exec(function (err, results) {
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.execBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>execBuffer ()](#apidoc.element.ioredis.Cluster.prototype.execBuffer)
- description and source-code
```javascript
execBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.executeOfflineCommands"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>executeOfflineCommands ()](#apidoc.element.ioredis.Cluster.prototype.executeOfflineCommands)
- description and source-code
```javascript
executeOfflineCommands = function () {
  if (this.offlineQueue.length) {
    debug('send %d commands in offline queue', this.offlineQueue.length);
    var offlineQueue = this.offlineQueue;
    this.resetOfflineQueue();
    while (offlineQueue.length > 0) {
      var item = offlineQueue.shift();
      this.sendCommand(item.command, item.stream, item.node);
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.exists"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>exists ()](#apidoc.element.ioredis.Cluster.prototype.exists)
- description and source-code
```javascript
exists = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
        message: error.message
      };
    } else if (commonError.name !== error.name || commonError.message !== error.message) {
      retriable = false;
      break;
    }
  } else if (!inTransaction) {
    var isReadOnly = commands.exists(command.name) && commands.hasFlag(command.name, 'readonly');
    if (!isReadOnly) {
      retriable = false;
      break;
    }
  }
}
if (commonError && retriable) {
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.existsBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>existsBuffer ()](#apidoc.element.ioredis.Cluster.prototype.existsBuffer)
- description and source-code
```javascript
existsBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.expire"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>expire ()](#apidoc.element.ioredis.Cluster.prototype.expire)
- description and source-code
```javascript
expire = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.expireBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>expireBuffer ()](#apidoc.element.ioredis.Cluster.prototype.expireBuffer)
- description and source-code
```javascript
expireBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.expireat"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>expireat ()](#apidoc.element.ioredis.Cluster.prototype.expireat)
- description and source-code
```javascript
expireat = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.expireatBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>expireatBuffer ()](#apidoc.element.ioredis.Cluster.prototype.expireatBuffer)
- description and source-code
```javascript
expireatBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.flushQueue"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>flushQueue (error)](#apidoc.element.ioredis.Cluster.prototype.flushQueue)
- description and source-code
```javascript
flushQueue = function (error) {
  var item;
  while (this.offlineQueue.length > 0) {
    item = this.offlineQueue.shift();
    item.command.reject(error);
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.flushall"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>flushall ()](#apidoc.element.ioredis.Cluster.prototype.flushall)
- description and source-code
```javascript
flushall = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.flushallBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>flushallBuffer ()](#apidoc.element.ioredis.Cluster.prototype.flushallBuffer)
- description and source-code
```javascript
flushallBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.flushdb"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>flushdb ()](#apidoc.element.ioredis.Cluster.prototype.flushdb)
- description and source-code
```javascript
flushdb = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...

'Cluster#nodes()' accepts a parameter role, which can be "master", "slave" and "all" (default), and returns an array of 'Redis'
instance. For example:

'''javascript
// Send 'FLUSHDB' command to all slaves:
var slaves = cluster.nodes('slave');
Promise.all(slaves.map(function (node) {
  return node.flushdb();
}));

// Get keys of all the masters:
var masters = cluster.nodes('master');
Promise.all(masters.map(function (node) {
  return node.keys();
})).then(function (keys) {
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.flushdbBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>flushdbBuffer ()](#apidoc.element.ioredis.Cluster.prototype.flushdbBuffer)
- description and source-code
```javascript
flushdbBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.geoadd"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geoadd ()](#apidoc.element.ioredis.Cluster.prototype.geoadd)
- description and source-code
```javascript
geoadd = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.geoaddBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geoaddBuffer ()](#apidoc.element.ioredis.Cluster.prototype.geoaddBuffer)
- description and source-code
```javascript
geoaddBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.geodist"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geodist ()](#apidoc.element.ioredis.Cluster.prototype.geodist)
- description and source-code
```javascript
geodist = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.geodistBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geodistBuffer ()](#apidoc.element.ioredis.Cluster.prototype.geodistBuffer)
- description and source-code
```javascript
geodistBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.geohash"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geohash ()](#apidoc.element.ioredis.Cluster.prototype.geohash)
- description and source-code
```javascript
geohash = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.geohashBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geohashBuffer ()](#apidoc.element.ioredis.Cluster.prototype.geohashBuffer)
- description and source-code
```javascript
geohashBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.geopos"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geopos ()](#apidoc.element.ioredis.Cluster.prototype.geopos)
- description and source-code
```javascript
geopos = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.geoposBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>geoposBuffer ()](#apidoc.element.ioredis.Cluster.prototype.geoposBuffer)
- description and source-code
```javascript
geoposBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.georadius"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>georadius ()](#apidoc.element.ioredis.Cluster.prototype.georadius)
- description and source-code
```javascript
georadius = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.georadiusBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>georadiusBuffer ()](#apidoc.element.ioredis.Cluster.prototype.georadiusBuffer)
- description and source-code
```javascript
georadiusBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.georadiusbymember"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>georadiusbymember ()](#apidoc.element.ioredis.Cluster.prototype.georadiusbymember)
- description and source-code
```javascript
georadiusbymember = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.georadiusbymemberBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>georadiusbymemberBuffer ()](#apidoc.element.ioredis.Cluster.prototype.georadiusbymemberBuffer)
- description and source-code
```javascript
georadiusbymemberBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.get"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>get ()](#apidoc.element.ioredis.Cluster.prototype.get)
- description and source-code
```javascript
get = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
## Basic Usage

'''javascript
var Redis = require('ioredis');
var redis = new Redis();

redis.set('foo', 'bar');
redis.get('foo', function (err, result) {
  console.log(result);
});

// Or using a promise if the last argument isn't a function
redis.get('foo').then(function (result) {
  console.log(result);
});
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.getBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getBuffer ()](#apidoc.element.ioredis.Cluster.prototype.getBuffer)
- description and source-code
```javascript
getBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
redis.set('foo', new Buffer('bar'));
'''

And every command has a method that returns a Buffer (by adding a suffix of "Buffer" to the command name).
To get a buffer instead of a utf8 string:

'''javascript
redis.getBuffer('foo', function (err, result) {
  // result is a buffer.
});
'''

## Pipelining
If you want to send a batch of commands (e.g. > 5), you can use pipelining to queue
the commands in memory and then send them to Redis all at once. This way the performance improves by 50%~300% (See [benchmark section
](#benchmark)).
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.getBuiltinCommands"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getBuiltinCommands ()](#apidoc.element.ioredis.Cluster.prototype.getBuiltinCommands)
- description and source-code
```javascript
getBuiltinCommands = function () {
  return _.clone(commands);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.getInfoFromNode"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getInfoFromNode (redis, callback)](#apidoc.element.ioredis.Cluster.prototype.getInfoFromNode)
- description and source-code
```javascript
getInfoFromNode = function (redis, callback) {
  if (!redis) {
    return callback(new Error('Node is disconnected'));
  }
  var _this = this;
  redis.cluster('slots', utils.timeout(function (err, result) {
    if (err) {
      redis.disconnect();
      return callback(err);
    }
    var nodes = [];

    for (var i = 0; i < result.length; ++i) {
      var items = result[i];
      var slotRangeStart = items[0];
      var slotRangeEnd = items[1];

      var keys = [];
      for (var j = 2; j < items.length; j++) {
        items[j] = { host: items[j][0], port: items[j][1] };
        items[j].readOnly = j !== 2;
        nodes.push(items[j]);
        keys.push(items[j].host + ':' + items[j].port);
      }

      for (var slot = slotRangeStart; slot <= slotRangeEnd; slot++) {
        _this.slots[slot] = keys;
      }
    }

    _this.connectionPool.reset(nodes);
    callback();
  }, 1000));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.getbit"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getbit ()](#apidoc.element.ioredis.Cluster.prototype.getbit)
- description and source-code
```javascript
getbit = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.getbitBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getbitBuffer ()](#apidoc.element.ioredis.Cluster.prototype.getbitBuffer)
- description and source-code
```javascript
getbitBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.getrange"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getrange ()](#apidoc.element.ioredis.Cluster.prototype.getrange)
- description and source-code
```javascript
getrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.getrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getrangeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.getrangeBuffer)
- description and source-code
```javascript
getrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.getset"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getset ()](#apidoc.element.ioredis.Cluster.prototype.getset)
- description and source-code
```javascript
getset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.getsetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>getsetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.getsetBuffer)
- description and source-code
```javascript
getsetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.handleError"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>handleError (error, ttl, handlers)](#apidoc.element.ioredis.Cluster.prototype.handleError)
- description and source-code
```javascript
handleError = function (error, ttl, handlers) {
  if (typeof ttl.value === 'undefined') {
    ttl.value = this.options.maxRedirections;
  } else {
    ttl.value -= 1;
  }
  if (ttl.value <= 0) {
    handlers.maxRedirections(new Error('Too many Cluster redirections. Last error: ' + error));
    return;
  }
  var errv = error.message.split(' ');
  if (errv[0] === 'MOVED' || errv[0] === 'ASK') {
    handlers[errv[0] === 'MOVED' ? 'moved' : 'ask'](errv[1], errv[2]);
  } else if (errv[0] === 'TRYAGAIN') {
    this.delayQueue.push('tryagain', handlers.tryagain, {
      timeout: this.options.retryDelayOnTryAgain
    });
  } else if (errv[0] === 'CLUSTERDOWN' && this.options.retryDelayOnClusterDown > 0) {
    this.delayQueue.push('clusterdown', handlers.connectionClosed, {
      timeout: this.options.retryDelayOnClusterDown,
      callback: this.refreshSlotsCache.bind(this)
    });
  } else if (error.message === utils.CONNECTION_CLOSED_ERROR_MSG && this.options.retryDelayOnFailover > 0) {
    this.delayQueue.push('failover', handlers.connectionClosed, {
      timeout: this.options.retryDelayOnFailover,
      callback: this.refreshSlotsCache.bind(this)
    });
  } else {
    handlers.defaults();
  }
}
```
- example usage
```shell
...
var matched = true;
if (typeof this.leftRedirections === 'undefined') {
  this.leftRedirections = {};
}
var exec = function () {
  _this.exec();
};
this.redis.handleError(commonError, this.leftRedirections, {
  moved: function (slot, key) {
    _this.preferKey = key;
    _this.redis.slots[errv[1]] = [key];
    _this.redis.refreshSlotsCache();
    _this.exec();
  },
  ask: function (slot, key) {
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hdel"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hdel ()](#apidoc.element.ioredis.Cluster.prototype.hdel)
- description and source-code
```javascript
hdel = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hdelBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hdelBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hdelBuffer)
- description and source-code
```javascript
hdelBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hexists"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hexists ()](#apidoc.element.ioredis.Cluster.prototype.hexists)
- description and source-code
```javascript
hexists = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hexistsBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hexistsBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hexistsBuffer)
- description and source-code
```javascript
hexistsBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hget"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hget ()](#apidoc.element.ioredis.Cluster.prototype.hget)
- description and source-code
```javascript
hget = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hgetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hgetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hgetBuffer)
- description and source-code
```javascript
hgetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hgetall"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hgetall ()](#apidoc.element.ioredis.Cluster.prototype.hgetall)
- description and source-code
```javascript
hgetall = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hgetallBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hgetallBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hgetallBuffer)
- description and source-code
```javascript
hgetallBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hincrby"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hincrby ()](#apidoc.element.ioredis.Cluster.prototype.hincrby)
- description and source-code
```javascript
hincrby = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hincrbyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hincrbyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hincrbyBuffer)
- description and source-code
```javascript
hincrbyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hincrbyfloat"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hincrbyfloat ()](#apidoc.element.ioredis.Cluster.prototype.hincrbyfloat)
- description and source-code
```javascript
hincrbyfloat = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hincrbyfloatBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hincrbyfloatBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hincrbyfloatBuffer)
- description and source-code
```javascript
hincrbyfloatBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hkeys"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hkeys ()](#apidoc.element.ioredis.Cluster.prototype.hkeys)
- description and source-code
```javascript
hkeys = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hkeysBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hkeysBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hkeysBuffer)
- description and source-code
```javascript
hkeysBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hlen"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hlen ()](#apidoc.element.ioredis.Cluster.prototype.hlen)
- description and source-code
```javascript
hlen = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hlenBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hlenBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hlenBuffer)
- description and source-code
```javascript
hlenBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hmget"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hmget ()](#apidoc.element.ioredis.Cluster.prototype.hmget)
- description and source-code
```javascript
hmget = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hmgetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hmgetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hmgetBuffer)
- description and source-code
```javascript
hmgetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hmset"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hmset ()](#apidoc.element.ioredis.Cluster.prototype.hmset)
- description and source-code
```javascript
hmset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hmsetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hmsetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hmsetBuffer)
- description and source-code
```javascript
hmsetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hscan"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hscan ()](#apidoc.element.ioredis.Cluster.prototype.hscan)
- description and source-code
```javascript
hscan = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hscanBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hscanBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hscanBuffer)
- description and source-code
```javascript
hscanBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hscanBufferStream"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hscanBufferStream (key, options)](#apidoc.element.ioredis.Cluster.prototype.hscanBufferStream)
- description and source-code
```javascript
hscanBufferStream = function (key, options) {
  return new ScanStream(_.defaults({
    objectMode: true,
    key: key,
    redis: this,
    command: command
  }, options));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hscanStream"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hscanStream (key, options)](#apidoc.element.ioredis.Cluster.prototype.hscanStream)
- description and source-code
```javascript
hscanStream = function (key, options) {
  return new ScanStream(_.defaults({
    objectMode: true,
    key: key,
    redis: this,
    command: command
  }, options));
}
```
- example usage
```shell
...
Just like other commands, 'scanStream' has a binary version 'scanBufferStream', which returns an array of buffers. It's useful when
the key names are not utf8 strings.

There are also 'hscanStream', 'zscanStream' and 'sscanStream' to iterate through elements in a hash, zset and set. The interface
 of each is
similar to 'scanStream' except the first argument is the key name:

'''javascript
var stream = redis.hscanStream('myhash', {
  match: 'age:??'
});
'''

You can learn more from the [Redis documentation](http://redis.io/commands/scan).

## Auto-reconnect
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hset"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hset ()](#apidoc.element.ioredis.Cluster.prototype.hset)
- description and source-code
```javascript
hset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hsetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hsetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hsetBuffer)
- description and source-code
```javascript
hsetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hsetnx"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hsetnx ()](#apidoc.element.ioredis.Cluster.prototype.hsetnx)
- description and source-code
```javascript
hsetnx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hsetnxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hsetnxBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hsetnxBuffer)
- description and source-code
```javascript
hsetnxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hstrlen"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hstrlen ()](#apidoc.element.ioredis.Cluster.prototype.hstrlen)
- description and source-code
```javascript
hstrlen = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hstrlenBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hstrlenBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hstrlenBuffer)
- description and source-code
```javascript
hstrlenBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hvals"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hvals ()](#apidoc.element.ioredis.Cluster.prototype.hvals)
- description and source-code
```javascript
hvals = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.hvalsBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>hvalsBuffer ()](#apidoc.element.ioredis.Cluster.prototype.hvalsBuffer)
- description and source-code
```javascript
hvalsBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.incr"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>incr ()](#apidoc.element.ioredis.Cluster.prototype.incr)
- description and source-code
```javascript
incr = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.incrBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>incrBuffer ()](#apidoc.element.ioredis.Cluster.prototype.incrBuffer)
- description and source-code
```javascript
incrBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.incrby"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>incrby ()](#apidoc.element.ioredis.Cluster.prototype.incrby)
- description and source-code
```javascript
incrby = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.incrbyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>incrbyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.incrbyBuffer)
- description and source-code
```javascript
incrbyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.incrbyfloat"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>incrbyfloat ()](#apidoc.element.ioredis.Cluster.prototype.incrbyfloat)
- description and source-code
```javascript
incrbyfloat = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.incrbyfloatBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>incrbyfloatBuffer ()](#apidoc.element.ioredis.Cluster.prototype.incrbyfloatBuffer)
- description and source-code
```javascript
incrbyfloatBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.info"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>info ()](#apidoc.element.ioredis.Cluster.prototype.info)
- description and source-code
```javascript
info = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.infoBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>infoBuffer ()](#apidoc.element.ioredis.Cluster.prototype.infoBuffer)
- description and source-code
```javascript
infoBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.keys"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>keys ()](#apidoc.element.ioredis.Cluster.prototype.keys)
- description and source-code
```javascript
keys = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
Promise.all(slaves.map(function (node) {
  return node.flushdb();
}));

// Get keys of all the masters:
var masters = cluster.nodes('master');
Promise.all(masters.map(function (node) {
  return node.keys();
})).then(function (keys) {
  // keys: [['key1', 'key2'], ['key3', 'key4']]
});
'''

### Transaction and pipeline in Cluster mode
Almost all features that are supported by 'Redis' are also supported by 'Redis.Cluster', e.g. custom commands, transaction and pipeline
.
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.keysBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>keysBuffer ()](#apidoc.element.ioredis.Cluster.prototype.keysBuffer)
- description and source-code
```javascript
keysBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lastsave"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lastsave ()](#apidoc.element.ioredis.Cluster.prototype.lastsave)
- description and source-code
```javascript
lastsave = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lastsaveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lastsaveBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lastsaveBuffer)
- description and source-code
```javascript
lastsaveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.latency"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>latency ()](#apidoc.element.ioredis.Cluster.prototype.latency)
- description and source-code
```javascript
latency = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.latencyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>latencyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.latencyBuffer)
- description and source-code
```javascript
latencyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lindex"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lindex ()](#apidoc.element.ioredis.Cluster.prototype.lindex)
- description and source-code
```javascript
lindex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lindexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lindexBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lindexBuffer)
- description and source-code
```javascript
lindexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.linsert"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>linsert ()](#apidoc.element.ioredis.Cluster.prototype.linsert)
- description and source-code
```javascript
linsert = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.linsertBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>linsertBuffer ()](#apidoc.element.ioredis.Cluster.prototype.linsertBuffer)
- description and source-code
```javascript
linsertBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.llen"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>llen ()](#apidoc.element.ioredis.Cluster.prototype.llen)
- description and source-code
```javascript
llen = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.llenBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>llenBuffer ()](#apidoc.element.ioredis.Cluster.prototype.llenBuffer)
- description and source-code
```javascript
llenBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lpop"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lpop ()](#apidoc.element.ioredis.Cluster.prototype.lpop)
- description and source-code
```javascript
lpop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lpopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lpopBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lpopBuffer)
- description and source-code
```javascript
lpopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lpush"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lpush ()](#apidoc.element.ioredis.Cluster.prototype.lpush)
- description and source-code
```javascript
lpush = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lpushBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lpushBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lpushBuffer)
- description and source-code
```javascript
lpushBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lpushx"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lpushx ()](#apidoc.element.ioredis.Cluster.prototype.lpushx)
- description and source-code
```javascript
lpushx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lpushxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lpushxBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lpushxBuffer)
- description and source-code
```javascript
lpushxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lrange"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lrange ()](#apidoc.element.ioredis.Cluster.prototype.lrange)
- description and source-code
```javascript
lrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lrangeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lrangeBuffer)
- description and source-code
```javascript
lrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lrem"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lrem ()](#apidoc.element.ioredis.Cluster.prototype.lrem)
- description and source-code
```javascript
lrem = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lremBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lremBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lremBuffer)
- description and source-code
```javascript
lremBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lset"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lset ()](#apidoc.element.ioredis.Cluster.prototype.lset)
- description and source-code
```javascript
lset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.lsetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>lsetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.lsetBuffer)
- description and source-code
```javascript
lsetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.ltrim"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>ltrim ()](#apidoc.element.ioredis.Cluster.prototype.ltrim)
- description and source-code
```javascript
ltrim = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.ltrimBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>ltrimBuffer ()](#apidoc.element.ioredis.Cluster.prototype.ltrimBuffer)
- description and source-code
```javascript
ltrimBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.memory"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>memory ()](#apidoc.element.ioredis.Cluster.prototype.memory)
- description and source-code
```javascript
memory = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.memoryBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>memoryBuffer ()](#apidoc.element.ioredis.Cluster.prototype.memoryBuffer)
- description and source-code
```javascript
memoryBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.mget"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>mget ()](#apidoc.element.ioredis.Cluster.prototype.mget)
- description and source-code
```javascript
mget = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.mgetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>mgetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.mgetBuffer)
- description and source-code
```javascript
mgetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.migrate"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>migrate ()](#apidoc.element.ioredis.Cluster.prototype.migrate)
- description and source-code
```javascript
migrate = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.migrateBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>migrateBuffer ()](#apidoc.element.ioredis.Cluster.prototype.migrateBuffer)
- description and source-code
```javascript
migrateBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.module"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>module ()](#apidoc.element.ioredis.Cluster.prototype.module)
- description and source-code
```javascript
module = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.moduleBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>moduleBuffer ()](#apidoc.element.ioredis.Cluster.prototype.moduleBuffer)
- description and source-code
```javascript
moduleBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.move"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>move ()](#apidoc.element.ioredis.Cluster.prototype.move)
- description and source-code
```javascript
move = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.moveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>moveBuffer ()](#apidoc.element.ioredis.Cluster.prototype.moveBuffer)
- description and source-code
```javascript
moveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.mset"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>mset ()](#apidoc.element.ioredis.Cluster.prototype.mset)
- description and source-code
```javascript
mset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
'''

There are three built-in transformers, two argument transformers for 'hmset' & 'mset' and
a reply transformer for 'hgetall'. Transformers for 'hmset' and 'hgetall' were mentioned
above, and the transformer for 'mset' is similar to the one for 'hmset':

'''javascript
redis.mset({ k1: 'v1', k2: 'v2' });
redis.get('k1', function (err, result) {
// result === 'v1';
});

redis.mset(new Map([['k3', 'v3'], ['k4', 'v4']]));
redis.get('k3', function (err, result) {
// result === 'v3';
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.msetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>msetBuffer ()](#apidoc.element.ioredis.Cluster.prototype.msetBuffer)
- description and source-code
```javascript
msetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.msetnx"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>msetnx ()](#apidoc.element.ioredis.Cluster.prototype.msetnx)
- description and source-code
```javascript
msetnx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.msetnxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>msetnxBuffer ()](#apidoc.element.ioredis.Cluster.prototype.msetnxBuffer)
- description and source-code
```javascript
msetnxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.multi"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>multi (commands, options)](#apidoc.element.ioredis.Cluster.prototype.multi)
- description and source-code
```javascript
multi = function (commands, options) {
  if (typeof options === 'undefined' && !Array.isArray(commands)) {
    options = commands;
    commands = null;
  }
  if (options && options.pipeline === false) {
    return multi.call(this);
  }
  var pipeline = new Pipeline(this);
  pipeline.multi();
  if (Array.isArray(commands)) {
    pipeline.addBatch(commands);
  }
  var exec = pipeline.exec;
  pipeline.exec = function (callback) {
    if (this._transactions > 0) {
      exec.call(pipeline);
    }
    var promise = exec.call(pipeline);
    return promise.then(function (result) {
      var execResult = result[result.length - 1];
      if (execResult[0]) {
        execResult[0].previousErrors = [];
        for (var i = 0; i < result.length - 1; ++i) {
          if (result[i][0]) {
            execResult[0].previousErrors.push(result[i][0]);
          }
        }
        throw execResult[0];
      }
      return utils.wrapMultiResult(execResult[1]);
    }).nodeify(callback);
  };

  var execBuffer = pipeline.execBuffer;
  pipeline.execBuffer = function (callback) {
    if (this._transactions > 0) {
      execBuffer.call(pipeline);
    }
    return pipeline.exec(callback);
  };
  return pipeline;
}
```
- example usage
```shell
...

## Transaction
Most of the time, the transaction commands 'multi' & 'exec' are used together with pipeline.
Therefore, when 'multi' is called, a 'Pipeline' instance is created automatically by default,
so you can use 'multi' just like 'pipeline':

'''javascript
redis.multi().set('foo', 'bar').get('foo').exec(function (err, results) {
  // results === [[null, 'OK'], [null, 'bar']]
});
'''
If there's a syntax error in the transaction's command chain (e.g. wrong number of arguments, wrong command name, etc),
then none of the commands would be executed, and an error is returned:

'''javascript
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.multiBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>multiBuffer ()](#apidoc.element.ioredis.Cluster.prototype.multiBuffer)
- description and source-code
```javascript
multiBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.nodes"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>nodes (role)](#apidoc.element.ioredis.Cluster.prototype.nodes)
- description and source-code
```javascript
nodes = function (role) {
  role = role || 'all';
  if (role !== 'all' && role !== 'master' && role !== 'slave') {
    throw new Error('Invalid role "' + role + '". Expected "all", "master" or "slave"');
  }
  return _.values(this.connectionPool.nodes[role]);
}
```
- example usage
```shell
...

Sometimes you may want to send a command to multiple nodes (masters or slaves) of the cluster, you can get the nodes via 'Cluster
#nodes()' method.

'Cluster#nodes()' accepts a parameter role, which can be "master", "slave" and "all" (default), and returns an array of 'Redis'
instance. For example:

'''javascript
// Send 'FLUSHDB' command to all slaves:
var slaves = cluster.nodes('slave');
Promise.all(slaves.map(function (node) {
  return node.flushdb();
}));

// Get keys of all the masters:
var masters = cluster.nodes('master');
Promise.all(masters.map(function (node) {
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.object"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>object ()](#apidoc.element.ioredis.Cluster.prototype.object)
- description and source-code
```javascript
object = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.objectBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>objectBuffer ()](#apidoc.element.ioredis.Cluster.prototype.objectBuffer)
- description and source-code
```javascript
objectBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.persist"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>persist ()](#apidoc.element.ioredis.Cluster.prototype.persist)
- description and source-code
```javascript
persist = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.persistBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>persistBuffer ()](#apidoc.element.ioredis.Cluster.prototype.persistBuffer)
- description and source-code
```javascript
persistBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pexpire"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pexpire ()](#apidoc.element.ioredis.Cluster.prototype.pexpire)
- description and source-code
```javascript
pexpire = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pexpireBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pexpireBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pexpireBuffer)
- description and source-code
```javascript
pexpireBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pexpireat"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pexpireat ()](#apidoc.element.ioredis.Cluster.prototype.pexpireat)
- description and source-code
```javascript
pexpireat = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pexpireatBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pexpireatBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pexpireatBuffer)
- description and source-code
```javascript
pexpireatBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pfadd"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfadd ()](#apidoc.element.ioredis.Cluster.prototype.pfadd)
- description and source-code
```javascript
pfadd = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pfaddBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfaddBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pfaddBuffer)
- description and source-code
```javascript
pfaddBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pfcount"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfcount ()](#apidoc.element.ioredis.Cluster.prototype.pfcount)
- description and source-code
```javascript
pfcount = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pfcountBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfcountBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pfcountBuffer)
- description and source-code
```javascript
pfcountBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pfdebug"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfdebug ()](#apidoc.element.ioredis.Cluster.prototype.pfdebug)
- description and source-code
```javascript
pfdebug = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pfdebugBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfdebugBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pfdebugBuffer)
- description and source-code
```javascript
pfdebugBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pfmerge"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfmerge ()](#apidoc.element.ioredis.Cluster.prototype.pfmerge)
- description and source-code
```javascript
pfmerge = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pfmergeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfmergeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pfmergeBuffer)
- description and source-code
```javascript
pfmergeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pfselftest"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfselftest ()](#apidoc.element.ioredis.Cluster.prototype.pfselftest)
- description and source-code
```javascript
pfselftest = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pfselftestBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pfselftestBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pfselftestBuffer)
- description and source-code
```javascript
pfselftestBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.ping"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>ping ()](#apidoc.element.ioredis.Cluster.prototype.ping)
- description and source-code
```javascript
ping = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pingBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pingBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pingBuffer)
- description and source-code
```javascript
pingBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pipeline"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pipeline (commands)](#apidoc.element.ioredis.Cluster.prototype.pipeline)
- description and source-code
```javascript
pipeline = function (commands) {
  var pipeline = new Pipeline(this);
  if (Array.isArray(commands)) {
    pipeline.addBatch(commands);
  }
  return pipeline;
}
```
- example usage
```shell
...
});
'''

## Pipelining
If you want to send a batch of commands (e.g. > 5), you can use pipelining to queue
the commands in memory and then send them to Redis all at once. This way the performance improves by 50%~300% (See [benchmark section
](#benchmark)).

'redis.pipeline()' creates a 'Pipeline' instance. You can call any Redis
commands on it just like the 'Redis' instance. The commands are queued in memory
and flushed to Redis by calling the 'exec' method:

'''javascript
var pipeline = redis.pipeline();
pipeline.set('foo', 'bar');
pipeline.del('cc');
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.post"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>post ()](#apidoc.element.ioredis.Cluster.prototype.post)
- description and source-code
```javascript
post = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.postBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>postBuffer ()](#apidoc.element.ioredis.Cluster.prototype.postBuffer)
- description and source-code
```javascript
postBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.psetex"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>psetex ()](#apidoc.element.ioredis.Cluster.prototype.psetex)
- description and source-code
```javascript
psetex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.psetexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>psetexBuffer ()](#apidoc.element.ioredis.Cluster.prototype.psetexBuffer)
- description and source-code
```javascript
psetexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.psubscribe"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>psubscribe ()](#apidoc.element.ioredis.Cluster.prototype.psubscribe)
- description and source-code
```javascript
psubscribe = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
  // Both 'channel' and 'message' are buffers.
});
'''

'PSUBSCRIBE' is also supported in a similar way:

'''javascript
redis.psubscribe('pat?ern', function (err, count) {});
redis.on('pmessage', function (pattern, channel, message) {});
redis.on('pmessageBuffer', function (pattern, channel, message) {});
'''

When a client issues a SUBSCRIBE or PSUBSCRIBE, that connection is put into a "subscriber" mode.
At that point, only commands that modify the subscription set are valid.
When the subscription set is empty, the connection is put back into regular mode.
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.psubscribeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>psubscribeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.psubscribeBuffer)
- description and source-code
```javascript
psubscribeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.psync"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>psync ()](#apidoc.element.ioredis.Cluster.prototype.psync)
- description and source-code
```javascript
psync = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.psyncBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>psyncBuffer ()](#apidoc.element.ioredis.Cluster.prototype.psyncBuffer)
- description and source-code
```javascript
psyncBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pttl"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pttl ()](#apidoc.element.ioredis.Cluster.prototype.pttl)
- description and source-code
```javascript
pttl = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pttlBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pttlBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pttlBuffer)
- description and source-code
```javascript
pttlBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.publish"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>publish ()](#apidoc.element.ioredis.Cluster.prototype.publish)
- description and source-code
```javascript
publish = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
var Redis = require('ioredis');
var redis = new Redis();
var pub = new Redis();
redis.subscribe('news', 'music', function (err, count) {
// Now we are subscribed to both the 'news' and 'music' channels.
// 'count' represents the number of channels we are currently subscribed to.

pub.publish('news', 'Hello world!');
pub.publish('music', 'Hello again!');
});

redis.on('message', function (channel, message) {
// Receive message Hello world! from channel news
// Receive message Hello again! from channel music
console.log('Receive message %s from channel %s', message, channel);
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.publishBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>publishBuffer ()](#apidoc.element.ioredis.Cluster.prototype.publishBuffer)
- description and source-code
```javascript
publishBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pubsub"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pubsub ()](#apidoc.element.ioredis.Cluster.prototype.pubsub)
- description and source-code
```javascript
pubsub = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.pubsubBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>pubsubBuffer ()](#apidoc.element.ioredis.Cluster.prototype.pubsubBuffer)
- description and source-code
```javascript
pubsubBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.punsubscribe"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>punsubscribe ()](#apidoc.element.ioredis.Cluster.prototype.punsubscribe)
- description and source-code
```javascript
punsubscribe = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.punsubscribeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>punsubscribeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.punsubscribeBuffer)
- description and source-code
```javascript
punsubscribeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.quit"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>quit (callback)](#apidoc.element.ioredis.Cluster.prototype.quit)
- description and source-code
```javascript
quit = function (callback) {
  var status = this.status;
  this.setStatus('disconnecting');

  this.manuallyClosing = true;

  if (this.reconnectTimeout) {
    clearTimeout(this.reconnectTimeout);
    this.reconnectTimeout = null;
  }
  if (status === 'wait') {
    var ret = Promise.resolve('OK').nodeify(callback);

    // use setImmediate to make sure "close" event
    // being emitted after quit() is returned
    setImmediate(function () {
      this.setStatus('close');
      this._handleCloseEvent();
    }.bind(this));

    return ret;
  }
  return Promise.all(this.nodes().map(function (node) {
    return node.quit();
  })).then(function () {
    return 'OK';
  }).nodeify(callback);
}
```
- example usage
```shell
...
});
'''

You can learn more from the [Redis documentation](http://redis.io/commands/scan).

## Auto-reconnect
By default, ioredis will try to reconnect when the connection to Redis is lost
except when the connection is closed manually by 'redis.disconnect()' or 'redis.quit()'.

It's very flexible to control how long to wait to reconnect after disconnection
using the 'retryStrategy' option:

'''javascript
var redis = new Redis({
// This is the default value of 'retryStrategy'
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.quitBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>quitBuffer ()](#apidoc.element.ioredis.Cluster.prototype.quitBuffer)
- description and source-code
```javascript
quitBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.randomkey"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>randomkey ()](#apidoc.element.ioredis.Cluster.prototype.randomkey)
- description and source-code
```javascript
randomkey = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.randomkeyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>randomkeyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.randomkeyBuffer)
- description and source-code
```javascript
randomkeyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.readonly"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>readonly ()](#apidoc.element.ioredis.Cluster.prototype.readonly)
- description and source-code
```javascript
readonly = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.readonlyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>readonlyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.readonlyBuffer)
- description and source-code
```javascript
readonlyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.readwrite"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>readwrite ()](#apidoc.element.ioredis.Cluster.prototype.readwrite)
- description and source-code
```javascript
readwrite = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.readwriteBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>readwriteBuffer ()](#apidoc.element.ioredis.Cluster.prototype.readwriteBuffer)
- description and source-code
```javascript
readwriteBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.refreshSlotsCache"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>refreshSlotsCache (callback)](#apidoc.element.ioredis.Cluster.prototype.refreshSlotsCache)
- description and source-code
```javascript
refreshSlotsCache = function (callback) {
  if (this.isRefreshing) {
    if (typeof callback === 'function') {
      process.nextTick(callback);
    }
    return;
  }
  this.isRefreshing = true;

  var _this = this;
  var wrapper = function () {
    _this.isRefreshing = false;
    if (typeof callback === 'function') {
      callback.apply(null, arguments);
    }
  };

  var keys = _.shuffle(Object.keys(this.connectionPool.nodes.all));

  var lastNodeError = null;

  function tryNode(index) {
    if (index === keys.length) {
      var error = new Error('Failed to refresh slots cache.');
      error.lastNodeError = lastNodeError;
      return wrapper(error);
    }
    debug('getting slot cache from %s', keys[index]);
    _this.getInfoFromNode(_this.connectionPool.nodes.all[keys[index]], function (err) {
      if (_this.status === 'end') {
        return wrapper(new Error('Cluster is disconnected.'));
      }
      if (err) {
        _this.emit('node error', err);
        lastNodeError = err;
        tryNode(index + 1);
      } else {
        _this.emit('refresh');
        wrapper();
      }
    });
  }

  tryNode(0);
}
```
- example usage
```shell
...
var exec = function () {
  _this.exec();
};
this.redis.handleError(commonError, this.leftRedirections, {
  moved: function (slot, key) {
    _this.preferKey = key;
    _this.redis.slots[errv[1]] = [key];
    _this.redis.refreshSlotsCache();
    _this.exec();
  },
  ask: function (slot, key) {
    _this.preferKey = key;
    _this.exec();
  },
  tryagain: exec,
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.rename"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rename ()](#apidoc.element.ioredis.Cluster.prototype.rename)
- description and source-code
```javascript
rename = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.renameBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>renameBuffer ()](#apidoc.element.ioredis.Cluster.prototype.renameBuffer)
- description and source-code
```javascript
renameBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.renamenx"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>renamenx ()](#apidoc.element.ioredis.Cluster.prototype.renamenx)
- description and source-code
```javascript
renamenx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.renamenxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>renamenxBuffer ()](#apidoc.element.ioredis.Cluster.prototype.renamenxBuffer)
- description and source-code
```javascript
renamenxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.replconf"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>replconf ()](#apidoc.element.ioredis.Cluster.prototype.replconf)
- description and source-code
```javascript
replconf = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.replconfBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>replconfBuffer ()](#apidoc.element.ioredis.Cluster.prototype.replconfBuffer)
- description and source-code
```javascript
replconfBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.resetOfflineQueue"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>resetOfflineQueue ()](#apidoc.element.ioredis.Cluster.prototype.resetOfflineQueue)
- description and source-code
```javascript
resetOfflineQueue = function () {
  this.offlineQueue = new Deque();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.restore"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>restore ()](#apidoc.element.ioredis.Cluster.prototype.restore)
- description and source-code
```javascript
restore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.restore-asking"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>restore-asking ()](#apidoc.element.ioredis.Cluster.prototype.restore-asking)
- description and source-code
```javascript
restore-asking = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.restore-askingBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>restore-askingBuffer ()](#apidoc.element.ioredis.Cluster.prototype.restore-askingBuffer)
- description and source-code
```javascript
restore-askingBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.restoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>restoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.restoreBuffer)
- description and source-code
```javascript
restoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.role"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>role ()](#apidoc.element.ioredis.Cluster.prototype.role)
- description and source-code
```javascript
role = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.roleBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>roleBuffer ()](#apidoc.element.ioredis.Cluster.prototype.roleBuffer)
- description and source-code
```javascript
roleBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.rpop"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpop ()](#apidoc.element.ioredis.Cluster.prototype.rpop)
- description and source-code
```javascript
rpop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.rpopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpopBuffer ()](#apidoc.element.ioredis.Cluster.prototype.rpopBuffer)
- description and source-code
```javascript
rpopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.rpoplpush"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpoplpush ()](#apidoc.element.ioredis.Cluster.prototype.rpoplpush)
- description and source-code
```javascript
rpoplpush = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.rpoplpushBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpoplpushBuffer ()](#apidoc.element.ioredis.Cluster.prototype.rpoplpushBuffer)
- description and source-code
```javascript
rpoplpushBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.rpush"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpush ()](#apidoc.element.ioredis.Cluster.prototype.rpush)
- description and source-code
```javascript
rpush = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.rpushBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpushBuffer ()](#apidoc.element.ioredis.Cluster.prototype.rpushBuffer)
- description and source-code
```javascript
rpushBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.rpushx"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpushx ()](#apidoc.element.ioredis.Cluster.prototype.rpushx)
- description and source-code
```javascript
rpushx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.rpushxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>rpushxBuffer ()](#apidoc.element.ioredis.Cluster.prototype.rpushxBuffer)
- description and source-code
```javascript
rpushxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sadd"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sadd ()](#apidoc.element.ioredis.Cluster.prototype.sadd)
- description and source-code
```javascript
sadd = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...

// Or using a promise if the last argument isn't a function
redis.get('foo').then(function (result) {
  console.log(result);
});

// Arguments to commands are flattened, so the following are the same:
redis.sadd('set', 1, 3, 5, 7);
redis.sadd('set', [1, 3, 5, 7]);

// All arguments are passed directly to the redis server:
redis.set('key', 100, 'EX', 10);
'''

See the 'examples/' folder for more examples.
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.saddBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>saddBuffer ()](#apidoc.element.ioredis.Cluster.prototype.saddBuffer)
- description and source-code
```javascript
saddBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.save"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>save ()](#apidoc.element.ioredis.Cluster.prototype.save)
- description and source-code
```javascript
save = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.saveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>saveBuffer ()](#apidoc.element.ioredis.Cluster.prototype.saveBuffer)
- description and source-code
```javascript
saveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.scan"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>scan ()](#apidoc.element.ioredis.Cluster.prototype.scan)
- description and source-code
```javascript
scan = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.scanBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>scanBuffer ()](#apidoc.element.ioredis.Cluster.prototype.scanBuffer)
- description and source-code
```javascript
scanBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.scard"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>scard ()](#apidoc.element.ioredis.Cluster.prototype.scard)
- description and source-code
```javascript
scard = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.scardBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>scardBuffer ()](#apidoc.element.ioredis.Cluster.prototype.scardBuffer)
- description and source-code
```javascript
scardBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.script"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>script ()](#apidoc.element.ioredis.Cluster.prototype.script)
- description and source-code
```javascript
script = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
}

var _this = this;
if (!scripts.length) {
  return execPipeline();
}

return this.redis.script('exists', scripts.map(function (item) {
  return item.sha;
})).then(function (results) {
  var pending = [];
  for (var i = 0; i < results.length; ++i) {
    if (!results[i]) {
      pending.push(scripts[i]);
    }
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.scriptBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>scriptBuffer ()](#apidoc.element.ioredis.Cluster.prototype.scriptBuffer)
- description and source-code
```javascript
scriptBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sdiff"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sdiff ()](#apidoc.element.ioredis.Cluster.prototype.sdiff)
- description and source-code
```javascript
sdiff = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sdiffBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sdiffBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sdiffBuffer)
- description and source-code
```javascript
sdiffBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sdiffstore"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sdiffstore ()](#apidoc.element.ioredis.Cluster.prototype.sdiffstore)
- description and source-code
```javascript
sdiffstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sdiffstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sdiffstoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sdiffstoreBuffer)
- description and source-code
```javascript
sdiffstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.select"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>select ()](#apidoc.element.ioredis.Cluster.prototype.select)
- description and source-code
```javascript
select = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.selectBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>selectBuffer ()](#apidoc.element.ioredis.Cluster.prototype.selectBuffer)
- description and source-code
```javascript
selectBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.selectSubscriber"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>selectSubscriber ()](#apidoc.element.ioredis.Cluster.prototype.selectSubscriber)
- description and source-code
```javascript
selectSubscriber = function () {
  this.subscriber = _.sample(this.nodes());
  if (!this.subscriber) {
    return;
  }
  // Re-subscribe previous channels
  var previousChannels = { subscribe: [], psubscribe: [] };
  if (this.lastActiveSubscriber && this.lastActiveSubscriber.prevCondition) {
    var subscriber = this.lastActiveSubscriber.prevCondition.subscriber;
    if (subscriber) {
      previousChannels.subscribe = subscriber.channels('subscribe');
      previousChannels.psubscribe = subscriber.channels('psubscribe');
    }
  }
  var _this = this;
  if (previousChannels.subscribe.length || previousChannels.psubscribe.length) {
    var pending = 0;
    _.forEach(['subscribe', 'psubscribe'], function (type) {
      var channels = previousChannels[type];
      if (channels.length) {
        pending += 1;
        debug('%s %d channels', type, channels.length);
        _this.subscriber[type](channels).then(function () {
          if (!--pending) {
            _this.lastActiveSubscriber = _this.subscriber;
          }
        }).catch(_.noop);
      }
    });
  } else {
    if (this.subscriber.status === 'wait') {
      this.subscriber.connect().catch(_.noop);
    }
    this.lastActiveSubscriber = this.subscriber;
  }
  _.forEach(['message', 'messageBuffer'], function (event) {
    _this.subscriber.on(event, function (arg1, arg2) {
      _this.emit(event, arg1, arg2);
    });
  });
  _.forEach(['pmessage', 'pmessageBuffer'], function (event) {
    _this.subscriber.on(event, function (arg1, arg2, arg3) {
      _this.emit(event, arg1, arg2, arg3);
    });
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sendCommand"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sendCommand (command, stream, node)](#apidoc.element.ioredis.Cluster.prototype.sendCommand)
- description and source-code
```javascript
sendCommand = function (command, stream, node) {
  if (this.status === 'wait') {
    this.connect().catch(_.noop);
  }
  if (this.status === 'end') {
    command.reject(new Error(utils.CONNECTION_CLOSED_ERROR_MSG));
    return command.promise;
  }
  var to = this.options.scaleReads;
  if (to !== 'master') {
    var isCommandReadOnly = commands.exists(command.name) && commands.hasFlag(command.name, 'readonly');
    if (!isCommandReadOnly) {
      to = 'master';
    }
  }

  var targetSlot = node ? node.slot : command.getSlot();
  var ttl = {};
  var _this = this;
  if (!node && !command.__is_reject_overwritten) {
    command.__is_reject_overwritten = true;
    var reject = command.reject;
    command.reject = function (err) {
      var partialTry = _.partial(tryConnection, true);
      _this.handleError(err, ttl, {
        moved: function (slot, key) {
          debug('command %s is moved to %s', command.name, key);
          if (_this.slots[slot]) {
            _this.slots[slot][0] = key;
          } else {
            _this.slots[slot] = [key];
          }
          var splitKey = key.split(':');
          _this.connectionPool.findOrCreate({ host: splitKey[0], port: Number(splitKey[1]) });
          tryConnection();
          _this.refreshSlotsCache();
        },
        ask: function (slot, key) {
          debug('command %s is required to ask %s:%s', command.name, key);
          var splitKey = key.split(':');
          _this.connectionPool.findOrCreate({ host: splitKey[0], port: Number(splitKey[1]) });
          tryConnection(false, key);
        },
        tryagain: partialTry,
        clusterDown: partialTry,
        connectionClosed: partialTry,
        maxRedirections: function (redirectionError) {
          reject.call(command, redirectionError);
        },
        defaults: function () {
          reject.call(command, err);
        }
      });
    };
  }
  tryConnection();

  function tryConnection(random, asking) {
    if (_this.status === 'end') {
      command.reject(new Error('Cluster is ended.'));
      return;
    }
    var redis;
    if (_this.status === 'ready' || (command.name === 'cluster')) {
      if (node && node.redis) {
        redis = node.redis;
      } else if (Command.checkFlag('ENTER_SUBSCRIBER_MODE', command.name) ||
                 Command.checkFlag('EXIT_SUBSCRIBER_MODE', command.name)) {
        redis = _this.subscriber;
      } else {
        if (!random) {
          if (typeof targetSlot === 'number' && _this.slots[targetSlot]) {
            var nodeKeys = _this.slots[targetSlot];
            if (typeof to === 'function') {
              var nodes =
                  nodeKeys
                  .map(function (key) {
                    return _this.connectionPool.nodes.all[key];
                  });
              redis = to(nodes, command);
              if (Array.isArray(redis)) {
                redis = utils.sample(redis);
              }
              if (!redis) {
                redis = nodes[0];
              }
            } else {
              var key;
              if (to === 'all') {
                key = utils.sample(nodeKeys);
              } else if (to === 'slave' && nodeKeys.length > 1) {
                key = utils.sample(nodeKeys, 1);
              } else {
                key = nodeKeys[0];
              }
              redis = _this.connectionPool.nodes.all[key];
            }
          }
          if (asking) {
            redis = _this.connectionPool.nodes.all[asking];
            redis.asking();
          }
        }
        if (!redis) {
          redis = _.sample(_this.connectionPool.nodes[to]) ||
            _.sample(_this.connectionPool.nodes.all);
        }
      }
      if (node && !node.redis) {
        node.redis = redis;
      }
    }
    if (redis) {
      redis.sendCommand(command, stream);
    } else if (_this.options.enableOfflineQueue) {
      _this.offlineQueue.push({
        command: command,
        stream: stream,
        node: node
      });
    } else {
      command.reject(new Error('Cluster isn\'t ready and enableOfflineQueue options is false'));
    } ...
```
- example usage
```shell
...
  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
};
}

function generateScriptingFunction(_script, _encoding) {
return function () {
  var length = arguments.length;
  var lastArgIndex = length - 1;
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.send_command"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>send_command ()](#apidoc.element.ioredis.Cluster.prototype.send_command)
- description and source-code
```javascript
send_command = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sentinel"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sentinel ()](#apidoc.element.ioredis.Cluster.prototype.sentinel)
- description and source-code
```javascript
sentinel = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sentinelBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sentinelBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sentinelBuffer)
- description and source-code
```javascript
sentinelBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.set"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>set ()](#apidoc.element.ioredis.Cluster.prototype.set)
- description and source-code
```javascript
set = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...

## Basic Usage

'''javascript
var Redis = require('ioredis');
var redis = new Redis();

redis.set('foo', 'bar');
redis.get('foo', function (err, result) {
console.log(result);
});

// Or using a promise if the last argument isn't a function
redis.get('foo').then(function (result) {
console.log(result);
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.setBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setBuffer ()](#apidoc.element.ioredis.Cluster.prototype.setBuffer)
- description and source-code
```javascript
setBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.setStatus"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setStatus (status)](#apidoc.element.ioredis.Cluster.prototype.setStatus)
- description and source-code
```javascript
setStatus = function (status) {
  debug('status: %s -> %s', this.status || '[empty]', status);
  this.status = status;
  process.nextTick(this.emit.bind(this, status));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.setbit"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setbit ()](#apidoc.element.ioredis.Cluster.prototype.setbit)
- description and source-code
```javascript
setbit = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.setbitBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setbitBuffer ()](#apidoc.element.ioredis.Cluster.prototype.setbitBuffer)
- description and source-code
```javascript
setbitBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.setex"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setex ()](#apidoc.element.ioredis.Cluster.prototype.setex)
- description and source-code
```javascript
setex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.setexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setexBuffer ()](#apidoc.element.ioredis.Cluster.prototype.setexBuffer)
- description and source-code
```javascript
setexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.setnx"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setnx ()](#apidoc.element.ioredis.Cluster.prototype.setnx)
- description and source-code
```javascript
setnx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.setnxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setnxBuffer ()](#apidoc.element.ioredis.Cluster.prototype.setnxBuffer)
- description and source-code
```javascript
setnxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.setrange"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setrange ()](#apidoc.element.ioredis.Cluster.prototype.setrange)
- description and source-code
```javascript
setrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.setrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>setrangeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.setrangeBuffer)
- description and source-code
```javascript
setrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.shutdown"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>shutdown ()](#apidoc.element.ioredis.Cluster.prototype.shutdown)
- description and source-code
```javascript
shutdown = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.shutdownBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>shutdownBuffer ()](#apidoc.element.ioredis.Cluster.prototype.shutdownBuffer)
- description and source-code
```javascript
shutdownBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sinter"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sinter ()](#apidoc.element.ioredis.Cluster.prototype.sinter)
- description and source-code
```javascript
sinter = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sinterBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sinterBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sinterBuffer)
- description and source-code
```javascript
sinterBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sinterstore"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sinterstore ()](#apidoc.element.ioredis.Cluster.prototype.sinterstore)
- description and source-code
```javascript
sinterstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sinterstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sinterstoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sinterstoreBuffer)
- description and source-code
```javascript
sinterstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sismember"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sismember ()](#apidoc.element.ioredis.Cluster.prototype.sismember)
- description and source-code
```javascript
sismember = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sismemberBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sismemberBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sismemberBuffer)
- description and source-code
```javascript
sismemberBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.slaveof"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>slaveof ()](#apidoc.element.ioredis.Cluster.prototype.slaveof)
- description and source-code
```javascript
slaveof = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.slaveofBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>slaveofBuffer ()](#apidoc.element.ioredis.Cluster.prototype.slaveofBuffer)
- description and source-code
```javascript
slaveofBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.slowlog"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>slowlog ()](#apidoc.element.ioredis.Cluster.prototype.slowlog)
- description and source-code
```javascript
slowlog = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.slowlogBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>slowlogBuffer ()](#apidoc.element.ioredis.Cluster.prototype.slowlogBuffer)
- description and source-code
```javascript
slowlogBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.smembers"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>smembers ()](#apidoc.element.ioredis.Cluster.prototype.smembers)
- description and source-code
```javascript
smembers = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.smembersBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>smembersBuffer ()](#apidoc.element.ioredis.Cluster.prototype.smembersBuffer)
- description and source-code
```javascript
smembersBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.smove"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>smove ()](#apidoc.element.ioredis.Cluster.prototype.smove)
- description and source-code
```javascript
smove = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.smoveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>smoveBuffer ()](#apidoc.element.ioredis.Cluster.prototype.smoveBuffer)
- description and source-code
```javascript
smoveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sort"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sort ()](#apidoc.element.ioredis.Cluster.prototype.sort)
- description and source-code
```javascript
sort = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
  numberOfKeys: 2,
  lua: 'return {KEYS[1],KEYS[2],ARGV[1],ARGV[2]}'
});

// Works well with pipelining/transaction
fooRedis.pipeline()
  // Sends SORT foo:list BY foo:weight_*->fieldname
  .sort('list', 'BY', 'weight_*->fieldname')
  // Supports custom commands
  // Sends EVALSHA xxx foo:k1 foo:k2 a1 a2
  .echo('k1', 'k2', 'a1', 'a2')
  .exec()
'''

## Transforming Arguments & Replies
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sortBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sortBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sortBuffer)
- description and source-code
```javascript
sortBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.spop"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>spop ()](#apidoc.element.ioredis.Cluster.prototype.spop)
- description and source-code
```javascript
spop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.spopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>spopBuffer ()](#apidoc.element.ioredis.Cluster.prototype.spopBuffer)
- description and source-code
```javascript
spopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.srandmember"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>srandmember ()](#apidoc.element.ioredis.Cluster.prototype.srandmember)
- description and source-code
```javascript
srandmember = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.srandmemberBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>srandmemberBuffer ()](#apidoc.element.ioredis.Cluster.prototype.srandmemberBuffer)
- description and source-code
```javascript
srandmemberBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.srem"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>srem ()](#apidoc.element.ioredis.Cluster.prototype.srem)
- description and source-code
```javascript
srem = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sremBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sremBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sremBuffer)
- description and source-code
```javascript
sremBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sscan"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sscan ()](#apidoc.element.ioredis.Cluster.prototype.sscan)
- description and source-code
```javascript
sscan = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sscanBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sscanBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sscanBuffer)
- description and source-code
```javascript
sscanBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sscanBufferStream"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sscanBufferStream (key, options)](#apidoc.element.ioredis.Cluster.prototype.sscanBufferStream)
- description and source-code
```javascript
sscanBufferStream = function (key, options) {
  return new ScanStream(_.defaults({
    objectMode: true,
    key: key,
    redis: this,
    command: command
  }, options));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sscanStream"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sscanStream (key, options)](#apidoc.element.ioredis.Cluster.prototype.sscanStream)
- description and source-code
```javascript
sscanStream = function (key, options) {
  return new ScanStream(_.defaults({
    objectMode: true,
    key: key,
    redis: this,
    command: command
  }, options));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.strlen"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>strlen ()](#apidoc.element.ioredis.Cluster.prototype.strlen)
- description and source-code
```javascript
strlen = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.strlenBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>strlenBuffer ()](#apidoc.element.ioredis.Cluster.prototype.strlenBuffer)
- description and source-code
```javascript
strlenBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.subscribe"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>subscribe ()](#apidoc.element.ioredis.Cluster.prototype.subscribe)
- description and source-code
```javascript
subscribe = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
It subscribes to a channel with one connection
and publishes to that channel with the other:

'''javascript
var Redis = require('ioredis');
var redis = new Redis();
var pub = new Redis();
redis.subscribe('news', 'music', function (err, count) {
  // Now we are subscribed to both the 'news' and 'music' channels.
  // 'count' represents the number of channels we are currently subscribed to.

  pub.publish('news', 'Hello world!');
  pub.publish('music', 'Hello again!');
});
...
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.subscribeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>subscribeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.subscribeBuffer)
- description and source-code
```javascript
subscribeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.substr"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>substr ()](#apidoc.element.ioredis.Cluster.prototype.substr)
- description and source-code
```javascript
substr = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.substrBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>substrBuffer ()](#apidoc.element.ioredis.Cluster.prototype.substrBuffer)
- description and source-code
```javascript
substrBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sunion"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sunion ()](#apidoc.element.ioredis.Cluster.prototype.sunion)
- description and source-code
```javascript
sunion = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sunionBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sunionBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sunionBuffer)
- description and source-code
```javascript
sunionBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sunionstore"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sunionstore ()](#apidoc.element.ioredis.Cluster.prototype.sunionstore)
- description and source-code
```javascript
sunionstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sunionstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sunionstoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.sunionstoreBuffer)
- description and source-code
```javascript
sunionstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.swapdb"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>swapdb ()](#apidoc.element.ioredis.Cluster.prototype.swapdb)
- description and source-code
```javascript
swapdb = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.swapdbBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>swapdbBuffer ()](#apidoc.element.ioredis.Cluster.prototype.swapdbBuffer)
- description and source-code
```javascript
swapdbBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.sync"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>sync ()](#apidoc.element.ioredis.Cluster.prototype.sync)
- description and source-code
```javascript
sync = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.syncBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>syncBuffer ()](#apidoc.element.ioredis.Cluster.prototype.syncBuffer)
- description and source-code
```javascript
syncBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.time"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>time ()](#apidoc.element.ioredis.Cluster.prototype.time)
- description and source-code
```javascript
time = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.timeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>timeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.timeBuffer)
- description and source-code
```javascript
timeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.touch"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>touch ()](#apidoc.element.ioredis.Cluster.prototype.touch)
- description and source-code
```javascript
touch = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.touchBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>touchBuffer ()](#apidoc.element.ioredis.Cluster.prototype.touchBuffer)
- description and source-code
```javascript
touchBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.ttl"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>ttl ()](#apidoc.element.ioredis.Cluster.prototype.ttl)
- description and source-code
```javascript
ttl = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.ttlBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>ttlBuffer ()](#apidoc.element.ioredis.Cluster.prototype.ttlBuffer)
- description and source-code
```javascript
ttlBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.type"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>type ()](#apidoc.element.ioredis.Cluster.prototype.type)
- description and source-code
```javascript
type = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.typeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>typeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.typeBuffer)
- description and source-code
```javascript
typeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.unlink"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>unlink ()](#apidoc.element.ioredis.Cluster.prototype.unlink)
- description and source-code
```javascript
unlink = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.unlinkBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>unlinkBuffer ()](#apidoc.element.ioredis.Cluster.prototype.unlinkBuffer)
- description and source-code
```javascript
unlinkBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.unsubscribe"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>unsubscribe ()](#apidoc.element.ioredis.Cluster.prototype.unsubscribe)
- description and source-code
```javascript
unsubscribe = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.unsubscribeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>unsubscribeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.unsubscribeBuffer)
- description and source-code
```javascript
unsubscribeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.unwatch"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>unwatch ()](#apidoc.element.ioredis.Cluster.prototype.unwatch)
- description and source-code
```javascript
unwatch = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.unwatchBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>unwatchBuffer ()](#apidoc.element.ioredis.Cluster.prototype.unwatchBuffer)
- description and source-code
```javascript
unwatchBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.wait"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>wait ()](#apidoc.element.ioredis.Cluster.prototype.wait)
- description and source-code
```javascript
wait = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.waitBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>waitBuffer ()](#apidoc.element.ioredis.Cluster.prototype.waitBuffer)
- description and source-code
```javascript
waitBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.watch"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>watch ()](#apidoc.element.ioredis.Cluster.prototype.watch)
- description and source-code
```javascript
watch = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.watchBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>watchBuffer ()](#apidoc.element.ioredis.Cluster.prototype.watchBuffer)
- description and source-code
```javascript
watchBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zadd"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zadd ()](#apidoc.element.ioredis.Cluster.prototype.zadd)
- description and source-code
```javascript
zadd = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zaddBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zaddBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zaddBuffer)
- description and source-code
```javascript
zaddBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zcard"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zcard ()](#apidoc.element.ioredis.Cluster.prototype.zcard)
- description and source-code
```javascript
zcard = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zcardBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zcardBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zcardBuffer)
- description and source-code
```javascript
zcardBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zcount"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zcount ()](#apidoc.element.ioredis.Cluster.prototype.zcount)
- description and source-code
```javascript
zcount = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zcountBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zcountBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zcountBuffer)
- description and source-code
```javascript
zcountBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zincrby"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zincrby ()](#apidoc.element.ioredis.Cluster.prototype.zincrby)
- description and source-code
```javascript
zincrby = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zincrbyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zincrbyBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zincrbyBuffer)
- description and source-code
```javascript
zincrbyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zinterstore"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zinterstore ()](#apidoc.element.ioredis.Cluster.prototype.zinterstore)
- description and source-code
```javascript
zinterstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zinterstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zinterstoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zinterstoreBuffer)
- description and source-code
```javascript
zinterstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zlexcount"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zlexcount ()](#apidoc.element.ioredis.Cluster.prototype.zlexcount)
- description and source-code
```javascript
zlexcount = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zlexcountBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zlexcountBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zlexcountBuffer)
- description and source-code
```javascript
zlexcountBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrange"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrange ()](#apidoc.element.ioredis.Cluster.prototype.zrange)
- description and source-code
```javascript
zrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrangeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrangeBuffer)
- description and source-code
```javascript
zrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrangebylex"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrangebylex ()](#apidoc.element.ioredis.Cluster.prototype.zrangebylex)
- description and source-code
```javascript
zrangebylex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrangebylexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrangebylexBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrangebylexBuffer)
- description and source-code
```javascript
zrangebylexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrangebyscore"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrangebyscore ()](#apidoc.element.ioredis.Cluster.prototype.zrangebyscore)
- description and source-code
```javascript
zrangebyscore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrangebyscoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrangebyscoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrangebyscoreBuffer)
- description and source-code
```javascript
zrangebyscoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrank"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrank ()](#apidoc.element.ioredis.Cluster.prototype.zrank)
- description and source-code
```javascript
zrank = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrankBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrankBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrankBuffer)
- description and source-code
```javascript
zrankBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrem"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrem ()](#apidoc.element.ioredis.Cluster.prototype.zrem)
- description and source-code
```javascript
zrem = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zremBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zremBuffer)
- description and source-code
```javascript
zremBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zremrangebylex"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremrangebylex ()](#apidoc.element.ioredis.Cluster.prototype.zremrangebylex)
- description and source-code
```javascript
zremrangebylex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zremrangebylexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremrangebylexBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zremrangebylexBuffer)
- description and source-code
```javascript
zremrangebylexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zremrangebyrank"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremrangebyrank ()](#apidoc.element.ioredis.Cluster.prototype.zremrangebyrank)
- description and source-code
```javascript
zremrangebyrank = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zremrangebyrankBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremrangebyrankBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zremrangebyrankBuffer)
- description and source-code
```javascript
zremrangebyrankBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zremrangebyscore"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremrangebyscore ()](#apidoc.element.ioredis.Cluster.prototype.zremrangebyscore)
- description and source-code
```javascript
zremrangebyscore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zremrangebyscoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zremrangebyscoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zremrangebyscoreBuffer)
- description and source-code
```javascript
zremrangebyscoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrevrange"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrange ()](#apidoc.element.ioredis.Cluster.prototype.zrevrange)
- description and source-code
```javascript
zrevrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrevrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrangeBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrevrangeBuffer)
- description and source-code
```javascript
zrevrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrevrangebylex"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrangebylex ()](#apidoc.element.ioredis.Cluster.prototype.zrevrangebylex)
- description and source-code
```javascript
zrevrangebylex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrevrangebylexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrangebylexBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrevrangebylexBuffer)
- description and source-code
```javascript
zrevrangebylexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrevrangebyscore"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrangebyscore ()](#apidoc.element.ioredis.Cluster.prototype.zrevrangebyscore)
- description and source-code
```javascript
zrevrangebyscore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrevrangebyscoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrangebyscoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrevrangebyscoreBuffer)
- description and source-code
```javascript
zrevrangebyscoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrevrank"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrank ()](#apidoc.element.ioredis.Cluster.prototype.zrevrank)
- description and source-code
```javascript
zrevrank = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zrevrankBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zrevrankBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zrevrankBuffer)
- description and source-code
```javascript
zrevrankBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zscan"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zscan ()](#apidoc.element.ioredis.Cluster.prototype.zscan)
- description and source-code
```javascript
zscan = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zscanBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zscanBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zscanBuffer)
- description and source-code
```javascript
zscanBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zscanBufferStream"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zscanBufferStream (key, options)](#apidoc.element.ioredis.Cluster.prototype.zscanBufferStream)
- description and source-code
```javascript
zscanBufferStream = function (key, options) {
  return new ScanStream(_.defaults({
    objectMode: true,
    key: key,
    redis: this,
    command: command
  }, options));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zscanStream"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zscanStream (key, options)](#apidoc.element.ioredis.Cluster.prototype.zscanStream)
- description and source-code
```javascript
zscanStream = function (key, options) {
  return new ScanStream(_.defaults({
    objectMode: true,
    key: key,
    redis: this,
    command: command
  }, options));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zscore"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zscore ()](#apidoc.element.ioredis.Cluster.prototype.zscore)
- description and source-code
```javascript
zscore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zscoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zscoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zscoreBuffer)
- description and source-code
```javascript
zscoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zunionstore"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zunionstore ()](#apidoc.element.ioredis.Cluster.prototype.zunionstore)
- description and source-code
```javascript
zunionstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Cluster.prototype.zunionstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.Cluster.prototype.</span>zunionstoreBuffer ()](#apidoc.element.ioredis.Cluster.prototype.zunionstoreBuffer)
- description and source-code
```javascript
zunionstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Command"></a>[module ioredis.Command](#apidoc.module.ioredis.Command)

#### <a name="apidoc.element.ioredis.Command.Command"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Command (name, args, options, callback)](#apidoc.element.ioredis.Command.Command)
- description and source-code
```javascript
function Command(name, args, options, callback) {
  if (typeof options === 'undefined') {
    options = {};
  }
  this.name = name;
  this.replyEncoding = options.replyEncoding;
  this.errorStack = options.errorStack;
  this.args = args ? _.flatten(args) : [];
  this.callback = callback;
  this.initPromise();

  var keyPrefix = options.keyPrefix;
  if (keyPrefix) {
    this._iterateKeys(function (key) {
      return keyPrefix + key;
    });
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Command.checkFlag"></a>[function <span class="apidocSignatureSpan">ioredis.Command.</span>checkFlag (flagName, commandName)](#apidoc.element.ioredis.Command.checkFlag)
- description and source-code
```javascript
checkFlag = function (flagName, commandName) {
  return !!flagMap[flagName][commandName];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Command.setArgumentTransformer"></a>[function <span class="apidocSignatureSpan">ioredis.Command.</span>setArgumentTransformer (name, func)](#apidoc.element.ioredis.Command.setArgumentTransformer)
- description and source-code
```javascript
setArgumentTransformer = function (name, func) {
  Command._transformer.argument[name] = func;
}
```
- example usage
```shell
...

// Here's the built-in argument transformer converting
// hmset('key', { k1: 'v1', k2: 'v2' })
// or
// hmset('key', new Map([['k1', 'v1'], ['k2', 'v2']]))
// into
// hmset('key', 'k1', 'v1', 'k2', 'v2')
Redis.Command.setArgumentTransformer('hmset', function (args) {
if (args.length === 2) {
  if (typeof Map !== 'undefined' && args[1] instanceof Map) {
    // utils is a internal module of ioredis
    return [args[0]].concat(utils.convertMapToArray(args[1]));
  }
  if ( typeof args[1] === 'object' && args[1] !== null) {
    return [args[0]].concat(utils.convertObjectToArray(args[1]));
...
```

#### <a name="apidoc.element.ioredis.Command.setReplyTransformer"></a>[function <span class="apidocSignatureSpan">ioredis.Command.</span>setReplyTransformer (name, func)](#apidoc.element.ioredis.Command.setReplyTransformer)
- description and source-code
```javascript
setReplyTransformer = function (name, func) {
  Command._transformer.reply[name] = func;
}
```
- example usage
```shell
...
return args;
});

// Here's the built-in reply transformer converting the HGETALL reply
// ['k1', 'v1', 'k2', 'v2']
// into
// { k1: 'v1', 'k2': 'v2' }
Redis.Command.setReplyTransformer('hgetall', function (result) {
if (Array.isArray(result)) {
  var obj = {};
  for (var i = 0; i < result.length; i += 2) {
    obj[result[i]] = result[i + 1];
  }
  return obj;
}
...
```



# <a name="apidoc.module.ioredis.Command.prototype"></a>[module ioredis.Command.prototype](#apidoc.module.ioredis.Command.prototype)

#### <a name="apidoc.element.ioredis.Command.prototype._convertValue"></a>[function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>_convertValue (resolve)](#apidoc.element.ioredis.Command.prototype._convertValue)
- description and source-code
```javascript
_convertValue = function (resolve) {
  var _this = this;
  return function (value) {
    try {
      resolve(_this.transformReply(value));
    } catch (err) {
      _this.reject(err);
    }
    return _this.promise;
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Command.prototype._iterateKeys"></a>[function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>_iterateKeys (transform)](#apidoc.element.ioredis.Command.prototype._iterateKeys)
- description and source-code
```javascript
_iterateKeys = function (transform) {
  if (typeof this._keys === 'undefined') {
    if (typeof transform !== 'function') {
      transform = function (key) {
        return key;
      };
    }
    this._keys = [];
    if (commands.exists(this.name)) {
      var keyIndexes = commands.getKeyIndexes(this.name, this.args);
      for (var i = 0; i < keyIndexes.length; i++) {
        var index = keyIndexes[i];
        this.args[index] = transform(this.args[index]);
        this._keys.push(this.args[index]);
      }
    }
  }
  return this._keys;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Command.prototype.getKeys"></a>[function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>getKeys ()](#apidoc.element.ioredis.Command.prototype.getKeys)
- description and source-code
```javascript
getKeys = function () {
  return this._iterateKeys();
}
```
- example usage
```shell
...
this.resolve([]);
  }
  var pipelineSlot, i;
  if (this.isCluster) {
// List of the first key for each command
var sampleKeys = [];
for (i = 0; i < this._queue.length; i++) {
  var keys = this._queue[i].getKeys();
  if (keys.length) {
    sampleKeys.push(keys[0]);
  }
}

if (sampleKeys.length) {
  pipelineSlot = calculateSlot.generateMulti(sampleKeys);
...
```

#### <a name="apidoc.element.ioredis.Command.prototype.getSlot"></a>[function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>getSlot ()](#apidoc.element.ioredis.Command.prototype.getSlot)
- description and source-code
```javascript
getSlot = function () {
  if (typeof this._slot === 'undefined') {
    var key = this.getKeys()[0];
    if (key) {
      this.slot = calculateSlot(key);
    } else {
      this.slot = null;
    }
  }
  return this.slot;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Command.prototype.initPromise"></a>[function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>initPromise ()](#apidoc.element.ioredis.Command.prototype.initPromise)
- description and source-code
```javascript
initPromise = function () {
  var _this = this;
  this.promise = new Promise(function (resolve, reject) {
    if (!_this.transformed) {
      _this.transformed = true;
      var transformer = Command._transformer.argument[_this.name];
      if (transformer) {
        _this.args = transformer(_this.args);
      }
      _this.stringifyArguments();
    }

    _this.resolve = _this._convertValue(resolve);
    if (_this.errorStack) {
      _this.reject = function (err) {
        reject(utils.optimizeErrorStack(err, _this.errorStack, __dirname));
      };
    } else {
      _this.reject = reject;
    }
  }).nodeify(this.callback);
}
```
- example usage
```shell
...
  if (errv[0] === 'ASK' && !inTransaction &&
      queue[i].name !== 'asking' &&
      (!queue[i - 1] || queue[i - 1].name !== 'asking')) {
    var asking = new Command('asking');
    asking.ignore = true;
    this.sendCommand(asking);
  }
  queue[i].initPromise();
  this.sendCommand(queue[i]);
  if (queue[i].name === 'multi') {
    inTransaction = true;
  } else if (queue[i].name === 'exec') {
    inTransaction = false;
  }
}
...
```

#### <a name="apidoc.element.ioredis.Command.prototype.stringifyArguments"></a>[function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>stringifyArguments ()](#apidoc.element.ioredis.Command.prototype.stringifyArguments)
- description and source-code
```javascript
stringifyArguments = function () {
  for (var i = 0; i < this.args.length; ++i) {
    if (!(this.args[i] instanceof Buffer) && typeof this.args[i] !== 'string') {
      this.args[i] = utils.toArg(this.args[i]);
    }
  }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Command.prototype.toWritable"></a>[function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>toWritable ()](#apidoc.element.ioredis.Command.prototype.toWritable)
- description and source-code
```javascript
toWritable = function () {
  var bufferMode = false;
  var i;
  for (i = 0; i < this.args.length; ++i) {
    if (this.args[i] instanceof Buffer) {
      bufferMode = true;
      break;
    }
  }

  var result, arg;
  var commandStr = '*' + (this.args.length + 1) + '\r\n$' + this.name.length + '\r\n' + this.name + '\r\n';
  if (bufferMode) {
    var resultBuffer = new fbuffer.FlexBuffer(0);
    resultBuffer.write(commandStr);
    for (i = 0; i < this.args.length; ++i) {
      arg = this.args[i];
      if (arg instanceof Buffer) {
        if (arg.length === 0) {
          resultBuffer.write('$0\r\n\r\n');
        } else {
          resultBuffer.write('$' + arg.length + '\r\n');
          resultBuffer.write(arg);
          resultBuffer.write('\r\n');
        }
      } else {
        resultBuffer.write('$' + Buffer.byteLength(arg) + '\r\n' + arg + '\r\n');
      }
    }
    result = resultBuffer.getBuffer();
  } else {
    result = commandStr;
    for (i = 0; i < this.args.length; ++i) {
      result += '$' + Buffer.byteLength(this.args[i]) + '\r\n' + this.args[i] + '\r\n';
    }
  }
  return result;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Command.prototype.transformReply"></a>[function <span class="apidocSignatureSpan">ioredis.Command.prototype.</span>transformReply (result)](#apidoc.element.ioredis.Command.prototype.transformReply)
- description and source-code
```javascript
transformReply = function (result) {
  if (this.replyEncoding) {
    result = utils.convertBufferToString(result, this.replyEncoding);
  }
  var transformer = Command._transformer.reply[this.name];
  if (transformer) {
    result = transformer(result);
  }

  return result;
}
```
- example usage
```shell
...
  var execLength = value[1].length;
  for (i = 0; i < execLength; i++) {
    if (value[1][i] instanceof Error) {
      continue;
    }
    var cmd = this._queue[position - (execLength - i)];
    try {
      value[1][i] = cmd.transformReply(value[1][i]);
    } catch (err) {
      value[1][i] = err;
    }
  }
}
this._result[position] = value;
...
```



# <a name="apidoc.module.ioredis.Promise"></a>[module ioredis.Promise](#apidoc.module.ioredis.Promise)

#### <a name="apidoc.element.ioredis.Promise.Promise"></a>[function <span class="apidocSignatureSpan">ioredis.</span>Promise (executor)](#apidoc.element.ioredis.Promise.Promise)
- description and source-code
```javascript
function Promise(executor) {
    if (executor !== INTERNAL) {
        check(this, executor);
    }
    this._bitField = 0;
    this._fulfillmentHandler0 = undefined;
    this._rejectionHandler0 = undefined;
    this._promise0 = undefined;
    this._receiver0 = undefined;
    this._resolveFromExecutor(executor);
    this._promiseCreated();
    this._fireEvent("promiseCreated", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>AggregateError (message)](#apidoc.element.ioredis.Promise.AggregateError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.CancellationError"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>CancellationError (message)](#apidoc.element.ioredis.Promise.CancellationError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.OperationalError"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>OperationalError (message)](#apidoc.element.ioredis.Promise.OperationalError)
- description and source-code
```javascript
function OperationalError(message) {
    if (!(this instanceof OperationalError))
        return new OperationalError(message);
    notEnumerableProp(this, "name", "OperationalError");
    notEnumerableProp(this, "message", message);
    this.cause = message;
    this["isOperational"] = true;

    if (message instanceof Error) {
        notEnumerableProp(this, "message", message.message);
        notEnumerableProp(this, "stack", message.stack);
    } else if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    }

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.PromiseInspection"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>PromiseInspection (promise)](#apidoc.element.ioredis.Promise.PromiseInspection)
- description and source-code
```javascript
function PromiseInspection(promise) {
    if (promise !== undefined) {
        promise = promise._target();
        this._bitField = promise._bitField;
        this._settledValueField = promise._isFateSealed()
            ? promise._settledValue() : undefined;
    }
    else {
        this._bitField = 0;
        this._settledValueField = undefined;
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.RangeError"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>RangeError ()](#apidoc.element.ioredis.Promise.RangeError)
- description and source-code
```javascript
function RangeError() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.RejectionError"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>RejectionError (message)](#apidoc.element.ioredis.Promise.RejectionError)
- description and source-code
```javascript
function OperationalError(message) {
    if (!(this instanceof OperationalError))
        return new OperationalError(message);
    notEnumerableProp(this, "name", "OperationalError");
    notEnumerableProp(this, "message", message);
    this.cause = message;
    this["isOperational"] = true;

    if (message instanceof Error) {
        notEnumerableProp(this, "message", message.message);
        notEnumerableProp(this, "stack", message.stack);
    } else if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    }

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.TimeoutError"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>TimeoutError (message)](#apidoc.element.ioredis.Promise.TimeoutError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.TypeError"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>TypeError ()](#apidoc.element.ioredis.Promise.TypeError)
- description and source-code
```javascript
function TypeError() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>_SomePromiseArray (values)](#apidoc.element.ioredis.Promise._SomePromiseArray)
- description and source-code
```javascript
function SomePromiseArray(values) {
    this.constructor$(values);
    this._howMany = 0;
    this._unwrap = false;
    this._initialized = false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._peekContext"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>_peekContext ()](#apidoc.element.ioredis.Promise._peekContext)
- description and source-code
```javascript
_peekContext = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.all"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>all (promises)](#apidoc.element.ioredis.Promise.all)
- description and source-code
```javascript
all = function (promises) {
    return new PromiseArray(promises).promise();
}
```
- example usage
```shell
...
Sometimes you may want to send a command to multiple nodes (masters or slaves) of the cluster, you can get the nodes via 'Cluster
#nodes()' method.

'Cluster#nodes()' accepts a parameter role, which can be "master", "slave" and "all" (default), and returns an array of 'Redis'
instance. For example:

'''javascript
// Send 'FLUSHDB' command to all slaves:
var slaves = cluster.nodes('slave');
Promise.all(slaves.map(function (node) {
return node.flushdb();
}));

// Get keys of all the masters:
var masters = cluster.nodes('master');
Promise.all(masters.map(function (node) {
return node.keys();
...
```

#### <a name="apidoc.element.ioredis.Promise.any"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>any (promises)](#apidoc.element.ioredis.Promise.any)
- description and source-code
```javascript
any = function (promises) {
    return any(promises);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.attempt"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>attempt (fn)](#apidoc.element.ioredis.Promise.attempt)
- description and source-code
```javascript
attempt = function (fn) {
    if (typeof fn !== "function") {
        return apiRejection("expecting a function but got " + util.classString(fn));
    }
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    ret._pushContext();
    var value;
    if (arguments.length > 1) {
        debug.deprecated("calling Promise.try with more than 1 argument");
        var arg = arguments[1];
        var ctx = arguments[2];
        value = util.isArray(arg) ? tryCatch(fn).apply(ctx, arg)
                                  : tryCatch(fn).call(ctx, arg);
    } else {
        value = tryCatch(fn)();
    }
    var promiseCreated = ret._popContext();
    debug.checkForgottenReturns(
        value, promiseCreated, "Promise.try", ret);
    ret._resolveFromSyncValue(value);
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.bind"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>bind (thisArg, value)](#apidoc.element.ioredis.Promise.bind)
- description and source-code
```javascript
bind = function (thisArg, value) {
    return Promise.resolve(value).bind(thisArg);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.cast"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>cast (obj)](#apidoc.element.ioredis.Promise.cast)
- description and source-code
```javascript
cast = function (obj) {
    var ret = tryConvertToPromise(obj);
    if (!(ret instanceof Promise)) {
        ret = new Promise(INTERNAL);
        ret._captureStackTrace();
        ret._setFulfilled();
        ret._rejectionHandler0 = obj;
    }
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.config"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>config (opts)](#apidoc.element.ioredis.Promise.config)
- description and source-code
```javascript
config = function (opts) {
    opts = Object(opts);
    if ("longStackTraces" in opts) {
        if (opts.longStackTraces) {
            Promise.longStackTraces();
        } else if (!opts.longStackTraces && Promise.hasLongStackTraces()) {
            disableLongStackTraces();
        }
    }
    if ("warnings" in opts) {
        var warningsOption = opts.warnings;
        config.warnings = !!warningsOption;
        wForgottenReturn = config.warnings;

        if (util.isObject(warningsOption)) {
            if ("wForgottenReturn" in warningsOption) {
                wForgottenReturn = !!warningsOption.wForgottenReturn;
            }
        }
    }
    if ("cancellation" in opts && opts.cancellation && !config.cancellation) {
        if (async.haveItemsQueued()) {
            throw new Error(
                "cannot enable cancellation after promises are in use");
        }
        Promise.prototype._clearCancellationData =
            cancellationClearCancellationData;
        Promise.prototype._propagateFrom = cancellationPropagateFrom;
        Promise.prototype._onCancel = cancellationOnCancel;
        Promise.prototype._setOnCancel = cancellationSetOnCancel;
        Promise.prototype._attachCancellationCallback =
            cancellationAttachCancellationCallback;
        Promise.prototype._execute = cancellationExecute;
        propagateFromFunction = cancellationPropagateFrom;
        config.cancellation = true;
    }
    if ("monitoring" in opts) {
        if (opts.monitoring && !config.monitoring) {
            config.monitoring = true;
            Promise.prototype._fireEvent = activeFireEvent;
        } else if (!opts.monitoring && config.monitoring) {
            config.monitoring = false;
            Promise.prototype._fireEvent = defaultFireEvent;
        }
    }
    return Promise;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.coroutine"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>coroutine (generatorFunction, options)](#apidoc.element.ioredis.Promise.coroutine)
- description and source-code
```javascript
coroutine = function (generatorFunction, options) {
    if (typeof generatorFunction !== "function") {
        throw new TypeError("generatorFunction must be a function\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }
    var yieldHandler = Object(options).yieldHandler;
    var PromiseSpawn$ = PromiseSpawn;
    var stack = new Error().stack;
    return function () {
        var generator = generatorFunction.apply(this, arguments);
        var spawn = new PromiseSpawn$(undefined, undefined, yieldHandler,
                                      stack);
        var ret = spawn.promise();
        spawn._generator = generator;
        spawn._promiseFulfilled(undefined);
        return ret;
    };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.defer"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>defer ()](#apidoc.element.ioredis.Promise.defer)
- description and source-code
```javascript
defer = function () {
    debug.deprecated("Promise.defer", "new Promise");
    var promise = new Promise(INTERNAL);
    return {
        promise: promise,
        resolve: deferResolve,
        reject: deferReject
    };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.delay"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>delay (ms, value)](#apidoc.element.ioredis.Promise.delay)
- description and source-code
```javascript
delay = function (ms, value) {
    var ret;
    var handle;
    if (value !== undefined) {
        ret = Promise.resolve(value)
                ._then(afterValue, null, null, ms, undefined);
        if (debug.cancellation() && value instanceof Promise) {
            ret._setOnCancel(value);
        }
    } else {
        ret = new Promise(INTERNAL);
        handle = setTimeout(function() { ret._fulfill(); }, +ms);
        if (debug.cancellation()) {
            ret._setOnCancel(new HandleWrapper(handle));
        }
        ret._captureStackTrace();
    }
    ret._setAsyncGuaranteed();
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.each"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>each (promises, fn)](#apidoc.element.ioredis.Promise.each)
- description and source-code
```javascript
each = function (promises, fn) {
    return PromiseReduce(promises, fn, INTERNAL, 0)
              ._then(promiseAllThis, undefined, undefined, promises, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.filter"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>filter (promises, fn, options)](#apidoc.element.ioredis.Promise.filter)
- description and source-code
```javascript
filter = function (promises, fn, options) {
    return PromiseMap(promises, fn, options, INTERNAL);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.fromCallback"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>fromCallback (fn)](#apidoc.element.ioredis.Promise.fromCallback)
- description and source-code
```javascript
fromCallback = function (fn) {
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    var multiArgs = arguments.length > 1 ? !!Object(arguments[1]).multiArgs
                                         : false;
    var result = tryCatch(fn)(nodebackForPromise(ret, multiArgs));
    if (result === errorObj) {
        ret._rejectCallback(result.e, true);
    }
    if (!ret._isFateSealed()) ret._setAsyncGuaranteed();
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.fromNode"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>fromNode (fn)](#apidoc.element.ioredis.Promise.fromNode)
- description and source-code
```javascript
fromNode = function (fn) {
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    var multiArgs = arguments.length > 1 ? !!Object(arguments[1]).multiArgs
                                         : false;
    var result = tryCatch(fn)(nodebackForPromise(ret, multiArgs));
    if (result === errorObj) {
        ret._rejectCallback(result.e, true);
    }
    if (!ret._isFateSealed()) ret._setAsyncGuaranteed();
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.fulfilled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>fulfilled (obj)](#apidoc.element.ioredis.Promise.fulfilled)
- description and source-code
```javascript
fulfilled = function (obj) {
    var ret = tryConvertToPromise(obj);
    if (!(ret instanceof Promise)) {
        ret = new Promise(INTERNAL);
        ret._captureStackTrace();
        ret._setFulfilled();
        ret._rejectionHandler0 = obj;
    }
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.getNewLibraryCopy"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>getNewLibraryCopy ()](#apidoc.element.ioredis.Promise.getNewLibraryCopy)
- description and source-code
```javascript
getNewLibraryCopy = function () {
var makeSelfResolutionError = function () {
    return new TypeError("circular promise resolution chain\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
};
var reflectHandler = function() {
    return new Promise.PromiseInspection(this._target());
};
var apiRejection = function(msg) {
    return Promise.reject(new TypeError(msg));
};
function Proxyable() {}
var UNDEFINED_BINDING = {};
var util = require("./util");

var getDomain;
if (util.isNode) {
    getDomain = function() {
        var ret = process.domain;
        if (ret === undefined) ret = null;
        return ret;
    };
} else {
    getDomain = function() {
        return null;
    };
}
util.notEnumerableProp(Promise, "_getDomain", getDomain);

var es5 = require("./es5");
var Async = require("./async");
var async = new Async();
es5.defineProperty(Promise, "_async", {value: async});
var errors = require("./errors");
var TypeError = Promise.TypeError = errors.TypeError;
Promise.RangeError = errors.RangeError;
var CancellationError = Promise.CancellationError = errors.CancellationError;
Promise.TimeoutError = errors.TimeoutError;
Promise.OperationalError = errors.OperationalError;
Promise.RejectionError = errors.OperationalError;
Promise.AggregateError = errors.AggregateError;
var INTERNAL = function(){};
var APPLY = {};
var NEXT_FILTER = {};
var tryConvertToPromise = require("./thenables")(Promise, INTERNAL);
var PromiseArray =
    require("./promise_array")(Promise, INTERNAL,
                               tryConvertToPromise, apiRejection, Proxyable);
var Context = require("./context")(Promise);
<span class="apidocCodeCommentSpan"> /*jshint unused:false*/
</span>var createContext = Context.create;
var debug = require("./debuggability")(Promise, Context);
var CapturedTrace = debug.CapturedTrace;
var PassThroughHandlerContext =
    require("./finally")(Promise, tryConvertToPromise, NEXT_FILTER);
var catchFilter = require("./catch_filter")(NEXT_FILTER);
var nodebackForPromise = require("./nodeback");
var errorObj = util.errorObj;
var tryCatch = util.tryCatch;
function check(self, executor) {
    if (self == null || self.constructor !== Promise) {
        throw new TypeError("the promise constructor cannot be invoked directly\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }
    if (typeof executor !== "function") {
        throw new TypeError("expecting a function but got " + util.classString(executor));
    }

}

function Promise(executor) {
    if (executor !== INTERNAL) {
        check(this, executor);
    }
    this._bitField = 0;
    this._fulfillmentHandler0 = undefined;
    this._rejectionHandler0 = undefined;
    this._promise0 = undefined;
    this._receiver0 = undefined;
    this._resolveFromExecutor(executor);
    this._promiseCreated();
    this._fireEvent("promiseCreated", this);
}

Promise.prototype.toString = function () {
    return "[object Promise]";
};

Promise.prototype.caught = Promise.prototype["catch"] = function (fn) {
    var len = arguments.length;
    if (len > 1) {
        var catchInstances = new Array(len - 1),
            j = 0, i;
        for (i = 0; i < len - 1; ++i) {
            var item = arguments[i];
            if (util.isObject(item)) {
                catchInstances[j++] = item;
            } else {
                return apiRejection("Catch statement predicate: " +
                    "expecting an object but got " + util.classString(item));
            }
        }
        catchInstances.length = j;
        fn = arguments[i];
        return this.then(undefined, catchFilter(catchInstances, fn, this));
    }
    return this.then(undefined, fn);
};

Promise.prototype.reflect = function () {
    return this._then(reflectHandler,
        reflectHandler, undefined, this, undefined);
};

Promise.prototype.then = function (didFulfill, didReject) {
    if (debug.warnings() && arguments.length > 0 &&
        typeof didFulfill !== "function" &&
        typeof didReject !== "function") {
        var msg = ".then() only accepts functions but was passed: " +
                util.classString(didFulfill);
        if (arguments.length > 1) {
            msg += ", ...
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.hasLongStackTraces"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>hasLongStackTraces ()](#apidoc.element.ioredis.Promise.hasLongStackTraces)
- description and source-code
```javascript
hasLongStackTraces = function () {
    return config.longStackTraces && longStackTracesIsSupported();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.is"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>is (val)](#apidoc.element.ioredis.Promise.is)
- description and source-code
```javascript
is = function (val) {
    return val instanceof Promise;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.join"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>join ()](#apidoc.element.ioredis.Promise.join)
- description and source-code
```javascript
join = function () {
    var last = arguments.length - 1;
    var fn;
    if (last > 0 && typeof arguments[last] === "function") {
        fn = arguments[last];
        if (!false) {
            if (last <= 8 && canEvaluate) {
                var ret = new Promise(INTERNAL);
                ret._captureStackTrace();
                var HolderClass = holderClasses[last - 1];
                var holder = new HolderClass(fn);
                var callbacks = thenCallbacks;

                for (var i = 0; i < last; ++i) {
                    var maybePromise = tryConvertToPromise(arguments[i], ret);
                    if (maybePromise instanceof Promise) {
                        maybePromise = maybePromise._target();
                        var bitField = maybePromise._bitField;
                        ;
                        if (((bitField & 50397184) === 0)) {
                            maybePromise._then(callbacks[i], reject,
                                               undefined, ret, holder);
                            promiseSetters[i](maybePromise, holder);
                            holder.asyncNeeded = false;
                        } else if (((bitField & 33554432) !== 0)) {
                            callbacks[i].call(ret,
                                              maybePromise._value(), holder);
                        } else if (((bitField & 16777216) !== 0)) {
                            ret._reject(maybePromise._reason());
                        } else {
                            ret._cancel();
                        }
                    } else {
                        callbacks[i].call(ret, maybePromise, holder);
                    }
                }

                if (!ret._isFateSealed()) {
                    if (holder.asyncNeeded) {
                        var domain = getDomain();
                        if (domain !== null) {
                            holder.fn = util.domainBind(domain, holder.fn);
                        }
                    }
                    ret._setAsyncGuaranteed();
                    ret._setOnCancel(holder);
                }
                return ret;
            }
        }
    }
    var $_len = arguments.length;var args = new Array($_len); for(var $_i = 0; $_i < $_len; ++$_i) {args[$_i] = arguments[$_i];};
    if (fn) args.pop();
    var ret = new PromiseArray(args).promise();
    return fn !== undefined ? ret.spread(fn) : ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.longStackTraces"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>longStackTraces ()](#apidoc.element.ioredis.Promise.longStackTraces)
- description and source-code
```javascript
longStackTraces = function () {
    if (async.haveItemsQueued() && !config.longStackTraces) {
        throw new Error("cannot enable long stack traces after promises have been created\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }
    if (!config.longStackTraces && longStackTracesIsSupported()) {
        var Promise_captureStackTrace = Promise.prototype._captureStackTrace;
        var Promise_attachExtraTrace = Promise.prototype._attachExtraTrace;
        config.longStackTraces = true;
        disableLongStackTraces = function() {
            if (async.haveItemsQueued() && !config.longStackTraces) {
                throw new Error("cannot enable long stack traces after promises have been created\u000a\u000a    See http://goo.
gl/MqrFmX\u000a");
            }
            Promise.prototype._captureStackTrace = Promise_captureStackTrace;
            Promise.prototype._attachExtraTrace = Promise_attachExtraTrace;
            Context.deactivateLongStackTraces();
            async.enableTrampoline();
            config.longStackTraces = false;
        };
        Promise.prototype._captureStackTrace = longStackTracesCaptureStackTrace;
        Promise.prototype._attachExtraTrace = longStackTracesAttachExtraTrace;
        Context.activateLongStackTraces();
        async.disableTrampolineIfNecessary();
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.map"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>map (promises, fn, options, _filter)](#apidoc.element.ioredis.Promise.map)
- description and source-code
```javascript
map = function (promises, fn, options, _filter) {
    return map(promises, fn, options, _filter);
}
```
- example usage
```shell
...
Sometimes you may want to send a command to multiple nodes (masters or slaves) of the cluster, you can get the nodes via 'Cluster
#nodes()' method.

'Cluster#nodes()' accepts a parameter role, which can be "master", "slave" and "all" (default), and returns an array of 'Redis'
instance. For example:

'''javascript
// Send 'FLUSHDB' command to all slaves:
var slaves = cluster.nodes('slave');
Promise.all(slaves.map(function (node) {
return node.flushdb();
}));

// Get keys of all the masters:
var masters = cluster.nodes('master');
Promise.all(masters.map(function (node) {
return node.keys();
...
```

#### <a name="apidoc.element.ioredis.Promise.mapSeries"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>mapSeries (promises, fn)](#apidoc.element.ioredis.Promise.mapSeries)
- description and source-code
```javascript
function PromiseMapSeries(promises, fn) {
    return PromiseReduce(promises, fn, INTERNAL, INTERNAL);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.method"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>method (fn)](#apidoc.element.ioredis.Promise.method)
- description and source-code
```javascript
method = function (fn) {
    if (typeof fn !== "function") {
        throw new Promise.TypeError("expecting a function but got " + util.classString(fn));
    }
    return function () {
        var ret = new Promise(INTERNAL);
        ret._captureStackTrace();
        ret._pushContext();
        var value = tryCatch(fn).apply(this, arguments);
        var promiseCreated = ret._popContext();
        debug.checkForgottenReturns(
            value, promiseCreated, "Promise.method", ret);
        ret._resolveFromSyncValue(value);
        return ret;
    };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.noConflict"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>noConflict ()](#apidoc.element.ioredis.Promise.noConflict)
- description and source-code
```javascript
function noConflict() {
    try { if (Promise === bluebird) Promise = old; }
    catch (e) {}
    return bluebird;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.onPossiblyUnhandledRejection"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>onPossiblyUnhandledRejection (fn)](#apidoc.element.ioredis.Promise.onPossiblyUnhandledRejection)
- description and source-code
```javascript
onPossiblyUnhandledRejection = function (fn) {
    var domain = getDomain();
    possiblyUnhandledRejection =
        typeof fn === "function" ? (domain === null ?
                                            fn : util.domainBind(domain, fn))
                                 : undefined;
}
```
- example usage
```shell
...
However, it would decrease the performance significantly to optimize the error stack. So by
default, this option is disabled and can only be used for debugging purposes. You **shouldn't** use this feature in a production
 environment.

If you want to catch all unhandled errors without decreased performance, there's another way:

'''javascript
var Redis = require('ioredis');
Redis.Promise.onPossiblyUnhandledRejection(function (error) {
  // you can log the error here.
  // error.command.name is the command name, here is 'set'
  // error.command.args is the command arguments, here is ['foo']
});
var redis = new Redis();
redis.set('foo');
'''
...
```

#### <a name="apidoc.element.ioredis.Promise.onUnhandledRejectionHandled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>onUnhandledRejectionHandled (fn)](#apidoc.element.ioredis.Promise.onUnhandledRejectionHandled)
- description and source-code
```javascript
onUnhandledRejectionHandled = function (fn) {
    var domain = getDomain();
    unhandledRejectionHandled =
        typeof fn === "function" ? (domain === null ?
                                            fn : util.domainBind(domain, fn))
                                 : undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.pending"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>pending ()](#apidoc.element.ioredis.Promise.pending)
- description and source-code
```javascript
pending = function () {
    debug.deprecated("Promise.defer", "new Promise");
    var promise = new Promise(INTERNAL);
    return {
        promise: promise,
        resolve: deferResolve,
        reject: deferReject
    };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.promisify"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>promisify (fn, options)](#apidoc.element.ioredis.Promise.promisify)
- description and source-code
```javascript
promisify = function (fn, options) {
    if (typeof fn !== "function") {
        throw new TypeError("expecting a function but got " + util.classString(fn));
    }
    if (isPromisified(fn)) {
        return fn;
    }
    options = Object(options);
    var receiver = options.context === undefined ? THIS : options.context;
    var multiArgs = !!options.multiArgs;
    var ret = promisify(fn, receiver, multiArgs);
    util.copyDescriptors(fn, ret, propsFilter);
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.promisifyAll"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>promisifyAll (target, options)](#apidoc.element.ioredis.Promise.promisifyAll)
- description and source-code
```javascript
promisifyAll = function (target, options) {
    if (typeof target !== "function" && typeof target !== "object") {
        throw new TypeError("the target of promisifyAll must be an object or a function\u000a\u000a    See http://goo.gl/MqrFmX\
u000a");
    }
    options = Object(options);
    var multiArgs = !!options.multiArgs;
    var suffix = options.suffix;
    if (typeof suffix !== "string") suffix = defaultSuffix;
    var filter = options.filter;
    if (typeof filter !== "function") filter = defaultFilter;
    var promisifier = options.promisifier;
    if (typeof promisifier !== "function") promisifier = makeNodePromisified;

    if (!util.isIdentifier(suffix)) {
        throw new RangeError("suffix must be a valid identifier\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }

    var keys = util.inheritedDataKeys(target);
    for (var i = 0; i < keys.length; ++i) {
        var value = target[keys[i]];
        if (keys[i] !== "constructor" &&
            util.isClass(value)) {
            promisifyAll(value.prototype, suffix, filter, promisifier,
                multiArgs);
            promisifyAll(value, suffix, filter, promisifier, multiArgs);
        }
    }

    return promisifyAll(target, suffix, filter, promisifier, multiArgs);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.props"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>props (promises)](#apidoc.element.ioredis.Promise.props)
- description and source-code
```javascript
props = function (promises) {
    return props(promises);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.race"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>race (promises)](#apidoc.element.ioredis.Promise.race)
- description and source-code
```javascript
race = function (promises) {
    return race(promises, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.reduce"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>reduce (promises, fn, initialValue, _each)](#apidoc.element.ioredis.Promise.reduce)
- description and source-code
```javascript
reduce = function (promises, fn, initialValue, _each) {
    return reduce(promises, fn, initialValue, _each);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.reject"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>reject (reason)](#apidoc.element.ioredis.Promise.reject)
- description and source-code
```javascript
reject = function (reason) {
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    ret._rejectCallback(reason, true);
    return ret;
}
```
- example usage
```shell
...
for (var i = firstArgIndex; i < length; ++i) {
  args[i - firstArgIndex] = arguments[i];
}

var options;
if (this.options.dropBufferSupport) {
  if (!_encoding) {
    return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
  }
  options = { replyEncoding: null };
} else {
  options = { replyEncoding: _encoding };
}

if (this.options.showFriendlyErrorStack) {
...
```

#### <a name="apidoc.element.ioredis.Promise.rejected"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>rejected (reason)](#apidoc.element.ioredis.Promise.rejected)
- description and source-code
```javascript
rejected = function (reason) {
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    ret._rejectCallback(reason, true);
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.resolve"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>resolve (obj)](#apidoc.element.ioredis.Promise.resolve)
- description and source-code
```javascript
resolve = function (obj) {
    var ret = tryConvertToPromise(obj);
    if (!(ret instanceof Promise)) {
        ret = new Promise(INTERNAL);
        ret._captureStackTrace();
        ret._setFulfilled();
        ret._rejectionHandler0 = obj;
    }
    return ret;
}
```
- example usage
```shell
...
var ignoredCount = 0;
for (i = 0; i < this._queue.length - ignoredCount; ++i) {
  if (this._queue[i + ignoredCount].ignore) {
    ignoredCount += 1;
  }
  this._result[i] = this._result[i + ignoredCount];
}
this.resolve(this._result.slice(0, this._result.length - ignoredCount));
};

Pipeline.prototype.sendCommand = function (command) {
var position = this._queue.length;

var _this = this;
...
```

#### <a name="apidoc.element.ioredis.Promise.setScheduler"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>setScheduler (fn)](#apidoc.element.ioredis.Promise.setScheduler)
- description and source-code
```javascript
setScheduler = function (fn) {
    if (typeof fn !== "function") {
        throw new TypeError("expecting a function but got " + util.classString(fn));
    }
    return async.setScheduler(fn);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.settle"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>settle (promises)](#apidoc.element.ioredis.Promise.settle)
- description and source-code
```javascript
settle = function (promises) {
    debug.deprecated(".settle()", ".reflect()");
    return new SettledPromiseArray(promises).promise();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.some"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>some (promises, howMany)](#apidoc.element.ioredis.Promise.some)
- description and source-code
```javascript
some = function (promises, howMany) {
    return some(promises, howMany);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.spawn"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>spawn (generatorFunction)](#apidoc.element.ioredis.Promise.spawn)
- description and source-code
```javascript
spawn = function (generatorFunction) {
    debug.deprecated("Promise.spawn()", "Promise.coroutine()");
    if (typeof generatorFunction !== "function") {
        return apiRejection("generatorFunction must be a function\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }
    var spawn = new PromiseSpawn(generatorFunction, this);
    var ret = spawn.promise();
    spawn._run(Promise.spawn);
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.try"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>try (fn)](#apidoc.element.ioredis.Promise.try)
- description and source-code
```javascript
try = function (fn) {
    if (typeof fn !== "function") {
        return apiRejection("expecting a function but got " + util.classString(fn));
    }
    var ret = new Promise(INTERNAL);
    ret._captureStackTrace();
    ret._pushContext();
    var value;
    if (arguments.length > 1) {
        debug.deprecated("calling Promise.try with more than 1 argument");
        var arg = arguments[1];
        var ctx = arguments[2];
        value = util.isArray(arg) ? tryCatch(fn).apply(ctx, arg)
                                  : tryCatch(fn).call(ctx, arg);
    } else {
        value = tryCatch(fn)();
    }
    var promiseCreated = ret._popContext();
    debug.checkForgottenReturns(
        value, promiseCreated, "Promise.try", ret);
    ret._resolveFromSyncValue(value);
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.using"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>using ()](#apidoc.element.ioredis.Promise.using)
- description and source-code
```javascript
using = function () {
    var len = arguments.length;
    if (len < 2) return apiRejection(
                    "you must pass at least 2 arguments to Promise.using");
    var fn = arguments[len - 1];
    if (typeof fn !== "function") {
        return apiRejection("expecting a function but got " + util.classString(fn));
    }
    var input;
    var spreadArgs = true;
    if (len === 2 && Array.isArray(arguments[0])) {
        input = arguments[0];
        len = input.length;
        spreadArgs = false;
    } else {
        input = arguments;
        len--;
    }
    var resources = new ResourceList(len);
    for (var i = 0; i < len; ++i) {
        var resource = input[i];
        if (Disposer.isDisposer(resource)) {
            var disposer = resource;
            resource = resource.promise();
            resource._setDisposable(disposer);
        } else {
            var maybePromise = tryConvertToPromise(resource);
            if (maybePromise instanceof Promise) {
                resource =
                    maybePromise._then(maybeUnwrapDisposer, null, null, {
                        resources: resources,
                        index: i
                }, undefined);
            }
        }
        resources[i] = resource;
    }

    var reflectedResources = new Array(resources.length);
    for (var i = 0; i < reflectedResources.length; ++i) {
        reflectedResources[i] = Promise.resolve(resources[i]).reflect();
    }

    var resultPromise = Promise.all(reflectedResources)
        .then(function(inspections) {
            for (var i = 0; i < inspections.length; ++i) {
                var inspection = inspections[i];
                if (inspection.isRejected()) {
                    errorObj.e = inspection.error();
                    return errorObj;
                } else if (!inspection.isFulfilled()) {
                    resultPromise.cancel();
                    return;
                }
                inspections[i] = inspection.value();
            }
            promise._pushContext();

            fn = tryCatch(fn);
            var ret = spreadArgs
                ? fn.apply(undefined, inspections) : fn(inspections);
            var promiseCreated = promise._popContext();
            debug.checkForgottenReturns(
                ret, promiseCreated, "Promise.using", promise);
            return ret;
        });

    var promise = resultPromise.lastly(function() {
        var inspection = new Promise.PromiseInspection(resultPromise);
        return dispose(resources, inspection);
    });
    resources.promise = promise;
    promise._setOnCancel(resources);
    return promise;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise.AggregateError"></a>[module ioredis.Promise.AggregateError](#apidoc.module.ioredis.Promise.AggregateError)

#### <a name="apidoc.element.ioredis.Promise.AggregateError.AggregateError"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>AggregateError (message)](#apidoc.element.ioredis.Promise.AggregateError.AggregateError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise.AggregateError.prototype"></a>[module ioredis.Promise.AggregateError.prototype](#apidoc.module.ioredis.Promise.AggregateError.prototype)

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.constructor"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>constructor (message)](#apidoc.element.ioredis.Promise.AggregateError.prototype.constructor)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.every"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>every ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.every)
- description and source-code
```javascript
function every() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.filter"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>filter ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.filter)
- description and source-code
```javascript
function filter() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.forEach"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>forEach ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.forEach)
- description and source-code
```javascript
function forEach() { [native code] }
```
- example usage
```shell
...
Commander.prototype.createBuiltinCommand = function (commandName) {
  return {
    string: generateFunction(commandName, 'utf8'),
    buffer: generateFunction(commandName, null)
  };
};

_.forEach(commands, function (commandName) {
  Commander.prototype[commandName] = generateFunction(commandName, 'utf8');
  Commander.prototype[commandName + 'Buffer'] = generateFunction(commandName, null);
});

Commander.prototype.call = generateFunction('utf8');
Commander.prototype.callBuffer = generateFunction(null);
Commander.prototype.send_command = Commander.prototype.call;
...
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.indexOf"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>indexOf ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.indexOf)
- description and source-code
```javascript
function indexOf() { [native code] }
```
- example usage
```shell
...

var evalsha = new Command('evalsha', [this.sha].concat(args), options);
evalsha.isCustomCommand = true;
var result = container.sendCommand(evalsha);
if (result instanceof Promise) {
  var _this = this;
  return result.catch(function (err) {
    if (err.toString().indexOf('NOSCRIPT') === -1) {
      throw err;
    }
    return container.sendCommand(new Command('eval', [_this.lua].concat(args), options));
  }).nodeify(callback);
}

// result is not a Promise--probably returned from a pipeline chain; however,
...
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.join"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>join ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.join)
- description and source-code
```javascript
function join() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.lastIndexOf"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>lastIndexOf ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.lastIndexOf)
- description and source-code
```javascript
function lastIndexOf() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.map"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>map ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.map)
- description and source-code
```javascript
function map() { [native code] }
```
- example usage
```shell
...
Sometimes you may want to send a command to multiple nodes (masters or slaves) of the cluster, you can get the nodes via 'Cluster
#nodes()' method.

'Cluster#nodes()' accepts a parameter role, which can be "master", "slave" and "all" (default), and returns an array of 'Redis'
instance. For example:

'''javascript
// Send 'FLUSHDB' command to all slaves:
var slaves = cluster.nodes('slave');
Promise.all(slaves.map(function (node) {
return node.flushdb();
}));

// Get keys of all the masters:
var masters = cluster.nodes('master');
Promise.all(masters.map(function (node) {
return node.keys();
...
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.pop"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>pop ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.pop)
- description and source-code
```javascript
function pop() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.push"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>push ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.push)
- description and source-code
```javascript
function push() { [native code] }
```
- example usage
```shell
...
var redis = new Redis();
// Create a readable stream (object mode)
var stream = redis.scanStream();
var keys = [];
stream.on('data', function (resultKeys) {
  // 'resultKeys' is an array of strings representing key names
  for (var i = 0; i < resultKeys.length; i++) {
    keys.push(resultKeys[i]);
  }
});
stream.on('end', function () {
  console.log('done with the keys: ', keys);
});
'''
...
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.reduce"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>reduce ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.reduce)
- description and source-code
```javascript
function reduce() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.reduceRight"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>reduceRight ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.reduceRight)
- description and source-code
```javascript
function reduceRight() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.reverse"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>reverse ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.reverse)
- description and source-code
```javascript
function reverse() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.shift"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>shift ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.shift)
- description and source-code
```javascript
function shift() { [native code] }
```
- example usage
```shell
...

  return this;
};

Pipeline.prototype.addBatch = function (commands) {
  for (var i = 0; i < commands.length; ++i) {
    var command = commands[i];
    var commandName = command.shift();
    this[commandName].apply(this, command);
  }

  return this;
};

var multi = Pipeline.prototype.multi;
...
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.slice"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>slice ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.slice)
- description and source-code
```javascript
function slice() { [native code] }
```
- example usage
```shell
...

Besides auto-reconnect when the connection is closed, ioredis supports reconnecting on the specified errors by the 'reconnectOnError
' option. Here's an example that will reconnect when receiving 'READONLY' error:

'''javascript
var redis = new Redis({
  reconnectOnError: function (err) {
    var targetError = 'READONLY';
    if (err.message.slice(0, targetError.length) === targetError) {
      // Only reconnect when the error starts with "READONLY"
      return true; // or 'return 1;'
    }
  }
});
'''
...
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.some"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>some ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.some)
- description and source-code
```javascript
function some() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.sort"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>sort ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.sort)
- description and source-code
```javascript
function sort() { [native code] }
```
- example usage
```shell
...
  numberOfKeys: 2,
  lua: 'return {KEYS[1],KEYS[2],ARGV[1],ARGV[2]}'
});

// Works well with pipelining/transaction
fooRedis.pipeline()
  // Sends SORT foo:list BY foo:weight_*->fieldname
  .sort('list', 'BY', 'weight_*->fieldname')
  // Supports custom commands
  // Sends EVALSHA xxx foo:k1 foo:k2 a1 a2
  .echo('k1', 'k2', 'a1', 'a2')
  .exec()
'''

## Transforming Arguments & Replies
...
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.toString"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>toString ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.toString)
- description and source-code
```javascript
toString = function () {
    var indent = Array(level * 4 + 1).join(" ");
    var ret = "\n" + indent + "AggregateError of:" + "\n";
    level++;
    indent = Array(level * 4 + 1).join(" ");
    for (var i = 0; i < this.length; ++i) {
        var str = this[i] === this ? "[Circular AggregateError]" : this[i] + "";
        var lines = str.split("\n");
        for (var j = 0; j < lines.length; ++j) {
            lines[j] = indent + lines[j];
        }
        str = lines.join("\n");
        ret += str + "\n";
    }
    level--;
    return ret;
}
```
- example usage
```shell
...
  }
  var _this = this;
  this.opt.redis[this.opt.command](args, function (err, res) {
    if (err) {
      _this.emit('error', err);
      return;
    }
    _this._redisCursor = (res[0] instanceof Buffer) ? res[0].toString() : res[0];
    if (_this._redisCursor === '0') {
      _this._redisDrained = true;
    }
    _this.push(res[1]);
  });
};
...
```

#### <a name="apidoc.element.ioredis.Promise.AggregateError.prototype.unshift"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.AggregateError.prototype.</span>unshift ()](#apidoc.element.ioredis.Promise.AggregateError.prototype.unshift)
- description and source-code
```javascript
function unshift() { [native code] }
```
- example usage
```shell
...
ScanStream.prototype._read = function () {
if (this._redisDrained) {
  this.push(null);
  return;
}
var args = [this._redisCursor];
if (this.opt.key) {
  args.unshift(this.opt.key);
}
if (this.opt.match) {
  args.push('MATCH', this.opt.match);
}
if (this.opt.count) {
  args.push('COUNT', this.opt.count);
}
...
```



# <a name="apidoc.module.ioredis.Promise.CancellationError"></a>[module ioredis.Promise.CancellationError](#apidoc.module.ioredis.Promise.CancellationError)

#### <a name="apidoc.element.ioredis.Promise.CancellationError.CancellationError"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>CancellationError (message)](#apidoc.element.ioredis.Promise.CancellationError.CancellationError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise.CancellationError.prototype"></a>[module ioredis.Promise.CancellationError.prototype](#apidoc.module.ioredis.Promise.CancellationError.prototype)

#### <a name="apidoc.element.ioredis.Promise.CancellationError.prototype.constructor"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.CancellationError.prototype.</span>constructor (message)](#apidoc.element.ioredis.Promise.CancellationError.prototype.constructor)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise.OperationalError"></a>[module ioredis.Promise.OperationalError](#apidoc.module.ioredis.Promise.OperationalError)

#### <a name="apidoc.element.ioredis.Promise.OperationalError.OperationalError"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>OperationalError (message)](#apidoc.element.ioredis.Promise.OperationalError.OperationalError)
- description and source-code
```javascript
function OperationalError(message) {
    if (!(this instanceof OperationalError))
        return new OperationalError(message);
    notEnumerableProp(this, "name", "OperationalError");
    notEnumerableProp(this, "message", message);
    this.cause = message;
    this["isOperational"] = true;

    if (message instanceof Error) {
        notEnumerableProp(this, "message", message.message);
        notEnumerableProp(this, "stack", message.stack);
    } else if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    }

}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise.OperationalError.prototype"></a>[module ioredis.Promise.OperationalError.prototype](#apidoc.module.ioredis.Promise.OperationalError.prototype)

#### <a name="apidoc.element.ioredis.Promise.OperationalError.prototype.constructor"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.OperationalError.prototype.</span>constructor (message)](#apidoc.element.ioredis.Promise.OperationalError.prototype.constructor)
- description and source-code
```javascript
function OperationalError(message) {
    if (!(this instanceof OperationalError))
        return new OperationalError(message);
    notEnumerableProp(this, "name", "OperationalError");
    notEnumerableProp(this, "message", message);
    this.cause = message;
    this["isOperational"] = true;

    if (message instanceof Error) {
        notEnumerableProp(this, "message", message.message);
        notEnumerableProp(this, "stack", message.stack);
    } else if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    }

}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise.PromiseInspection"></a>[module ioredis.Promise.PromiseInspection](#apidoc.module.ioredis.Promise.PromiseInspection)

#### <a name="apidoc.element.ioredis.Promise.PromiseInspection.PromiseInspection"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>PromiseInspection (promise)](#apidoc.element.ioredis.Promise.PromiseInspection.PromiseInspection)
- description and source-code
```javascript
function PromiseInspection(promise) {
    if (promise !== undefined) {
        promise = promise._target();
        this._bitField = promise._bitField;
        this._settledValueField = promise._isFateSealed()
            ? promise._settledValue() : undefined;
    }
    else {
        this._bitField = 0;
        this._settledValueField = undefined;
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise.PromiseInspection.prototype"></a>[module ioredis.Promise.PromiseInspection.prototype](#apidoc.module.ioredis.Promise.PromiseInspection.prototype)

#### <a name="apidoc.element.ioredis.Promise.PromiseInspection.prototype._settledValue"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>_settledValue ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype._settledValue)
- description and source-code
```javascript
_settledValue = function () {
    return this._settledValueField;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.PromiseInspection.prototype.error"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>error ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.error)
- description and source-code
```javascript
error = function () {
    if (!this.isRejected()) {
        throw new TypeError("cannot get rejection reason of a non-rejected promise\u000a\u000a    See http://goo.gl/MqrFmX\u000a
");
    }
    return this._settledValue();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.PromiseInspection.prototype.isCancelled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>isCancelled ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.isCancelled)
- description and source-code
```javascript
isCancelled = function () {
    return (this._bitField & 8454144) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.PromiseInspection.prototype.isFulfilled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>isFulfilled ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.isFulfilled)
- description and source-code
```javascript
isFulfilled = function () {
    return (this._bitField & 33554432) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.PromiseInspection.prototype.isPending"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>isPending ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.isPending)
- description and source-code
```javascript
isPending = function () {
    return (this._bitField & 50397184) === 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.PromiseInspection.prototype.isRejected"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>isRejected ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.isRejected)
- description and source-code
```javascript
isRejected = function () {
    return (this._bitField & 16777216) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.PromiseInspection.prototype.isResolved"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>isResolved ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.isResolved)
- description and source-code
```javascript
isResolved = function () {
    return (this._bitField & 50331648) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.PromiseInspection.prototype.reason"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>reason ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.reason)
- description and source-code
```javascript
reason = function () {
    if (!this.isRejected()) {
        throw new TypeError("cannot get rejection reason of a non-rejected promise\u000a\u000a    See http://goo.gl/MqrFmX\u000a
");
    }
    return this._settledValue();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.PromiseInspection.prototype.value"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.PromiseInspection.prototype.</span>value ()](#apidoc.element.ioredis.Promise.PromiseInspection.prototype.value)
- description and source-code
```javascript
value = function () {
    if (!this.isFulfilled()) {
        throw new TypeError("cannot get fulfillment value of a non-fulfilled promise\u000a\u000a    See http://goo.gl/MqrFmX\u000a
");
    }
    return this._settledValue();
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise.TimeoutError"></a>[module ioredis.Promise.TimeoutError](#apidoc.module.ioredis.Promise.TimeoutError)

#### <a name="apidoc.element.ioredis.Promise.TimeoutError.TimeoutError"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>TimeoutError (message)](#apidoc.element.ioredis.Promise.TimeoutError.TimeoutError)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise.TimeoutError.prototype"></a>[module ioredis.Promise.TimeoutError.prototype](#apidoc.module.ioredis.Promise.TimeoutError.prototype)

#### <a name="apidoc.element.ioredis.Promise.TimeoutError.prototype.constructor"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.TimeoutError.prototype.</span>constructor (message)](#apidoc.element.ioredis.Promise.TimeoutError.prototype.constructor)
- description and source-code
```javascript
function SubError(message) {
    if (!(this instanceof SubError)) return new SubError(message);
    notEnumerableProp(this, "message",
        typeof message === "string" ? message : defaultMessage);
    notEnumerableProp(this, "name", nameProperty);
    if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
    } else {
        Error.call(this);
    }
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise._SomePromiseArray"></a>[module ioredis.Promise._SomePromiseArray](#apidoc.module.ioredis.Promise._SomePromiseArray)

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray._SomePromiseArray"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>_SomePromiseArray (values)](#apidoc.element.ioredis.Promise._SomePromiseArray._SomePromiseArray)
- description and source-code
```javascript
function SomePromiseArray(values) {
    this.constructor$(values);
    this._howMany = 0;
    this._unwrap = false;
    this._initialized = false;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise._SomePromiseArray.prototype"></a>[module ioredis.Promise._SomePromiseArray.prototype](#apidoc.module.ioredis.Promise._SomePromiseArray.prototype)

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype._addFulfilled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_addFulfilled (value)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._addFulfilled)
- description and source-code
```javascript
_addFulfilled = function (value) {
    this._values[this._totalResolved++] = value;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype._addRejected"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_addRejected (reason)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._addRejected)
- description and source-code
```javascript
_addRejected = function (reason) {
    this._values.push(reason);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype._canPossiblyFulfill"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_canPossiblyFulfill ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._canPossiblyFulfill)
- description and source-code
```javascript
_canPossiblyFulfill = function () {
    return this.length() - this._rejected();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype._checkOutcome"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_checkOutcome ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._checkOutcome)
- description and source-code
```javascript
_checkOutcome = function () {
    if (this.howMany() > this._canPossiblyFulfill()) {
        var e = new AggregateError();
        for (var i = this.length(); i < this._values.length; ++i) {
            if (this._values[i] !== CANCELLATION) {
                e.push(this._values[i]);
            }
        }
        if (e.length > 0) {
            this._reject(e);
        } else {
            this._cancel();
        }
        return true;
    }
    return false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype._fulfilled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_fulfilled ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._fulfilled)
- description and source-code
```javascript
_fulfilled = function () {
    return this._totalResolved;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype._getRangeError"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_getRangeError (count)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._getRangeError)
- description and source-code
```javascript
_getRangeError = function (count) {
    var message = "Input array must contain at least " +
            this._howMany + " items but contains only " + count + " items";
    return new RangeError(message);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype._init"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_init ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._init)
- description and source-code
```javascript
_init = function () {
    if (!this._initialized) {
        return;
    }
    if (this._howMany === 0) {
        this._resolve([]);
        return;
    }
    this._init$(undefined, -5);
    var isArrayResolved = isArray(this._values);
    if (!this._isResolved() &&
        isArrayResolved &&
        this._howMany > this._canPossiblyFulfill()) {
        this._reject(this._getRangeError(this.length()));
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype._promiseCancelled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_promiseCancelled ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._promiseCancelled)
- description and source-code
```javascript
_promiseCancelled = function () {
    if (this._values instanceof Promise || this._values == null) {
        return this._cancel();
    }
    this._addRejected(CANCELLATION);
    return this._checkOutcome();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype._promiseFulfilled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_promiseFulfilled (value)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._promiseFulfilled)
- description and source-code
```javascript
_promiseFulfilled = function (value) {
    this._addFulfilled(value);
    if (this._fulfilled() === this.howMany()) {
        this._values.length = this.howMany();
        if (this.howMany() === 1 && this._unwrap) {
            this._resolve(this._values[0]);
        } else {
            this._resolve(this._values);
        }
        return true;
    }
    return false;

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype._promiseRejected"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_promiseRejected (reason)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._promiseRejected)
- description and source-code
```javascript
_promiseRejected = function (reason) {
    this._addRejected(reason);
    return this._checkOutcome();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype._rejected"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_rejected ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._rejected)
- description and source-code
```javascript
_rejected = function () {
    return this._values.length - this.length();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype._resolveEmptyArray"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>_resolveEmptyArray ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype._resolveEmptyArray)
- description and source-code
```javascript
_resolveEmptyArray = function () {
    this._reject(this._getRangeError(0));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype.constructor"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>constructor (values)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype.constructor)
- description and source-code
```javascript
function SomePromiseArray(values) {
    this.constructor$(values);
    this._howMany = 0;
    this._unwrap = false;
    this._initialized = false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype.howMany"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>howMany ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype.howMany)
- description and source-code
```javascript
howMany = function () {
    return this._howMany;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype.init"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>init ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype.init)
- description and source-code
```javascript
init = function () {
    this._initialized = true;
    this._init();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype.setHowMany"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>setHowMany (count)](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype.setHowMany)
- description and source-code
```javascript
setHowMany = function (count) {
    this._howMany = count;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise._SomePromiseArray.prototype.setUnwrap"></a>[function <span class="apidocSignatureSpan">ioredis.Promise._SomePromiseArray.prototype.</span>setUnwrap ()](#apidoc.element.ioredis.Promise._SomePromiseArray.prototype.setUnwrap)
- description and source-code
```javascript
setUnwrap = function () {
    this._unwrap = true;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise.coroutine"></a>[module ioredis.Promise.coroutine](#apidoc.module.ioredis.Promise.coroutine)

#### <a name="apidoc.element.ioredis.Promise.coroutine.coroutine"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.</span>coroutine (generatorFunction, options)](#apidoc.element.ioredis.Promise.coroutine.coroutine)
- description and source-code
```javascript
coroutine = function (generatorFunction, options) {
    if (typeof generatorFunction !== "function") {
        throw new TypeError("generatorFunction must be a function\u000a\u000a    See http://goo.gl/MqrFmX\u000a");
    }
    var yieldHandler = Object(options).yieldHandler;
    var PromiseSpawn$ = PromiseSpawn;
    var stack = new Error().stack;
    return function () {
        var generator = generatorFunction.apply(this, arguments);
        var spawn = new PromiseSpawn$(undefined, undefined, yieldHandler,
                                      stack);
        var ret = spawn.promise();
        spawn._generator = generator;
        spawn._promiseFulfilled(undefined);
        return ret;
    };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.coroutine.addYieldHandler"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.coroutine.</span>addYieldHandler (fn)](#apidoc.element.ioredis.Promise.coroutine.addYieldHandler)
- description and source-code
```javascript
addYieldHandler = function (fn) {
    if (typeof fn !== "function") {
        throw new TypeError("expecting a function but got " + util.classString(fn));
    }
    yieldHandlers.push(fn);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.Promise.prototype"></a>[module ioredis.Promise.prototype](#apidoc.module.ioredis.Promise.prototype)

#### <a name="apidoc.element.ioredis.Promise.prototype.__isCancelled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>__isCancelled ()](#apidoc.element.ioredis.Promise.prototype.__isCancelled)
- description and source-code
```javascript
__isCancelled = function () {
    return (this._bitField & 65536) === 65536;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._addCallbacks"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_addCallbacks ( fulfill, reject, promise, receiver, domain )](#apidoc.element.ioredis.Promise.prototype._addCallbacks)
- description and source-code
```javascript
_addCallbacks = function ( fulfill, reject, promise, receiver, domain ) {
    var index = this._length();

    if (index >= 65535 - 4) {
        index = 0;
        this._setLength(0);
    }

    if (index === 0) {
        this._promise0 = promise;
        this._receiver0 = receiver;
        if (typeof fulfill === "function") {
            this._fulfillmentHandler0 =
                domain === null ? fulfill : util.domainBind(domain, fulfill);
        }
        if (typeof reject === "function") {
            this._rejectionHandler0 =
                domain === null ? reject : util.domainBind(domain, reject);
        }
    } else {
        var base = index * 4 - 4;
        this[base + 2] = promise;
        this[base + 3] = receiver;
        if (typeof fulfill === "function") {
            this[base + 0] =
                domain === null ? fulfill : util.domainBind(domain, fulfill);
        }
        if (typeof reject === "function") {
            this[base + 1] =
                domain === null ? reject : util.domainBind(domain, reject);
        }
    }
    this._setLength(index + 1);
    return index;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._attachCancellationCallback"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_attachCancellationCallback (onCancel)](#apidoc.element.ioredis.Promise.prototype._attachCancellationCallback)
- description and source-code
```javascript
_attachCancellationCallback = function (onCancel) {
    ;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._attachExtraTrace"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_attachExtraTrace ()](#apidoc.element.ioredis.Promise.prototype._attachExtraTrace)
- description and source-code
```javascript
_attachExtraTrace = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._boundValue"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_boundValue ()](#apidoc.element.ioredis.Promise.prototype._boundValue)
- description and source-code
```javascript
_boundValue = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._branchHasCancelled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_branchHasCancelled ()](#apidoc.element.ioredis.Promise.prototype._branchHasCancelled)
- description and source-code
```javascript
_branchHasCancelled = function () {
    this._branchesRemainingToCancel--;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._cancel"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_cancel ()](#apidoc.element.ioredis.Promise.prototype._cancel)
- description and source-code
```javascript
_cancel = function () {
    if (!this._isCancellable()) return;
    this._setCancelled();
    async.invoke(this._cancelPromises, this, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._cancelBranched"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_cancelBranched ()](#apidoc.element.ioredis.Promise.prototype._cancelBranched)
- description and source-code
```javascript
_cancelBranched = function () {
    if (this._enoughBranchesHaveCancelled()) {
        this._cancel();
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._cancelBy"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_cancelBy (canceller)](#apidoc.element.ioredis.Promise.prototype._cancelBy)
- description and source-code
```javascript
_cancelBy = function (canceller) {
    if (canceller === this) {
        this._branchesRemainingToCancel = 0;
        this._invokeOnCancel();
        return true;
    } else {
        this._branchHasCancelled();
        if (this._enoughBranchesHaveCancelled()) {
            this._invokeOnCancel();
            return true;
        }
    }
    return false;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._cancelPromises"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_cancelPromises ()](#apidoc.element.ioredis.Promise.prototype._cancelPromises)
- description and source-code
```javascript
_cancelPromises = function () {
    if (this._length() > 0) this._settlePromises();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._captureStackTrace"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_captureStackTrace ()](#apidoc.element.ioredis.Promise.prototype._captureStackTrace)
- description and source-code
```javascript
_captureStackTrace = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._clearCallbackDataAtIndex"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_clearCallbackDataAtIndex (index)](#apidoc.element.ioredis.Promise.prototype._clearCallbackDataAtIndex)
- description and source-code
```javascript
_clearCallbackDataAtIndex = function (index) {
    var base = index * 4 - 4;
    this[base + 2] =
    this[base + 3] =
    this[base + 0] =
    this[base + 1] = undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._clearCancellationData"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_clearCancellationData ()](#apidoc.element.ioredis.Promise.prototype._clearCancellationData)
- description and source-code
```javascript
_clearCancellationData = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._doInvokeOnCancel"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_doInvokeOnCancel (onCancelCallback, internalOnly)](#apidoc.element.ioredis.Promise.prototype._doInvokeOnCancel)
- description and source-code
```javascript
_doInvokeOnCancel = function (onCancelCallback, internalOnly) {
    if (util.isArray(onCancelCallback)) {
        for (var i = 0; i < onCancelCallback.length; ++i) {
            this._doInvokeOnCancel(onCancelCallback[i], internalOnly);
        }
    } else if (onCancelCallback !== undefined) {
        if (typeof onCancelCallback === "function") {
            if (!internalOnly) {
                var e = tryCatch(onCancelCallback).call(this._boundValue());
                if (e === errorObj) {
                    this._attachExtraTrace(e.e);
                    async.throwLater(e.e);
                }
            }
        } else {
            onCancelCallback._resultCancelled(this);
        }
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._enoughBranchesHaveCancelled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_enoughBranchesHaveCancelled ()](#apidoc.element.ioredis.Promise.prototype._enoughBranchesHaveCancelled)
- description and source-code
```javascript
_enoughBranchesHaveCancelled = function () {
    return this._branchesRemainingToCancel === undefined ||
           this._branchesRemainingToCancel <= 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._ensurePossibleRejectionHandled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_ensurePossibleRejectionHandled ()](#apidoc.element.ioredis.Promise.prototype._ensurePossibleRejectionHandled)
- description and source-code
```javascript
_ensurePossibleRejectionHandled = function () {
    if ((this._bitField & 524288) !== 0) return;
    this._setRejectionIsUnhandled();
    async.invokeLater(this._notifyUnhandledRejection, this, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._execute"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_execute (executor, resolve, reject)](#apidoc.element.ioredis.Promise.prototype._execute)
- description and source-code
```javascript
_execute = function (executor, resolve, reject) {
    try {
        executor(resolve, reject);
    } catch (e) {
        return e;
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._fireEvent"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_fireEvent ()](#apidoc.element.ioredis.Promise.prototype._fireEvent)
- description and source-code
```javascript
function defaultFireEvent() { return false; }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._followee"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_followee ()](#apidoc.element.ioredis.Promise.prototype._followee)
- description and source-code
```javascript
_followee = function () {
    return this._rejectionHandler0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._fulfill"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_fulfill (value)](#apidoc.element.ioredis.Promise.prototype._fulfill)
- description and source-code
```javascript
_fulfill = function (value) {
    var bitField = this._bitField;
    if (((bitField & 117506048) >>> 16)) return;
    if (value === this) {
        var err = makeSelfResolutionError();
        this._attachExtraTrace(err);
        return this._reject(err);
    }
    this._setFulfilled();
    this._rejectionHandler0 = value;

    if ((bitField & 65535) > 0) {
        if (((bitField & 134217728) !== 0)) {
            this._settlePromises();
        } else {
            async.settlePromises(this);
        }
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._fulfillPromises"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_fulfillPromises (len, value)](#apidoc.element.ioredis.Promise.prototype._fulfillPromises)
- description and source-code
```javascript
_fulfillPromises = function (len, value) {
    for (var i = 1; i < len; i++) {
        var handler = this._fulfillmentHandlerAt(i);
        var promise = this._promiseAt(i);
        var receiver = this._receiverAt(i);
        this._clearCallbackDataAtIndex(i);
        this._settlePromise(promise, handler, receiver, value);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._fulfillmentHandlerAt"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_fulfillmentHandlerAt (index)](#apidoc.element.ioredis.Promise.prototype._fulfillmentHandlerAt)
- description and source-code
```javascript
_fulfillmentHandlerAt = function (index) {
    return this[
            index * 4 - 4 + 0];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._getDisposer"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_getDisposer ()](#apidoc.element.ioredis.Promise.prototype._getDisposer)
- description and source-code
```javascript
_getDisposer = function () {
    return this._disposer;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._invokeInternalOnCancel"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_invokeInternalOnCancel ()](#apidoc.element.ioredis.Promise.prototype._invokeInternalOnCancel)
- description and source-code
```javascript
_invokeInternalOnCancel = function () {
    if (this._isCancellable()) {
        this._doInvokeOnCancel(this._onCancel(), true);
        this._unsetOnCancel();
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._invokeOnCancel"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_invokeOnCancel ()](#apidoc.element.ioredis.Promise.prototype._invokeOnCancel)
- description and source-code
```javascript
_invokeOnCancel = function () {
    var onCancelCallback = this._onCancel();
    this._unsetOnCancel();
    async.invoke(this._doInvokeOnCancel, this, onCancelCallback);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._isBound"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isBound ()](#apidoc.element.ioredis.Promise.prototype._isBound)
- description and source-code
```javascript
_isBound = function () {
    return (this._bitField & 2097152) === 2097152;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._isCancellable"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isCancellable ()](#apidoc.element.ioredis.Promise.prototype._isCancellable)
- description and source-code
```javascript
_isCancellable = function () {
    return this.isPending() && !this._isCancelled();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._isCancelled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isCancelled ()](#apidoc.element.ioredis.Promise.prototype._isCancelled)
- description and source-code
```javascript
_isCancelled = function () {
    return this._target().__isCancelled();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._isDisposable"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isDisposable ()](#apidoc.element.ioredis.Promise.prototype._isDisposable)
- description and source-code
```javascript
_isDisposable = function () {
    return (this._bitField & 131072) > 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._isFateSealed"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isFateSealed ()](#apidoc.element.ioredis.Promise.prototype._isFateSealed)
- description and source-code
```javascript
_isFateSealed = function () {
    return (this._bitField & 117506048) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._isFinal"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isFinal ()](#apidoc.element.ioredis.Promise.prototype._isFinal)
- description and source-code
```javascript
_isFinal = function () {
    return (this._bitField & 4194304) > 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._isFollowing"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isFollowing ()](#apidoc.element.ioredis.Promise.prototype._isFollowing)
- description and source-code
```javascript
_isFollowing = function () {
    return (this._bitField & 67108864) === 67108864;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._isRejectionUnhandled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isRejectionUnhandled ()](#apidoc.element.ioredis.Promise.prototype._isRejectionUnhandled)
- description and source-code
```javascript
_isRejectionUnhandled = function () {
    return (this._bitField & 1048576) > 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._isUnhandledRejectionNotified"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_isUnhandledRejectionNotified ()](#apidoc.element.ioredis.Promise.prototype._isUnhandledRejectionNotified)
- description and source-code
```javascript
_isUnhandledRejectionNotified = function () {
    return (this._bitField & 262144) > 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._length"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_length ()](#apidoc.element.ioredis.Promise.prototype._length)
- description and source-code
```javascript
_length = function () {
    return this._bitField & 65535;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._migrateCallback0"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_migrateCallback0 (follower)](#apidoc.element.ioredis.Promise.prototype._migrateCallback0)
- description and source-code
```javascript
_migrateCallback0 = function (follower) {
    var bitField = follower._bitField;
    var fulfill = follower._fulfillmentHandler0;
    var reject = follower._rejectionHandler0;
    var promise = follower._promise0;
    var receiver = follower._receiverAt(0);
    if (receiver === undefined) receiver = UNDEFINED_BINDING;
    this._addCallbacks(fulfill, reject, promise, receiver, null);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._migrateCallbackAt"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_migrateCallbackAt (follower, index)](#apidoc.element.ioredis.Promise.prototype._migrateCallbackAt)
- description and source-code
```javascript
_migrateCallbackAt = function (follower, index) {
    var fulfill = follower._fulfillmentHandlerAt(index);
    var reject = follower._rejectionHandlerAt(index);
    var promise = follower._promiseAt(index);
    var receiver = follower._receiverAt(index);
    if (receiver === undefined) receiver = UNDEFINED_BINDING;
    this._addCallbacks(fulfill, reject, promise, receiver, null);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._notifyUnhandledRejection"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_notifyUnhandledRejection ()](#apidoc.element.ioredis.Promise.prototype._notifyUnhandledRejection)
- description and source-code
```javascript
_notifyUnhandledRejection = function () {
    if (this._isRejectionUnhandled()) {
        var reason = this._settledValue();
        this._setUnhandledRejectionIsNotified();
        fireRejectionEvent("unhandledRejection",
                                      possiblyUnhandledRejection, reason, this);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._notifyUnhandledRejectionIsHandled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_notifyUnhandledRejectionIsHandled ()](#apidoc.element.ioredis.Promise.prototype._notifyUnhandledRejectionIsHandled)
- description and source-code
```javascript
_notifyUnhandledRejectionIsHandled = function () {
    fireRejectionEvent("rejectionHandled",
                                  unhandledRejectionHandled, undefined, this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._onCancel"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_onCancel ()](#apidoc.element.ioredis.Promise.prototype._onCancel)
- description and source-code
```javascript
_onCancel = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._passThrough"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_passThrough (handler, type, success, fail)](#apidoc.element.ioredis.Promise.prototype._passThrough)
- description and source-code
```javascript
_passThrough = function (handler, type, success, fail) {
    if (typeof handler !== "function") return this.then();
    return this._then(success,
                      fail,
                      undefined,
                      new PassThroughHandlerContext(this, type, handler),
                      undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._peekContext"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_peekContext ()](#apidoc.element.ioredis.Promise.prototype._peekContext)
- description and source-code
```javascript
_peekContext = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._popContext"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_popContext ()](#apidoc.element.ioredis.Promise.prototype._popContext)
- description and source-code
```javascript
_popContext = function () {return null;}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._promiseAt"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_promiseAt (index)](#apidoc.element.ioredis.Promise.prototype._promiseAt)
- description and source-code
```javascript
_promiseAt = function (index) {
    return this[
            index * 4 - 4 + 2];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._promiseCreated"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_promiseCreated ()](#apidoc.element.ioredis.Promise.prototype._promiseCreated)
- description and source-code
```javascript
_promiseCreated = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._propagateFrom"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_propagateFrom (parent, flags)](#apidoc.element.ioredis.Promise.prototype._propagateFrom)
- description and source-code
```javascript
_propagateFrom = function (parent, flags) {
    ;
    ;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._proxy"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_proxy (proxyable, arg)](#apidoc.element.ioredis.Promise.prototype._proxy)
- description and source-code
```javascript
_proxy = function (proxyable, arg) {
    this._addCallbacks(undefined, undefined, arg, proxyable, null);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._pushContext"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_pushContext ()](#apidoc.element.ioredis.Promise.prototype._pushContext)
- description and source-code
```javascript
_pushContext = function () {}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._reason"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_reason ()](#apidoc.element.ioredis.Promise.prototype._reason)
- description and source-code
```javascript
_reason = function () {
    this._unsetRejectionIsUnhandled();
    return this._settledValue();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._receiverAt"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_receiverAt (index)](#apidoc.element.ioredis.Promise.prototype._receiverAt)
- description and source-code
```javascript
_receiverAt = function (index) {
    var ret = index === 0 ? this._receiver0 : this[
            index * 4 - 4 + 3];
    if (ret === UNDEFINED_BINDING) {
        return undefined;
    } else if (ret === undefined && this._isBound()) {
        return this._boundValue();
    }
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._reject"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_reject (reason)](#apidoc.element.ioredis.Promise.prototype._reject)
- description and source-code
```javascript
_reject = function (reason) {
    var bitField = this._bitField;
    if (((bitField & 117506048) >>> 16)) return;
    this._setRejected();
    this._fulfillmentHandler0 = reason;

    if (this._isFinal()) {
        return async.fatalError(reason, util.isNode);
    }

    if ((bitField & 65535) > 0) {
        async.settlePromises(this);
    } else {
        this._ensurePossibleRejectionHandled();
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._rejectCallback"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_rejectCallback (reason, synchronous, ignoreNonErrorWarnings)](#apidoc.element.ioredis.Promise.prototype._rejectCallback)
- description and source-code
```javascript
_rejectCallback = function (reason, synchronous, ignoreNonErrorWarnings) {
    var trace = util.ensureErrorObject(reason);
    var hasStack = trace === reason;
    if (!hasStack && !ignoreNonErrorWarnings && debug.warnings()) {
        var message = "a promise was rejected with a non-error: " +
            util.classString(reason);
        this._warn(message, true);
    }
    this._attachExtraTrace(trace, synchronous ? hasStack : false);
    this._reject(reason);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._rejectPromises"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_rejectPromises (len, reason)](#apidoc.element.ioredis.Promise.prototype._rejectPromises)
- description and source-code
```javascript
_rejectPromises = function (len, reason) {
    for (var i = 1; i < len; i++) {
        var handler = this._rejectionHandlerAt(i);
        var promise = this._promiseAt(i);
        var receiver = this._receiverAt(i);
        this._clearCallbackDataAtIndex(i);
        this._settlePromise(promise, handler, receiver, reason);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._rejectionHandlerAt"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_rejectionHandlerAt (index)](#apidoc.element.ioredis.Promise.prototype._rejectionHandlerAt)
- description and source-code
```javascript
_rejectionHandlerAt = function (index) {
    return this[
            index * 4 - 4 + 1];
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._resolveCallback"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_resolveCallback (value, shouldBind)](#apidoc.element.ioredis.Promise.prototype._resolveCallback)
- description and source-code
```javascript
_resolveCallback = function (value, shouldBind) {
    if (((this._bitField & 117506048) !== 0)) return;
    if (value === this)
        return this._rejectCallback(makeSelfResolutionError(), false);
    var maybePromise = tryConvertToPromise(value, this);
    if (!(maybePromise instanceof Promise)) return this._fulfill(value);

    if (shouldBind) this._propagateFrom(maybePromise, 2);

    var promise = maybePromise._target();

    if (promise === this) {
        this._reject(makeSelfResolutionError());
        return;
    }

    var bitField = promise._bitField;
    if (((bitField & 50397184) === 0)) {
        var len = this._length();
        if (len > 0) promise._migrateCallback0(this);
        for (var i = 1; i < len; ++i) {
            promise._migrateCallbackAt(this, i);
        }
        this._setFollowing();
        this._setLength(0);
        this._setFollowee(promise);
    } else if (((bitField & 33554432) !== 0)) {
        this._fulfill(promise._value());
    } else if (((bitField & 16777216) !== 0)) {
        this._reject(promise._reason());
    } else {
        var reason = new CancellationError("late cancellation observer");
        promise._attachExtraTrace(reason);
        this._reject(reason);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._resolveFromExecutor"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_resolveFromExecutor (executor)](#apidoc.element.ioredis.Promise.prototype._resolveFromExecutor)
- description and source-code
```javascript
_resolveFromExecutor = function (executor) {
    if (executor === INTERNAL) return;
    var promise = this;
    this._captureStackTrace();
    this._pushContext();
    var synchronous = true;
    var r = this._execute(executor, function(value) {
        promise._resolveCallback(value);
    }, function (reason) {
        promise._rejectCallback(reason, synchronous);
    });
    synchronous = false;
    this._popContext();

    if (r !== undefined) {
        promise._rejectCallback(r, true);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._resolveFromSyncValue"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_resolveFromSyncValue (value)](#apidoc.element.ioredis.Promise.prototype._resolveFromSyncValue)
- description and source-code
```javascript
_resolveFromSyncValue = function (value) {
    if (value === util.errorObj) {
        this._rejectCallback(value.e, false);
    } else {
        this._resolveCallback(value, true);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._resultCancelled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_resultCancelled ()](#apidoc.element.ioredis.Promise.prototype._resultCancelled)
- description and source-code
```javascript
_resultCancelled = function () {
    this.cancel();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._returnedNonUndefined"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_returnedNonUndefined ()](#apidoc.element.ioredis.Promise.prototype._returnedNonUndefined)
- description and source-code
```javascript
_returnedNonUndefined = function () {
    return (this._bitField & 268435456) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setAsyncGuaranteed"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setAsyncGuaranteed ()](#apidoc.element.ioredis.Promise.prototype._setAsyncGuaranteed)
- description and source-code
```javascript
_setAsyncGuaranteed = function () {
    if (async.hasCustomScheduler()) return;
    this._bitField = this._bitField | 134217728;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setBoundTo"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setBoundTo (obj)](#apidoc.element.ioredis.Promise.prototype._setBoundTo)
- description and source-code
```javascript
_setBoundTo = function (obj) {
    if (obj !== undefined) {
        this._bitField = this._bitField | 2097152;
        this._boundTo = obj;
    } else {
        this._bitField = this._bitField & (~2097152);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setCancelled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setCancelled ()](#apidoc.element.ioredis.Promise.prototype._setCancelled)
- description and source-code
```javascript
_setCancelled = function () {
    this._bitField = this._bitField | 65536;
    this._fireEvent("promiseCancelled", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setDisposable"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setDisposable (disposer)](#apidoc.element.ioredis.Promise.prototype._setDisposable)
- description and source-code
```javascript
_setDisposable = function (disposer) {
    this._bitField = this._bitField | 131072;
    this._disposer = disposer;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setFollowee"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setFollowee (promise)](#apidoc.element.ioredis.Promise.prototype._setFollowee)
- description and source-code
```javascript
_setFollowee = function (promise) {
    this._rejectionHandler0 = promise;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setFollowing"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setFollowing ()](#apidoc.element.ioredis.Promise.prototype._setFollowing)
- description and source-code
```javascript
_setFollowing = function () {
    this._bitField = this._bitField | 67108864;
    this._fireEvent("promiseResolved", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setFulfilled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setFulfilled ()](#apidoc.element.ioredis.Promise.prototype._setFulfilled)
- description and source-code
```javascript
_setFulfilled = function () {
    this._bitField = this._bitField | 33554432;
    this._fireEvent("promiseFulfilled", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setIsFinal"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setIsFinal ()](#apidoc.element.ioredis.Promise.prototype._setIsFinal)
- description and source-code
```javascript
_setIsFinal = function () {
    this._bitField = this._bitField | 4194304;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setLength"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setLength (len)](#apidoc.element.ioredis.Promise.prototype._setLength)
- description and source-code
```javascript
_setLength = function (len) {
    this._bitField = (this._bitField & -65536) |
        (len & 65535);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setOnCancel"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setOnCancel (handler)](#apidoc.element.ioredis.Promise.prototype._setOnCancel)
- description and source-code
```javascript
_setOnCancel = function (handler) { ; }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setRejected"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setRejected ()](#apidoc.element.ioredis.Promise.prototype._setRejected)
- description and source-code
```javascript
_setRejected = function () {
    this._bitField = this._bitField | 16777216;
    this._fireEvent("promiseRejected", this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setRejectionIsUnhandled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setRejectionIsUnhandled ()](#apidoc.element.ioredis.Promise.prototype._setRejectionIsUnhandled)
- description and source-code
```javascript
_setRejectionIsUnhandled = function () {
    this._bitField = this._bitField | 1048576;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setReturnedNonUndefined"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setReturnedNonUndefined ()](#apidoc.element.ioredis.Promise.prototype._setReturnedNonUndefined)
- description and source-code
```javascript
_setReturnedNonUndefined = function () {
    this._bitField = this._bitField | 268435456;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setUnhandledRejectionIsNotified"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setUnhandledRejectionIsNotified ()](#apidoc.element.ioredis.Promise.prototype._setUnhandledRejectionIsNotified)
- description and source-code
```javascript
_setUnhandledRejectionIsNotified = function () {
    this._bitField = this._bitField | 262144;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._setWillBeCancelled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_setWillBeCancelled ()](#apidoc.element.ioredis.Promise.prototype._setWillBeCancelled)
- description and source-code
```javascript
_setWillBeCancelled = function () {
    this._bitField = this._bitField | 8388608;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._settlePromise"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settlePromise (promise, handler, receiver, value)](#apidoc.element.ioredis.Promise.prototype._settlePromise)
- description and source-code
```javascript
_settlePromise = function (promise, handler, receiver, value) {
    var isPromise = promise instanceof Promise;
    var bitField = this._bitField;
    var asyncGuaranteed = ((bitField & 134217728) !== 0);
    if (((bitField & 65536) !== 0)) {
        if (isPromise) promise._invokeInternalOnCancel();

        if (receiver instanceof PassThroughHandlerContext &&
            receiver.isFinallyHandler()) {
            receiver.cancelPromise = promise;
            if (tryCatch(handler).call(receiver, value) === errorObj) {
                promise._reject(errorObj.e);
            }
        } else if (handler === reflectHandler) {
            promise._fulfill(reflectHandler.call(receiver));
        } else if (receiver instanceof Proxyable) {
            receiver._promiseCancelled(promise);
        } else if (isPromise || promise instanceof PromiseArray) {
            promise._cancel();
        } else {
            receiver.cancel();
        }
    } else if (typeof handler === "function") {
        if (!isPromise) {
            handler.call(receiver, value, promise);
        } else {
            if (asyncGuaranteed) promise._setAsyncGuaranteed();
            this._settlePromiseFromHandler(handler, receiver, value, promise);
        }
    } else if (receiver instanceof Proxyable) {
        if (!receiver._isResolved()) {
            if (((bitField & 33554432) !== 0)) {
                receiver._promiseFulfilled(value, promise);
            } else {
                receiver._promiseRejected(value, promise);
            }
        }
    } else if (isPromise) {
        if (asyncGuaranteed) promise._setAsyncGuaranteed();
        if (((bitField & 33554432) !== 0)) {
            promise._fulfill(value);
        } else {
            promise._reject(value);
        }
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._settlePromise0"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settlePromise0 (handler, value, bitField)](#apidoc.element.ioredis.Promise.prototype._settlePromise0)
- description and source-code
```javascript
_settlePromise0 = function (handler, value, bitField) {
    var promise = this._promise0;
    var receiver = this._receiverAt(0);
    this._promise0 = undefined;
    this._receiver0 = undefined;
    this._settlePromise(promise, handler, receiver, value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._settlePromiseCtx"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settlePromiseCtx (ctx)](#apidoc.element.ioredis.Promise.prototype._settlePromiseCtx)
- description and source-code
```javascript
_settlePromiseCtx = function (ctx) {
    this._settlePromise(ctx.promise, ctx.handler, ctx.receiver, ctx.value);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._settlePromiseFromHandler"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settlePromiseFromHandler ( handler, receiver, value, promise )](#apidoc.element.ioredis.Promise.prototype._settlePromiseFromHandler)
- description and source-code
```javascript
_settlePromiseFromHandler = function ( handler, receiver, value, promise ) {
    var bitField = promise._bitField;
    if (((bitField & 65536) !== 0)) return;
    promise._pushContext();
    var x;
    if (receiver === APPLY) {
        if (!value || typeof value.length !== "number") {
            x = errorObj;
            x.e = new TypeError("cannot .spread() a non-array: " +
                                    util.classString(value));
        } else {
            x = tryCatch(handler).apply(this._boundValue(), value);
        }
    } else {
        x = tryCatch(handler).call(receiver, value);
    }
    var promiseCreated = promise._popContext();
    bitField = promise._bitField;
    if (((bitField & 65536) !== 0)) return;

    if (x === NEXT_FILTER) {
        promise._reject(value);
    } else if (x === errorObj) {
        promise._rejectCallback(x.e, false);
    } else {
        debug.checkForgottenReturns(x, promiseCreated, "",  promise, this);
        promise._resolveCallback(x);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._settlePromiseLateCancellationObserver"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settlePromiseLateCancellationObserver (ctx)](#apidoc.element.ioredis.Promise.prototype._settlePromiseLateCancellationObserver)
- description and source-code
```javascript
_settlePromiseLateCancellationObserver = function (ctx) {
    var handler = ctx.handler;
    var promise = ctx.promise;
    var receiver = ctx.receiver;
    var value = ctx.value;
    if (typeof handler === "function") {
        if (!(promise instanceof Promise)) {
            handler.call(receiver, value, promise);
        } else {
            this._settlePromiseFromHandler(handler, receiver, value, promise);
        }
    } else if (promise instanceof Promise) {
        promise._reject(value);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._settlePromises"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settlePromises ()](#apidoc.element.ioredis.Promise.prototype._settlePromises)
- description and source-code
```javascript
_settlePromises = function () {
    var bitField = this._bitField;
    var len = (bitField & 65535);

    if (len > 0) {
        if (((bitField & 16842752) !== 0)) {
            var reason = this._fulfillmentHandler0;
            this._settlePromise0(this._rejectionHandler0, reason, bitField);
            this._rejectPromises(len, reason);
        } else {
            var value = this._rejectionHandler0;
            this._settlePromise0(this._fulfillmentHandler0, value, bitField);
            this._fulfillPromises(len, value);
        }
        this._setLength(0);
    }
    this._clearCancellationData();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._settledValue"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_settledValue ()](#apidoc.element.ioredis.Promise.prototype._settledValue)
- description and source-code
```javascript
_settledValue = function () {
    var bitField = this._bitField;
    if (((bitField & 33554432) !== 0)) {
        return this._rejectionHandler0;
    } else if (((bitField & 16777216) !== 0)) {
        return this._fulfillmentHandler0;
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._target"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_target ()](#apidoc.element.ioredis.Promise.prototype._target)
- description and source-code
```javascript
_target = function () {
    var ret = this;
    while (ret._isFollowing()) ret = ret._followee();
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._then"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_then ( didFulfill, didReject, _, receiver, internalData )](#apidoc.element.ioredis.Promise.prototype._then)
- description and source-code
```javascript
_then = function ( didFulfill, didReject, _, receiver, internalData ) {
    var haveInternalData = internalData !== undefined;
    var promise = haveInternalData ? internalData : new Promise(INTERNAL);
    var target = this._target();
    var bitField = target._bitField;

    if (!haveInternalData) {
        promise._propagateFrom(this, 3);
        promise._captureStackTrace();
        if (receiver === undefined &&
            ((this._bitField & 2097152) !== 0)) {
            if (!((bitField & 50397184) === 0)) {
                receiver = this._boundValue();
            } else {
                receiver = target === this ? undefined : this._boundTo;
            }
        }
        this._fireEvent("promiseChained", this, promise);
    }

    var domain = getDomain();
    if (!((bitField & 50397184) === 0)) {
        var handler, value, settler = target._settlePromiseCtx;
        if (((bitField & 33554432) !== 0)) {
            value = target._rejectionHandler0;
            handler = didFulfill;
        } else if (((bitField & 16777216) !== 0)) {
            value = target._fulfillmentHandler0;
            handler = didReject;
            target._unsetRejectionIsUnhandled();
        } else {
            settler = target._settlePromiseLateCancellationObserver;
            value = new CancellationError("late cancellation observer");
            target._attachExtraTrace(value);
            handler = didReject;
        }

        async.invoke(settler, target, {
            handler: domain === null ? handler
                : (typeof handler === "function" &&
                    util.domainBind(domain, handler)),
            promise: promise,
            receiver: receiver,
            value: value
        });
    } else {
        target._addCallbacks(didFulfill, didReject, promise, receiver, domain);
    }

    return promise;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._unsetCancelled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_unsetCancelled ()](#apidoc.element.ioredis.Promise.prototype._unsetCancelled)
- description and source-code
```javascript
_unsetCancelled = function () {
    this._bitField = this._bitField & (~65536);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._unsetDisposable"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_unsetDisposable ()](#apidoc.element.ioredis.Promise.prototype._unsetDisposable)
- description and source-code
```javascript
_unsetDisposable = function () {
    this._bitField = this._bitField & (~131072);
    this._disposer = undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._unsetOnCancel"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_unsetOnCancel ()](#apidoc.element.ioredis.Promise.prototype._unsetOnCancel)
- description and source-code
```javascript
_unsetOnCancel = function () {
    this._onCancelField = undefined;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._unsetRejectionIsUnhandled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_unsetRejectionIsUnhandled ()](#apidoc.element.ioredis.Promise.prototype._unsetRejectionIsUnhandled)
- description and source-code
```javascript
_unsetRejectionIsUnhandled = function () {
    this._bitField = this._bitField & (~1048576);
    if (this._isUnhandledRejectionNotified()) {
        this._unsetUnhandledRejectionIsNotified();
        this._notifyUnhandledRejectionIsHandled();
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._unsetUnhandledRejectionIsNotified"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_unsetUnhandledRejectionIsNotified ()](#apidoc.element.ioredis.Promise.prototype._unsetUnhandledRejectionIsNotified)
- description and source-code
```javascript
_unsetUnhandledRejectionIsNotified = function () {
    this._bitField = this._bitField & (~262144);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._value"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_value ()](#apidoc.element.ioredis.Promise.prototype._value)
- description and source-code
```javascript
_value = function () {
    return this._settledValue();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype._warn"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>_warn (message, shouldUseOwnTrace, promise)](#apidoc.element.ioredis.Promise.prototype._warn)
- description and source-code
```javascript
_warn = function (message, shouldUseOwnTrace, promise) {
    return warn(message, shouldUseOwnTrace, promise || this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.all"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>all ()](#apidoc.element.ioredis.Promise.prototype.all)
- description and source-code
```javascript
all = function () {
    if (arguments.length > 0) {
        this._warn(".all() was passed arguments but it does not take any");
    }
    return new PromiseArray(this).promise();
}
```
- example usage
```shell
...
Sometimes you may want to send a command to multiple nodes (masters or slaves) of the cluster, you can get the nodes via 'Cluster
#nodes()' method.

'Cluster#nodes()' accepts a parameter role, which can be "master", "slave" and "all" (default), and returns an array of 'Redis'
instance. For example:

'''javascript
// Send 'FLUSHDB' command to all slaves:
var slaves = cluster.nodes('slave');
Promise.all(slaves.map(function (node) {
return node.flushdb();
}));

// Get keys of all the masters:
var masters = cluster.nodes('master');
Promise.all(masters.map(function (node) {
return node.keys();
...
```

#### <a name="apidoc.element.ioredis.Promise.prototype.any"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>any ()](#apidoc.element.ioredis.Promise.prototype.any)
- description and source-code
```javascript
any = function () {
    return any(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.asCallback"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>asCallback (nodeback, options)](#apidoc.element.ioredis.Promise.prototype.asCallback)
- description and source-code
```javascript
asCallback = function (nodeback, options) {
    if (typeof nodeback == "function") {
        var adapter = successAdapter;
        if (options !== undefined && Object(options).spread) {
            adapter = spreadAdapter;
        }
        this._then(
            adapter,
            errorAdapter,
            undefined,
            this,
            nodeback
        );
    }
    return this;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.bind"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>bind (thisArg)](#apidoc.element.ioredis.Promise.prototype.bind)
- description and source-code
```javascript
bind = function (thisArg) {
    if (!calledBind) {
        calledBind = true;
        Promise.prototype._propagateFrom = debug.propagateFromFunction();
        Promise.prototype._boundValue = debug.boundValueFunction();
    }
    var maybePromise = tryConvertToPromise(thisArg);
    var ret = new Promise(INTERNAL);
    ret._propagateFrom(this, 1);
    var target = this._target();
    ret._setBoundTo(maybePromise);
    if (maybePromise instanceof Promise) {
        var context = {
            promiseRejectionQueued: false,
            promise: ret,
            target: target,
            bindingPromise: maybePromise
        };
        target._then(INTERNAL, targetRejected, undefined, ret, context);
        maybePromise._then(
            bindingResolved, bindingRejected, undefined, ret, context);
        ret._setOnCancel(maybePromise);
    } else {
        ret._resolveCallback(target);
    }
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.break"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>break ()](#apidoc.element.ioredis.Promise.prototype.break)
- description and source-code
```javascript
break = function () {
    if (!debug.cancellation()) return this._warn("cancellation is disabled");

    var promise = this;
    var child = promise;
    while (promise._isCancellable()) {
        if (!promise._cancelBy(child)) {
            if (child._isFollowing()) {
                child._followee().cancel();
            } else {
                child._cancelBranched();
            }
            break;
        }

        var parent = promise._cancellationParent;
        if (parent == null || !parent._isCancellable()) {
            if (promise._isFollowing()) {
                promise._followee().cancel();
            } else {
                promise._cancelBranched();
            }
            break;
        } else {
            if (promise._isFollowing()) promise._followee().cancel();
            promise._setWillBeCancelled();
            child = promise;
            promise = parent;
        }
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.call"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>call (methodName)](#apidoc.element.ioredis.Promise.prototype.call)
- description and source-code
```javascript
call = function (methodName) {
    var $_len = arguments.length;var args = new Array(Math.max($_len - 1, 0)); for(var $_i = 1; $_i < $_len; ++$_i) {args[$_i -
1] = arguments[$_i];};
    if (!false) {
        if (canEvaluate) {
            var maybeCaller = getMethodCaller(methodName);
            if (maybeCaller !== null) {
                return this._then(
                    maybeCaller, undefined, undefined, args, undefined);
            }
        }
    }
    args.push(methodName);
    return this._then(caller, undefined, undefined, args, undefined);
}
```
- example usage
```shell
...
var fbuffer = require('flexbuffer');
var Promise = require('bluebird');
var util = require('util');
var commands = require('redis-commands');
var calculateSlot = require('cluster-key-slot');

function Pipeline(redis) {
Commander.call(this);

this.redis = redis;
this.isCluster = this.redis.constructor.name === 'Cluster';
this.options = redis.options;
this._queue = [];
this._result = [];
this._transactions = 0;
...
```

#### <a name="apidoc.element.ioredis.Promise.prototype.cancel"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>cancel ()](#apidoc.element.ioredis.Promise.prototype.cancel)
- description and source-code
```javascript
cancel = function () {
    if (!debug.cancellation()) return this._warn("cancellation is disabled");

    var promise = this;
    var child = promise;
    while (promise._isCancellable()) {
        if (!promise._cancelBy(child)) {
            if (child._isFollowing()) {
                child._followee().cancel();
            } else {
                child._cancelBranched();
            }
            break;
        }

        var parent = promise._cancellationParent;
        if (parent == null || !parent._isCancellable()) {
            if (promise._isFollowing()) {
                promise._followee().cancel();
            } else {
                promise._cancelBranched();
            }
            break;
        } else {
            if (promise._isFollowing()) promise._followee().cancel();
            promise._setWillBeCancelled();
            child = promise;
            promise = parent;
        }
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.catch"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>catch (fn)](#apidoc.element.ioredis.Promise.prototype.catch)
- description and source-code
```javascript
catch = function (fn) {
    var len = arguments.length;
    if (len > 1) {
        var catchInstances = new Array(len - 1),
            j = 0, i;
        for (i = 0; i < len - 1; ++i) {
            var item = arguments[i];
            if (util.isObject(item)) {
                catchInstances[j++] = item;
            } else {
                return apiRejection("Catch statement predicate: " +
                    "expecting an object but got " + util.classString(item));
            }
        }
        catchInstances.length = j;
        fn = arguments[i];
        return this.then(undefined, catchFilter(catchInstances, fn, this));
    }
    return this.then(undefined, fn);
}
```
- example usage
```shell
...
Pipeline.prototype.sendCommand = function (command) {
  var position = this._queue.length;

  var _this = this;

  command.promise.then(function (result) {
    _this.fillResult([null, result], position);
  }).catch(function (error) {
    _this.fillResult([error], position);
  });

  this._queue.push(command);

  return this;
};
...
```

#### <a name="apidoc.element.ioredis.Promise.prototype.catchReturn"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>catchReturn (value)](#apidoc.element.ioredis.Promise.prototype.catchReturn)
- description and source-code
```javascript
catchReturn = function (value) {
    if (arguments.length <= 1) {
        if (value instanceof Promise) value.suppressUnhandledRejections();
        return this._then(
            undefined, returner, undefined, {value: value}, undefined);
    } else {
        var _value = arguments[1];
        if (_value instanceof Promise) _value.suppressUnhandledRejections();
        var handler = function() {return _value;};
        return this.caught(value, handler);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.catchThrow"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>catchThrow (reason)](#apidoc.element.ioredis.Promise.prototype.catchThrow)
- description and source-code
```javascript
catchThrow = function (reason) {
    if (arguments.length <= 1) {
        return this._then(
            undefined, thrower, undefined, {reason: reason}, undefined);
    } else {
        var _reason = arguments[1];
        var handler = function() {throw _reason;};
        return this.caught(reason, handler);
    }
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.caught"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>caught (fn)](#apidoc.element.ioredis.Promise.prototype.caught)
- description and source-code
```javascript
caught = function (fn) {
    var len = arguments.length;
    if (len > 1) {
        var catchInstances = new Array(len - 1),
            j = 0, i;
        for (i = 0; i < len - 1; ++i) {
            var item = arguments[i];
            if (util.isObject(item)) {
                catchInstances[j++] = item;
            } else {
                return apiRejection("Catch statement predicate: " +
                    "expecting an object but got " + util.classString(item));
            }
        }
        catchInstances.length = j;
        fn = arguments[i];
        return this.then(undefined, catchFilter(catchInstances, fn, this));
    }
    return this.then(undefined, fn);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.delay"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>delay (ms)](#apidoc.element.ioredis.Promise.prototype.delay)
- description and source-code
```javascript
delay = function (ms) {
    return delay(ms, this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.disposer"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>disposer (fn)](#apidoc.element.ioredis.Promise.prototype.disposer)
- description and source-code
```javascript
disposer = function (fn) {
    if (typeof fn === "function") {
        return new FunctionDisposer(fn, this, createContext());
    }
    throw new TypeError();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.done"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>done (didFulfill, didReject)](#apidoc.element.ioredis.Promise.prototype.done)
- description and source-code
```javascript
done = function (didFulfill, didReject) {
    var promise =
        this._then(didFulfill, didReject, undefined, undefined, undefined);
    promise._setIsFinal();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.each"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>each (fn)](#apidoc.element.ioredis.Promise.prototype.each)
- description and source-code
```javascript
each = function (fn) {
    return PromiseReduce(this, fn, INTERNAL, 0)
              ._then(promiseAllThis, undefined, undefined, this, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.error"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>error (fn)](#apidoc.element.ioredis.Promise.prototype.error)
- description and source-code
```javascript
error = function (fn) {
    return this.caught(util.originatesFromRejection, fn);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.filter"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>filter (fn, options)](#apidoc.element.ioredis.Promise.prototype.filter)
- description and source-code
```javascript
filter = function (fn, options) {
    return PromiseMap(this, fn, options, INTERNAL);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.finally"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>finally (handler)](#apidoc.element.ioredis.Promise.prototype.finally)
- description and source-code
```javascript
finally = function (handler) {
    return this._passThrough(handler,
                             0,
                             finallyHandler,
                             finallyHandler);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.get"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>get (propertyName)](#apidoc.element.ioredis.Promise.prototype.get)
- description and source-code
```javascript
get = function (propertyName) {
    var isIndex = (typeof propertyName === "number");
    var getter;
    if (!isIndex) {
        if (canEvaluate) {
            var maybeGetter = getGetter(propertyName);
            getter = maybeGetter !== null ? maybeGetter : namedGetter;
        } else {
            getter = namedGetter;
        }
    } else {
        getter = indexedGetter;
    }
    return this._then(getter, undefined, undefined, propertyName, undefined);
}
```
- example usage
```shell
...
## Basic Usage

'''javascript
var Redis = require('ioredis');
var redis = new Redis();

redis.set('foo', 'bar');
redis.get('foo', function (err, result) {
  console.log(result);
});

// Or using a promise if the last argument isn't a function
redis.get('foo').then(function (result) {
  console.log(result);
});
...
```

#### <a name="apidoc.element.ioredis.Promise.prototype.isCancellable"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>isCancellable ()](#apidoc.element.ioredis.Promise.prototype.isCancellable)
- description and source-code
```javascript
isCancellable = function () {
    return this.isPending() && !this.isCancelled();
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.isCancelled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>isCancelled ()](#apidoc.element.ioredis.Promise.prototype.isCancelled)
- description and source-code
```javascript
isCancelled = function () {
    return (this._target()._bitField & 8454144) !== 0;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.isFulfilled"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>isFulfilled ()](#apidoc.element.ioredis.Promise.prototype.isFulfilled)
- description and source-code
```javascript
isFulfilled = function () {
    return isFulfilled.call(this._target());
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.isPending"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>isPending ()](#apidoc.element.ioredis.Promise.prototype.isPending)
- description and source-code
```javascript
isPending = function () {
    return isPending.call(this._target());
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.isRejected"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>isRejected ()](#apidoc.element.ioredis.Promise.prototype.isRejected)
- description and source-code
```javascript
isRejected = function () {
    return isRejected.call(this._target());
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.isResolved"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>isResolved ()](#apidoc.element.ioredis.Promise.prototype.isResolved)
- description and source-code
```javascript
isResolved = function () {
    return isResolved.call(this._target());
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.lastly"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>lastly (handler)](#apidoc.element.ioredis.Promise.prototype.lastly)
- description and source-code
```javascript
lastly = function (handler) {
    return this._passThrough(handler,
                             0,
                             finallyHandler,
                             finallyHandler);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.map"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>map (fn, options)](#apidoc.element.ioredis.Promise.prototype.map)
- description and source-code
```javascript
map = function (fn, options) {
    return map(this, fn, options, null);
}
```
- example usage
```shell
...
Sometimes you may want to send a command to multiple nodes (masters or slaves) of the cluster, you can get the nodes via 'Cluster
#nodes()' method.

'Cluster#nodes()' accepts a parameter role, which can be "master", "slave" and "all" (default), and returns an array of 'Redis'
instance. For example:

'''javascript
// Send 'FLUSHDB' command to all slaves:
var slaves = cluster.nodes('slave');
Promise.all(slaves.map(function (node) {
return node.flushdb();
}));

// Get keys of all the masters:
var masters = cluster.nodes('master');
Promise.all(masters.map(function (node) {
return node.keys();
...
```

#### <a name="apidoc.element.ioredis.Promise.prototype.mapSeries"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>mapSeries (fn)](#apidoc.element.ioredis.Promise.prototype.mapSeries)
- description and source-code
```javascript
mapSeries = function (fn) {
    return PromiseReduce(this, fn, INTERNAL, INTERNAL);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.nodeify"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>nodeify (nodeback, options)](#apidoc.element.ioredis.Promise.prototype.nodeify)
- description and source-code
```javascript
nodeify = function (nodeback, options) {
    if (typeof nodeback == "function") {
        var adapter = successAdapter;
        if (options !== undefined && Object(options).spread) {
            adapter = spreadAdapter;
        }
        this._then(
            adapter,
            errorAdapter,
            undefined,
            this,
            nodeback
        );
    }
    return this;
}
```
- example usage
```shell
...
for (var i = firstArgIndex; i < length; ++i) {
  args[i - firstArgIndex] = arguments[i];
}

var options;
if (this.options.dropBufferSupport) {
  if (!_encoding) {
    return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
  }
  options = { replyEncoding: null };
} else {
  options = { replyEncoding: _encoding };
}

if (this.options.showFriendlyErrorStack) {
...
```

#### <a name="apidoc.element.ioredis.Promise.prototype.props"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>props ()](#apidoc.element.ioredis.Promise.prototype.props)
- description and source-code
```javascript
props = function () {
    return props(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.race"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>race ()](#apidoc.element.ioredis.Promise.prototype.race)
- description and source-code
```javascript
race = function () {
    return race(this, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.reason"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>reason ()](#apidoc.element.ioredis.Promise.prototype.reason)
- description and source-code
```javascript
reason = function () {
    var target = this._target();
    target._unsetRejectionIsUnhandled();
    return reason.call(target);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.reduce"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>reduce (fn, initialValue)](#apidoc.element.ioredis.Promise.prototype.reduce)
- description and source-code
```javascript
reduce = function (fn, initialValue) {
    return reduce(this, fn, initialValue, null);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.reflect"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>reflect ()](#apidoc.element.ioredis.Promise.prototype.reflect)
- description and source-code
```javascript
reflect = function () {
    return this._then(reflectHandler,
        reflectHandler, undefined, this, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.return"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>return (value)](#apidoc.element.ioredis.Promise.prototype.return)
- description and source-code
```javascript
return = function (value) {
    if (value instanceof Promise) value.suppressUnhandledRejections();
    return this._then(
        returner, undefined, undefined, {value: value}, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.settle"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>settle ()](#apidoc.element.ioredis.Promise.prototype.settle)
- description and source-code
```javascript
settle = function () {
    return Promise.settle(this);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.some"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>some (howMany)](#apidoc.element.ioredis.Promise.prototype.some)
- description and source-code
```javascript
some = function (howMany) {
    return some(this, howMany);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.spread"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>spread (fn)](#apidoc.element.ioredis.Promise.prototype.spread)
- description and source-code
```javascript
spread = function (fn) {
    if (typeof fn !== "function") {
        return apiRejection("expecting a function but got " + util.classString(fn));
    }
    return this.all()._then(fn, undefined, undefined, APPLY, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.suppressUnhandledRejections"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>suppressUnhandledRejections ()](#apidoc.element.ioredis.Promise.prototype.suppressUnhandledRejections)
- description and source-code
```javascript
suppressUnhandledRejections = function () {
    var target = this._target();
    target._bitField = ((target._bitField & (~1048576)) |
                      524288);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.tap"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>tap (handler)](#apidoc.element.ioredis.Promise.prototype.tap)
- description and source-code
```javascript
tap = function (handler) {
    return this._passThrough(handler, 1, finallyHandler);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.tapCatch"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>tapCatch (handlerOrPredicate)](#apidoc.element.ioredis.Promise.prototype.tapCatch)
- description and source-code
```javascript
tapCatch = function (handlerOrPredicate) {
    var len = arguments.length;
    if(len === 1) {
        return this._passThrough(handlerOrPredicate,
                                 1,
                                 undefined,
                                 finallyHandler);
    } else {
         var catchInstances = new Array(len - 1),
            j = 0, i;
        for (i = 0; i < len - 1; ++i) {
            var item = arguments[i];
            if (util.isObject(item)) {
                catchInstances[j++] = item;
            } else {
                return Promise.reject(new TypeError(
                    "tapCatch statement predicate: "
                    + "expecting an object but got " + util.classString(item)
                ));
            }
        }
        catchInstances.length = j;
        var handler = arguments[i];
        return this._passThrough(catchFilter(catchInstances, handler, this),
                                 1,
                                 undefined,
                                 finallyHandler);
    }

}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.then"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>then (didFulfill, didReject)](#apidoc.element.ioredis.Promise.prototype.then)
- description and source-code
```javascript
then = function (didFulfill, didReject) {
    if (debug.warnings() && arguments.length > 0 &&
        typeof didFulfill !== "function" &&
        typeof didReject !== "function") {
        var msg = ".then() only accepts functions but was passed: " +
                util.classString(didFulfill);
        if (arguments.length > 1) {
            msg += ", " + util.classString(didReject);
        }
        this._warn(msg);
    }
    return this._then(didFulfill, didReject, undefined, undefined, undefined);
}
```
- example usage
```shell
...

redis.set('foo', 'bar');
redis.get('foo', function (err, result) {
  console.log(result);
});

// Or using a promise if the last argument isn't a function
redis.get('foo').then(function (result) {
  console.log(result);
});

// Arguments to commands are flattened, so the following are the same:
redis.sadd('set', 1, 3, 5, 7);
redis.sadd('set', [1, 3, 5, 7]);
...
```

#### <a name="apidoc.element.ioredis.Promise.prototype.thenReturn"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>thenReturn (value)](#apidoc.element.ioredis.Promise.prototype.thenReturn)
- description and source-code
```javascript
thenReturn = function (value) {
    if (value instanceof Promise) value.suppressUnhandledRejections();
    return this._then(
        returner, undefined, undefined, {value: value}, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.thenThrow"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>thenThrow (reason)](#apidoc.element.ioredis.Promise.prototype.thenThrow)
- description and source-code
```javascript
thenThrow = function (reason) {
    return this._then(
        thrower, undefined, undefined, {reason: reason}, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.throw"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>throw (reason)](#apidoc.element.ioredis.Promise.prototype.throw)
- description and source-code
```javascript
throw = function (reason) {
    return this._then(
        thrower, undefined, undefined, {reason: reason}, undefined);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.timeout"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>timeout (ms, message)](#apidoc.element.ioredis.Promise.prototype.timeout)
- description and source-code
```javascript
timeout = function (ms, message) {
    ms = +ms;
    var ret, parent;

    var handleWrapper = new HandleWrapper(setTimeout(function timeoutTimeout() {
        if (ret.isPending()) {
            afterTimeout(ret, message, parent);
        }
    }, ms));

    if (debug.cancellation()) {
        parent = this.then();
        ret = parent._then(successClear, failureClear,
                            undefined, handleWrapper, undefined);
        ret._setOnCancel(handleWrapper);
    } else {
        ret = this._then(successClear, failureClear,
                            undefined, handleWrapper, undefined);
    }

    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.toJSON"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>toJSON ()](#apidoc.element.ioredis.Promise.prototype.toJSON)
- description and source-code
```javascript
toJSON = function () {
    var ret = {
        isFulfilled: false,
        isRejected: false,
        fulfillmentValue: undefined,
        rejectionReason: undefined
    };
    if (this.isFulfilled()) {
        ret.fulfillmentValue = this.value();
        ret.isFulfilled = true;
    } else if (this.isRejected()) {
        ret.rejectionReason = this.reason();
        ret.isRejected = true;
    }
    return ret;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.Promise.prototype.toString"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>toString ()](#apidoc.element.ioredis.Promise.prototype.toString)
- description and source-code
```javascript
toString = function () {
    return "[object Promise]";
}
```
- example usage
```shell
...
  }
  var _this = this;
  this.opt.redis[this.opt.command](args, function (err, res) {
    if (err) {
      _this.emit('error', err);
      return;
    }
    _this._redisCursor = (res[0] instanceof Buffer) ? res[0].toString() : res[0];
    if (_this._redisCursor === '0') {
      _this._redisDrained = true;
    }
    _this.push(res[1]);
  });
};
...
```

#### <a name="apidoc.element.ioredis.Promise.prototype.value"></a>[function <span class="apidocSignatureSpan">ioredis.Promise.prototype.</span>value ()](#apidoc.element.ioredis.Promise.prototype.value)
- description and source-code
```javascript
value = function () {
    return value.call(this._target());
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.ReplyError"></a>[module ioredis.ReplyError](#apidoc.module.ioredis.ReplyError)

#### <a name="apidoc.element.ioredis.ReplyError.ReplyError"></a>[function <span class="apidocSignatureSpan">ioredis.</span>ReplyError (message)](#apidoc.element.ioredis.ReplyError.ReplyError)
- description and source-code
```javascript
function ReplyError(message) {
  Error.captureStackTrace(this, this.constructor);
  this.name = this.constructor.name;
  this.message = message;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.ReplyError.super_"></a>[function <span class="apidocSignatureSpan">ioredis.ReplyError.</span>super_ ()](#apidoc.element.ioredis.ReplyError.super_)
- description and source-code
```javascript
function Error() { [native code] }
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.ReplyError.super_"></a>[module ioredis.ReplyError.super_](#apidoc.module.ioredis.ReplyError.super_)

#### <a name="apidoc.element.ioredis.ReplyError.super_.super_"></a>[function <span class="apidocSignatureSpan">ioredis.ReplyError.</span>super_ ()](#apidoc.element.ioredis.ReplyError.super_.super_)
- description and source-code
```javascript
function Error() { [native code] }
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.ReplyError.super_.captureStackTrace"></a>[function <span class="apidocSignatureSpan">ioredis.ReplyError.super_.</span>captureStackTrace ()](#apidoc.element.ioredis.ReplyError.super_.captureStackTrace)
- description and source-code
```javascript
function captureStackTrace() { [native code] }
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.commander"></a>[module ioredis.commander](#apidoc.module.ioredis.commander)

#### <a name="apidoc.element.ioredis.commander.commander"></a>[function <span class="apidocSignatureSpan">ioredis.</span>commander ()](#apidoc.element.ioredis.commander.commander)
- description and source-code
```javascript
function Commander() {
  this.options = _.defaults({}, this.options || {}, {
    showFriendlyErrorStack: false
  });
  this.scriptsSet = {};
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.commander.prototype"></a>[module ioredis.commander.prototype](#apidoc.module.ioredis.commander.prototype)

#### <a name="apidoc.element.ioredis.commander.prototype.append"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>append ()](#apidoc.element.ioredis.commander.prototype.append)
- description and source-code
```javascript
append = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.appendBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>appendBuffer ()](#apidoc.element.ioredis.commander.prototype.appendBuffer)
- description and source-code
```javascript
appendBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.asking"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>asking ()](#apidoc.element.ioredis.commander.prototype.asking)
- description and source-code
```javascript
asking = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.askingBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>askingBuffer ()](#apidoc.element.ioredis.commander.prototype.askingBuffer)
- description and source-code
```javascript
askingBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.auth"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>auth ()](#apidoc.element.ioredis.commander.prototype.auth)
- description and source-code
```javascript
auth = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.authBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>authBuffer ()](#apidoc.element.ioredis.commander.prototype.authBuffer)
- description and source-code
```javascript
authBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.bgrewriteaof"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bgrewriteaof ()](#apidoc.element.ioredis.commander.prototype.bgrewriteaof)
- description and source-code
```javascript
bgrewriteaof = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.bgrewriteaofBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bgrewriteaofBuffer ()](#apidoc.element.ioredis.commander.prototype.bgrewriteaofBuffer)
- description and source-code
```javascript
bgrewriteaofBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.bgsave"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bgsave ()](#apidoc.element.ioredis.commander.prototype.bgsave)
- description and source-code
```javascript
bgsave = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.bgsaveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bgsaveBuffer ()](#apidoc.element.ioredis.commander.prototype.bgsaveBuffer)
- description and source-code
```javascript
bgsaveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.bitcount"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitcount ()](#apidoc.element.ioredis.commander.prototype.bitcount)
- description and source-code
```javascript
bitcount = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.bitcountBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitcountBuffer ()](#apidoc.element.ioredis.commander.prototype.bitcountBuffer)
- description and source-code
```javascript
bitcountBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.bitfield"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitfield ()](#apidoc.element.ioredis.commander.prototype.bitfield)
- description and source-code
```javascript
bitfield = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.bitfieldBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitfieldBuffer ()](#apidoc.element.ioredis.commander.prototype.bitfieldBuffer)
- description and source-code
```javascript
bitfieldBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.bitop"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitop ()](#apidoc.element.ioredis.commander.prototype.bitop)
- description and source-code
```javascript
bitop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.bitopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitopBuffer ()](#apidoc.element.ioredis.commander.prototype.bitopBuffer)
- description and source-code
```javascript
bitopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.bitpos"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitpos ()](#apidoc.element.ioredis.commander.prototype.bitpos)
- description and source-code
```javascript
bitpos = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.bitposBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>bitposBuffer ()](#apidoc.element.ioredis.commander.prototype.bitposBuffer)
- description and source-code
```javascript
bitposBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.blpop"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>blpop ()](#apidoc.element.ioredis.commander.prototype.blpop)
- description and source-code
```javascript
blpop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.blpopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>blpopBuffer ()](#apidoc.element.ioredis.commander.prototype.blpopBuffer)
- description and source-code
```javascript
blpopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.brpop"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>brpop ()](#apidoc.element.ioredis.commander.prototype.brpop)
- description and source-code
```javascript
brpop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.brpopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>brpopBuffer ()](#apidoc.element.ioredis.commander.prototype.brpopBuffer)
- description and source-code
```javascript
brpopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.brpoplpush"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>brpoplpush ()](#apidoc.element.ioredis.commander.prototype.brpoplpush)
- description and source-code
```javascript
brpoplpush = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.brpoplpushBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>brpoplpushBuffer ()](#apidoc.element.ioredis.commander.prototype.brpoplpushBuffer)
- description and source-code
```javascript
brpoplpushBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.call"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>call ()](#apidoc.element.ioredis.commander.prototype.call)
- description and source-code
```javascript
call = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
var fbuffer = require('flexbuffer');
var Promise = require('bluebird');
var util = require('util');
var commands = require('redis-commands');
var calculateSlot = require('cluster-key-slot');

function Pipeline(redis) {
Commander.call(this);

this.redis = redis;
this.isCluster = this.redis.constructor.name === 'Cluster';
this.options = redis.options;
this._queue = [];
this._result = [];
this._transactions = 0;
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.callBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>callBuffer ()](#apidoc.element.ioredis.commander.prototype.callBuffer)
- description and source-code
```javascript
callBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.client"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>client ()](#apidoc.element.ioredis.commander.prototype.client)
- description and source-code
```javascript
client = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.clientBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>clientBuffer ()](#apidoc.element.ioredis.commander.prototype.clientBuffer)
- description and source-code
```javascript
clientBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.cluster"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>cluster ()](#apidoc.element.ioredis.commander.prototype.cluster)
- description and source-code
```javascript
cluster = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.clusterBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>clusterBuffer ()](#apidoc.element.ioredis.commander.prototype.clusterBuffer)
- description and source-code
```javascript
clusterBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.command"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>command ()](#apidoc.element.ioredis.commander.prototype.command)
- description and source-code
```javascript
command = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.commandBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>commandBuffer ()](#apidoc.element.ioredis.commander.prototype.commandBuffer)
- description and source-code
```javascript
commandBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.config"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>config ()](#apidoc.element.ioredis.commander.prototype.config)
- description and source-code
```javascript
config = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.configBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>configBuffer ()](#apidoc.element.ioredis.commander.prototype.configBuffer)
- description and source-code
```javascript
configBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.createBuiltinCommand"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>createBuiltinCommand (commandName)](#apidoc.element.ioredis.commander.prototype.createBuiltinCommand)
- description and source-code
```javascript
createBuiltinCommand = function (commandName) {
  return {
    string: generateFunction(commandName, 'utf8'),
    buffer: generateFunction(commandName, null)
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.dbsize"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>dbsize ()](#apidoc.element.ioredis.commander.prototype.dbsize)
- description and source-code
```javascript
dbsize = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.dbsizeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>dbsizeBuffer ()](#apidoc.element.ioredis.commander.prototype.dbsizeBuffer)
- description and source-code
```javascript
dbsizeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.debug"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>debug ()](#apidoc.element.ioredis.commander.prototype.debug)
- description and source-code
```javascript
debug = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.debugBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>debugBuffer ()](#apidoc.element.ioredis.commander.prototype.debugBuffer)
- description and source-code
```javascript
debugBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.decr"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>decr ()](#apidoc.element.ioredis.commander.prototype.decr)
- description and source-code
```javascript
decr = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.decrBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>decrBuffer ()](#apidoc.element.ioredis.commander.prototype.decrBuffer)
- description and source-code
```javascript
decrBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.decrby"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>decrby ()](#apidoc.element.ioredis.commander.prototype.decrby)
- description and source-code
```javascript
decrby = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.decrbyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>decrbyBuffer ()](#apidoc.element.ioredis.commander.prototype.decrbyBuffer)
- description and source-code
```javascript
decrbyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.defineCommand"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>defineCommand (name, definition)](#apidoc.element.ioredis.commander.prototype.defineCommand)
- description and source-code
```javascript
defineCommand = function (name, definition) {
  var script = new Script(definition.lua, definition.numberOfKeys,
                          this.options.keyPrefix);
  this.scriptsSet[name] = script;
  this[name] = generateScriptingFunction(script, 'utf8');
  this[name + 'Buffer'] = generateScriptingFunction(script, null);
}
```
- example usage
```shell
...
care of script caching and to detect when to use 'EVAL' and when to use 'EVALSHA'.
ioredis expose a 'defineCommand' method to make scripting much easier to use:

'''javascript
var redis = new Redis();

// This will define a command echo:
redis.defineCommand('echo', {
  numberOfKeys: 2,
  lua: 'return {KEYS[1],KEYS[2],ARGV[1],ARGV[2]}'
});

// Now 'echo' can be used just like any other ordinary command,
// and ioredis will try to use 'EVALSHA' internally when possible for better performance.
redis.echo('k1', 'k2', 'a1', 'a2', function (err, result) {
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.del"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>del ()](#apidoc.element.ioredis.commander.prototype.del)
- description and source-code
```javascript
del = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
'redis.pipeline()' creates a 'Pipeline' instance. You can call any Redis
commands on it just like the 'Redis' instance. The commands are queued in memory
and flushed to Redis by calling the 'exec' method:

'''javascript
var pipeline = redis.pipeline();
pipeline.set('foo', 'bar');
pipeline.del('cc');
pipeline.exec(function (err, results) {
  // 'err' is always null, and 'results' is an array of responses
  // corresponding to the sequence of queued commands.
  // Each response follows the format '[err, result]'.
});

// You can even chain the commands:
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.delBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>delBuffer ()](#apidoc.element.ioredis.commander.prototype.delBuffer)
- description and source-code
```javascript
delBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.discard"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>discard ()](#apidoc.element.ioredis.commander.prototype.discard)
- description and source-code
```javascript
discard = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.discardBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>discardBuffer ()](#apidoc.element.ioredis.commander.prototype.discardBuffer)
- description and source-code
```javascript
discardBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.dump"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>dump ()](#apidoc.element.ioredis.commander.prototype.dump)
- description and source-code
```javascript
dump = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.dumpBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>dumpBuffer ()](#apidoc.element.ioredis.commander.prototype.dumpBuffer)
- description and source-code
```javascript
dumpBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.echo"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>echo ()](#apidoc.element.ioredis.commander.prototype.echo)
- description and source-code
```javascript
echo = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
redis.defineCommand('echo', {
  numberOfKeys: 2,
  lua: 'return {KEYS[1],KEYS[2],ARGV[1],ARGV[2]}'
});

// Now 'echo' can be used just like any other ordinary command,
// and ioredis will try to use 'EVALSHA' internally when possible for better performance.
redis.echo('k1', 'k2', 'a1', 'a2', function (err, result) {
  // result === ['k1', 'k2', 'a1', 'a2']
});

// 'echoBuffer' is also defined automatically to return buffers instead of strings:
redis.echoBuffer('k1', 'k2', 'a1', 'a2', function (err, result) {
  // result[0] === new Buffer('k1');
});
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.echoBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>echoBuffer ()](#apidoc.element.ioredis.commander.prototype.echoBuffer)
- description and source-code
```javascript
echoBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
// Now 'echo' can be used just like any other ordinary command,
// and ioredis will try to use 'EVALSHA' internally when possible for better performance.
redis.echo('k1', 'k2', 'a1', 'a2', function (err, result) {
  // result === ['k1', 'k2', 'a1', 'a2']
});

// 'echoBuffer' is also defined automatically to return buffers instead of strings:
redis.echoBuffer('k1', 'k2', 'a1', 'a2', function (err, result) {
  // result[0] === new Buffer('k1');
});

// And of course it works with pipeline:
redis.pipeline().set('foo', 'bar').echo('k1', 'k2', 'a1', 'a2').exec();
'''
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.eval"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>eval ()](#apidoc.element.ioredis.commander.prototype.eval)
- description and source-code
```javascript
eval = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.evalBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>evalBuffer ()](#apidoc.element.ioredis.commander.prototype.evalBuffer)
- description and source-code
```javascript
evalBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.evalsha"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>evalsha ()](#apidoc.element.ioredis.commander.prototype.evalsha)
- description and source-code
```javascript
evalsha = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.evalshaBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>evalshaBuffer ()](#apidoc.element.ioredis.commander.prototype.evalshaBuffer)
- description and source-code
```javascript
evalshaBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.exec"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>exec ()](#apidoc.element.ioredis.commander.prototype.exec)
- description and source-code
```javascript
exec = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
commands on it just like the 'Redis' instance. The commands are queued in memory
and flushed to Redis by calling the 'exec' method:

'''javascript
var pipeline = redis.pipeline();
pipeline.set('foo', 'bar');
pipeline.del('cc');
pipeline.exec(function (err, results) {
  // 'err' is always null, and 'results' is an array of responses
  // corresponding to the sequence of queued commands.
  // Each response follows the format '[err, result]'.
});

// You can even chain the commands:
redis.pipeline().set('foo', 'bar').del('cc').exec(function (err, results) {
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.execBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>execBuffer ()](#apidoc.element.ioredis.commander.prototype.execBuffer)
- description and source-code
```javascript
execBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.exists"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>exists ()](#apidoc.element.ioredis.commander.prototype.exists)
- description and source-code
```javascript
exists = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
        message: error.message
      };
    } else if (commonError.name !== error.name || commonError.message !== error.message) {
      retriable = false;
      break;
    }
  } else if (!inTransaction) {
    var isReadOnly = commands.exists(command.name) && commands.hasFlag(command.name, 'readonly');
    if (!isReadOnly) {
      retriable = false;
      break;
    }
  }
}
if (commonError && retriable) {
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.existsBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>existsBuffer ()](#apidoc.element.ioredis.commander.prototype.existsBuffer)
- description and source-code
```javascript
existsBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.expire"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>expire ()](#apidoc.element.ioredis.commander.prototype.expire)
- description and source-code
```javascript
expire = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.expireBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>expireBuffer ()](#apidoc.element.ioredis.commander.prototype.expireBuffer)
- description and source-code
```javascript
expireBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.expireat"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>expireat ()](#apidoc.element.ioredis.commander.prototype.expireat)
- description and source-code
```javascript
expireat = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.expireatBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>expireatBuffer ()](#apidoc.element.ioredis.commander.prototype.expireatBuffer)
- description and source-code
```javascript
expireatBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.flushall"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>flushall ()](#apidoc.element.ioredis.commander.prototype.flushall)
- description and source-code
```javascript
flushall = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.flushallBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>flushallBuffer ()](#apidoc.element.ioredis.commander.prototype.flushallBuffer)
- description and source-code
```javascript
flushallBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.flushdb"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>flushdb ()](#apidoc.element.ioredis.commander.prototype.flushdb)
- description and source-code
```javascript
flushdb = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...

'Cluster#nodes()' accepts a parameter role, which can be "master", "slave" and "all" (default), and returns an array of 'Redis'
instance. For example:

'''javascript
// Send 'FLUSHDB' command to all slaves:
var slaves = cluster.nodes('slave');
Promise.all(slaves.map(function (node) {
  return node.flushdb();
}));

// Get keys of all the masters:
var masters = cluster.nodes('master');
Promise.all(masters.map(function (node) {
  return node.keys();
})).then(function (keys) {
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.flushdbBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>flushdbBuffer ()](#apidoc.element.ioredis.commander.prototype.flushdbBuffer)
- description and source-code
```javascript
flushdbBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.geoadd"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geoadd ()](#apidoc.element.ioredis.commander.prototype.geoadd)
- description and source-code
```javascript
geoadd = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.geoaddBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geoaddBuffer ()](#apidoc.element.ioredis.commander.prototype.geoaddBuffer)
- description and source-code
```javascript
geoaddBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.geodist"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geodist ()](#apidoc.element.ioredis.commander.prototype.geodist)
- description and source-code
```javascript
geodist = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.geodistBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geodistBuffer ()](#apidoc.element.ioredis.commander.prototype.geodistBuffer)
- description and source-code
```javascript
geodistBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.geohash"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geohash ()](#apidoc.element.ioredis.commander.prototype.geohash)
- description and source-code
```javascript
geohash = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.geohashBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geohashBuffer ()](#apidoc.element.ioredis.commander.prototype.geohashBuffer)
- description and source-code
```javascript
geohashBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.geopos"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geopos ()](#apidoc.element.ioredis.commander.prototype.geopos)
- description and source-code
```javascript
geopos = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.geoposBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>geoposBuffer ()](#apidoc.element.ioredis.commander.prototype.geoposBuffer)
- description and source-code
```javascript
geoposBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.georadius"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>georadius ()](#apidoc.element.ioredis.commander.prototype.georadius)
- description and source-code
```javascript
georadius = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.georadiusBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>georadiusBuffer ()](#apidoc.element.ioredis.commander.prototype.georadiusBuffer)
- description and source-code
```javascript
georadiusBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.georadiusbymember"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>georadiusbymember ()](#apidoc.element.ioredis.commander.prototype.georadiusbymember)
- description and source-code
```javascript
georadiusbymember = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.georadiusbymemberBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>georadiusbymemberBuffer ()](#apidoc.element.ioredis.commander.prototype.georadiusbymemberBuffer)
- description and source-code
```javascript
georadiusbymemberBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.get"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>get ()](#apidoc.element.ioredis.commander.prototype.get)
- description and source-code
```javascript
get = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
## Basic Usage

'''javascript
var Redis = require('ioredis');
var redis = new Redis();

redis.set('foo', 'bar');
redis.get('foo', function (err, result) {
  console.log(result);
});

// Or using a promise if the last argument isn't a function
redis.get('foo').then(function (result) {
  console.log(result);
});
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.getBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getBuffer ()](#apidoc.element.ioredis.commander.prototype.getBuffer)
- description and source-code
```javascript
getBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
redis.set('foo', new Buffer('bar'));
'''

And every command has a method that returns a Buffer (by adding a suffix of "Buffer" to the command name).
To get a buffer instead of a utf8 string:

'''javascript
redis.getBuffer('foo', function (err, result) {
  // result is a buffer.
});
'''

## Pipelining
If you want to send a batch of commands (e.g. > 5), you can use pipelining to queue
the commands in memory and then send them to Redis all at once. This way the performance improves by 50%~300% (See [benchmark section
](#benchmark)).
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.getBuiltinCommands"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getBuiltinCommands ()](#apidoc.element.ioredis.commander.prototype.getBuiltinCommands)
- description and source-code
```javascript
getBuiltinCommands = function () {
  return _.clone(commands);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.getbit"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getbit ()](#apidoc.element.ioredis.commander.prototype.getbit)
- description and source-code
```javascript
getbit = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.getbitBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getbitBuffer ()](#apidoc.element.ioredis.commander.prototype.getbitBuffer)
- description and source-code
```javascript
getbitBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.getrange"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getrange ()](#apidoc.element.ioredis.commander.prototype.getrange)
- description and source-code
```javascript
getrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.getrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getrangeBuffer ()](#apidoc.element.ioredis.commander.prototype.getrangeBuffer)
- description and source-code
```javascript
getrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.getset"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getset ()](#apidoc.element.ioredis.commander.prototype.getset)
- description and source-code
```javascript
getset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.getsetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>getsetBuffer ()](#apidoc.element.ioredis.commander.prototype.getsetBuffer)
- description and source-code
```javascript
getsetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hdel"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hdel ()](#apidoc.element.ioredis.commander.prototype.hdel)
- description and source-code
```javascript
hdel = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hdelBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hdelBuffer ()](#apidoc.element.ioredis.commander.prototype.hdelBuffer)
- description and source-code
```javascript
hdelBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hexists"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hexists ()](#apidoc.element.ioredis.commander.prototype.hexists)
- description and source-code
```javascript
hexists = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hexistsBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hexistsBuffer ()](#apidoc.element.ioredis.commander.prototype.hexistsBuffer)
- description and source-code
```javascript
hexistsBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hget"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hget ()](#apidoc.element.ioredis.commander.prototype.hget)
- description and source-code
```javascript
hget = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hgetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hgetBuffer ()](#apidoc.element.ioredis.commander.prototype.hgetBuffer)
- description and source-code
```javascript
hgetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hgetall"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hgetall ()](#apidoc.element.ioredis.commander.prototype.hgetall)
- description and source-code
```javascript
hgetall = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hgetallBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hgetallBuffer ()](#apidoc.element.ioredis.commander.prototype.hgetallBuffer)
- description and source-code
```javascript
hgetallBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hincrby"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hincrby ()](#apidoc.element.ioredis.commander.prototype.hincrby)
- description and source-code
```javascript
hincrby = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hincrbyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hincrbyBuffer ()](#apidoc.element.ioredis.commander.prototype.hincrbyBuffer)
- description and source-code
```javascript
hincrbyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hincrbyfloat"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hincrbyfloat ()](#apidoc.element.ioredis.commander.prototype.hincrbyfloat)
- description and source-code
```javascript
hincrbyfloat = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hincrbyfloatBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hincrbyfloatBuffer ()](#apidoc.element.ioredis.commander.prototype.hincrbyfloatBuffer)
- description and source-code
```javascript
hincrbyfloatBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hkeys"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hkeys ()](#apidoc.element.ioredis.commander.prototype.hkeys)
- description and source-code
```javascript
hkeys = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hkeysBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hkeysBuffer ()](#apidoc.element.ioredis.commander.prototype.hkeysBuffer)
- description and source-code
```javascript
hkeysBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hlen"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hlen ()](#apidoc.element.ioredis.commander.prototype.hlen)
- description and source-code
```javascript
hlen = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hlenBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hlenBuffer ()](#apidoc.element.ioredis.commander.prototype.hlenBuffer)
- description and source-code
```javascript
hlenBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hmget"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hmget ()](#apidoc.element.ioredis.commander.prototype.hmget)
- description and source-code
```javascript
hmget = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hmgetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hmgetBuffer ()](#apidoc.element.ioredis.commander.prototype.hmgetBuffer)
- description and source-code
```javascript
hmgetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hmset"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hmset ()](#apidoc.element.ioredis.commander.prototype.hmset)
- description and source-code
```javascript
hmset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hmsetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hmsetBuffer ()](#apidoc.element.ioredis.commander.prototype.hmsetBuffer)
- description and source-code
```javascript
hmsetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hscan"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hscan ()](#apidoc.element.ioredis.commander.prototype.hscan)
- description and source-code
```javascript
hscan = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hscanBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hscanBuffer ()](#apidoc.element.ioredis.commander.prototype.hscanBuffer)
- description and source-code
```javascript
hscanBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hset"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hset ()](#apidoc.element.ioredis.commander.prototype.hset)
- description and source-code
```javascript
hset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hsetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hsetBuffer ()](#apidoc.element.ioredis.commander.prototype.hsetBuffer)
- description and source-code
```javascript
hsetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hsetnx"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hsetnx ()](#apidoc.element.ioredis.commander.prototype.hsetnx)
- description and source-code
```javascript
hsetnx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hsetnxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hsetnxBuffer ()](#apidoc.element.ioredis.commander.prototype.hsetnxBuffer)
- description and source-code
```javascript
hsetnxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hstrlen"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hstrlen ()](#apidoc.element.ioredis.commander.prototype.hstrlen)
- description and source-code
```javascript
hstrlen = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hstrlenBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hstrlenBuffer ()](#apidoc.element.ioredis.commander.prototype.hstrlenBuffer)
- description and source-code
```javascript
hstrlenBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hvals"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hvals ()](#apidoc.element.ioredis.commander.prototype.hvals)
- description and source-code
```javascript
hvals = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.hvalsBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>hvalsBuffer ()](#apidoc.element.ioredis.commander.prototype.hvalsBuffer)
- description and source-code
```javascript
hvalsBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.incr"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>incr ()](#apidoc.element.ioredis.commander.prototype.incr)
- description and source-code
```javascript
incr = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.incrBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>incrBuffer ()](#apidoc.element.ioredis.commander.prototype.incrBuffer)
- description and source-code
```javascript
incrBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.incrby"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>incrby ()](#apidoc.element.ioredis.commander.prototype.incrby)
- description and source-code
```javascript
incrby = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.incrbyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>incrbyBuffer ()](#apidoc.element.ioredis.commander.prototype.incrbyBuffer)
- description and source-code
```javascript
incrbyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.incrbyfloat"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>incrbyfloat ()](#apidoc.element.ioredis.commander.prototype.incrbyfloat)
- description and source-code
```javascript
incrbyfloat = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.incrbyfloatBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>incrbyfloatBuffer ()](#apidoc.element.ioredis.commander.prototype.incrbyfloatBuffer)
- description and source-code
```javascript
incrbyfloatBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.info"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>info ()](#apidoc.element.ioredis.commander.prototype.info)
- description and source-code
```javascript
info = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.infoBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>infoBuffer ()](#apidoc.element.ioredis.commander.prototype.infoBuffer)
- description and source-code
```javascript
infoBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.keys"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>keys ()](#apidoc.element.ioredis.commander.prototype.keys)
- description and source-code
```javascript
keys = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
Promise.all(slaves.map(function (node) {
  return node.flushdb();
}));

// Get keys of all the masters:
var masters = cluster.nodes('master');
Promise.all(masters.map(function (node) {
  return node.keys();
})).then(function (keys) {
  // keys: [['key1', 'key2'], ['key3', 'key4']]
});
'''

### Transaction and pipeline in Cluster mode
Almost all features that are supported by 'Redis' are also supported by 'Redis.Cluster', e.g. custom commands, transaction and pipeline
.
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.keysBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>keysBuffer ()](#apidoc.element.ioredis.commander.prototype.keysBuffer)
- description and source-code
```javascript
keysBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lastsave"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lastsave ()](#apidoc.element.ioredis.commander.prototype.lastsave)
- description and source-code
```javascript
lastsave = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lastsaveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lastsaveBuffer ()](#apidoc.element.ioredis.commander.prototype.lastsaveBuffer)
- description and source-code
```javascript
lastsaveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.latency"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>latency ()](#apidoc.element.ioredis.commander.prototype.latency)
- description and source-code
```javascript
latency = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.latencyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>latencyBuffer ()](#apidoc.element.ioredis.commander.prototype.latencyBuffer)
- description and source-code
```javascript
latencyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lindex"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lindex ()](#apidoc.element.ioredis.commander.prototype.lindex)
- description and source-code
```javascript
lindex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lindexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lindexBuffer ()](#apidoc.element.ioredis.commander.prototype.lindexBuffer)
- description and source-code
```javascript
lindexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.linsert"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>linsert ()](#apidoc.element.ioredis.commander.prototype.linsert)
- description and source-code
```javascript
linsert = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.linsertBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>linsertBuffer ()](#apidoc.element.ioredis.commander.prototype.linsertBuffer)
- description and source-code
```javascript
linsertBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.llen"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>llen ()](#apidoc.element.ioredis.commander.prototype.llen)
- description and source-code
```javascript
llen = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.llenBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>llenBuffer ()](#apidoc.element.ioredis.commander.prototype.llenBuffer)
- description and source-code
```javascript
llenBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lpop"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lpop ()](#apidoc.element.ioredis.commander.prototype.lpop)
- description and source-code
```javascript
lpop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lpopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lpopBuffer ()](#apidoc.element.ioredis.commander.prototype.lpopBuffer)
- description and source-code
```javascript
lpopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lpush"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lpush ()](#apidoc.element.ioredis.commander.prototype.lpush)
- description and source-code
```javascript
lpush = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lpushBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lpushBuffer ()](#apidoc.element.ioredis.commander.prototype.lpushBuffer)
- description and source-code
```javascript
lpushBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lpushx"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lpushx ()](#apidoc.element.ioredis.commander.prototype.lpushx)
- description and source-code
```javascript
lpushx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lpushxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lpushxBuffer ()](#apidoc.element.ioredis.commander.prototype.lpushxBuffer)
- description and source-code
```javascript
lpushxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lrange"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lrange ()](#apidoc.element.ioredis.commander.prototype.lrange)
- description and source-code
```javascript
lrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lrangeBuffer ()](#apidoc.element.ioredis.commander.prototype.lrangeBuffer)
- description and source-code
```javascript
lrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lrem"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lrem ()](#apidoc.element.ioredis.commander.prototype.lrem)
- description and source-code
```javascript
lrem = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lremBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lremBuffer ()](#apidoc.element.ioredis.commander.prototype.lremBuffer)
- description and source-code
```javascript
lremBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lset"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lset ()](#apidoc.element.ioredis.commander.prototype.lset)
- description and source-code
```javascript
lset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.lsetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>lsetBuffer ()](#apidoc.element.ioredis.commander.prototype.lsetBuffer)
- description and source-code
```javascript
lsetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.ltrim"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>ltrim ()](#apidoc.element.ioredis.commander.prototype.ltrim)
- description and source-code
```javascript
ltrim = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.ltrimBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>ltrimBuffer ()](#apidoc.element.ioredis.commander.prototype.ltrimBuffer)
- description and source-code
```javascript
ltrimBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.memory"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>memory ()](#apidoc.element.ioredis.commander.prototype.memory)
- description and source-code
```javascript
memory = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.memoryBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>memoryBuffer ()](#apidoc.element.ioredis.commander.prototype.memoryBuffer)
- description and source-code
```javascript
memoryBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.mget"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>mget ()](#apidoc.element.ioredis.commander.prototype.mget)
- description and source-code
```javascript
mget = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.mgetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>mgetBuffer ()](#apidoc.element.ioredis.commander.prototype.mgetBuffer)
- description and source-code
```javascript
mgetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.migrate"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>migrate ()](#apidoc.element.ioredis.commander.prototype.migrate)
- description and source-code
```javascript
migrate = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.migrateBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>migrateBuffer ()](#apidoc.element.ioredis.commander.prototype.migrateBuffer)
- description and source-code
```javascript
migrateBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.module"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>module ()](#apidoc.element.ioredis.commander.prototype.module)
- description and source-code
```javascript
module = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.moduleBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>moduleBuffer ()](#apidoc.element.ioredis.commander.prototype.moduleBuffer)
- description and source-code
```javascript
moduleBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.move"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>move ()](#apidoc.element.ioredis.commander.prototype.move)
- description and source-code
```javascript
move = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.moveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>moveBuffer ()](#apidoc.element.ioredis.commander.prototype.moveBuffer)
- description and source-code
```javascript
moveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.mset"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>mset ()](#apidoc.element.ioredis.commander.prototype.mset)
- description and source-code
```javascript
mset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
'''

There are three built-in transformers, two argument transformers for 'hmset' & 'mset' and
a reply transformer for 'hgetall'. Transformers for 'hmset' and 'hgetall' were mentioned
above, and the transformer for 'mset' is similar to the one for 'hmset':

'''javascript
redis.mset({ k1: 'v1', k2: 'v2' });
redis.get('k1', function (err, result) {
// result === 'v1';
});

redis.mset(new Map([['k3', 'v3'], ['k4', 'v4']]));
redis.get('k3', function (err, result) {
// result === 'v3';
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.msetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>msetBuffer ()](#apidoc.element.ioredis.commander.prototype.msetBuffer)
- description and source-code
```javascript
msetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.msetnx"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>msetnx ()](#apidoc.element.ioredis.commander.prototype.msetnx)
- description and source-code
```javascript
msetnx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.msetnxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>msetnxBuffer ()](#apidoc.element.ioredis.commander.prototype.msetnxBuffer)
- description and source-code
```javascript
msetnxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.multi"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>multi ()](#apidoc.element.ioredis.commander.prototype.multi)
- description and source-code
```javascript
multi = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...

## Transaction
Most of the time, the transaction commands 'multi' & 'exec' are used together with pipeline.
Therefore, when 'multi' is called, a 'Pipeline' instance is created automatically by default,
so you can use 'multi' just like 'pipeline':

'''javascript
redis.multi().set('foo', 'bar').get('foo').exec(function (err, results) {
  // results === [[null, 'OK'], [null, 'bar']]
});
'''
If there's a syntax error in the transaction's command chain (e.g. wrong number of arguments, wrong command name, etc),
then none of the commands would be executed, and an error is returned:

'''javascript
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.multiBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>multiBuffer ()](#apidoc.element.ioredis.commander.prototype.multiBuffer)
- description and source-code
```javascript
multiBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.object"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>object ()](#apidoc.element.ioredis.commander.prototype.object)
- description and source-code
```javascript
object = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.objectBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>objectBuffer ()](#apidoc.element.ioredis.commander.prototype.objectBuffer)
- description and source-code
```javascript
objectBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.persist"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>persist ()](#apidoc.element.ioredis.commander.prototype.persist)
- description and source-code
```javascript
persist = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.persistBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>persistBuffer ()](#apidoc.element.ioredis.commander.prototype.persistBuffer)
- description and source-code
```javascript
persistBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pexpire"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pexpire ()](#apidoc.element.ioredis.commander.prototype.pexpire)
- description and source-code
```javascript
pexpire = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pexpireBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pexpireBuffer ()](#apidoc.element.ioredis.commander.prototype.pexpireBuffer)
- description and source-code
```javascript
pexpireBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pexpireat"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pexpireat ()](#apidoc.element.ioredis.commander.prototype.pexpireat)
- description and source-code
```javascript
pexpireat = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pexpireatBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pexpireatBuffer ()](#apidoc.element.ioredis.commander.prototype.pexpireatBuffer)
- description and source-code
```javascript
pexpireatBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pfadd"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfadd ()](#apidoc.element.ioredis.commander.prototype.pfadd)
- description and source-code
```javascript
pfadd = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pfaddBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfaddBuffer ()](#apidoc.element.ioredis.commander.prototype.pfaddBuffer)
- description and source-code
```javascript
pfaddBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pfcount"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfcount ()](#apidoc.element.ioredis.commander.prototype.pfcount)
- description and source-code
```javascript
pfcount = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pfcountBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfcountBuffer ()](#apidoc.element.ioredis.commander.prototype.pfcountBuffer)
- description and source-code
```javascript
pfcountBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pfdebug"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfdebug ()](#apidoc.element.ioredis.commander.prototype.pfdebug)
- description and source-code
```javascript
pfdebug = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pfdebugBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfdebugBuffer ()](#apidoc.element.ioredis.commander.prototype.pfdebugBuffer)
- description and source-code
```javascript
pfdebugBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pfmerge"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfmerge ()](#apidoc.element.ioredis.commander.prototype.pfmerge)
- description and source-code
```javascript
pfmerge = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pfmergeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfmergeBuffer ()](#apidoc.element.ioredis.commander.prototype.pfmergeBuffer)
- description and source-code
```javascript
pfmergeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pfselftest"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfselftest ()](#apidoc.element.ioredis.commander.prototype.pfselftest)
- description and source-code
```javascript
pfselftest = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pfselftestBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pfselftestBuffer ()](#apidoc.element.ioredis.commander.prototype.pfselftestBuffer)
- description and source-code
```javascript
pfselftestBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.ping"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>ping ()](#apidoc.element.ioredis.commander.prototype.ping)
- description and source-code
```javascript
ping = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pingBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pingBuffer ()](#apidoc.element.ioredis.commander.prototype.pingBuffer)
- description and source-code
```javascript
pingBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.post"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>post ()](#apidoc.element.ioredis.commander.prototype.post)
- description and source-code
```javascript
post = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.postBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>postBuffer ()](#apidoc.element.ioredis.commander.prototype.postBuffer)
- description and source-code
```javascript
postBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.psetex"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>psetex ()](#apidoc.element.ioredis.commander.prototype.psetex)
- description and source-code
```javascript
psetex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.psetexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>psetexBuffer ()](#apidoc.element.ioredis.commander.prototype.psetexBuffer)
- description and source-code
```javascript
psetexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.psubscribe"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>psubscribe ()](#apidoc.element.ioredis.commander.prototype.psubscribe)
- description and source-code
```javascript
psubscribe = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
  // Both 'channel' and 'message' are buffers.
});
'''

'PSUBSCRIBE' is also supported in a similar way:

'''javascript
redis.psubscribe('pat?ern', function (err, count) {});
redis.on('pmessage', function (pattern, channel, message) {});
redis.on('pmessageBuffer', function (pattern, channel, message) {});
'''

When a client issues a SUBSCRIBE or PSUBSCRIBE, that connection is put into a "subscriber" mode.
At that point, only commands that modify the subscription set are valid.
When the subscription set is empty, the connection is put back into regular mode.
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.psubscribeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>psubscribeBuffer ()](#apidoc.element.ioredis.commander.prototype.psubscribeBuffer)
- description and source-code
```javascript
psubscribeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.psync"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>psync ()](#apidoc.element.ioredis.commander.prototype.psync)
- description and source-code
```javascript
psync = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.psyncBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>psyncBuffer ()](#apidoc.element.ioredis.commander.prototype.psyncBuffer)
- description and source-code
```javascript
psyncBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pttl"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pttl ()](#apidoc.element.ioredis.commander.prototype.pttl)
- description and source-code
```javascript
pttl = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pttlBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pttlBuffer ()](#apidoc.element.ioredis.commander.prototype.pttlBuffer)
- description and source-code
```javascript
pttlBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.publish"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>publish ()](#apidoc.element.ioredis.commander.prototype.publish)
- description and source-code
```javascript
publish = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
var Redis = require('ioredis');
var redis = new Redis();
var pub = new Redis();
redis.subscribe('news', 'music', function (err, count) {
// Now we are subscribed to both the 'news' and 'music' channels.
// 'count' represents the number of channels we are currently subscribed to.

pub.publish('news', 'Hello world!');
pub.publish('music', 'Hello again!');
});

redis.on('message', function (channel, message) {
// Receive message Hello world! from channel news
// Receive message Hello again! from channel music
console.log('Receive message %s from channel %s', message, channel);
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.publishBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>publishBuffer ()](#apidoc.element.ioredis.commander.prototype.publishBuffer)
- description and source-code
```javascript
publishBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pubsub"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pubsub ()](#apidoc.element.ioredis.commander.prototype.pubsub)
- description and source-code
```javascript
pubsub = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.pubsubBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>pubsubBuffer ()](#apidoc.element.ioredis.commander.prototype.pubsubBuffer)
- description and source-code
```javascript
pubsubBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.punsubscribe"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>punsubscribe ()](#apidoc.element.ioredis.commander.prototype.punsubscribe)
- description and source-code
```javascript
punsubscribe = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.punsubscribeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>punsubscribeBuffer ()](#apidoc.element.ioredis.commander.prototype.punsubscribeBuffer)
- description and source-code
```javascript
punsubscribeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.quit"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>quit ()](#apidoc.element.ioredis.commander.prototype.quit)
- description and source-code
```javascript
quit = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
});
'''

You can learn more from the [Redis documentation](http://redis.io/commands/scan).

## Auto-reconnect
By default, ioredis will try to reconnect when the connection to Redis is lost
except when the connection is closed manually by 'redis.disconnect()' or 'redis.quit()'.

It's very flexible to control how long to wait to reconnect after disconnection
using the 'retryStrategy' option:

'''javascript
var redis = new Redis({
// This is the default value of 'retryStrategy'
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.quitBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>quitBuffer ()](#apidoc.element.ioredis.commander.prototype.quitBuffer)
- description and source-code
```javascript
quitBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.randomkey"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>randomkey ()](#apidoc.element.ioredis.commander.prototype.randomkey)
- description and source-code
```javascript
randomkey = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.randomkeyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>randomkeyBuffer ()](#apidoc.element.ioredis.commander.prototype.randomkeyBuffer)
- description and source-code
```javascript
randomkeyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.readonly"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>readonly ()](#apidoc.element.ioredis.commander.prototype.readonly)
- description and source-code
```javascript
readonly = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.readonlyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>readonlyBuffer ()](#apidoc.element.ioredis.commander.prototype.readonlyBuffer)
- description and source-code
```javascript
readonlyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.readwrite"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>readwrite ()](#apidoc.element.ioredis.commander.prototype.readwrite)
- description and source-code
```javascript
readwrite = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.readwriteBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>readwriteBuffer ()](#apidoc.element.ioredis.commander.prototype.readwriteBuffer)
- description and source-code
```javascript
readwriteBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.rename"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rename ()](#apidoc.element.ioredis.commander.prototype.rename)
- description and source-code
```javascript
rename = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.renameBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>renameBuffer ()](#apidoc.element.ioredis.commander.prototype.renameBuffer)
- description and source-code
```javascript
renameBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.renamenx"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>renamenx ()](#apidoc.element.ioredis.commander.prototype.renamenx)
- description and source-code
```javascript
renamenx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.renamenxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>renamenxBuffer ()](#apidoc.element.ioredis.commander.prototype.renamenxBuffer)
- description and source-code
```javascript
renamenxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.replconf"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>replconf ()](#apidoc.element.ioredis.commander.prototype.replconf)
- description and source-code
```javascript
replconf = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.replconfBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>replconfBuffer ()](#apidoc.element.ioredis.commander.prototype.replconfBuffer)
- description and source-code
```javascript
replconfBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.restore"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>restore ()](#apidoc.element.ioredis.commander.prototype.restore)
- description and source-code
```javascript
restore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.restore-asking"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>restore-asking ()](#apidoc.element.ioredis.commander.prototype.restore-asking)
- description and source-code
```javascript
restore-asking = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.restore-askingBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>restore-askingBuffer ()](#apidoc.element.ioredis.commander.prototype.restore-askingBuffer)
- description and source-code
```javascript
restore-askingBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.restoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>restoreBuffer ()](#apidoc.element.ioredis.commander.prototype.restoreBuffer)
- description and source-code
```javascript
restoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.role"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>role ()](#apidoc.element.ioredis.commander.prototype.role)
- description and source-code
```javascript
role = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.roleBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>roleBuffer ()](#apidoc.element.ioredis.commander.prototype.roleBuffer)
- description and source-code
```javascript
roleBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.rpop"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpop ()](#apidoc.element.ioredis.commander.prototype.rpop)
- description and source-code
```javascript
rpop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.rpopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpopBuffer ()](#apidoc.element.ioredis.commander.prototype.rpopBuffer)
- description and source-code
```javascript
rpopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.rpoplpush"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpoplpush ()](#apidoc.element.ioredis.commander.prototype.rpoplpush)
- description and source-code
```javascript
rpoplpush = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.rpoplpushBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpoplpushBuffer ()](#apidoc.element.ioredis.commander.prototype.rpoplpushBuffer)
- description and source-code
```javascript
rpoplpushBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.rpush"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpush ()](#apidoc.element.ioredis.commander.prototype.rpush)
- description and source-code
```javascript
rpush = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.rpushBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpushBuffer ()](#apidoc.element.ioredis.commander.prototype.rpushBuffer)
- description and source-code
```javascript
rpushBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.rpushx"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpushx ()](#apidoc.element.ioredis.commander.prototype.rpushx)
- description and source-code
```javascript
rpushx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.rpushxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>rpushxBuffer ()](#apidoc.element.ioredis.commander.prototype.rpushxBuffer)
- description and source-code
```javascript
rpushxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sadd"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sadd ()](#apidoc.element.ioredis.commander.prototype.sadd)
- description and source-code
```javascript
sadd = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...

// Or using a promise if the last argument isn't a function
redis.get('foo').then(function (result) {
  console.log(result);
});

// Arguments to commands are flattened, so the following are the same:
redis.sadd('set', 1, 3, 5, 7);
redis.sadd('set', [1, 3, 5, 7]);

// All arguments are passed directly to the redis server:
redis.set('key', 100, 'EX', 10);
'''

See the 'examples/' folder for more examples.
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.saddBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>saddBuffer ()](#apidoc.element.ioredis.commander.prototype.saddBuffer)
- description and source-code
```javascript
saddBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.save"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>save ()](#apidoc.element.ioredis.commander.prototype.save)
- description and source-code
```javascript
save = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.saveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>saveBuffer ()](#apidoc.element.ioredis.commander.prototype.saveBuffer)
- description and source-code
```javascript
saveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.scan"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>scan ()](#apidoc.element.ioredis.commander.prototype.scan)
- description and source-code
```javascript
scan = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.scanBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>scanBuffer ()](#apidoc.element.ioredis.commander.prototype.scanBuffer)
- description and source-code
```javascript
scanBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.scard"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>scard ()](#apidoc.element.ioredis.commander.prototype.scard)
- description and source-code
```javascript
scard = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.scardBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>scardBuffer ()](#apidoc.element.ioredis.commander.prototype.scardBuffer)
- description and source-code
```javascript
scardBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.script"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>script ()](#apidoc.element.ioredis.commander.prototype.script)
- description and source-code
```javascript
script = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
}

var _this = this;
if (!scripts.length) {
  return execPipeline();
}

return this.redis.script('exists', scripts.map(function (item) {
  return item.sha;
})).then(function (results) {
  var pending = [];
  for (var i = 0; i < results.length; ++i) {
    if (!results[i]) {
      pending.push(scripts[i]);
    }
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.scriptBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>scriptBuffer ()](#apidoc.element.ioredis.commander.prototype.scriptBuffer)
- description and source-code
```javascript
scriptBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sdiff"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sdiff ()](#apidoc.element.ioredis.commander.prototype.sdiff)
- description and source-code
```javascript
sdiff = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sdiffBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sdiffBuffer ()](#apidoc.element.ioredis.commander.prototype.sdiffBuffer)
- description and source-code
```javascript
sdiffBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sdiffstore"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sdiffstore ()](#apidoc.element.ioredis.commander.prototype.sdiffstore)
- description and source-code
```javascript
sdiffstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sdiffstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sdiffstoreBuffer ()](#apidoc.element.ioredis.commander.prototype.sdiffstoreBuffer)
- description and source-code
```javascript
sdiffstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.select"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>select ()](#apidoc.element.ioredis.commander.prototype.select)
- description and source-code
```javascript
select = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.selectBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>selectBuffer ()](#apidoc.element.ioredis.commander.prototype.selectBuffer)
- description and source-code
```javascript
selectBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sendCommand"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sendCommand ()](#apidoc.element.ioredis.commander.prototype.sendCommand)
- description and source-code
```javascript
sendCommand = function () {}
```
- example usage
```shell
...
  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
};
}

function generateScriptingFunction(_script, _encoding) {
return function () {
  var length = arguments.length;
  var lastArgIndex = length - 1;
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.send_command"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>send_command ()](#apidoc.element.ioredis.commander.prototype.send_command)
- description and source-code
```javascript
send_command = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sentinel"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sentinel ()](#apidoc.element.ioredis.commander.prototype.sentinel)
- description and source-code
```javascript
sentinel = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sentinelBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sentinelBuffer ()](#apidoc.element.ioredis.commander.prototype.sentinelBuffer)
- description and source-code
```javascript
sentinelBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.set"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>set ()](#apidoc.element.ioredis.commander.prototype.set)
- description and source-code
```javascript
set = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...

## Basic Usage

'''javascript
var Redis = require('ioredis');
var redis = new Redis();

redis.set('foo', 'bar');
redis.get('foo', function (err, result) {
console.log(result);
});

// Or using a promise if the last argument isn't a function
redis.get('foo').then(function (result) {
console.log(result);
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.setBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setBuffer ()](#apidoc.element.ioredis.commander.prototype.setBuffer)
- description and source-code
```javascript
setBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.setbit"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setbit ()](#apidoc.element.ioredis.commander.prototype.setbit)
- description and source-code
```javascript
setbit = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.setbitBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setbitBuffer ()](#apidoc.element.ioredis.commander.prototype.setbitBuffer)
- description and source-code
```javascript
setbitBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.setex"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setex ()](#apidoc.element.ioredis.commander.prototype.setex)
- description and source-code
```javascript
setex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.setexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setexBuffer ()](#apidoc.element.ioredis.commander.prototype.setexBuffer)
- description and source-code
```javascript
setexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.setnx"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setnx ()](#apidoc.element.ioredis.commander.prototype.setnx)
- description and source-code
```javascript
setnx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.setnxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setnxBuffer ()](#apidoc.element.ioredis.commander.prototype.setnxBuffer)
- description and source-code
```javascript
setnxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.setrange"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setrange ()](#apidoc.element.ioredis.commander.prototype.setrange)
- description and source-code
```javascript
setrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.setrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>setrangeBuffer ()](#apidoc.element.ioredis.commander.prototype.setrangeBuffer)
- description and source-code
```javascript
setrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.shutdown"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>shutdown ()](#apidoc.element.ioredis.commander.prototype.shutdown)
- description and source-code
```javascript
shutdown = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.shutdownBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>shutdownBuffer ()](#apidoc.element.ioredis.commander.prototype.shutdownBuffer)
- description and source-code
```javascript
shutdownBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sinter"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sinter ()](#apidoc.element.ioredis.commander.prototype.sinter)
- description and source-code
```javascript
sinter = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sinterBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sinterBuffer ()](#apidoc.element.ioredis.commander.prototype.sinterBuffer)
- description and source-code
```javascript
sinterBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sinterstore"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sinterstore ()](#apidoc.element.ioredis.commander.prototype.sinterstore)
- description and source-code
```javascript
sinterstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sinterstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sinterstoreBuffer ()](#apidoc.element.ioredis.commander.prototype.sinterstoreBuffer)
- description and source-code
```javascript
sinterstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sismember"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sismember ()](#apidoc.element.ioredis.commander.prototype.sismember)
- description and source-code
```javascript
sismember = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sismemberBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sismemberBuffer ()](#apidoc.element.ioredis.commander.prototype.sismemberBuffer)
- description and source-code
```javascript
sismemberBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.slaveof"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>slaveof ()](#apidoc.element.ioredis.commander.prototype.slaveof)
- description and source-code
```javascript
slaveof = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.slaveofBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>slaveofBuffer ()](#apidoc.element.ioredis.commander.prototype.slaveofBuffer)
- description and source-code
```javascript
slaveofBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.slowlog"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>slowlog ()](#apidoc.element.ioredis.commander.prototype.slowlog)
- description and source-code
```javascript
slowlog = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.slowlogBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>slowlogBuffer ()](#apidoc.element.ioredis.commander.prototype.slowlogBuffer)
- description and source-code
```javascript
slowlogBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.smembers"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>smembers ()](#apidoc.element.ioredis.commander.prototype.smembers)
- description and source-code
```javascript
smembers = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.smembersBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>smembersBuffer ()](#apidoc.element.ioredis.commander.prototype.smembersBuffer)
- description and source-code
```javascript
smembersBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.smove"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>smove ()](#apidoc.element.ioredis.commander.prototype.smove)
- description and source-code
```javascript
smove = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.smoveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>smoveBuffer ()](#apidoc.element.ioredis.commander.prototype.smoveBuffer)
- description and source-code
```javascript
smoveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sort"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sort ()](#apidoc.element.ioredis.commander.prototype.sort)
- description and source-code
```javascript
sort = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
  numberOfKeys: 2,
  lua: 'return {KEYS[1],KEYS[2],ARGV[1],ARGV[2]}'
});

// Works well with pipelining/transaction
fooRedis.pipeline()
  // Sends SORT foo:list BY foo:weight_*->fieldname
  .sort('list', 'BY', 'weight_*->fieldname')
  // Supports custom commands
  // Sends EVALSHA xxx foo:k1 foo:k2 a1 a2
  .echo('k1', 'k2', 'a1', 'a2')
  .exec()
'''

## Transforming Arguments & Replies
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.sortBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sortBuffer ()](#apidoc.element.ioredis.commander.prototype.sortBuffer)
- description and source-code
```javascript
sortBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.spop"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>spop ()](#apidoc.element.ioredis.commander.prototype.spop)
- description and source-code
```javascript
spop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.spopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>spopBuffer ()](#apidoc.element.ioredis.commander.prototype.spopBuffer)
- description and source-code
```javascript
spopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.srandmember"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>srandmember ()](#apidoc.element.ioredis.commander.prototype.srandmember)
- description and source-code
```javascript
srandmember = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.srandmemberBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>srandmemberBuffer ()](#apidoc.element.ioredis.commander.prototype.srandmemberBuffer)
- description and source-code
```javascript
srandmemberBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.srem"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>srem ()](#apidoc.element.ioredis.commander.prototype.srem)
- description and source-code
```javascript
srem = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sremBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sremBuffer ()](#apidoc.element.ioredis.commander.prototype.sremBuffer)
- description and source-code
```javascript
sremBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sscan"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sscan ()](#apidoc.element.ioredis.commander.prototype.sscan)
- description and source-code
```javascript
sscan = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sscanBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sscanBuffer ()](#apidoc.element.ioredis.commander.prototype.sscanBuffer)
- description and source-code
```javascript
sscanBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.strlen"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>strlen ()](#apidoc.element.ioredis.commander.prototype.strlen)
- description and source-code
```javascript
strlen = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.strlenBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>strlenBuffer ()](#apidoc.element.ioredis.commander.prototype.strlenBuffer)
- description and source-code
```javascript
strlenBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.subscribe"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>subscribe ()](#apidoc.element.ioredis.commander.prototype.subscribe)
- description and source-code
```javascript
subscribe = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
It subscribes to a channel with one connection
and publishes to that channel with the other:

'''javascript
var Redis = require('ioredis');
var redis = new Redis();
var pub = new Redis();
redis.subscribe('news', 'music', function (err, count) {
  // Now we are subscribed to both the 'news' and 'music' channels.
  // 'count' represents the number of channels we are currently subscribed to.

  pub.publish('news', 'Hello world!');
  pub.publish('music', 'Hello again!');
});
...
```

#### <a name="apidoc.element.ioredis.commander.prototype.subscribeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>subscribeBuffer ()](#apidoc.element.ioredis.commander.prototype.subscribeBuffer)
- description and source-code
```javascript
subscribeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.substr"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>substr ()](#apidoc.element.ioredis.commander.prototype.substr)
- description and source-code
```javascript
substr = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.substrBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>substrBuffer ()](#apidoc.element.ioredis.commander.prototype.substrBuffer)
- description and source-code
```javascript
substrBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sunion"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sunion ()](#apidoc.element.ioredis.commander.prototype.sunion)
- description and source-code
```javascript
sunion = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sunionBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sunionBuffer ()](#apidoc.element.ioredis.commander.prototype.sunionBuffer)
- description and source-code
```javascript
sunionBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sunionstore"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sunionstore ()](#apidoc.element.ioredis.commander.prototype.sunionstore)
- description and source-code
```javascript
sunionstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sunionstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sunionstoreBuffer ()](#apidoc.element.ioredis.commander.prototype.sunionstoreBuffer)
- description and source-code
```javascript
sunionstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.swapdb"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>swapdb ()](#apidoc.element.ioredis.commander.prototype.swapdb)
- description and source-code
```javascript
swapdb = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.swapdbBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>swapdbBuffer ()](#apidoc.element.ioredis.commander.prototype.swapdbBuffer)
- description and source-code
```javascript
swapdbBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.sync"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>sync ()](#apidoc.element.ioredis.commander.prototype.sync)
- description and source-code
```javascript
sync = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.syncBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>syncBuffer ()](#apidoc.element.ioredis.commander.prototype.syncBuffer)
- description and source-code
```javascript
syncBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.time"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>time ()](#apidoc.element.ioredis.commander.prototype.time)
- description and source-code
```javascript
time = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.timeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>timeBuffer ()](#apidoc.element.ioredis.commander.prototype.timeBuffer)
- description and source-code
```javascript
timeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.touch"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>touch ()](#apidoc.element.ioredis.commander.prototype.touch)
- description and source-code
```javascript
touch = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.touchBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>touchBuffer ()](#apidoc.element.ioredis.commander.prototype.touchBuffer)
- description and source-code
```javascript
touchBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.ttl"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>ttl ()](#apidoc.element.ioredis.commander.prototype.ttl)
- description and source-code
```javascript
ttl = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.ttlBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>ttlBuffer ()](#apidoc.element.ioredis.commander.prototype.ttlBuffer)
- description and source-code
```javascript
ttlBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.type"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>type ()](#apidoc.element.ioredis.commander.prototype.type)
- description and source-code
```javascript
type = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.typeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>typeBuffer ()](#apidoc.element.ioredis.commander.prototype.typeBuffer)
- description and source-code
```javascript
typeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.unlink"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>unlink ()](#apidoc.element.ioredis.commander.prototype.unlink)
- description and source-code
```javascript
unlink = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.unlinkBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>unlinkBuffer ()](#apidoc.element.ioredis.commander.prototype.unlinkBuffer)
- description and source-code
```javascript
unlinkBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.unsubscribe"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>unsubscribe ()](#apidoc.element.ioredis.commander.prototype.unsubscribe)
- description and source-code
```javascript
unsubscribe = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.unsubscribeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>unsubscribeBuffer ()](#apidoc.element.ioredis.commander.prototype.unsubscribeBuffer)
- description and source-code
```javascript
unsubscribeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.unwatch"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>unwatch ()](#apidoc.element.ioredis.commander.prototype.unwatch)
- description and source-code
```javascript
unwatch = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.unwatchBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>unwatchBuffer ()](#apidoc.element.ioredis.commander.prototype.unwatchBuffer)
- description and source-code
```javascript
unwatchBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.wait"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>wait ()](#apidoc.element.ioredis.commander.prototype.wait)
- description and source-code
```javascript
wait = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.waitBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>waitBuffer ()](#apidoc.element.ioredis.commander.prototype.waitBuffer)
- description and source-code
```javascript
waitBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.watch"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>watch ()](#apidoc.element.ioredis.commander.prototype.watch)
- description and source-code
```javascript
watch = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.watchBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>watchBuffer ()](#apidoc.element.ioredis.commander.prototype.watchBuffer)
- description and source-code
```javascript
watchBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zadd"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zadd ()](#apidoc.element.ioredis.commander.prototype.zadd)
- description and source-code
```javascript
zadd = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zaddBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zaddBuffer ()](#apidoc.element.ioredis.commander.prototype.zaddBuffer)
- description and source-code
```javascript
zaddBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zcard"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zcard ()](#apidoc.element.ioredis.commander.prototype.zcard)
- description and source-code
```javascript
zcard = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zcardBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zcardBuffer ()](#apidoc.element.ioredis.commander.prototype.zcardBuffer)
- description and source-code
```javascript
zcardBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zcount"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zcount ()](#apidoc.element.ioredis.commander.prototype.zcount)
- description and source-code
```javascript
zcount = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zcountBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zcountBuffer ()](#apidoc.element.ioredis.commander.prototype.zcountBuffer)
- description and source-code
```javascript
zcountBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zincrby"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zincrby ()](#apidoc.element.ioredis.commander.prototype.zincrby)
- description and source-code
```javascript
zincrby = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zincrbyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zincrbyBuffer ()](#apidoc.element.ioredis.commander.prototype.zincrbyBuffer)
- description and source-code
```javascript
zincrbyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zinterstore"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zinterstore ()](#apidoc.element.ioredis.commander.prototype.zinterstore)
- description and source-code
```javascript
zinterstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zinterstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zinterstoreBuffer ()](#apidoc.element.ioredis.commander.prototype.zinterstoreBuffer)
- description and source-code
```javascript
zinterstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zlexcount"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zlexcount ()](#apidoc.element.ioredis.commander.prototype.zlexcount)
- description and source-code
```javascript
zlexcount = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zlexcountBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zlexcountBuffer ()](#apidoc.element.ioredis.commander.prototype.zlexcountBuffer)
- description and source-code
```javascript
zlexcountBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrange"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrange ()](#apidoc.element.ioredis.commander.prototype.zrange)
- description and source-code
```javascript
zrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrangeBuffer ()](#apidoc.element.ioredis.commander.prototype.zrangeBuffer)
- description and source-code
```javascript
zrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrangebylex"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrangebylex ()](#apidoc.element.ioredis.commander.prototype.zrangebylex)
- description and source-code
```javascript
zrangebylex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrangebylexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrangebylexBuffer ()](#apidoc.element.ioredis.commander.prototype.zrangebylexBuffer)
- description and source-code
```javascript
zrangebylexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrangebyscore"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrangebyscore ()](#apidoc.element.ioredis.commander.prototype.zrangebyscore)
- description and source-code
```javascript
zrangebyscore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrangebyscoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrangebyscoreBuffer ()](#apidoc.element.ioredis.commander.prototype.zrangebyscoreBuffer)
- description and source-code
```javascript
zrangebyscoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrank"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrank ()](#apidoc.element.ioredis.commander.prototype.zrank)
- description and source-code
```javascript
zrank = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrankBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrankBuffer ()](#apidoc.element.ioredis.commander.prototype.zrankBuffer)
- description and source-code
```javascript
zrankBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrem"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrem ()](#apidoc.element.ioredis.commander.prototype.zrem)
- description and source-code
```javascript
zrem = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zremBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremBuffer ()](#apidoc.element.ioredis.commander.prototype.zremBuffer)
- description and source-code
```javascript
zremBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zremrangebylex"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremrangebylex ()](#apidoc.element.ioredis.commander.prototype.zremrangebylex)
- description and source-code
```javascript
zremrangebylex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zremrangebylexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremrangebylexBuffer ()](#apidoc.element.ioredis.commander.prototype.zremrangebylexBuffer)
- description and source-code
```javascript
zremrangebylexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zremrangebyrank"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremrangebyrank ()](#apidoc.element.ioredis.commander.prototype.zremrangebyrank)
- description and source-code
```javascript
zremrangebyrank = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zremrangebyrankBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremrangebyrankBuffer ()](#apidoc.element.ioredis.commander.prototype.zremrangebyrankBuffer)
- description and source-code
```javascript
zremrangebyrankBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zremrangebyscore"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremrangebyscore ()](#apidoc.element.ioredis.commander.prototype.zremrangebyscore)
- description and source-code
```javascript
zremrangebyscore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zremrangebyscoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zremrangebyscoreBuffer ()](#apidoc.element.ioredis.commander.prototype.zremrangebyscoreBuffer)
- description and source-code
```javascript
zremrangebyscoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrevrange"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrange ()](#apidoc.element.ioredis.commander.prototype.zrevrange)
- description and source-code
```javascript
zrevrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrevrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrangeBuffer ()](#apidoc.element.ioredis.commander.prototype.zrevrangeBuffer)
- description and source-code
```javascript
zrevrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrevrangebylex"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrangebylex ()](#apidoc.element.ioredis.commander.prototype.zrevrangebylex)
- description and source-code
```javascript
zrevrangebylex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrevrangebylexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrangebylexBuffer ()](#apidoc.element.ioredis.commander.prototype.zrevrangebylexBuffer)
- description and source-code
```javascript
zrevrangebylexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrevrangebyscore"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrangebyscore ()](#apidoc.element.ioredis.commander.prototype.zrevrangebyscore)
- description and source-code
```javascript
zrevrangebyscore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrevrangebyscoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrangebyscoreBuffer ()](#apidoc.element.ioredis.commander.prototype.zrevrangebyscoreBuffer)
- description and source-code
```javascript
zrevrangebyscoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrevrank"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrank ()](#apidoc.element.ioredis.commander.prototype.zrevrank)
- description and source-code
```javascript
zrevrank = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zrevrankBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zrevrankBuffer ()](#apidoc.element.ioredis.commander.prototype.zrevrankBuffer)
- description and source-code
```javascript
zrevrankBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zscan"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zscan ()](#apidoc.element.ioredis.commander.prototype.zscan)
- description and source-code
```javascript
zscan = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zscanBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zscanBuffer ()](#apidoc.element.ioredis.commander.prototype.zscanBuffer)
- description and source-code
```javascript
zscanBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zscore"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zscore ()](#apidoc.element.ioredis.commander.prototype.zscore)
- description and source-code
```javascript
zscore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zscoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zscoreBuffer ()](#apidoc.element.ioredis.commander.prototype.zscoreBuffer)
- description and source-code
```javascript
zscoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zunionstore"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zunionstore ()](#apidoc.element.ioredis.commander.prototype.zunionstore)
- description and source-code
```javascript
zunionstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.commander.prototype.zunionstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.commander.prototype.</span>zunionstoreBuffer ()](#apidoc.element.ioredis.commander.prototype.zunionstoreBuffer)
- description and source-code
```javascript
zunionstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.defaultOptions"></a>[module ioredis.defaultOptions](#apidoc.module.ioredis.defaultOptions)

#### <a name="apidoc.element.ioredis.defaultOptions.retryStrategy"></a>[function <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>retryStrategy (times)](#apidoc.element.ioredis.defaultOptions.retryStrategy)
- description and source-code
```javascript
retryStrategy = function (times) {
  return Math.min(times * 2, 2000);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.defaultOptions.sentinelRetryStrategy"></a>[function <span class="apidocSignatureSpan">ioredis.defaultOptions.</span>sentinelRetryStrategy (times)](#apidoc.element.ioredis.defaultOptions.sentinelRetryStrategy)
- description and source-code
```javascript
sentinelRetryStrategy = function (times) {
  return Math.min(times * 10, 1000);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.pipeline"></a>[module ioredis.pipeline](#apidoc.module.ioredis.pipeline)

#### <a name="apidoc.element.ioredis.pipeline.pipeline"></a>[function <span class="apidocSignatureSpan">ioredis.</span>pipeline (redis)](#apidoc.element.ioredis.pipeline.pipeline)
- description and source-code
```javascript
function Pipeline(redis) {
  Commander.call(this);

  this.redis = redis;
  this.isCluster = this.redis.constructor.name === 'Cluster';
  this.options = redis.options;
  this._queue = [];
  this._result = [];
  this._transactions = 0;
  this._shaToScript = {};

  var _this = this;
  _.keys(redis.scriptsSet).forEach(function (name) {
    var script = redis.scriptsSet[name];
    _this._shaToScript[script.sha] = script;
    _this[name] = redis[name];
    _this[name + 'Buffer'] = redis[name + 'Buffer'];
  });

  this.promise = new Promise(function (resolve, reject) {
    _this.resolve = resolve;
    _this.reject = reject;
  });
}
```
- example usage
```shell
...
});
'''

## Pipelining
If you want to send a batch of commands (e.g. > 5), you can use pipelining to queue
the commands in memory and then send them to Redis all at once. This way the performance improves by 50%~300% (See [benchmark section
](#benchmark)).

'redis.pipeline()' creates a 'Pipeline' instance. You can call any Redis
commands on it just like the 'Redis' instance. The commands are queued in memory
and flushed to Redis by calling the 'exec' method:

'''javascript
var pipeline = redis.pipeline();
pipeline.set('foo', 'bar');
pipeline.del('cc');
...
```



# <a name="apidoc.module.ioredis.pipeline.prototype"></a>[module ioredis.pipeline.prototype](#apidoc.module.ioredis.pipeline.prototype)

#### <a name="apidoc.element.ioredis.pipeline.prototype.addBatch"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>addBatch (commands)](#apidoc.element.ioredis.pipeline.prototype.addBatch)
- description and source-code
```javascript
addBatch = function (commands) {
  for (var i = 0; i < commands.length; ++i) {
    var command = commands[i];
    var commandName = command.shift();
    this[commandName].apply(this, command);
  }

  return this;
}
```
- example usage
```shell
...
var Pipeline = require('./pipeline');
var utils = require('./utils');

exports.addTransactionSupport = function (redis) {
redis.pipeline = function (commands) {
  var pipeline = new Pipeline(this);
  if (Array.isArray(commands)) {
    pipeline.addBatch(commands);
  }
  return pipeline;
};

var multi = redis.multi;
redis.multi = function (commands, options) {
  if (typeof options === 'undefined' && !Array.isArray(commands)) {
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.append"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>append ()](#apidoc.element.ioredis.pipeline.prototype.append)
- description and source-code
```javascript
append = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.appendBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>appendBuffer ()](#apidoc.element.ioredis.pipeline.prototype.appendBuffer)
- description and source-code
```javascript
appendBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.asking"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>asking ()](#apidoc.element.ioredis.pipeline.prototype.asking)
- description and source-code
```javascript
asking = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.askingBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>askingBuffer ()](#apidoc.element.ioredis.pipeline.prototype.askingBuffer)
- description and source-code
```javascript
askingBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.auth"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>auth ()](#apidoc.element.ioredis.pipeline.prototype.auth)
- description and source-code
```javascript
auth = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.authBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>authBuffer ()](#apidoc.element.ioredis.pipeline.prototype.authBuffer)
- description and source-code
```javascript
authBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.bgrewriteaof"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bgrewriteaof ()](#apidoc.element.ioredis.pipeline.prototype.bgrewriteaof)
- description and source-code
```javascript
bgrewriteaof = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.bgrewriteaofBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bgrewriteaofBuffer ()](#apidoc.element.ioredis.pipeline.prototype.bgrewriteaofBuffer)
- description and source-code
```javascript
bgrewriteaofBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.bgsave"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bgsave ()](#apidoc.element.ioredis.pipeline.prototype.bgsave)
- description and source-code
```javascript
bgsave = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.bgsaveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bgsaveBuffer ()](#apidoc.element.ioredis.pipeline.prototype.bgsaveBuffer)
- description and source-code
```javascript
bgsaveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.bitcount"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitcount ()](#apidoc.element.ioredis.pipeline.prototype.bitcount)
- description and source-code
```javascript
bitcount = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.bitcountBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitcountBuffer ()](#apidoc.element.ioredis.pipeline.prototype.bitcountBuffer)
- description and source-code
```javascript
bitcountBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.bitfield"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitfield ()](#apidoc.element.ioredis.pipeline.prototype.bitfield)
- description and source-code
```javascript
bitfield = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.bitfieldBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitfieldBuffer ()](#apidoc.element.ioredis.pipeline.prototype.bitfieldBuffer)
- description and source-code
```javascript
bitfieldBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.bitop"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitop ()](#apidoc.element.ioredis.pipeline.prototype.bitop)
- description and source-code
```javascript
bitop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.bitopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitopBuffer ()](#apidoc.element.ioredis.pipeline.prototype.bitopBuffer)
- description and source-code
```javascript
bitopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.bitpos"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitpos ()](#apidoc.element.ioredis.pipeline.prototype.bitpos)
- description and source-code
```javascript
bitpos = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.bitposBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>bitposBuffer ()](#apidoc.element.ioredis.pipeline.prototype.bitposBuffer)
- description and source-code
```javascript
bitposBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.blpop"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>blpop ()](#apidoc.element.ioredis.pipeline.prototype.blpop)
- description and source-code
```javascript
blpop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.blpopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>blpopBuffer ()](#apidoc.element.ioredis.pipeline.prototype.blpopBuffer)
- description and source-code
```javascript
blpopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.brpop"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>brpop ()](#apidoc.element.ioredis.pipeline.prototype.brpop)
- description and source-code
```javascript
brpop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.brpopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>brpopBuffer ()](#apidoc.element.ioredis.pipeline.prototype.brpopBuffer)
- description and source-code
```javascript
brpopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.brpoplpush"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>brpoplpush ()](#apidoc.element.ioredis.pipeline.prototype.brpoplpush)
- description and source-code
```javascript
brpoplpush = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.brpoplpushBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>brpoplpushBuffer ()](#apidoc.element.ioredis.pipeline.prototype.brpoplpushBuffer)
- description and source-code
```javascript
brpoplpushBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.call"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>call ()](#apidoc.element.ioredis.pipeline.prototype.call)
- description and source-code
```javascript
call = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
var fbuffer = require('flexbuffer');
var Promise = require('bluebird');
var util = require('util');
var commands = require('redis-commands');
var calculateSlot = require('cluster-key-slot');

function Pipeline(redis) {
Commander.call(this);

this.redis = redis;
this.isCluster = this.redis.constructor.name === 'Cluster';
this.options = redis.options;
this._queue = [];
this._result = [];
this._transactions = 0;
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.callBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>callBuffer ()](#apidoc.element.ioredis.pipeline.prototype.callBuffer)
- description and source-code
```javascript
callBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.client"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>client ()](#apidoc.element.ioredis.pipeline.prototype.client)
- description and source-code
```javascript
client = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.clientBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>clientBuffer ()](#apidoc.element.ioredis.pipeline.prototype.clientBuffer)
- description and source-code
```javascript
clientBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.cluster"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>cluster ()](#apidoc.element.ioredis.pipeline.prototype.cluster)
- description and source-code
```javascript
cluster = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.clusterBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>clusterBuffer ()](#apidoc.element.ioredis.pipeline.prototype.clusterBuffer)
- description and source-code
```javascript
clusterBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.command"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>command ()](#apidoc.element.ioredis.pipeline.prototype.command)
- description and source-code
```javascript
command = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.commandBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>commandBuffer ()](#apidoc.element.ioredis.pipeline.prototype.commandBuffer)
- description and source-code
```javascript
commandBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.config"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>config ()](#apidoc.element.ioredis.pipeline.prototype.config)
- description and source-code
```javascript
config = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.configBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>configBuffer ()](#apidoc.element.ioredis.pipeline.prototype.configBuffer)
- description and source-code
```javascript
configBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.createBuiltinCommand"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>createBuiltinCommand (commandName)](#apidoc.element.ioredis.pipeline.prototype.createBuiltinCommand)
- description and source-code
```javascript
createBuiltinCommand = function (commandName) {
  return {
    string: generateFunction(commandName, 'utf8'),
    buffer: generateFunction(commandName, null)
  };
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.dbsize"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>dbsize ()](#apidoc.element.ioredis.pipeline.prototype.dbsize)
- description and source-code
```javascript
dbsize = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.dbsizeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>dbsizeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.dbsizeBuffer)
- description and source-code
```javascript
dbsizeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.debug"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>debug ()](#apidoc.element.ioredis.pipeline.prototype.debug)
- description and source-code
```javascript
debug = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.debugBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>debugBuffer ()](#apidoc.element.ioredis.pipeline.prototype.debugBuffer)
- description and source-code
```javascript
debugBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.decr"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>decr ()](#apidoc.element.ioredis.pipeline.prototype.decr)
- description and source-code
```javascript
decr = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.decrBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>decrBuffer ()](#apidoc.element.ioredis.pipeline.prototype.decrBuffer)
- description and source-code
```javascript
decrBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.decrby"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>decrby ()](#apidoc.element.ioredis.pipeline.prototype.decrby)
- description and source-code
```javascript
decrby = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.decrbyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>decrbyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.decrbyBuffer)
- description and source-code
```javascript
decrbyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.defineCommand"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>defineCommand (name, definition)](#apidoc.element.ioredis.pipeline.prototype.defineCommand)
- description and source-code
```javascript
defineCommand = function (name, definition) {
  var script = new Script(definition.lua, definition.numberOfKeys,
                          this.options.keyPrefix);
  this.scriptsSet[name] = script;
  this[name] = generateScriptingFunction(script, 'utf8');
  this[name + 'Buffer'] = generateScriptingFunction(script, null);
}
```
- example usage
```shell
...
care of script caching and to detect when to use 'EVAL' and when to use 'EVALSHA'.
ioredis expose a 'defineCommand' method to make scripting much easier to use:

'''javascript
var redis = new Redis();

// This will define a command echo:
redis.defineCommand('echo', {
  numberOfKeys: 2,
  lua: 'return {KEYS[1],KEYS[2],ARGV[1],ARGV[2]}'
});

// Now 'echo' can be used just like any other ordinary command,
// and ioredis will try to use 'EVALSHA' internally when possible for better performance.
redis.echo('k1', 'k2', 'a1', 'a2', function (err, result) {
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.del"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>del ()](#apidoc.element.ioredis.pipeline.prototype.del)
- description and source-code
```javascript
del = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
'redis.pipeline()' creates a 'Pipeline' instance. You can call any Redis
commands on it just like the 'Redis' instance. The commands are queued in memory
and flushed to Redis by calling the 'exec' method:

'''javascript
var pipeline = redis.pipeline();
pipeline.set('foo', 'bar');
pipeline.del('cc');
pipeline.exec(function (err, results) {
  // 'err' is always null, and 'results' is an array of responses
  // corresponding to the sequence of queued commands.
  // Each response follows the format '[err, result]'.
});

// You can even chain the commands:
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.delBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>delBuffer ()](#apidoc.element.ioredis.pipeline.prototype.delBuffer)
- description and source-code
```javascript
delBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.discard"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>discard ()](#apidoc.element.ioredis.pipeline.prototype.discard)
- description and source-code
```javascript
discard = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.discardBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>discardBuffer ()](#apidoc.element.ioredis.pipeline.prototype.discardBuffer)
- description and source-code
```javascript
discardBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.dump"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>dump ()](#apidoc.element.ioredis.pipeline.prototype.dump)
- description and source-code
```javascript
dump = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.dumpBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>dumpBuffer ()](#apidoc.element.ioredis.pipeline.prototype.dumpBuffer)
- description and source-code
```javascript
dumpBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.echo"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>echo ()](#apidoc.element.ioredis.pipeline.prototype.echo)
- description and source-code
```javascript
echo = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
redis.defineCommand('echo', {
  numberOfKeys: 2,
  lua: 'return {KEYS[1],KEYS[2],ARGV[1],ARGV[2]}'
});

// Now 'echo' can be used just like any other ordinary command,
// and ioredis will try to use 'EVALSHA' internally when possible for better performance.
redis.echo('k1', 'k2', 'a1', 'a2', function (err, result) {
  // result === ['k1', 'k2', 'a1', 'a2']
});

// 'echoBuffer' is also defined automatically to return buffers instead of strings:
redis.echoBuffer('k1', 'k2', 'a1', 'a2', function (err, result) {
  // result[0] === new Buffer('k1');
});
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.echoBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>echoBuffer ()](#apidoc.element.ioredis.pipeline.prototype.echoBuffer)
- description and source-code
```javascript
echoBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
// Now 'echo' can be used just like any other ordinary command,
// and ioredis will try to use 'EVALSHA' internally when possible for better performance.
redis.echo('k1', 'k2', 'a1', 'a2', function (err, result) {
  // result === ['k1', 'k2', 'a1', 'a2']
});

// 'echoBuffer' is also defined automatically to return buffers instead of strings:
redis.echoBuffer('k1', 'k2', 'a1', 'a2', function (err, result) {
  // result[0] === new Buffer('k1');
});

// And of course it works with pipeline:
redis.pipeline().set('foo', 'bar').echo('k1', 'k2', 'a1', 'a2').exec();
'''
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.eval"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>eval ()](#apidoc.element.ioredis.pipeline.prototype.eval)
- description and source-code
```javascript
eval = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.evalBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>evalBuffer ()](#apidoc.element.ioredis.pipeline.prototype.evalBuffer)
- description and source-code
```javascript
evalBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.evalsha"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>evalsha ()](#apidoc.element.ioredis.pipeline.prototype.evalsha)
- description and source-code
```javascript
evalsha = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.evalshaBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>evalshaBuffer ()](#apidoc.element.ioredis.pipeline.prototype.evalshaBuffer)
- description and source-code
```javascript
evalshaBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.exec"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>exec (callback)](#apidoc.element.ioredis.pipeline.prototype.exec)
- description and source-code
```javascript
exec = function (callback) {
  if (this._transactions > 0) {
    this._transactions -= 1;
    return (this.options.dropBufferSupport ? exec : execBuffer).apply(this, arguments);
  }
  if (!this.nodeifiedPromise) {
    this.nodeifiedPromise = true;
    this.promise.nodeify(callback);
  }
  if (_.isEmpty(this._queue)) {
    this.resolve([]);
  }
  var pipelineSlot, i;
  if (this.isCluster) {
    // List of the first key for each command
    var sampleKeys = [];
    for (i = 0; i < this._queue.length; i++) {
      var keys = this._queue[i].getKeys();
      if (keys.length) {
        sampleKeys.push(keys[0]);
      }
    }

    if (sampleKeys.length) {
      pipelineSlot = calculateSlot.generateMulti(sampleKeys);
      if (pipelineSlot < 0) {
        this.reject(new Error('All keys in the pipeline should belong to the same slot'));
        return this.promise;
      }
    } else {
      // Send the pipeline to a random node
      pipelineSlot = Math.random() * 16384 | 0;
    }
  }

  // Check whether scripts exists
  var scripts = [];
  for (i = 0; i < this._queue.length; ++i) {
    var item = this._queue[i];
    if (this.isCluster && item.isCustomCommand) {
      this.reject(new Error('Sending custom commands in pipeline is not supported in Cluster mode.'));
      return this.promise;
    }
    if (item.name !== 'evalsha') {
      continue;
    }
    var script = this._shaToScript[item.args[0]];
    if (!script) {
      continue;
    }
    scripts.push(script);
  }

  var _this = this;
  if (!scripts.length) {
    return execPipeline();
  }

  return this.redis.script('exists', scripts.map(function (item) {
    return item.sha;
  })).then(function (results) {
    var pending = [];
    for (var i = 0; i < results.length; ++i) {
      if (!results[i]) {
        pending.push(scripts[i]);
      }
    }
    return Promise.all(pending.map(function (script) {
      return _this.redis.script('load', script.lua);
    }));
  }).then(execPipeline);

  function execPipeline() {
    var data = '';
    var writePending = _this.replyPending = _this._queue.length;

    var node;
    if (_this.isCluster) {
      node = { slot: pipelineSlot, redis: _this.redis.connectionPool.nodes.all[_this.preferKey] };
    }
    var bufferMode = false;
    var stream = {
      write: function (writable) {
        if (writable instanceof Buffer) {
          bufferMode = true;
        }
        if (bufferMode) {
          if (typeof data === 'string') {
            var flexBuffer = new fbuffer.FlexBuffer(0);
            flexBuffer.write(data);
            data = flexBuffer;
          }
          data.write(writable);
        } else {
          data += writable;
        }
        if (!--writePending) {
          if (bufferMode) {
            data = data.getBuffer();
          }
          if (_this.isCluster) {
            node.redis.stream.write(data);
          } else {
            _this.redis.stream.write(data);
          }

          // Reset writePending for resending
          writePending = _this._queue.length;
          data = '';
          bufferMode = false;
        }
      }
    };

    for (var i = 0; i < _this._queue.length; ++i) {
      _this.redis.sendCommand(_this._queue[i], stream, node);
    }
    return _this.promise;
  }
}
```
- example usage
```shell
...
commands on it just like the 'Redis' instance. The commands are queued in memory
and flushed to Redis by calling the 'exec' method:

'''javascript
var pipeline = redis.pipeline();
pipeline.set('foo', 'bar');
pipeline.del('cc');
pipeline.exec(function (err, results) {
  // 'err' is always null, and 'results' is an array of responses
  // corresponding to the sequence of queued commands.
  // Each response follows the format '[err, result]'.
});

// You can even chain the commands:
redis.pipeline().set('foo', 'bar').del('cc').exec(function (err, results) {
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.execBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>execBuffer ()](#apidoc.element.ioredis.pipeline.prototype.execBuffer)
- description and source-code
```javascript
function deprecated() {
  warned = exports.printDeprecationMessage(msg, warned, deprecated);
  if (new.target) {
    return Reflect.construct(fn, arguments, new.target);
  }
  return fn.apply(this, arguments);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.exists"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>exists ()](#apidoc.element.ioredis.pipeline.prototype.exists)
- description and source-code
```javascript
exists = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
        message: error.message
      };
    } else if (commonError.name !== error.name || commonError.message !== error.message) {
      retriable = false;
      break;
    }
  } else if (!inTransaction) {
    var isReadOnly = commands.exists(command.name) && commands.hasFlag(command.name, 'readonly');
    if (!isReadOnly) {
      retriable = false;
      break;
    }
  }
}
if (commonError && retriable) {
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.existsBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>existsBuffer ()](#apidoc.element.ioredis.pipeline.prototype.existsBuffer)
- description and source-code
```javascript
existsBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.expire"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>expire ()](#apidoc.element.ioredis.pipeline.prototype.expire)
- description and source-code
```javascript
expire = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.expireBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>expireBuffer ()](#apidoc.element.ioredis.pipeline.prototype.expireBuffer)
- description and source-code
```javascript
expireBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.expireat"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>expireat ()](#apidoc.element.ioredis.pipeline.prototype.expireat)
- description and source-code
```javascript
expireat = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.expireatBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>expireatBuffer ()](#apidoc.element.ioredis.pipeline.prototype.expireatBuffer)
- description and source-code
```javascript
expireatBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.fillResult"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>fillResult (value, position)](#apidoc.element.ioredis.pipeline.prototype.fillResult)
- description and source-code
```javascript
fillResult = function (value, position) {
  var i;
  if (this._queue[position].name === 'exec' && Array.isArray(value[1])) {
    var execLength = value[1].length;
    for (i = 0; i < execLength; i++) {
      if (value[1][i] instanceof Error) {
        continue;
      }
      var cmd = this._queue[position - (execLength - i)];
      try {
        value[1][i] = cmd.transformReply(value[1][i]);
      } catch (err) {
        value[1][i] = err;
      }
    }
  }
  this._result[position] = value;

  if (--this.replyPending) {
    return;
  }

  if (this.isCluster) {
    var retriable = true;
    var commonError;
    var inTransaction;
    for (i = 0; i < this._result.length; ++i) {
      var error = this._result[i][0];
      var command = this._queue[i];
      if (command.name === 'multi') {
        inTransaction = true;
      } else if (command.name === 'exec') {
        inTransaction = false;
      }
      if (error) {
        if (command.name === 'exec' && error.message === 'EXECABORT Transaction discarded because of previous errors.') {
          continue;
        }
        if (!commonError) {
          commonError = {
            name: error.name,
            message: error.message
          };
        } else if (commonError.name !== error.name || commonError.message !== error.message) {
          retriable = false;
          break;
        }
      } else if (!inTransaction) {
        var isReadOnly = commands.exists(command.name) && commands.hasFlag(command.name, 'readonly');
        if (!isReadOnly) {
          retriable = false;
          break;
        }
      }
    }
    if (commonError && retriable) {
      var _this = this;
      var errv = commonError.message.split(' ');
      var queue = this._queue;
      inTransaction = false;
      this._queue = [];
      for (i = 0; i < queue.length; ++i) {
        if (errv[0] === 'ASK' && !inTransaction &&
            queue[i].name !== 'asking' &&
            (!queue[i - 1] || queue[i - 1].name !== 'asking')) {
          var asking = new Command('asking');
          asking.ignore = true;
          this.sendCommand(asking);
        }
        queue[i].initPromise();
        this.sendCommand(queue[i]);
        if (queue[i].name === 'multi') {
          inTransaction = true;
        } else if (queue[i].name === 'exec') {
          inTransaction = false;
        }
      }

      var matched = true;
      if (typeof this.leftRedirections === 'undefined') {
        this.leftRedirections = {};
      }
      var exec = function () {
        _this.exec();
      };
      this.redis.handleError(commonError, this.leftRedirections, {
        moved: function (slot, key) {
          _this.preferKey = key;
          _this.redis.slots[errv[1]] = [key];
          _this.redis.refreshSlotsCache();
          _this.exec();
        },
        ask: function (slot, key) {
          _this.preferKey = key;
          _this.exec();
        },
        tryagain: exec,
        clusterDown: exec,
        connectionClosed: exec,
        maxRedirections: function () {
          matched = false;
        },
        defaults: function () {
          matched = false;
        }
      });
      if (matched) {
        return;
      }
    }
  }

  var ignoredCount = 0;
  for (i = 0; i < this._queue.length - ignoredCount; ++i) {
    if (this._queue[i + ignoredCount].ignore) {
      ignoredCount += 1;
    }
    this._result[i] = this._result[i + ignoredCount];
  }
  this.resolve(this._result.slice(0, this._result.length - ignoredCount));
}
```
- example usage
```shell
...

Pipeline.prototype.sendCommand = function (command) {
var position = this._queue.length;

var _this = this;

command.promise.then(function (result) {
  _this.fillResult([null, result], position);
}).catch(function (error) {
  _this.fillResult([error], position);
});

this._queue.push(command);

return this;
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.flushall"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>flushall ()](#apidoc.element.ioredis.pipeline.prototype.flushall)
- description and source-code
```javascript
flushall = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.flushallBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>flushallBuffer ()](#apidoc.element.ioredis.pipeline.prototype.flushallBuffer)
- description and source-code
```javascript
flushallBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.flushdb"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>flushdb ()](#apidoc.element.ioredis.pipeline.prototype.flushdb)
- description and source-code
```javascript
flushdb = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...

'Cluster#nodes()' accepts a parameter role, which can be "master", "slave" and "all" (default), and returns an array of 'Redis'
instance. For example:

'''javascript
// Send 'FLUSHDB' command to all slaves:
var slaves = cluster.nodes('slave');
Promise.all(slaves.map(function (node) {
  return node.flushdb();
}));

// Get keys of all the masters:
var masters = cluster.nodes('master');
Promise.all(masters.map(function (node) {
  return node.keys();
})).then(function (keys) {
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.flushdbBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>flushdbBuffer ()](#apidoc.element.ioredis.pipeline.prototype.flushdbBuffer)
- description and source-code
```javascript
flushdbBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.geoadd"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geoadd ()](#apidoc.element.ioredis.pipeline.prototype.geoadd)
- description and source-code
```javascript
geoadd = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.geoaddBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geoaddBuffer ()](#apidoc.element.ioredis.pipeline.prototype.geoaddBuffer)
- description and source-code
```javascript
geoaddBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.geodist"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geodist ()](#apidoc.element.ioredis.pipeline.prototype.geodist)
- description and source-code
```javascript
geodist = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.geodistBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geodistBuffer ()](#apidoc.element.ioredis.pipeline.prototype.geodistBuffer)
- description and source-code
```javascript
geodistBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.geohash"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geohash ()](#apidoc.element.ioredis.pipeline.prototype.geohash)
- description and source-code
```javascript
geohash = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.geohashBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geohashBuffer ()](#apidoc.element.ioredis.pipeline.prototype.geohashBuffer)
- description and source-code
```javascript
geohashBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.geopos"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geopos ()](#apidoc.element.ioredis.pipeline.prototype.geopos)
- description and source-code
```javascript
geopos = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.geoposBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>geoposBuffer ()](#apidoc.element.ioredis.pipeline.prototype.geoposBuffer)
- description and source-code
```javascript
geoposBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.georadius"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>georadius ()](#apidoc.element.ioredis.pipeline.prototype.georadius)
- description and source-code
```javascript
georadius = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.georadiusBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>georadiusBuffer ()](#apidoc.element.ioredis.pipeline.prototype.georadiusBuffer)
- description and source-code
```javascript
georadiusBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.georadiusbymember"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>georadiusbymember ()](#apidoc.element.ioredis.pipeline.prototype.georadiusbymember)
- description and source-code
```javascript
georadiusbymember = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.georadiusbymemberBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>georadiusbymemberBuffer ()](#apidoc.element.ioredis.pipeline.prototype.georadiusbymemberBuffer)
- description and source-code
```javascript
georadiusbymemberBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.get"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>get ()](#apidoc.element.ioredis.pipeline.prototype.get)
- description and source-code
```javascript
get = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
## Basic Usage

'''javascript
var Redis = require('ioredis');
var redis = new Redis();

redis.set('foo', 'bar');
redis.get('foo', function (err, result) {
  console.log(result);
});

// Or using a promise if the last argument isn't a function
redis.get('foo').then(function (result) {
  console.log(result);
});
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.getBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getBuffer ()](#apidoc.element.ioredis.pipeline.prototype.getBuffer)
- description and source-code
```javascript
getBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
redis.set('foo', new Buffer('bar'));
'''

And every command has a method that returns a Buffer (by adding a suffix of "Buffer" to the command name).
To get a buffer instead of a utf8 string:

'''javascript
redis.getBuffer('foo', function (err, result) {
  // result is a buffer.
});
'''

## Pipelining
If you want to send a batch of commands (e.g. > 5), you can use pipelining to queue
the commands in memory and then send them to Redis all at once. This way the performance improves by 50%~300% (See [benchmark section
](#benchmark)).
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.getBuiltinCommands"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getBuiltinCommands ()](#apidoc.element.ioredis.pipeline.prototype.getBuiltinCommands)
- description and source-code
```javascript
getBuiltinCommands = function () {
  return _.clone(commands);
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.getbit"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getbit ()](#apidoc.element.ioredis.pipeline.prototype.getbit)
- description and source-code
```javascript
getbit = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.getbitBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getbitBuffer ()](#apidoc.element.ioredis.pipeline.prototype.getbitBuffer)
- description and source-code
```javascript
getbitBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.getrange"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getrange ()](#apidoc.element.ioredis.pipeline.prototype.getrange)
- description and source-code
```javascript
getrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.getrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getrangeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.getrangeBuffer)
- description and source-code
```javascript
getrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.getset"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getset ()](#apidoc.element.ioredis.pipeline.prototype.getset)
- description and source-code
```javascript
getset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.getsetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>getsetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.getsetBuffer)
- description and source-code
```javascript
getsetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hdel"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hdel ()](#apidoc.element.ioredis.pipeline.prototype.hdel)
- description and source-code
```javascript
hdel = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hdelBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hdelBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hdelBuffer)
- description and source-code
```javascript
hdelBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hexists"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hexists ()](#apidoc.element.ioredis.pipeline.prototype.hexists)
- description and source-code
```javascript
hexists = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hexistsBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hexistsBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hexistsBuffer)
- description and source-code
```javascript
hexistsBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hget"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hget ()](#apidoc.element.ioredis.pipeline.prototype.hget)
- description and source-code
```javascript
hget = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hgetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hgetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hgetBuffer)
- description and source-code
```javascript
hgetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hgetall"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hgetall ()](#apidoc.element.ioredis.pipeline.prototype.hgetall)
- description and source-code
```javascript
hgetall = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hgetallBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hgetallBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hgetallBuffer)
- description and source-code
```javascript
hgetallBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hincrby"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hincrby ()](#apidoc.element.ioredis.pipeline.prototype.hincrby)
- description and source-code
```javascript
hincrby = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hincrbyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hincrbyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hincrbyBuffer)
- description and source-code
```javascript
hincrbyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hincrbyfloat"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hincrbyfloat ()](#apidoc.element.ioredis.pipeline.prototype.hincrbyfloat)
- description and source-code
```javascript
hincrbyfloat = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hincrbyfloatBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hincrbyfloatBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hincrbyfloatBuffer)
- description and source-code
```javascript
hincrbyfloatBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hkeys"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hkeys ()](#apidoc.element.ioredis.pipeline.prototype.hkeys)
- description and source-code
```javascript
hkeys = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hkeysBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hkeysBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hkeysBuffer)
- description and source-code
```javascript
hkeysBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hlen"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hlen ()](#apidoc.element.ioredis.pipeline.prototype.hlen)
- description and source-code
```javascript
hlen = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hlenBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hlenBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hlenBuffer)
- description and source-code
```javascript
hlenBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hmget"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hmget ()](#apidoc.element.ioredis.pipeline.prototype.hmget)
- description and source-code
```javascript
hmget = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hmgetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hmgetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hmgetBuffer)
- description and source-code
```javascript
hmgetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hmset"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hmset ()](#apidoc.element.ioredis.pipeline.prototype.hmset)
- description and source-code
```javascript
hmset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hmsetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hmsetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hmsetBuffer)
- description and source-code
```javascript
hmsetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hscan"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hscan ()](#apidoc.element.ioredis.pipeline.prototype.hscan)
- description and source-code
```javascript
hscan = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hscanBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hscanBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hscanBuffer)
- description and source-code
```javascript
hscanBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hset"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hset ()](#apidoc.element.ioredis.pipeline.prototype.hset)
- description and source-code
```javascript
hset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hsetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hsetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hsetBuffer)
- description and source-code
```javascript
hsetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hsetnx"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hsetnx ()](#apidoc.element.ioredis.pipeline.prototype.hsetnx)
- description and source-code
```javascript
hsetnx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hsetnxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hsetnxBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hsetnxBuffer)
- description and source-code
```javascript
hsetnxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hstrlen"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hstrlen ()](#apidoc.element.ioredis.pipeline.prototype.hstrlen)
- description and source-code
```javascript
hstrlen = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hstrlenBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hstrlenBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hstrlenBuffer)
- description and source-code
```javascript
hstrlenBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hvals"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hvals ()](#apidoc.element.ioredis.pipeline.prototype.hvals)
- description and source-code
```javascript
hvals = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.hvalsBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>hvalsBuffer ()](#apidoc.element.ioredis.pipeline.prototype.hvalsBuffer)
- description and source-code
```javascript
hvalsBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.incr"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>incr ()](#apidoc.element.ioredis.pipeline.prototype.incr)
- description and source-code
```javascript
incr = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.incrBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>incrBuffer ()](#apidoc.element.ioredis.pipeline.prototype.incrBuffer)
- description and source-code
```javascript
incrBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.incrby"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>incrby ()](#apidoc.element.ioredis.pipeline.prototype.incrby)
- description and source-code
```javascript
incrby = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.incrbyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>incrbyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.incrbyBuffer)
- description and source-code
```javascript
incrbyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.incrbyfloat"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>incrbyfloat ()](#apidoc.element.ioredis.pipeline.prototype.incrbyfloat)
- description and source-code
```javascript
incrbyfloat = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.incrbyfloatBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>incrbyfloatBuffer ()](#apidoc.element.ioredis.pipeline.prototype.incrbyfloatBuffer)
- description and source-code
```javascript
incrbyfloatBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.info"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>info ()](#apidoc.element.ioredis.pipeline.prototype.info)
- description and source-code
```javascript
info = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.infoBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>infoBuffer ()](#apidoc.element.ioredis.pipeline.prototype.infoBuffer)
- description and source-code
```javascript
infoBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.keys"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>keys ()](#apidoc.element.ioredis.pipeline.prototype.keys)
- description and source-code
```javascript
keys = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
Promise.all(slaves.map(function (node) {
  return node.flushdb();
}));

// Get keys of all the masters:
var masters = cluster.nodes('master');
Promise.all(masters.map(function (node) {
  return node.keys();
})).then(function (keys) {
  // keys: [['key1', 'key2'], ['key3', 'key4']]
});
'''

### Transaction and pipeline in Cluster mode
Almost all features that are supported by 'Redis' are also supported by 'Redis.Cluster', e.g. custom commands, transaction and pipeline
.
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.keysBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>keysBuffer ()](#apidoc.element.ioredis.pipeline.prototype.keysBuffer)
- description and source-code
```javascript
keysBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lastsave"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lastsave ()](#apidoc.element.ioredis.pipeline.prototype.lastsave)
- description and source-code
```javascript
lastsave = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lastsaveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lastsaveBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lastsaveBuffer)
- description and source-code
```javascript
lastsaveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.latency"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>latency ()](#apidoc.element.ioredis.pipeline.prototype.latency)
- description and source-code
```javascript
latency = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.latencyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>latencyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.latencyBuffer)
- description and source-code
```javascript
latencyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lindex"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lindex ()](#apidoc.element.ioredis.pipeline.prototype.lindex)
- description and source-code
```javascript
lindex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lindexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lindexBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lindexBuffer)
- description and source-code
```javascript
lindexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.linsert"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>linsert ()](#apidoc.element.ioredis.pipeline.prototype.linsert)
- description and source-code
```javascript
linsert = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.linsertBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>linsertBuffer ()](#apidoc.element.ioredis.pipeline.prototype.linsertBuffer)
- description and source-code
```javascript
linsertBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.llen"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>llen ()](#apidoc.element.ioredis.pipeline.prototype.llen)
- description and source-code
```javascript
llen = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.llenBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>llenBuffer ()](#apidoc.element.ioredis.pipeline.prototype.llenBuffer)
- description and source-code
```javascript
llenBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lpop"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lpop ()](#apidoc.element.ioredis.pipeline.prototype.lpop)
- description and source-code
```javascript
lpop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lpopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lpopBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lpopBuffer)
- description and source-code
```javascript
lpopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lpush"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lpush ()](#apidoc.element.ioredis.pipeline.prototype.lpush)
- description and source-code
```javascript
lpush = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lpushBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lpushBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lpushBuffer)
- description and source-code
```javascript
lpushBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lpushx"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lpushx ()](#apidoc.element.ioredis.pipeline.prototype.lpushx)
- description and source-code
```javascript
lpushx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lpushxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lpushxBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lpushxBuffer)
- description and source-code
```javascript
lpushxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lrange"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lrange ()](#apidoc.element.ioredis.pipeline.prototype.lrange)
- description and source-code
```javascript
lrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lrangeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lrangeBuffer)
- description and source-code
```javascript
lrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lrem"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lrem ()](#apidoc.element.ioredis.pipeline.prototype.lrem)
- description and source-code
```javascript
lrem = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lremBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lremBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lremBuffer)
- description and source-code
```javascript
lremBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lset"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lset ()](#apidoc.element.ioredis.pipeline.prototype.lset)
- description and source-code
```javascript
lset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.lsetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>lsetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.lsetBuffer)
- description and source-code
```javascript
lsetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.ltrim"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>ltrim ()](#apidoc.element.ioredis.pipeline.prototype.ltrim)
- description and source-code
```javascript
ltrim = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.ltrimBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>ltrimBuffer ()](#apidoc.element.ioredis.pipeline.prototype.ltrimBuffer)
- description and source-code
```javascript
ltrimBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.memory"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>memory ()](#apidoc.element.ioredis.pipeline.prototype.memory)
- description and source-code
```javascript
memory = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.memoryBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>memoryBuffer ()](#apidoc.element.ioredis.pipeline.prototype.memoryBuffer)
- description and source-code
```javascript
memoryBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.mget"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>mget ()](#apidoc.element.ioredis.pipeline.prototype.mget)
- description and source-code
```javascript
mget = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.mgetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>mgetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.mgetBuffer)
- description and source-code
```javascript
mgetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.migrate"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>migrate ()](#apidoc.element.ioredis.pipeline.prototype.migrate)
- description and source-code
```javascript
migrate = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.migrateBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>migrateBuffer ()](#apidoc.element.ioredis.pipeline.prototype.migrateBuffer)
- description and source-code
```javascript
migrateBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.module"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>module ()](#apidoc.element.ioredis.pipeline.prototype.module)
- description and source-code
```javascript
module = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.moduleBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>moduleBuffer ()](#apidoc.element.ioredis.pipeline.prototype.moduleBuffer)
- description and source-code
```javascript
moduleBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.move"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>move ()](#apidoc.element.ioredis.pipeline.prototype.move)
- description and source-code
```javascript
move = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.moveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>moveBuffer ()](#apidoc.element.ioredis.pipeline.prototype.moveBuffer)
- description and source-code
```javascript
moveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.mset"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>mset ()](#apidoc.element.ioredis.pipeline.prototype.mset)
- description and source-code
```javascript
mset = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
'''

There are three built-in transformers, two argument transformers for 'hmset' & 'mset' and
a reply transformer for 'hgetall'. Transformers for 'hmset' and 'hgetall' were mentioned
above, and the transformer for 'mset' is similar to the one for 'hmset':

'''javascript
redis.mset({ k1: 'v1', k2: 'v2' });
redis.get('k1', function (err, result) {
// result === 'v1';
});

redis.mset(new Map([['k3', 'v3'], ['k4', 'v4']]));
redis.get('k3', function (err, result) {
// result === 'v3';
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.msetBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>msetBuffer ()](#apidoc.element.ioredis.pipeline.prototype.msetBuffer)
- description and source-code
```javascript
msetBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.msetnx"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>msetnx ()](#apidoc.element.ioredis.pipeline.prototype.msetnx)
- description and source-code
```javascript
msetnx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.msetnxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>msetnxBuffer ()](#apidoc.element.ioredis.pipeline.prototype.msetnxBuffer)
- description and source-code
```javascript
msetnxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.multi"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>multi ()](#apidoc.element.ioredis.pipeline.prototype.multi)
- description and source-code
```javascript
multi = function () {
  this._transactions += 1;
  return multi.apply(this, arguments);
}
```
- example usage
```shell
...

## Transaction
Most of the time, the transaction commands 'multi' & 'exec' are used together with pipeline.
Therefore, when 'multi' is called, a 'Pipeline' instance is created automatically by default,
so you can use 'multi' just like 'pipeline':

'''javascript
redis.multi().set('foo', 'bar').get('foo').exec(function (err, results) {
  // results === [[null, 'OK'], [null, 'bar']]
});
'''
If there's a syntax error in the transaction's command chain (e.g. wrong number of arguments, wrong command name, etc),
then none of the commands would be executed, and an error is returned:

'''javascript
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.multiBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>multiBuffer ()](#apidoc.element.ioredis.pipeline.prototype.multiBuffer)
- description and source-code
```javascript
multiBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.object"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>object ()](#apidoc.element.ioredis.pipeline.prototype.object)
- description and source-code
```javascript
object = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.objectBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>objectBuffer ()](#apidoc.element.ioredis.pipeline.prototype.objectBuffer)
- description and source-code
```javascript
objectBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.persist"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>persist ()](#apidoc.element.ioredis.pipeline.prototype.persist)
- description and source-code
```javascript
persist = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.persistBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>persistBuffer ()](#apidoc.element.ioredis.pipeline.prototype.persistBuffer)
- description and source-code
```javascript
persistBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pexpire"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pexpire ()](#apidoc.element.ioredis.pipeline.prototype.pexpire)
- description and source-code
```javascript
pexpire = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pexpireBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pexpireBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pexpireBuffer)
- description and source-code
```javascript
pexpireBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pexpireat"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pexpireat ()](#apidoc.element.ioredis.pipeline.prototype.pexpireat)
- description and source-code
```javascript
pexpireat = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pexpireatBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pexpireatBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pexpireatBuffer)
- description and source-code
```javascript
pexpireatBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pfadd"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfadd ()](#apidoc.element.ioredis.pipeline.prototype.pfadd)
- description and source-code
```javascript
pfadd = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pfaddBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfaddBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pfaddBuffer)
- description and source-code
```javascript
pfaddBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pfcount"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfcount ()](#apidoc.element.ioredis.pipeline.prototype.pfcount)
- description and source-code
```javascript
pfcount = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pfcountBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfcountBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pfcountBuffer)
- description and source-code
```javascript
pfcountBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pfdebug"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfdebug ()](#apidoc.element.ioredis.pipeline.prototype.pfdebug)
- description and source-code
```javascript
pfdebug = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pfdebugBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfdebugBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pfdebugBuffer)
- description and source-code
```javascript
pfdebugBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pfmerge"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfmerge ()](#apidoc.element.ioredis.pipeline.prototype.pfmerge)
- description and source-code
```javascript
pfmerge = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pfmergeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfmergeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pfmergeBuffer)
- description and source-code
```javascript
pfmergeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pfselftest"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfselftest ()](#apidoc.element.ioredis.pipeline.prototype.pfselftest)
- description and source-code
```javascript
pfselftest = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pfselftestBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pfselftestBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pfselftestBuffer)
- description and source-code
```javascript
pfselftestBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.ping"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>ping ()](#apidoc.element.ioredis.pipeline.prototype.ping)
- description and source-code
```javascript
ping = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pingBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pingBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pingBuffer)
- description and source-code
```javascript
pingBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.post"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>post ()](#apidoc.element.ioredis.pipeline.prototype.post)
- description and source-code
```javascript
post = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.postBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>postBuffer ()](#apidoc.element.ioredis.pipeline.prototype.postBuffer)
- description and source-code
```javascript
postBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.psetex"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>psetex ()](#apidoc.element.ioredis.pipeline.prototype.psetex)
- description and source-code
```javascript
psetex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.psetexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>psetexBuffer ()](#apidoc.element.ioredis.pipeline.prototype.psetexBuffer)
- description and source-code
```javascript
psetexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.psubscribe"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>psubscribe ()](#apidoc.element.ioredis.pipeline.prototype.psubscribe)
- description and source-code
```javascript
psubscribe = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
  // Both 'channel' and 'message' are buffers.
});
'''

'PSUBSCRIBE' is also supported in a similar way:

'''javascript
redis.psubscribe('pat?ern', function (err, count) {});
redis.on('pmessage', function (pattern, channel, message) {});
redis.on('pmessageBuffer', function (pattern, channel, message) {});
'''

When a client issues a SUBSCRIBE or PSUBSCRIBE, that connection is put into a "subscriber" mode.
At that point, only commands that modify the subscription set are valid.
When the subscription set is empty, the connection is put back into regular mode.
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.psubscribeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>psubscribeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.psubscribeBuffer)
- description and source-code
```javascript
psubscribeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.psync"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>psync ()](#apidoc.element.ioredis.pipeline.prototype.psync)
- description and source-code
```javascript
psync = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.psyncBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>psyncBuffer ()](#apidoc.element.ioredis.pipeline.prototype.psyncBuffer)
- description and source-code
```javascript
psyncBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pttl"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pttl ()](#apidoc.element.ioredis.pipeline.prototype.pttl)
- description and source-code
```javascript
pttl = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pttlBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pttlBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pttlBuffer)
- description and source-code
```javascript
pttlBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.publish"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>publish ()](#apidoc.element.ioredis.pipeline.prototype.publish)
- description and source-code
```javascript
publish = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
var Redis = require('ioredis');
var redis = new Redis();
var pub = new Redis();
redis.subscribe('news', 'music', function (err, count) {
// Now we are subscribed to both the 'news' and 'music' channels.
// 'count' represents the number of channels we are currently subscribed to.

pub.publish('news', 'Hello world!');
pub.publish('music', 'Hello again!');
});

redis.on('message', function (channel, message) {
// Receive message Hello world! from channel news
// Receive message Hello again! from channel music
console.log('Receive message %s from channel %s', message, channel);
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.publishBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>publishBuffer ()](#apidoc.element.ioredis.pipeline.prototype.publishBuffer)
- description and source-code
```javascript
publishBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pubsub"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pubsub ()](#apidoc.element.ioredis.pipeline.prototype.pubsub)
- description and source-code
```javascript
pubsub = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.pubsubBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>pubsubBuffer ()](#apidoc.element.ioredis.pipeline.prototype.pubsubBuffer)
- description and source-code
```javascript
pubsubBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.punsubscribe"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>punsubscribe ()](#apidoc.element.ioredis.pipeline.prototype.punsubscribe)
- description and source-code
```javascript
punsubscribe = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.punsubscribeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>punsubscribeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.punsubscribeBuffer)
- description and source-code
```javascript
punsubscribeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.quit"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>quit ()](#apidoc.element.ioredis.pipeline.prototype.quit)
- description and source-code
```javascript
quit = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
});
'''

You can learn more from the [Redis documentation](http://redis.io/commands/scan).

## Auto-reconnect
By default, ioredis will try to reconnect when the connection to Redis is lost
except when the connection is closed manually by 'redis.disconnect()' or 'redis.quit()'.

It's very flexible to control how long to wait to reconnect after disconnection
using the 'retryStrategy' option:

'''javascript
var redis = new Redis({
// This is the default value of 'retryStrategy'
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.quitBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>quitBuffer ()](#apidoc.element.ioredis.pipeline.prototype.quitBuffer)
- description and source-code
```javascript
quitBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.randomkey"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>randomkey ()](#apidoc.element.ioredis.pipeline.prototype.randomkey)
- description and source-code
```javascript
randomkey = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.randomkeyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>randomkeyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.randomkeyBuffer)
- description and source-code
```javascript
randomkeyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.readonly"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>readonly ()](#apidoc.element.ioredis.pipeline.prototype.readonly)
- description and source-code
```javascript
readonly = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.readonlyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>readonlyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.readonlyBuffer)
- description and source-code
```javascript
readonlyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.readwrite"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>readwrite ()](#apidoc.element.ioredis.pipeline.prototype.readwrite)
- description and source-code
```javascript
readwrite = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.readwriteBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>readwriteBuffer ()](#apidoc.element.ioredis.pipeline.prototype.readwriteBuffer)
- description and source-code
```javascript
readwriteBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.rename"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rename ()](#apidoc.element.ioredis.pipeline.prototype.rename)
- description and source-code
```javascript
rename = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.renameBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>renameBuffer ()](#apidoc.element.ioredis.pipeline.prototype.renameBuffer)
- description and source-code
```javascript
renameBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.renamenx"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>renamenx ()](#apidoc.element.ioredis.pipeline.prototype.renamenx)
- description and source-code
```javascript
renamenx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.renamenxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>renamenxBuffer ()](#apidoc.element.ioredis.pipeline.prototype.renamenxBuffer)
- description and source-code
```javascript
renamenxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.replconf"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>replconf ()](#apidoc.element.ioredis.pipeline.prototype.replconf)
- description and source-code
```javascript
replconf = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.replconfBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>replconfBuffer ()](#apidoc.element.ioredis.pipeline.prototype.replconfBuffer)
- description and source-code
```javascript
replconfBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.restore"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>restore ()](#apidoc.element.ioredis.pipeline.prototype.restore)
- description and source-code
```javascript
restore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.restore-asking"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>restore-asking ()](#apidoc.element.ioredis.pipeline.prototype.restore-asking)
- description and source-code
```javascript
restore-asking = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.restore-askingBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>restore-askingBuffer ()](#apidoc.element.ioredis.pipeline.prototype.restore-askingBuffer)
- description and source-code
```javascript
restore-askingBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.restoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>restoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.restoreBuffer)
- description and source-code
```javascript
restoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.role"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>role ()](#apidoc.element.ioredis.pipeline.prototype.role)
- description and source-code
```javascript
role = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.roleBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>roleBuffer ()](#apidoc.element.ioredis.pipeline.prototype.roleBuffer)
- description and source-code
```javascript
roleBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.rpop"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpop ()](#apidoc.element.ioredis.pipeline.prototype.rpop)
- description and source-code
```javascript
rpop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.rpopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpopBuffer ()](#apidoc.element.ioredis.pipeline.prototype.rpopBuffer)
- description and source-code
```javascript
rpopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.rpoplpush"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpoplpush ()](#apidoc.element.ioredis.pipeline.prototype.rpoplpush)
- description and source-code
```javascript
rpoplpush = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.rpoplpushBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpoplpushBuffer ()](#apidoc.element.ioredis.pipeline.prototype.rpoplpushBuffer)
- description and source-code
```javascript
rpoplpushBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.rpush"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpush ()](#apidoc.element.ioredis.pipeline.prototype.rpush)
- description and source-code
```javascript
rpush = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.rpushBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpushBuffer ()](#apidoc.element.ioredis.pipeline.prototype.rpushBuffer)
- description and source-code
```javascript
rpushBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.rpushx"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpushx ()](#apidoc.element.ioredis.pipeline.prototype.rpushx)
- description and source-code
```javascript
rpushx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.rpushxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>rpushxBuffer ()](#apidoc.element.ioredis.pipeline.prototype.rpushxBuffer)
- description and source-code
```javascript
rpushxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sadd"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sadd ()](#apidoc.element.ioredis.pipeline.prototype.sadd)
- description and source-code
```javascript
sadd = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...

// Or using a promise if the last argument isn't a function
redis.get('foo').then(function (result) {
  console.log(result);
});

// Arguments to commands are flattened, so the following are the same:
redis.sadd('set', 1, 3, 5, 7);
redis.sadd('set', [1, 3, 5, 7]);

// All arguments are passed directly to the redis server:
redis.set('key', 100, 'EX', 10);
'''

See the 'examples/' folder for more examples.
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.saddBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>saddBuffer ()](#apidoc.element.ioredis.pipeline.prototype.saddBuffer)
- description and source-code
```javascript
saddBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.save"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>save ()](#apidoc.element.ioredis.pipeline.prototype.save)
- description and source-code
```javascript
save = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.saveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>saveBuffer ()](#apidoc.element.ioredis.pipeline.prototype.saveBuffer)
- description and source-code
```javascript
saveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.scan"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>scan ()](#apidoc.element.ioredis.pipeline.prototype.scan)
- description and source-code
```javascript
scan = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.scanBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>scanBuffer ()](#apidoc.element.ioredis.pipeline.prototype.scanBuffer)
- description and source-code
```javascript
scanBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.scard"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>scard ()](#apidoc.element.ioredis.pipeline.prototype.scard)
- description and source-code
```javascript
scard = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.scardBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>scardBuffer ()](#apidoc.element.ioredis.pipeline.prototype.scardBuffer)
- description and source-code
```javascript
scardBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.script"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>script ()](#apidoc.element.ioredis.pipeline.prototype.script)
- description and source-code
```javascript
script = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
}

var _this = this;
if (!scripts.length) {
  return execPipeline();
}

return this.redis.script('exists', scripts.map(function (item) {
  return item.sha;
})).then(function (results) {
  var pending = [];
  for (var i = 0; i < results.length; ++i) {
    if (!results[i]) {
      pending.push(scripts[i]);
    }
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.scriptBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>scriptBuffer ()](#apidoc.element.ioredis.pipeline.prototype.scriptBuffer)
- description and source-code
```javascript
scriptBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sdiff"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sdiff ()](#apidoc.element.ioredis.pipeline.prototype.sdiff)
- description and source-code
```javascript
sdiff = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sdiffBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sdiffBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sdiffBuffer)
- description and source-code
```javascript
sdiffBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sdiffstore"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sdiffstore ()](#apidoc.element.ioredis.pipeline.prototype.sdiffstore)
- description and source-code
```javascript
sdiffstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sdiffstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sdiffstoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sdiffstoreBuffer)
- description and source-code
```javascript
sdiffstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.select"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>select ()](#apidoc.element.ioredis.pipeline.prototype.select)
- description and source-code
```javascript
select = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.selectBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>selectBuffer ()](#apidoc.element.ioredis.pipeline.prototype.selectBuffer)
- description and source-code
```javascript
selectBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sendCommand"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sendCommand (command)](#apidoc.element.ioredis.pipeline.prototype.sendCommand)
- description and source-code
```javascript
sendCommand = function (command) {
  var position = this._queue.length;

  var _this = this;

  command.promise.then(function (result) {
    _this.fillResult([null, result], position);
  }).catch(function (error) {
    _this.fillResult([error], position);
  });

  this._queue.push(command);

  return this;
}
```
- example usage
```shell
...
  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
};
}

function generateScriptingFunction(_script, _encoding) {
return function () {
  var length = arguments.length;
  var lastArgIndex = length - 1;
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.send_command"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>send_command ()](#apidoc.element.ioredis.pipeline.prototype.send_command)
- description and source-code
```javascript
send_command = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sentinel"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sentinel ()](#apidoc.element.ioredis.pipeline.prototype.sentinel)
- description and source-code
```javascript
sentinel = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sentinelBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sentinelBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sentinelBuffer)
- description and source-code
```javascript
sentinelBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.set"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>set ()](#apidoc.element.ioredis.pipeline.prototype.set)
- description and source-code
```javascript
set = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...

## Basic Usage

'''javascript
var Redis = require('ioredis');
var redis = new Redis();

redis.set('foo', 'bar');
redis.get('foo', function (err, result) {
console.log(result);
});

// Or using a promise if the last argument isn't a function
redis.get('foo').then(function (result) {
console.log(result);
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.setBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setBuffer ()](#apidoc.element.ioredis.pipeline.prototype.setBuffer)
- description and source-code
```javascript
setBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.setbit"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setbit ()](#apidoc.element.ioredis.pipeline.prototype.setbit)
- description and source-code
```javascript
setbit = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.setbitBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setbitBuffer ()](#apidoc.element.ioredis.pipeline.prototype.setbitBuffer)
- description and source-code
```javascript
setbitBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.setex"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setex ()](#apidoc.element.ioredis.pipeline.prototype.setex)
- description and source-code
```javascript
setex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.setexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setexBuffer ()](#apidoc.element.ioredis.pipeline.prototype.setexBuffer)
- description and source-code
```javascript
setexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.setnx"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setnx ()](#apidoc.element.ioredis.pipeline.prototype.setnx)
- description and source-code
```javascript
setnx = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.setnxBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setnxBuffer ()](#apidoc.element.ioredis.pipeline.prototype.setnxBuffer)
- description and source-code
```javascript
setnxBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.setrange"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setrange ()](#apidoc.element.ioredis.pipeline.prototype.setrange)
- description and source-code
```javascript
setrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.setrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>setrangeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.setrangeBuffer)
- description and source-code
```javascript
setrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.shutdown"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>shutdown ()](#apidoc.element.ioredis.pipeline.prototype.shutdown)
- description and source-code
```javascript
shutdown = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.shutdownBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>shutdownBuffer ()](#apidoc.element.ioredis.pipeline.prototype.shutdownBuffer)
- description and source-code
```javascript
shutdownBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sinter"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sinter ()](#apidoc.element.ioredis.pipeline.prototype.sinter)
- description and source-code
```javascript
sinter = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sinterBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sinterBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sinterBuffer)
- description and source-code
```javascript
sinterBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sinterstore"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sinterstore ()](#apidoc.element.ioredis.pipeline.prototype.sinterstore)
- description and source-code
```javascript
sinterstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sinterstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sinterstoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sinterstoreBuffer)
- description and source-code
```javascript
sinterstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sismember"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sismember ()](#apidoc.element.ioredis.pipeline.prototype.sismember)
- description and source-code
```javascript
sismember = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sismemberBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sismemberBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sismemberBuffer)
- description and source-code
```javascript
sismemberBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.slaveof"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>slaveof ()](#apidoc.element.ioredis.pipeline.prototype.slaveof)
- description and source-code
```javascript
slaveof = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.slaveofBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>slaveofBuffer ()](#apidoc.element.ioredis.pipeline.prototype.slaveofBuffer)
- description and source-code
```javascript
slaveofBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.slowlog"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>slowlog ()](#apidoc.element.ioredis.pipeline.prototype.slowlog)
- description and source-code
```javascript
slowlog = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.slowlogBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>slowlogBuffer ()](#apidoc.element.ioredis.pipeline.prototype.slowlogBuffer)
- description and source-code
```javascript
slowlogBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.smembers"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>smembers ()](#apidoc.element.ioredis.pipeline.prototype.smembers)
- description and source-code
```javascript
smembers = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.smembersBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>smembersBuffer ()](#apidoc.element.ioredis.pipeline.prototype.smembersBuffer)
- description and source-code
```javascript
smembersBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.smove"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>smove ()](#apidoc.element.ioredis.pipeline.prototype.smove)
- description and source-code
```javascript
smove = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.smoveBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>smoveBuffer ()](#apidoc.element.ioredis.pipeline.prototype.smoveBuffer)
- description and source-code
```javascript
smoveBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sort"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sort ()](#apidoc.element.ioredis.pipeline.prototype.sort)
- description and source-code
```javascript
sort = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
  numberOfKeys: 2,
  lua: 'return {KEYS[1],KEYS[2],ARGV[1],ARGV[2]}'
});

// Works well with pipelining/transaction
fooRedis.pipeline()
  // Sends SORT foo:list BY foo:weight_*->fieldname
  .sort('list', 'BY', 'weight_*->fieldname')
  // Supports custom commands
  // Sends EVALSHA xxx foo:k1 foo:k2 a1 a2
  .echo('k1', 'k2', 'a1', 'a2')
  .exec()
'''

## Transforming Arguments & Replies
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sortBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sortBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sortBuffer)
- description and source-code
```javascript
sortBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.spop"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>spop ()](#apidoc.element.ioredis.pipeline.prototype.spop)
- description and source-code
```javascript
spop = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.spopBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>spopBuffer ()](#apidoc.element.ioredis.pipeline.prototype.spopBuffer)
- description and source-code
```javascript
spopBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.srandmember"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>srandmember ()](#apidoc.element.ioredis.pipeline.prototype.srandmember)
- description and source-code
```javascript
srandmember = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.srandmemberBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>srandmemberBuffer ()](#apidoc.element.ioredis.pipeline.prototype.srandmemberBuffer)
- description and source-code
```javascript
srandmemberBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.srem"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>srem ()](#apidoc.element.ioredis.pipeline.prototype.srem)
- description and source-code
```javascript
srem = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sremBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sremBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sremBuffer)
- description and source-code
```javascript
sremBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sscan"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sscan ()](#apidoc.element.ioredis.pipeline.prototype.sscan)
- description and source-code
```javascript
sscan = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sscanBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sscanBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sscanBuffer)
- description and source-code
```javascript
sscanBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.strlen"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>strlen ()](#apidoc.element.ioredis.pipeline.prototype.strlen)
- description and source-code
```javascript
strlen = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.strlenBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>strlenBuffer ()](#apidoc.element.ioredis.pipeline.prototype.strlenBuffer)
- description and source-code
```javascript
strlenBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.subscribe"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>subscribe ()](#apidoc.element.ioredis.pipeline.prototype.subscribe)
- description and source-code
```javascript
subscribe = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
...
It subscribes to a channel with one connection
and publishes to that channel with the other:

'''javascript
var Redis = require('ioredis');
var redis = new Redis();
var pub = new Redis();
redis.subscribe('news', 'music', function (err, count) {
  // Now we are subscribed to both the 'news' and 'music' channels.
  // 'count' represents the number of channels we are currently subscribed to.

  pub.publish('news', 'Hello world!');
  pub.publish('music', 'Hello again!');
});
...
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.subscribeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>subscribeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.subscribeBuffer)
- description and source-code
```javascript
subscribeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.substr"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>substr ()](#apidoc.element.ioredis.pipeline.prototype.substr)
- description and source-code
```javascript
substr = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.substrBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>substrBuffer ()](#apidoc.element.ioredis.pipeline.prototype.substrBuffer)
- description and source-code
```javascript
substrBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sunion"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sunion ()](#apidoc.element.ioredis.pipeline.prototype.sunion)
- description and source-code
```javascript
sunion = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sunionBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sunionBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sunionBuffer)
- description and source-code
```javascript
sunionBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sunionstore"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sunionstore ()](#apidoc.element.ioredis.pipeline.prototype.sunionstore)
- description and source-code
```javascript
sunionstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sunionstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sunionstoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.sunionstoreBuffer)
- description and source-code
```javascript
sunionstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.swapdb"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>swapdb ()](#apidoc.element.ioredis.pipeline.prototype.swapdb)
- description and source-code
```javascript
swapdb = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.swapdbBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>swapdbBuffer ()](#apidoc.element.ioredis.pipeline.prototype.swapdbBuffer)
- description and source-code
```javascript
swapdbBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.sync"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>sync ()](#apidoc.element.ioredis.pipeline.prototype.sync)
- description and source-code
```javascript
sync = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.syncBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>syncBuffer ()](#apidoc.element.ioredis.pipeline.prototype.syncBuffer)
- description and source-code
```javascript
syncBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.time"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>time ()](#apidoc.element.ioredis.pipeline.prototype.time)
- description and source-code
```javascript
time = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.timeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>timeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.timeBuffer)
- description and source-code
```javascript
timeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.touch"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>touch ()](#apidoc.element.ioredis.pipeline.prototype.touch)
- description and source-code
```javascript
touch = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.touchBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>touchBuffer ()](#apidoc.element.ioredis.pipeline.prototype.touchBuffer)
- description and source-code
```javascript
touchBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.ttl"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>ttl ()](#apidoc.element.ioredis.pipeline.prototype.ttl)
- description and source-code
```javascript
ttl = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.ttlBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>ttlBuffer ()](#apidoc.element.ioredis.pipeline.prototype.ttlBuffer)
- description and source-code
```javascript
ttlBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.type"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>type ()](#apidoc.element.ioredis.pipeline.prototype.type)
- description and source-code
```javascript
type = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.typeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>typeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.typeBuffer)
- description and source-code
```javascript
typeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.unlink"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>unlink ()](#apidoc.element.ioredis.pipeline.prototype.unlink)
- description and source-code
```javascript
unlink = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.unlinkBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>unlinkBuffer ()](#apidoc.element.ioredis.pipeline.prototype.unlinkBuffer)
- description and source-code
```javascript
unlinkBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.unsubscribe"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>unsubscribe ()](#apidoc.element.ioredis.pipeline.prototype.unsubscribe)
- description and source-code
```javascript
unsubscribe = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.unsubscribeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>unsubscribeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.unsubscribeBuffer)
- description and source-code
```javascript
unsubscribeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.unwatch"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>unwatch ()](#apidoc.element.ioredis.pipeline.prototype.unwatch)
- description and source-code
```javascript
unwatch = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.unwatchBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>unwatchBuffer ()](#apidoc.element.ioredis.pipeline.prototype.unwatchBuffer)
- description and source-code
```javascript
unwatchBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.wait"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>wait ()](#apidoc.element.ioredis.pipeline.prototype.wait)
- description and source-code
```javascript
wait = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.waitBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>waitBuffer ()](#apidoc.element.ioredis.pipeline.prototype.waitBuffer)
- description and source-code
```javascript
waitBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.watch"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>watch ()](#apidoc.element.ioredis.pipeline.prototype.watch)
- description and source-code
```javascript
watch = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.watchBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>watchBuffer ()](#apidoc.element.ioredis.pipeline.prototype.watchBuffer)
- description and source-code
```javascript
watchBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zadd"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zadd ()](#apidoc.element.ioredis.pipeline.prototype.zadd)
- description and source-code
```javascript
zadd = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zaddBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zaddBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zaddBuffer)
- description and source-code
```javascript
zaddBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zcard"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zcard ()](#apidoc.element.ioredis.pipeline.prototype.zcard)
- description and source-code
```javascript
zcard = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zcardBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zcardBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zcardBuffer)
- description and source-code
```javascript
zcardBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zcount"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zcount ()](#apidoc.element.ioredis.pipeline.prototype.zcount)
- description and source-code
```javascript
zcount = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zcountBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zcountBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zcountBuffer)
- description and source-code
```javascript
zcountBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zincrby"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zincrby ()](#apidoc.element.ioredis.pipeline.prototype.zincrby)
- description and source-code
```javascript
zincrby = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zincrbyBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zincrbyBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zincrbyBuffer)
- description and source-code
```javascript
zincrbyBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zinterstore"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zinterstore ()](#apidoc.element.ioredis.pipeline.prototype.zinterstore)
- description and source-code
```javascript
zinterstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zinterstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zinterstoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zinterstoreBuffer)
- description and source-code
```javascript
zinterstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zlexcount"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zlexcount ()](#apidoc.element.ioredis.pipeline.prototype.zlexcount)
- description and source-code
```javascript
zlexcount = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zlexcountBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zlexcountBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zlexcountBuffer)
- description and source-code
```javascript
zlexcountBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrange"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrange ()](#apidoc.element.ioredis.pipeline.prototype.zrange)
- description and source-code
```javascript
zrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrangeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrangeBuffer)
- description and source-code
```javascript
zrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrangebylex"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrangebylex ()](#apidoc.element.ioredis.pipeline.prototype.zrangebylex)
- description and source-code
```javascript
zrangebylex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrangebylexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrangebylexBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrangebylexBuffer)
- description and source-code
```javascript
zrangebylexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrangebyscore"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrangebyscore ()](#apidoc.element.ioredis.pipeline.prototype.zrangebyscore)
- description and source-code
```javascript
zrangebyscore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrangebyscoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrangebyscoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrangebyscoreBuffer)
- description and source-code
```javascript
zrangebyscoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrank"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrank ()](#apidoc.element.ioredis.pipeline.prototype.zrank)
- description and source-code
```javascript
zrank = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrankBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrankBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrankBuffer)
- description and source-code
```javascript
zrankBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrem"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrem ()](#apidoc.element.ioredis.pipeline.prototype.zrem)
- description and source-code
```javascript
zrem = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zremBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zremBuffer)
- description and source-code
```javascript
zremBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zremrangebylex"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremrangebylex ()](#apidoc.element.ioredis.pipeline.prototype.zremrangebylex)
- description and source-code
```javascript
zremrangebylex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zremrangebylexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremrangebylexBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zremrangebylexBuffer)
- description and source-code
```javascript
zremrangebylexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zremrangebyrank"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremrangebyrank ()](#apidoc.element.ioredis.pipeline.prototype.zremrangebyrank)
- description and source-code
```javascript
zremrangebyrank = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zremrangebyrankBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremrangebyrankBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zremrangebyrankBuffer)
- description and source-code
```javascript
zremrangebyrankBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zremrangebyscore"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremrangebyscore ()](#apidoc.element.ioredis.pipeline.prototype.zremrangebyscore)
- description and source-code
```javascript
zremrangebyscore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zremrangebyscoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zremrangebyscoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zremrangebyscoreBuffer)
- description and source-code
```javascript
zremrangebyscoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrevrange"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrange ()](#apidoc.element.ioredis.pipeline.prototype.zrevrange)
- description and source-code
```javascript
zrevrange = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrevrangeBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrangeBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrevrangeBuffer)
- description and source-code
```javascript
zrevrangeBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrevrangebylex"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrangebylex ()](#apidoc.element.ioredis.pipeline.prototype.zrevrangebylex)
- description and source-code
```javascript
zrevrangebylex = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrevrangebylexBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrangebylexBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrevrangebylexBuffer)
- description and source-code
```javascript
zrevrangebylexBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrevrangebyscore"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrangebyscore ()](#apidoc.element.ioredis.pipeline.prototype.zrevrangebyscore)
- description and source-code
```javascript
zrevrangebyscore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrevrangebyscoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrangebyscoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrevrangebyscoreBuffer)
- description and source-code
```javascript
zrevrangebyscoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrevrank"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrank ()](#apidoc.element.ioredis.pipeline.prototype.zrevrank)
- description and source-code
```javascript
zrevrank = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zrevrankBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zrevrankBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zrevrankBuffer)
- description and source-code
```javascript
zrevrankBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zscan"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zscan ()](#apidoc.element.ioredis.pipeline.prototype.zscan)
- description and source-code
```javascript
zscan = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zscanBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zscanBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zscanBuffer)
- description and source-code
```javascript
zscanBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zscore"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zscore ()](#apidoc.element.ioredis.pipeline.prototype.zscore)
- description and source-code
```javascript
zscore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zscoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zscoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zscoreBuffer)
- description and source-code
```javascript
zscoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zunionstore"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zunionstore ()](#apidoc.element.ioredis.pipeline.prototype.zunionstore)
- description and source-code
```javascript
zunionstore = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.pipeline.prototype.zunionstoreBuffer"></a>[function <span class="apidocSignatureSpan">ioredis.pipeline.prototype.</span>zunionstoreBuffer ()](#apidoc.element.ioredis.pipeline.prototype.zunionstoreBuffer)
- description and source-code
```javascript
zunionstoreBuffer = function () {
  var firstArgIndex = 0;
  var commandName = _commandName;
  if (commandName === null) {
    commandName = arguments[0];
    firstArgIndex = 1;
  }
  var length = arguments.length;
  var lastArgIndex = length - 1;
  var callback = arguments[lastArgIndex];
  if (typeof callback !== 'function') {
    callback = undefined;
  } else {
    length = lastArgIndex;
  }
  var args = new Array(length - firstArgIndex);
  for (var i = firstArgIndex; i < length; ++i) {
    args[i - firstArgIndex] = arguments[i];
  }

  var options;
  if (this.options.dropBufferSupport) {
    if (!_encoding) {
      return Promise.reject(new Error(DROP_BUFFER_SUPPORT_ERROR)).nodeify(callback);
    }
    options = { replyEncoding: null };
  } else {
    options = { replyEncoding: _encoding };
  }

  if (this.options.showFriendlyErrorStack) {
    options.errorStack = new Error().stack;
  }
  if (this.options.keyPrefix) {
    options.keyPrefix = this.options.keyPrefix;
  }

  return this.sendCommand(new Command(commandName, args, options, callback));
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.scan_stream"></a>[module ioredis.scan_stream](#apidoc.module.ioredis.scan_stream)

#### <a name="apidoc.element.ioredis.scan_stream.scan_stream"></a>[function <span class="apidocSignatureSpan">ioredis.</span>scan_stream (opt)](#apidoc.element.ioredis.scan_stream.scan_stream)
- description and source-code
```javascript
function ScanStream(opt) {
  Readable.call(this, opt);
  this._redisCursor = '0';
  this.opt = opt;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.scan_stream.super_"></a>[function <span class="apidocSignatureSpan">ioredis.scan_stream.</span>super_ (options)](#apidoc.element.ioredis.scan_stream.super_)
- description and source-code
```javascript
function Readable(options) {
  if (!(this instanceof Readable))
    return new Readable(options);

  this._readableState = new ReadableState(options, this);

  // legacy
  this.readable = true;

  if (options && typeof options.read === 'function')
    this._read = options.read;

  Stream.call(this);
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.scan_stream.prototype"></a>[module ioredis.scan_stream.prototype](#apidoc.module.ioredis.scan_stream.prototype)

#### <a name="apidoc.element.ioredis.scan_stream.prototype._read"></a>[function <span class="apidocSignatureSpan">ioredis.scan_stream.prototype.</span>_read ()](#apidoc.element.ioredis.scan_stream.prototype._read)
- description and source-code
```javascript
_read = function () {
  if (this._redisDrained) {
    this.push(null);
    return;
  }
  var args = [this._redisCursor];
  if (this.opt.key) {
    args.unshift(this.opt.key);
  }
  if (this.opt.match) {
    args.push('MATCH', this.opt.match);
  }
  if (this.opt.count) {
    args.push('COUNT', this.opt.count);
  }
  var _this = this;
  this.opt.redis[this.opt.command](args, function (err, res) {
    if (err) {
      _this.emit('error', err);
      return;
    }
    _this._redisCursor = (res[0] instanceof Buffer) ? res[0].toString() : res[0];
    if (_this._redisCursor === '0') {
      _this._redisDrained = true;
    }
    _this.push(res[1]);
  });
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.scan_stream.prototype.close"></a>[function <span class="apidocSignatureSpan">ioredis.scan_stream.prototype.</span>close ()](#apidoc.element.ioredis.scan_stream.prototype.close)
- description and source-code
```javascript
close = function () {
  this._redisDrained = true;
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.script"></a>[module ioredis.script](#apidoc.module.ioredis.script)

#### <a name="apidoc.element.ioredis.script.script"></a>[function <span class="apidocSignatureSpan">ioredis.</span>script (lua, numberOfKeys, keyPrefix)](#apidoc.element.ioredis.script.script)
- description and source-code
```javascript
function Script(lua, numberOfKeys, keyPrefix) {
  this.lua = lua;
  this.sha = crypto.createHash('sha1').update(this.lua).digest('hex');
  this.numberOfKeys = typeof numberOfKeys === 'number' ? numberOfKeys : null;
  this.keyPrefix = keyPrefix ? keyPrefix : '';
}
```
- example usage
```shell
...
}

var _this = this;
if (!scripts.length) {
  return execPipeline();
}

return this.redis.script('exists', scripts.map(function (item) {
  return item.sha;
})).then(function (results) {
  var pending = [];
  for (var i = 0; i < results.length; ++i) {
    if (!results[i]) {
      pending.push(scripts[i]);
    }
...
```



# <a name="apidoc.module.ioredis.script.prototype"></a>[module ioredis.script.prototype](#apidoc.module.ioredis.script.prototype)

#### <a name="apidoc.element.ioredis.script.prototype.execute"></a>[function <span class="apidocSignatureSpan">ioredis.script.prototype.</span>execute (container, args, options, callback)](#apidoc.element.ioredis.script.prototype.execute)
- description and source-code
```javascript
execute = function (container, args, options, callback) {
  if (typeof this.numberOfKeys === 'number') {
    args.unshift(this.numberOfKeys);
  }
  if (this.keyPrefix) {
    options.keyPrefix = this.keyPrefix;
  }

  var evalsha = new Command('evalsha', [this.sha].concat(args), options);
  evalsha.isCustomCommand = true;
  var result = container.sendCommand(evalsha);
  if (result instanceof Promise) {
    var _this = this;
    return result.catch(function (err) {
      if (err.toString().indexOf('NOSCRIPT') === -1) {
        throw err;
      }
      return container.sendCommand(new Command('eval', [_this.lua].concat(args), options));
    }).nodeify(callback);
  }

  // result is not a Promise--probably returned from a pipeline chain; however,
  // we still need the callback to fire when the script is evaluated
  evalsha.promise.nodeify(callback);

  return result;
}
```
- example usage
```shell
...
      options = { replyEncoding: _encoding };
    }

    if (this.options.showFriendlyErrorStack) {
      options.errorStack = new Error().stack;
    }

    return _script.execute(this, args, options, callback);
  };
}

module.exports = Commander;
...
```



# <a name="apidoc.module.ioredis.subscription_set"></a>[module ioredis.subscription_set](#apidoc.module.ioredis.subscription_set)

#### <a name="apidoc.element.ioredis.subscription_set.subscription_set"></a>[function <span class="apidocSignatureSpan">ioredis.</span>subscription_set ()](#apidoc.element.ioredis.subscription_set.subscription_set)
- description and source-code
```javascript
function SubscriptionSet() {
  this.set = {
    subscribe: {},
    psubscribe: {}
  };
}
```
- example usage
```shell
n/a
```



# <a name="apidoc.module.ioredis.subscription_set.prototype"></a>[module ioredis.subscription_set.prototype](#apidoc.module.ioredis.subscription_set.prototype)

#### <a name="apidoc.element.ioredis.subscription_set.prototype.add"></a>[function <span class="apidocSignatureSpan">ioredis.subscription_set.prototype.</span>add (set, channel)](#apidoc.element.ioredis.subscription_set.prototype.add)
- description and source-code
```javascript
add = function (set, channel) {
  this.set[mapSet(set)][channel] = true;
}
```
- example usage
```shell
n/a
```

#### <a name="apidoc.element.ioredis.subscription_set.prototype.channels"></a>[function <span class="apidocSignatureSpan">ioredis.subscription_set.prototype.</span>channels (set)](#apidoc.element.ioredis.subscription_set.prototype.channels)
- description and source-code
```javascript
channels = function (set) {
  return Object.keys(this.set[mapSet(set)]);
}
```
- example usage
```shell
...
};

SubscriptionSet.prototype.channels = function (set) {
return Object.keys(this.set[mapSet(set)]);
};

SubscriptionSet.prototype.isEmpty = function () {
return this.channels('subscribe').length === 0 && this.channels('psubscribe').length === 0;
};

function mapSet(set) {
if (set === 'unsubscribe') {
  return 'subscribe';
}
if (set === 'punsubscribe') {
...
```

#### <a name="apidoc.element.ioredis.subscription_set.prototype.del"></a>[function <span class="apidocSignatureSpan">ioredis.subscription_set.prototype.</span>del (set, channel)](#apidoc.element.ioredis.subscription_set.prototype.del)
- description and source-code
```javascript
del = function (set, channel) {
  delete this.set[mapSet(set)][channel];
}
```
- example usage
```shell
...
'redis.pipeline()' creates a 'Pipeline' instance. You can call any Redis
commands on it just like the 'Redis' instance. The commands are queued in memory
and flushed to Redis by calling the 'exec' method:

'''javascript
var pipeline = redis.pipeline();
pipeline.set('foo', 'bar');
pipeline.del('cc');
pipeline.exec(function (err, results) {
  // 'err' is always null, and 'results' is an array of responses
  // corresponding to the sequence of queued commands.
  // Each response follows the format '[err, result]'.
});

// You can even chain the commands:
...
```

#### <a name="apidoc.element.ioredis.subscription_set.prototype.isEmpty"></a>[function <span class="apidocSignatureSpan">ioredis.subscription_set.prototype.</span>isEmpty ()](#apidoc.element.ioredis.subscription_set.prototype.isEmpty)
- description and source-code
```javascript
isEmpty = function () {
  return this.channels('subscribe').length === 0 && this.channels('psubscribe').length === 0;
}
```
- example usage
```shell
...
  this._transactions -= 1;
  return (this.options.dropBufferSupport ? exec : execBuffer).apply(this, arguments);
}
if (!this.nodeifiedPromise) {
  this.nodeifiedPromise = true;
  this.promise.nodeify(callback);
}
if (_.isEmpty(this._queue)) {
  this.resolve([]);
}
var pipelineSlot, i;
if (this.isCluster) {
  // List of the first key for each command
  var sampleKeys = [];
  for (i = 0; i < this._queue.length; i++) {
...
```



# <a name="apidoc.module.ioredis.transaction"></a>[module ioredis.transaction](#apidoc.module.ioredis.transaction)

#### <a name="apidoc.element.ioredis.transaction.addTransactionSupport"></a>[function <span class="apidocSignatureSpan">ioredis.transaction.</span>addTransactionSupport (redis)](#apidoc.element.ioredis.transaction.addTransactionSupport)
- description and source-code
```javascript
addTransactionSupport = function (redis) {
  redis.pipeline = function (commands) {
    var pipeline = new Pipeline(this);
    if (Array.isArray(commands)) {
      pipeline.addBatch(commands);
    }
    return pipeline;
  };

  var multi = redis.multi;
  redis.multi = function (commands, options) {
    if (typeof options === 'undefined' && !Array.isArray(commands)) {
      options = commands;
      commands = null;
    }
    if (options && options.pipeline === false) {
      return multi.call(this);
    }
    var pipeline = new Pipeline(this);
    pipeline.multi();
    if (Array.isArray(commands)) {
      pipeline.addBatch(commands);
    }
    var exec = pipeline.exec;
    pipeline.exec = function (callback) {
      if (this._transactions > 0) {
        exec.call(pipeline);
      }
      var promise = exec.call(pipeline);
      return promise.then(function (result) {
        var execResult = result[result.length - 1];
        if (execResult[0]) {
          execResult[0].previousErrors = [];
          for (var i = 0; i < result.length - 1; ++i) {
            if (result[i][0]) {
              execResult[0].previousErrors.push(result[i][0]);
            }
          }
          throw execResult[0];
        }
        return utils.wrapMultiResult(execResult[1]);
      }).nodeify(callback);
    };

    var execBuffer = pipeline.execBuffer;
    pipeline.execBuffer = function (callback) {
      if (this._transactions > 0) {
        execBuffer.call(pipeline);
      }
      return pipeline.exec(callback);
    };
    return pipeline;
  };

  var exec = redis.exec;
  redis.exec = function (callback) {
    return exec.call(this).then(function (results) {
      if (Array.isArray(results)) {
        results = utils.wrapMultiResult(results);
      }
      return results;
    }).nodeify(callback);
  };
}
```
- example usage
```shell
n/a
```



# misc
- this document was created with [utility2](https://github.com/kaizhu256/node-utility2)
